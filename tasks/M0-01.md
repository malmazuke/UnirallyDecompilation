# M0-01 — PAL ROM identity manifest

## Assignment

- Status: in_progress
- Milestone: M0
- Coordinator: primary agent (this session)
- Worker/session/runtime/model: primary agent, Claude Code desktop session, `claude-fable-5-1`
- Reviewer: fresh sequential session (see M0-05)
- Dependencies and evidence of acceptance: M0-00 accepted (`bbce3e4`); user-supplied ROM path in ignored `local/rom-location.txt`
- Base commit: the commit that adds this record (resolve with `git log --format=%h -1 -- tasks/M0-01.md` on `main`)
- Branch and isolated worktree: `task/M0-01-rom-identity`, primary checkout (single sequential worker)
- Owned paths and shared interfaces: `tools/project.py`, `tools/unirally_lab/`, `tests/tooling/`, `tests/manifests/rom/`, `docs/research/R-0001-*`. Shared interface: the JSON report schema and CLI exit codes, which M0-02 reuses
- Claim/lease/heartbeat/checkpoint location: this file; checkpoints are commits on the task branch
- Session time limit, concurrency allocation and actual spend authorization if relevant: current interactive session, one worker, no additional purchases

## Outcome and boundaries

Deliver a reproducible identity for the user-selected PAL Unirally ROM and the first stable CLI entry point:

1. `python3 tools/project.py rom inspect --path <file>` hashes the original bytes without modifying them, identifies the SNES header location, mapping, region, internal checksum validity and interrupt vectors, and writes a manifest.
2. `--expect <manifest>` compares an input against the tracked manifest and fails clearly on an unexpected revision.
3. A tracked manifest in `tests/manifests/rom/` records the identity. The ROM bytes stay in ignored local storage.
4. A research record documents the evidence and its limits.

Not in scope: disassembly, extraction, any emulator work, normalizing or de-headering ROM images. The header decode is diagnostic; region and mapping are read from the header and are not yet confirmed by execution.

## Inputs and prerequisites

- ROM: `Unirally (Europe).sfc`, 2,097,152 bytes, SHA-256 `a1105819d48c04d680c8292bbfa9abbce05224f1bc231afd66af43b7e0a1fd4e`. Local path is in ignored `local/rom-location.txt`. A fresh host must obtain the identical file privately; the manifest lets it verify the copy.
- Python 3.14.3 (standard library only). No other dependencies.
- Known baseline failures: none; no tooling existed before this task.

## Acceptance

| Criterion | Command or experiment | Expected result | Required artifact |
| --- | --- | --- | --- |
| Original bytes preserved | `shasum -a 256 <rom>` before and after `rom inspect` | Identical hash; the tool opens the file read-only | Task handoff |
| Identity recorded | `python3 tools/project.py rom inspect --path <rom> --report artifacts/<run>/rom-inspect.json` | Manifest with SHA-256, SHA-1, MD5, CRC32, size, header offset, mapping, region, checksum verification | `tests/manifests/rom/unirally-pal.json` |
| Unexpected revision rejected | `rom inspect --path <other file> --expect tests/manifests/rom/unirally-pal.json` | Exit code 1 with a per-field mismatch list | Report |
| Missing input reported | `rom inspect --path /nonexistent` | Exit code 2, not 0 | Report |
| Synthetic tests | `python3 -m unittest discover -s tests/tooling -v` | All pass without a ROM; cover copier header, LoROM/HiROM detection, mismatch and missing-file paths | Test output in handoff |
| Local arrangement documented | Read `docs/research/R-0001-rom-identity.md` | Explains where the ROM lives locally and how another host verifies its copy | Research record |

## Evidence and attempts

| Attempt | Hypothesis | Experiment | Observation | Next decision |
| --- | --- | --- | --- | --- |
| 1 | The supplied file is an unheadered LoROM PAL image | Read-only hashing and header dump at 0x7FC0/0xFFC0 | Size 2,097,152 (no 512-byte copier header); 0x7FC0 title `UNIRALLY`, map 0x30, country 0x02, developer 0x33 with extended header maker `01`, game code `4L  `; complement 0x752c + checksum 0x8ad3 = 0xFFFF; computed byte sum 0x8ad3; 0xFFC0 is all 0xFF | Encode these checks in the tool and manifest |
| 2 | The zip beside it is the same content | Listed and hashed the zip member from a pipe | Single member, same name, CRC32 d8583ed7, identical SHA-256; zip comment `TORRENTZIPPED-83EBB1D4` | Record as provenance only; the tool takes the `.sfc` |

## Handoff

- Current base/head commit and uncommitted state: see registry and task branch log.
- Verified findings: see attempts above; full record in `docs/research/R-0001-rom-identity.md` once written.
- Current hypothesis and failed approaches: none failed.
- Commands executed, outcomes and report hashes: to be filled at checkpoint.
- Unavailable/skipped checks: cross-check against an external ROM database was not performed; the manifest records the observed identity only.
- Exact next experiment/command: implement `tools/project.py rom inspect`, then run the acceptance table.
- Remaining dependencies: none.
- Elapsed work and provider usage when known: not metered.

## Review and integration

- Reviewer and independent reproduction/withheld-case results: pending
- Required changes or acceptance rationale: pending
- Exact merge candidate and required-check results: pending
- Integrated commit and evidence location: pending
- Scope still unverified: PAL timing and region behaviour under execution (M0-03 onward)
