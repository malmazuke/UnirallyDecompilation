# M4-12 — Autonomous native ZOOM ZOO movement trial

## Assignment

- Status: accepted; integrated on 13 September 2026 (claimed at 10:24 UTC).
- Milestone: M4 (not accepted); execution policy: [D-0006](../docs/decisions/D-0006-capability-driven-work.md).
- Provider/primary: OpenAI `gpt-6-astra`/medium, standard service mode. Primary
  owns investigation, native implementation and coordination; no routine worker
  handoff or separate planning consultation. Targeted high escalation only under D-0004.
- Reviewer: primary MUST automatically spawn fresh `gpt-5.6-sol`/medium in an
  isolated checkout at the immutable candidate; one active child maximum.
- Budget/reset: [D-0004 trial exception](../docs/decisions/D-0004-model-and-usage-budget.md#m4-12-trial-exception).
  No 20-point task cap; final 20% weekly reserve remains. Reset not redeemed or
  authorized by this preparation. Startup must check fresh allowance and any
  explicit user reset confirmation; availability alone is not permission.
- Accepted gameplay/evidence base: `15237954fe636ecf83cf105186570207593c900b`.
  At claim, record the exact synchronized main tip containing this preparation
  as the dispatch base, verify ancestry, then create `task/M4-12-native-zoom-zoo`
  and an isolated `.worktrees/m4-12-native-zoom-zoo` checkout. Do not start from
  the older base without these policy changes. Record actual paths if occupied.
- Primary owns task-scoped `src/core/`, native runner/build wiring, serialization,
  extraction/pack interfaces, tooling/tests/manifests and supporting source/evidence
  documentation. The same primary owns registry/state updates when integrating.
  Reviewer owns its report and independent cases in its own checkout.
- Frozen: accepted reference manifests/digests/ROM/core identity and accepted
  DRAGSTER behavior. Existing M4 research remains reproducible. Add versioned
  interfaces when necessary; preserve old packs/states or explicitly reject new
  incompatible versions without relabeling old identities. Core/lock changes need
  a recorded necessity and independent scrutiny, not silent baseline replacement.
- Excluded: full-track finish, second-track presentation/audio, other modes,
  networking/editor, public release/deployment, spending and provider switching.
  Recover any AI/progress/control dependency actually needed for scoped movement.

## Outcome and frozen domain

Deliver a native two-rider update that runs from one initial state, authenticated
static content and controller inputs only. No original CPU execution, later
captured state, replay-driven motion tables or per-frame repairs may supply
native gameplay. Recovered Python equations are evidence, not the delivered runner.

The accepted 1650--1700 window is an internal checkpoint. The primary acceptance
interval is **frames 1650--1849 inclusive**, from the authenticated end-1649 seed:
200 PAL updates (four seconds), selected before new trial results to force growth
beyond the repeated 51-frame window while bounding this investment. The primary
may extend this interval before tuning, based on reference branch coverage; it
must not shorten it to match a candidate or hide a divergence. Full-track support
is not implied by this interval. At least one relevant additional motion/contact/
pose transition beyond the old window must be observed and reproduced; extend
the reference interval first if the nominal extension fails that coverage gate.

Freeze the exact primary field set, input scripts, initial-state representation,
static inputs, source identities and expected reference outputs in a separate
commit before native tuning. Include both riders' positions/residues, velocities,
pose/orientation, response/contact state and all inventoried control/timer/AI/
progress state that affects future scoped updates. Audit reads/writers to close
that dependency inventory; missing or unobserved inputs remain unknown, not zero.
The new reference may be additive; the accepted short-window expectations stay frozen.

## Execution and first checks

Read [NEXT_SESSION](NEXT_SESSION.md), [STATE](../docs/STATE.md), AGENTS.md,
D-0004's exception, D-0006 and [AGENT_WORKFLOW](../docs/AGENT_WORKFLOW.md).
Use [R-0029](../docs/research/R-0029-zoom-zoo-vertical-velocity.md), its linked
M4-05--M4-10 evidence and [native source guide](../src/core/README.md) as the
focused dependency map. Read older task histories only when needed.

At startup record actual model/effort, clock, usage/reserve, dispatch commit and
worktree state. Verify supplied ROM identity using the implemented command:

```sh
python3 tools/project.py rom inspect --expect tests/manifests/rom/unirally-pal.json
python3 -m unittest tests.tooling.test_zoom_zoo_position tests.tooling.test_zoom_zoo_contact tests.tooling.test_zoom_zoo_vertical_contact tests.tooling.test_zoom_zoo_reflected_vertical_contact tests.tooling.test_zoom_zoo_composition tests.tooling.test_zoom_zoo_response_b tests.tooling.test_zoom_zoo_vertical_velocity -v
```

The accepted foundation has 60 tests. If its fixtures/toolchain are absent,
locate the existing ignored inputs/cache or reproduce them from the evidence
recipes; don't ask the user to regenerate inputs the agent can obtain itself.
ROM locator is `local/rom-location.txt`; original bytes and captures stay ignored.
Use process timeouts for captures/builds/tests. The native ZOOM ZOO comparison and
restore commands do **not** exist yet; implement them and record exact working
invocations in BUILD_AND_VALIDATION before review.

Use reference-first discovery to map the coupled horizontal-velocity, pose,
reflection/history, control/jump and contact dependencies. Implement recovered
behavior in small C++ updates early and run a native comparison as soon as a
component is callable; do not build a complete second Python simulation first.
Track dependencies in this task, not one new task per word or routine. Choose
experiments for discriminating power (e.g. an observed jump writer or support/
orientation transition), not simply the next adjacent release frame.

## Acceptance

| Criterion | Required evidence |
| --- | --- |
| Authentic reference | Repeatable primary and variation references, exact seed/content/ROM/core identity and complete relevant captures; no weakened accepted baselines. Commit expanded reference contract before tuning. |
| Native autonomy | Both riders match every declared field/update through 1849 (or the predeclared extension) from one seed. Runtime inputs are initial state, static content and controllers only. Remove access to reference samples after setup and demonstrate the native run still reproduces its canonical states. |
| Dependency closure | Inventory every future-affecting input in the scoped update and its native producer or authenticated initial/static source. Dynamic captured inputs remaining: zero. Preserve all accepted short-window comparisons, writer order and width semantics. |
| Generalization | Two reviewer-owned input variations, selected and recorded before evaluating the candidate, run from the same seed through the full declared horizon. Collect repeatable independent references. At least one variation must change a recovered producer/branch or state transition relevant to this capability; inert controller differences alone do not satisfy the gate. Extend/change reference experiments before tuning if necessary and retain failed/negative evidence. |
| Save/restore | Canonical native serialization includes all scoped future-affecting state. Fresh-process restoration at an interior point and an observed motion/contact/pose transition reproduces the uninterrupted native continuation and reference projection. |
| Integrity/readability | Focused arithmetic/order/width and dependency-injection tests; semantic event validation where evidence claims rely on it. No captured-value repairs, missing-input defaults or undocumented processor-shaped public interface. D-0003 source/evidence guide is navigable. |
| Integration | Staged checks below pass, fresh Sol review approves the corrected exact candidate, private main is pushed and its remote ref verified. |

The primary owns fixing reviewer findings and automatically requests re-review.
If a withheld failure is used for tuning, preserve it as a regression and obtain
a fresh withheld case; do not quietly keep calling the tuned case withheld.
All acceptance rows are required. A budget stop or only matching the initial
51-frame window leaves M4-12 incomplete, even if useful research is committed.

## Validation and review dispatch

Follow [validation by stage](../docs/BUILD_AND_VALIDATION.md#validation-by-stage).
During edits use focused C++/Python and native differential checks. Before review,
record exact invocations for primary/variation/restore, frozen ZOOM ZOO research
and affected DRAGSTER gates. Reviewer independently inspects ROM/evidence and
native data flow in addition to running the new harness.

The final integration candidate must pass app-debug and app-sanitize synthetic
suites (including CTests/repeatability), the new private native gates, accepted
ZOOM ZOO replay/contract checks, Classic pack validation, DRAGSTER full-race/
opponent-first/restore checks and frozen presentation checks. Reuse exact
implemented recipes from BUILD_AND_VALIDATION and linked acceptance evidence;
record command/report identities in one trial evidence record. Hosted macOS/Linux
CI on the pushed integration must pass. No duplicate broad run per experiment.

Dispatch the reviewer with explicit `model: "gpt-5.6-sol"`,
`reasoning_effort: "medium"`, `fork_turns: "none"` if using collaboration tools.
Give it the exact candidate, isolated checkout path, this task, focused evidence,
owned `tasks/M4-12-review.md` output, private input locations and the two independent
case requirement. Do not include the implementation conversation or predetermine
its verdict. Wait for the result while doing useful integration preparation;
never ask the user to launch, relay or monitor review. The primary integrates
only after approval, fixes failures autonomously and verifies private-origin sync.

## Durable checkpoint and completion

At least every ten minutes and before expensive work, record the current commit,
next command, first divergence, native interval/field coverage, dynamic captured
inputs remaining, new branches reached and usage. Reassess approaches every
45 minutes without automatically ending the session or changing agents. Report
measured time and allowance delta across primary and reviewer; account-wide
usage can include other work. No artificial benchmark workload is required.

At the review reserve, stop expanding scope. Review a viable complete candidate
or preserve incomplete work, exact remaining dependencies and the next experiment.
Do not accept partial research as this capability. On acceptance, update the
registry/state with the precise native boundary and compare the trial's observed
coverage/usage with the prior workflow without asserting causal model superiority.
Do not automatically dispatch M4-13 or claim all of M4 complete.

## Handoff at preparation

No native changes or trial experiments have run. M4-12 is unclaimed. The next
agent must record its actual dispatch hash, startup usage and prerequisite results
before implementation. Reset authorization and outcome: none for this trial.

## Trial execution — 13 September 2026

- Dispatch: `67877996a032d87888db84f30660d7f6c1c8f1ce`; clean main and freshly
  fetched origin/main agree; accepted M4-11 ancestry verified with merge-base.
- Primary: requested Astra/medium under D-0006; no model/effort switch performed
  by this agent. OpenAI runtime; standard service requested, no Fast opt-in.
- Checkout: `.worktrees/m4-12-native-zoom-zoo`, branch
  `task/M4-12-native-zoom-zoo`. Ignored local/artifacts symlinks reuse the main
  checkout's private caches; no original data is tracked.
- Resource baseline: 10:24 UTC, fresh Codex weekly telemetry 0% used / 100%
  remaining, duration 10080 minutes, reset epoch 1789899841. Zero available reset
  credits. No redemption requested, authorized or attempted. Preserve final 20%.
- Startup: both exact commands above passed with 180-second subprocess bounds:
  ROM identity 10/10 fields; focused foundation 60/60 tests (0.145 seconds).
- First experiment: capture original frames 1649--1849 with the unchanged
  primary replay, full persistent-state WRAM series and expanded motion watches.
  Inspect reads/writers and new transitions before freezing the native contract.
  No native implementation or new baseline tuning has begun; remaining dynamic
  dependencies are the M4-11 external inventory. Native coverage remains zero.

### Checkpoint — first native component, 10:38 UTC

- Current committed base `df8809e`; uncommitted additive vertical-contact C++
  component/probe and expanded reference-capture tool. No expanded native tuning
  has begun; the whole-update contract is being frozen first.
- Two fresh captures (`artifacts/m4-12/expanded-primary-{a,b}`) passed unchanged
  replay expectations, exact matching access documents, 3,679,052 instructions,
  1,821,598 accesses on frames 1649--1849; no unresolved stores, no truncated
  watches. Their WRAM series agrees with the authenticated M4-04 end-1649 seed.
- Native captured-argument contact probe matches 102/102 calls through 1700 on
  all 18 declared motion/contact outputs. This is component evidence only;
  autonomous native interval remains zero. The probe initially used stale scratch
  duration after an unknown RMW; comparing the caller's published duration fixes
  the diagnostic without changing native arithmetic or any expected reference.
- Additional reference coverage: opponent reflection transition and further
  support loss/reacquisition; frame 1823 reaches a special landing transform.
  Remaining native dependencies: coupled horizontal direction/AI, reflection
  transition, pose overrides, full slope/recontact and control-state closure.
- Native component build: `clang++ -std=c++20 -Wall -Wextra -Wconversion -I
  src/core tests/native/vertical_contact_probe.cpp src/core/vertical_contact.cpp
  src/core/track_sampling.cpp -o artifacts/m4-12/vertical_contact_probe`, bounded
  at 120 seconds. Exact short comparison: `PYTHONPATH=. python3
  artifacts/m4-12/check_contact.py`, subprocess bounded at 30 seconds.
- Disk briefly reported ENOSPC; exact duplicated payloads from the two new
  captures were verified then hard-linked (58,826,797 bytes deduplicated),
  preserving both capture reports. No unrelated files were deleted. Available
  disk subsequently recovered to 2.9 GiB. Fresh usage sample: 1% weekly used.
- Next: finish two fresh WRAM/cartridge-RAM projections with
  `python3 -m tools.unirally_lab.native.zoom_zoo_trial_reference --access-dir
  artifacts/m4-12/expanded-primary-a --out artifacts/m4-12/reference-a.json`,
  then freeze the expanded field/content contract before whole-update tuning.

### Checkpoint — native primary through 1822, recorded at 10:51 UTC

- Frozen expanded reference commit: `302f167`. Current uncommitted native runner
  and update match all 394 declared bytes from one seed through frame 1822
  (173 consecutive updates). First stop: frame 1823 special-tile landing
  transform. Native runtime receives seed, static files and controllers only.
- This is **not accepted closure**: the screen-coordinate boost-decay predicate
  still has an explicit provisional constant in the experimental update; it
  must be recovered/removed or justified by a source-derived invariant before
  claiming zero dynamic dependencies. Full guard/read audit remains pending.
- Recovered during differential work: reflection's ordinary phase gate is the
  inactive rider phase even while a transition is ongoing; the idle oscillator
  clears on pose override; first-probe signed-negative penetration seeds the
  reducer; reserved 0x1C00 marker penetration 127 uses the auxiliary early return,
  retaining previous uncorrected coordinates and incrementing duration at byte
  width. Sentinel recontact uses signed horizontal displacement and the current
  selected surface angle. Original instruction ranges are linked in code/prose.
- Native executable: `artifacts/m4-12/zoom_zoo_runner`; controller/seed/content
  files in the same ignored directory. `PYTHONPATH=. python3
  artifacts/m4-12/check_native.py` runs the 30-second bounded comparison and
  records first divergence. Broad suites/review/variation/restores not yet run.
- Fresh usage at 10:46 UTC: 4% weekly used, 96% remaining. No reset or purchases.
- Next experiment: frozen frame-1823 landing transform reads tables at WRAM
  0572/076A/0962. These are unchanged through the expanded interval and first
  match final contents at frame 1297. Capture their initialization writers on
  frames 1296--1297, recover static ROM derivation, freeze an additive content
  supplement, then implement signed multiply/high-byte selection. Exact command
  is `artifacts/m4-12/landing-table-init/command.json` (180-second worker bound).

### Checkpoint — primary exact, approximately 11:02 UTC

- Native experimental run now matches all 394 frozen bytes at all 201 states,
  seed 1649 plus updates 1650–1849. Frame 1824 needed the original $829D7F
  landing-path clear of the reflection-air-turn counter. No later state input.
- Account usage: 6% weekly used, 94% remaining; review reserve intact.
- Dependency audit found the opponent speed predicate reads a retained OAM byte
  at $150B: authentic seed value 101, no overlapping writer in the complete
  primary access inventory. Player boost ($11D9) is zero throughout. Freeze an
  additive one-byte seed field rather than retain a made-up screen constant;
  preserve the original 394-byte reference as a prefix. Reject player boost
  requiring the unrecovered camera predicate. This guard must also hold on
  independent cases. Broad validation, restores and review remain pending.
- Next: implement the additive seed field and reproducible harness, audit
  reached read dependencies and guards, then submit an immutable candidate
  to fresh Sol/medium review with independently selected input variations.

### 45-minute reassessment — 11:09 UTC

- Current committed reference base `4e96a23`; candidate implementation/tooling
  is uncommitted pending focused checks. No main integration or review yet.
- All 395 canonical bytes match all 201 states in two fresh original captures;
  primary native comparison and fresh-process restores at 1700/1804/1823 pass.
  Static extractor reproduces all frozen files from the ROM and pre-race setup.
- Removed provisional screen constant: opponent retained OAM byte is explicit
  seed state; player boost requiring camera state rejects. No later captured
  input enters the native runner; bounded dependency audit is documented for
  independent scrutiny. Right/neutral controls only; no production frontend.
- Fresh usage 7% weekly used / 93% remaining at 11:09 UTC (account-wide).
  Elapsed roughly 45 minutes; review reserve remains intact; no reset/purchase.
- Continue this approach: native expanded target is exact, and remaining work is
  coherent candidate validation, fresh Sol-owned variations/review, then full
  integration. Next commands are the documented `zoom_zoo_trial` captures and
  compare, focused native tests and app-debug synthetic suite. No user blocker.

### Validation checkpoint — 11:18 UTC

- Candidate `43198c6242ae2d55d025704522f947f88d2ed84e` is frozen and pushed
  to private `origin/task/M4-12-native-zoom-zoo`; remote ref was verified exact.
  Fresh Sol/medium reviewer `/root/m4_12_review` owns isolated checkout
  `.worktrees/m4-12-review`, report and preregistered independent cases.
- Primary native all 395 bytes / 200 updates and three restores pass in debug
  and sanitizer builds. Clean app-debug/app-sanitize each pass 400 checks.
  Frozen contract/pack/replay, full-race/opponent-first/restores and both
  presentation contracts pass; R-0030 records report identities and commands.
- Corrected an agent-created cache layout: local/artifacts are now real
  directories in the primary worktree, with cache reuse below them. Earlier
  environment/path failures are retained as non-passes. No game source changed.
- Hosted task-branch CI run 34754013410 is in progress. Main remains untouched
  and synchronized at dispatch. Next: handle independent findings/cases, obtain
  approval, integrate, get final-tip CI and verify private main synchronization.

## Accepted trial handoff

- Actual integration: `1889d9cbc56fec4437bc67835cd885677cc29e03`; reviewed
  implementation `aef2245e9b90ae648356752ca45e7a234910d836`. Independent Sol
  approval `9455a462b5d0e6ba6f8516148f5aa47213051ce4` is preserved in the review
  record and incorporated as `c220418`. Integration code/build/test source
  trees equal the tested candidate; subsequent closeout changes are docs only.
- Exact native interval: 200 updates, both riders, all 395 bytes, 1650–1849.
  Dynamic captured runtime inputs: zero. One authentic seed, 18 authenticated
  static files (16 read by runner), controller rows. Native runtime has no ROM,
  emulator, reference samples or later-state input. Static matrix extraction
  stops at pre-race frame 1297. Unsupported branches/controls fail closed.
- Independent untuned cases: neutral 1818–1826 and neutral 1769–1781, both
  materially change throttle/velocity/control state through 1849; exact full
  horizon and restores 1700/1804/1823. Neutral 1650–1660 exposed missing low-speed
  direction-counter gating; one correction round fixed it and retains it as a
  disclosed regression. Original failing evidence remains in the review record.
- Corrected-source validation: app-debug/app-sanitize 400/400 each, primary and
  all cases under sanitizers, DRAGSTER full-race/restores 18/18, opponent-first/
  restores 12/12, winner presentation 8/8, loser 2/2, ZOOM contract 3/3, Classic pack 3/3
  and two-fresh-run ZOOM replay 24/24. No required checks skipped. Exact report
  hashes and command arrays: R-0030 and the corrected private validation inventory.
- Hosted candidate CI 34754212973 passed macOS 15 and Ubuntu 24.04. The first
  candidate's CI was cancelled by its correction push, not counted as passed.
  Final main push/ref and final-tip CI are verified during closeout; use current
  git refs and the main workflow run for that immutable documentation successor.
- Measured startup to independent approval: approximately 60 minutes (10:24–11:24
  UTC); closing validation/synchronization follows. Account-wide usage grew
  from 0% to 11% by 11:24 (89% remaining); unrelated work may contribute. One active
  Sol reviewer, no implementation child, reset, purchase or provider fallback.
  The 20% review reserve was never approached.
- Trial assessment: a sustained capability task advanced from captured-state
  research to native two-rider continuation across later transitions, while
  independent review caught a real missing branch. This does not isolate a
  causal model-speed/cost advantage or accept full-track ZOOM ZOO/M4. General
  model policy is unchanged; the D-0006 trial ends here.
- Remaining product work: full-track finish, broader controls/player landing,
  camera-dependent boost, second-track presentation/audio. A future authorized
  experiment should freeze a larger/varied domain targeting those rejected
  paths; M4-13 remains unclaimed and was not started. No user blocker remains
  for the completed M4-12 scope.
