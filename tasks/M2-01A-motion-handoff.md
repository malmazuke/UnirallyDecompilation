# M2-01A motion worker handoff

Base/head at claim: `ba71142a6d766216772360949dd8c04760fa9ffe`.
Worktree `.worktrees/m2-01a-motion`, branch `codex/M2-01A-motion`.
Baseline doctor/bootstrap/debug build/synthetic pass (211 checks,199 Python).
Reports: `artifacts/m2-01a-motion/baseline/`. First replay invocation used an
invalid `--out` flag and exited3; corrected command uses `--artifacts`.
Preregistered primary ordering and independent variation in R-0011-motion.
Next: run motion_research.capture for primary and preregistered variation,
recover pose/jump integration and GO transition at1534. No withheld access.

Checkpoint 1: primary/variation isolated pose+jump arithmetic pass1548 values
each. Fresh full-primary capture passes original digest; full pose probe failed
on stale unwatched `$0E7B/$0FE3`, new full-primary-v2 capture in progress adds
those watches and integration/control addresses. No withheld opened. GO1534
recovered from countdown69, previous brake1, throttle432 and signed residue-1;
see R-0011-motion. Next rerun full-primary-v2 probe, then AI and spin/input
producer contract. Exact reproduction entry points are capture/probe modules.
