// Deliberately fails to compile when LAB_PROBE_COMPILE_ERROR is ON, so the
// tooling can prove that a failed build is never reported as available.
#error "deliberate compile failure for the report pipeline test"
