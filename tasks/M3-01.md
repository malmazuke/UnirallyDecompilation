# M3-01 — Native finish and full-race state

## Assignment

- Status: accepted — integrated 12 September 2026 after three independent
  review rounds and green macOS/Linux CI
- Milestone: M3, first implementation slice
- Coordinator: OpenAI Codex coordinator
- Task provider: OpenAI unless the user explicitly moves the continuation
- Worker/session/runtime/model: fresh Codex subagent `m3_01_worker`,
  `gpt-5.6-sol`, medium reasoning
- Actual model/reasoning effort and routing rationale: `gpt-5.6-sol`/medium for
  routine bounded reference research and implementation under D-0004; no
  frontier escalation is presently justified. The coordinator runtime reports
  Codex/GPT-5; its reasoning setting is not exposed to the task.
- Provider quota: resample at claim. Last M3-00 review sample was 44% used / 56%
  remaining in the seven-day Codex window on 12 September 2026; preserve the
  final 20% and the session's ten-point discretionary cap. This is account-wide
  telemetry, not task-attributed usage.
- Reviewer: three fresh sequential Sol/medium sessions; the first two returned
  exact arithmetic findings and the third approved submitted head `be63215`
- Dependencies: accepted M2 at tag `m2`; accepted M3-00 at `0bc4938`,
  [R-0012](../docs/research/R-0012-complete-race-evidence.md) and the
  [finish-state contract](../docs/state/race-finish.md)
- Base commit: `3d257c0b6905f8ca152ac5a774018341e874031a` (current `main`,
  which contains accepted M3-00 and the ready-task handover)
- Branch and isolated worktree: `task/M3-01-native-finish` at
  `.worktrees/m3-01-native-finish`
- Owned paths and shared interfaces: `src/core/` native state/update/runner,
  their CMake/C++ tests; `tools/unirally_lab/native/` and focused tooling tests;
  new full-race native manifests/expectations; additive finish-field/reference
  manifests needed for a freeze; M3-01 evidence/task records and relevant
  command documentation. Existing M2 cases/expectations, M3-00 input manifests,
  core patch/lock and accepted evidence are frozen.
- Claim/lease/checkpoint: claimed by the coordinator at 2026-09-12 16:43 AEST;
  no live `reference/worker.py` process was present. The worker will checkpoint
  in this task record at least every ten minutes and before expensive captures.
- Session/authority: reassess each 45 minutes and checkpoint at least every 10;
  one child only. Task-scoped commits/private CI allowed. No purchase, reset,
  publication, deployment, provider switch or tracked original content.

## Outcome and boundaries

Extend the human-readable native Crawler/DRAGSTER simulation from M2's frame
2999 boundary through both frozen M3-00 complete-race paths. Match the observed
per-rider finish flags, the 240-update post-player delay, neutral-axis override,
race timer and the future-affecting stored time/outcome/transition state needed
to continue deterministically. Revise canonical state explicitly, with
fresh-process save/restore checks around the finish boundaries.

The first action is a bounded reference experiment for the result-screen stored
time/outcome fields that M3-00 deliberately left unknown. Freeze its fields and
expected projections before native code computes them. If another
future-affecting dependency is found, record and freeze the smallest prerequisite
rather than injecting captured state or inventing a plausible rule.

Not in scope: native graphics, rider-frame mapping, BG1 gather, SDL/live input,
audio, menus, complete trick support, another track/rider/mode or M3 acceptance.
Those remain M3-02 through M3-04. Do not execute the original CPU or consume
per-frame reference values in the native runner.

## Inputs and prerequisites

- Frozen complete-race replays
  `race-crawler-dragster-12000-continuous-right-fields.json` and
  `race-crawler-dragster-12000-release-3000-3299-fields.json`, with sample
  digests `22e9babdcb867936…` and `d24a12347bcda335…`.
- M3-00 finish contract: player flags at3213/3318, opponent flag at3214,
  delay updates3214–3453 /3319–3558, derived axes forced neutral after the
  ordinary writers, black load on the next frame.
- Accepted M2 native cases,333-byte `URMV0001` serialization and continuation
  hash `7c799b4393d171f2`; all remain regression requirements.
- PAL ROM/core identities from R-0012 for reference-only freezing. ROM, traces,
  decoded bytes, states and large samples stay ignored.

## Acceptance

| Criterion | Command or experiment | Expected result | Required artifact |
| --- | --- | --- | --- |
| Stored result contract frozen first | Targeted access/controlled-input experiment on both M3-00 paths before native implementation | Per-rider stored time, outcome and future-affecting transition fields have exact addresses/writers/order or are explicitly proven not needed in the tested continuation | Additive state/evidence record and dedicated reference-freeze commit |
| Exact full-race native agreement | `native compare` (or an additive successor) on both frozen M3-00 paths through at least the first stable result screen | Exact agreement on all frozen gameplay/finish projections with useful first divergence; no original CPU or per-frame reference input | Two full-race case manifests, frozen expectations and JSON reports |
| Finish update order | Focused C++/Python boundary tests and reference comparison around3213–3214,3318–3319, delay239/240 and first post-delay update | Rider instances, next-frame dispatcher reaction, neutral override and transition timing match R-0012 exactly | Authored boundary tests plus native reports |
| Explicit state revision | Serialization inventory, malformed-input tests and source review | New future-affecting fields use a named format revision; no hidden globals/padding/host-order dependence; accepted `URMV0001` cases remain readable/reproducible or have an explicit compatible path | State schema/source guide and round-trip tests |
| Continuation | Fresh-process restore checks before/at/after both player finish boundaries and delay completion | Resumed suffix canonical state and projections equal uninterrupted native execution | Restore reports and platform-independent hash |
| M2 frozen regressions | Existing three native compare/restore cases plus synthetic/debug/sanitizer tests | All 13 M2 projections and `7c799b4393d171f2` continuation identity remain unchanged; no required skip | Reports on exact candidate |
| Independent review/integration | Fresh checkout with reviewer-owned finish boundary or input case, then coordinator merge candidate | Reviewer approves exact behavioral commit; local full matrix and macOS/Linux ROM-free CI pass | Review record, exact commits and CI/local reports |

## Evidence and attempts

| Attempt | Hypothesis | Experiment | Observation | Next decision |
| --- | --- | --- | --- | --- |
| 0 | The accepted finish flags/delay are sufficient to define the next update boundary, while stored result time/outcome remains the only known research prerequisite | M3-00 R-0012 and state-contract audit | Claimed at `3d257c0`; seven-day Codex usage is 44% used / 56% remaining, no credits balance, no live reference worker. No M3-01 experiment run yet | Reproduce M2/M3-00 baselines, then freeze the missing result-state contract before implementation |
| 1 | The assigned checkout and ignored fixtures reproduce the accepted starting state | `native compare` primary; `native restore-check` at 1631/2200; M3-00 compact analyzer against accepted ignored artifacts; `test --suite synthetic` | Claim head `db0ce93`: primary compare and both restore boundaries pass; analyzer passes 25/25 with SHA-256 `cfe83e3e...3416`; baseline suite passes 287/287. The first compare attempts correctly reported missing runtime/toolchain until the ignored accepted runtime was copied and bootstrap completed | Narrow result-state addresses from the accepted suffix access record, then run one bounded targeted capture before freezing projections |
| 2 | Stored result time is written at crossing, and winner/loser needs no additional original persistent word in this tested continuation | Targeted access captures of both frozen M3-00 inputs, frames3000–3680 /3180–3800, candidate WRAM/SRAM watches and a bounded `$0D00`–`$11FF` series | Both accepted sample/final digests reproduce; exact digit, centisecond, flag and delay writers are frozen in R-0013. Player times are3357/3566, opponent3358. The controlled outcome is determined by opponent-finished state when the player crosses | Commit the additive reference freeze before any native finish computation, then revise semantic state and comparison |
| 3 | An appended semantic finish substate can preserve M2 bytes while matching both full-race paths | Dedicated freezes `dcbc896`/`c5f2b68`; candidate `native finish-check` on continuous at3212/3213/3214/3453/3454 and variation at3213/3214/3317/3318/3319/3558/3559; C++/Python authored tests | Both projections are exact through3453/3558, finish/result contracts through3679/3800, two fresh processes repeat, and every restored suffix is canonical-state identical. `URMV0001` remains333 bytes and `URMV0002` is369 bytes after first crossing. Debug CTest17/17 and native CLI20/20 pass | Run complete frozen M2/debug/sanitizer regressions, update final evidence, commit focused candidate and submit for independent review |
| 4 | The finish extension leaves accepted M2 behavior and platform-independent tests unchanged | Three frozen M2 compares; primary restore1631/2200; release restore2761/2787; debug CTest; full `lab-sanitize` suite after an explicit sanitizer build | All comparisons/restores pass; debug CTest17/17; sanitizer passes 268 Python,17 CTest and three-process hash `0be347c529fadda9`. The earlier sanitizer invocation correctly reported its missing build and was rerun after building | Inspect/stage candidate, commit, reproduce focused reports at the exact commit, then request review |
| 5 | The returned speed37 mismatch is caused by ordering/general decrement semantics, not an isolated missing table entry | Reproduced reviewer case; inspected PAL `$83:E90D`–`$83:E932` and ordered access writers; replaced seven-value tail with non-crossing10 pre-adjustment, ordinary limiting and conditional24 neutral-drive removal; authored neighboring arithmetic test | Reviewer `37 -> 1 -> 0`, continuous `38 -> 2 -> 1 -> 0`, and release low tail all match; reviewer restores3226/3227/3228 and both original finish/delay restores pass. Divergence now includes values, prior sample and previous/current inputs | Run M2 and debug/sanitizer regressions, commit correction and submit exact head for fresh re-review |
| 6 | The second returned issue is the PAL negative equality asymmetry in the public helper | Inspect latest `tasks/M3-01-review.md`; change negative test from `<= -10` to `< -10`; assert positive9/10/11 and negative-11/-10/-9; focused `ctest -R movement_first_update`; full debug and sanitizer synthetic suites | Exact PAL boundary now gives `-11 -> -1`, `-10 -> -10`, `-9 -> -9`, while positive10 still reaches0. Focused CTest passes; both ROM-free suites pass 288/288 (268 Python,17 CTest, three-process hash `0be347c529fadda9`) with no sanitizer finding | Commit only helper/test/handoff and submit the correction for another fresh review |

## Handoff

- Current behavioral head: `66657d3`, the signed-boundary correction above
  returned review commit `b959bba`
- Verified findings: exact native gameplay rows through3453/3558; stored
  3357/3566 player and3358 opponent times; outcome/order, delay and result phase
  through3679/3800; V1/V2 fresh-process continuation at every declared boundary
- Commands executed: baseline and captures above; reviewer and both original
  full-race checks/restores; all three M2 compares; primary/release restores;
  corrected debug and sanitizer suites. Clean exact-head reports are
  `artifacts/m3-01-rereview-{reviewer,continuous,release}/report.json`, the five
  `artifacts/m3-01-rereview-m2-*` directories, and
  `artifacts/m3-01-rereview-{debug,sanitize}.json`. Signed-boundary suites are
  `artifacts/m3-01-signed-boundary-{debug,sanitize}.json`, both pass288/288,
  with report hashes `1cad991e...09e7` and `cd40141e...bdfa`
- Unavailable/skipped: no required local or hosted check was skipped
- Exact next experiment: M3-02A inventories every current runtime content read
  and freezes the logical Classic pack schema before implementing extraction
- Remaining dependencies: none for M3-01; M3-02A is ready
- Runtime needs: local ROM/core and ignored M3-00 artifacts were used for the
  private freeze/differential gate; public CI remained ROM-free
- Usage/outcome: claim sample was44% used and signed-boundary correction started
  at51% used. The post-CI account sample unexpectedly reports1% used and no
  credit balance; treat this discontinuity as account-wide telemetry rather
  than task attribution. The available reset credit was not redeemed.
- Corrected result hashes: reviewer final/series/report
  `cfe518bb...f12a`/`386fefeb...4022`/`d3bd8410...d7bd`; continuous
  `ca6bab5f...46fd`/`5b3da104...318c`/`a1f73ee0...dd5`; variation
  `eb2d3321...22ac`/`728a9f68...cb17`/`082fa808...33eb`; debug/sanitizer
  reports `f8ea09e3...cd8e`/`70054f21...411a`
- Review/integration: first review returned speed37; second returned signed-10
  equality; final review commit `5741a2c` approved exact submitted head
  `be63215`; coordinator merged it as `c393c54`

## Review and integration

- Reviewer/results: final fresh Sol/medium review approved exact submitted head
  `be63215` after independently confirming PAL bytes, all six signed boundaries,
  focused CTest1/1 and debug/sanitizer288/288 suites
- Required changes/rationale: replace enumerated positive low-speed tail with
  general arithmetic, then preserve PAL negative equality; implemented with
  explicit authored boundaries and locally green
- Exact merge candidate/checks: merge `c393c54`; all three full-race cases and
  their18 restore boundaries pass, all three M2 comparisons and four M2 restore
  boundaries pass, and debug/sanitizer suites each pass288/288 with repeat hash
  `0be347c529fadda9`
- Integrated commit/evidence: `c393c54b22476a58f0546a4c5e8f9329ef565098`;
  local reports under `artifacts/m3-01-integration-*`; private CI run
  `34682900511` passed macOS15 and Ubuntu24.04, including Linux sanitizers
- Scope still unverified: other track/rider/mode, presentation, live controls
  and playable packaging remain explicitly outside M3-01
