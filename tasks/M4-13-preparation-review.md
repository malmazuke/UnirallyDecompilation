# M4-13 preparation — independent documentation review

- Candidate: `c4eb59e8f48fbf15394ae7e3caa5f7b18e8bf79a`
- Reviewed range: `07e7ffa6f35a90e6350665613901e87623ca5af9..c4eb59e8f48fbf15394ae7e3caa5f7b18e8bf79a`
- Review branch/worktree: `review/m4-13-preparation`,
  `.worktrees/m4-13-preparation-review`
- Reviewer: fresh OpenAI `gpt-5.6-sol`/medium documentation reviewer
- Verdict: **approved without a material finding**

## Assessment

The preparation defines one bounded successor capability and keeps the accepted
M4-12 result frozen. M4-13 must reproduce all 1650--1849 behavior, authenticate
a player support-loss/landing event from state transitions or writer traces, and
freeze its controller script, event, landing frame, fields, identities and a
horizon containing at least 100 updates after landing before native tuning. It
explicitly requires an additive harness extension because the implemented M4-12
surface is fixed to 1650--1849; it does not advertise arbitrary horizons as an
existing capability.

Acceptance is feasible and resists fixture-specific success. The native primary
must close every newly reached future-affecting producer and field with zero
captured runtime state. A fresh Sol/medium reviewer in an isolated immutable
candidate checkout owns two preregistered input-timing variations. Each variation
must exercise its own materially different player landing/recovery, freeze its
own reference horizon before comparison and run at least 100 updates after its
landing. The primary and both reviewer cases require fresh-process restores both
before and after landing through their full horizons. Inert or tuned cases cannot
count, and a withheld failure used for a fix becomes a retained regression.

D-0004, D-0006, AGENTS, workflow, state, plan, registry and handoff consistently
assign direct recovery/native implementation to Astra/medium and automatic fresh
Sol/medium review, correction and re-review. The M4-13 exception waives the
20-percentage-point task cap while retaining the final 20% weekly allowance for
review, fixes, integration and recovery. The 13%-used/no-credit observation is
historical, fresh startup telemetry is required, and no reset, purchase or
provider switch is authorized. Focused gates precede first review; the full
frozen M4-12, DRAGSTER, content and presentation matrix follows resolved findings,
with affected revalidation after later changes and final-tip hosted CI/private-main
verification.

The documented cache correction uses real checkout-local `local/` and `artifacts/`
directories, read-only verified cache links only beneath `local/`, and distinct
fresh run/report directories. That resolves the retrospective's observed symlink
and report-placement failures without changing gameplay evidence.

## Checks and limits

- `git rev-parse HEAD` reproduced the exact candidate and `git diff --check` on
  the reviewed range passed.
- The changed set contains documentation and a Codex configuration comment/default
  annotation only; no gameplay source, frozen manifest or expected result changed.
- I inspected the M4-13 task, NEXT_SESSION, D-0004, D-0006, R-0031, STATE,
  AGENT_WORKFLOW, PROJECT_PLAN, BUILD_AND_VALIDATION, AGENTS and their cross-file
  policy references.
- The retrospective's ignored private closeout artifact is unavailable in this
  review checkout, so I did not independently validate that link or its contents.
  This does not affect the preparation contract; integration validation owns the
  private artifact check.
- Game/build suites were deliberately not run for this documentation-only review,
  as assigned. Historical gameplay results were not counted as candidate testing.

No candidate file was changed by this review.
