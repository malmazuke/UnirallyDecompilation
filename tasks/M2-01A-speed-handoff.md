# Speed research handoff

Coordinator isolated `.worktrees/m2-01a-speed`, branch`codex/M2-01A-speed`,
base`2d26b1e`. Submission is the commit containing this record. New research
model/capture/probe/tests only; existing source/reference files are unchanged.

`doctor`, `bootstrap`, baseline`build --preset lab-debug` passed. Fresh primary
capture from the tracked command reproduces sample/final-state expectations;
all14,660 isolated output values and1,466 counter recurrences match. Eight
ROM-free authored boundary tests pass. See R-0011-speed for exact ROM/core/input
hashes, formulas, mapping, arithmetic domains and commands. The first probe
matched; no model fitting or frozen-output changes were needed. A preliminary
linear disassembly used the wrong mode for counter code; observed coverage
mode3 resolved it to an8-bit increment/AND/store before the contract was recorded.

Next: clean-source full suite, independent review of critical cap/decay ordering
and a boundary, then readable native port integrated with contact/motion.
Research receives captured arguments and cannot accept autonomous movement.
Mode2/42 vertical cap remains explicitly unsupported. No withheld series opened,
no user-only prerequisite and no extra service/purchase required.
