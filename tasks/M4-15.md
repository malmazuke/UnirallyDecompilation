# M4-15 — Native ZOOM ZOO race completion

## Assignment

Status: reviewed; integration, remote ref and final-tip CI acceptance conditional. Accepted ancestor: `38c72e889d59b62270be6e26fca713b3fb3aa153`.
At claim, record the actual synchronized main tip containing this preparation,
clock, model/effort, quota, branch and worktree. Use `codex/m4-15-race-completion`
in `.worktrees/m4-15-race-completion` (record alternatives if occupied).

Read NEXT_SESSION, STATE, AGENTS, AGENT_WORKFLOW, D-0004/D-0006, PROJECT_PLAN,
BUILD_AND_VALIDATION and M4-14/R-0033/review. Reproduce the accepted sustained
primary and restores with fresh outputs before new work. Reuse authenticated
local fixtures/caches by hash, not by path alone.

Primary: Astra/medium directly investigates, implements and integrates. Fresh
Sol/medium independent review is automatically launched in an isolated checkout,
with explicit model/effort and no inherited conversation. One active child.
[D-0004](../docs/decisions/D-0004-model-and-usage-budget.md#m4-15-race-completion-extension)
waives the 20-point task cap but preserves the final 20% weekly reserve for
review/recovery. Sample quota at startup and checkpoints; no reset, purchase or
provider switch is authorized. Ten-minute checkpoints and 45-minute reassessments
are continuation points, not smaller task boundaries. A resource stop preserves
an incomplete handoff. Do not automatically dispatch M4-16.

## Outcome and reference feasibility

The intended outcome is **a complete native ZOOM ZOO race from the authentic
end-1649 seed**, escaping the repeatedly visited section and reproducing the
original's authenticated race result and stable post-finish continuation.
Preserve the accepted pre-seed history. No later seed, captured runtime repair,
CPU fallback or longer run trapped in the same section satisfies this target.
This proves completion from the existing seed, not native initialization from
race start, a playable frontend or general coverage of every input.

Reference exploration is the first internal phase of this task. Before native
tuning, explore controller strategies in the identified original PAL ROM using
repeatable scripts. Authenticate track markers, lap/progress and finish writers;
use them to distinguish obstacle escape and new track coverage from temporary
counter increases or elapsed frames. Keep meaningful failed strategies and the
reason for each change. Preserve the original opponent policy unless a justified
scenario decision is recorded before freeze; no altered game content or cheats.

Aim to demonstrate an entire reference race. Freeze the successful controller
timeline, seed/static identities, horizon, state inventory, escape/progress
criteria, race-result semantics and at least 200 post-finish updates in two fresh
matching processes before native implementation. Extend tooling if needed;
the current sustained laboratory's frame limit is not an acceptance boundary.
Reference-discovered state extensions must be frozen additively before tuning
against them; preserve old contracts and disclose later inventory corrections.

A substantial downstream milestone is a fallback only if evidence-led original
exploration cannot establish race completion. Record attempted strategy families,
why they fail, authenticated escape, distinct downstream markers reached and
remaining uncertainty. Before native tuning, freeze that alternative outcome and
its full horizon in two runs, with an explicit rationale subject to independent
review. Time pressure, a native divergence, a difficult new producer or an easy
short prefix do not justify lowering the target. Report any accepted fallback
as partial race progression, never completion; leave the full-race objective
outstanding. If neither reference outcome is established, continue investigation
within this task until a real resource/access boundary; do not accept research
alone as the native capability. No routine user decision is required.

## Implementation scope

Recover every coupled gameplay dependency needed by the frozen outcome: geometry,
contact, motion, control/pose, boost/camera inputs, opponent AI, progress/laps,
finish/result and future-affecting state/serialization. Own necessary source,
extraction/content contracts, harnesses, tests and evidence; coordinator owns
registry/integration. Keep readable functions, explicit integer semantics and
address/experiment links under D-0003. Small experiments and commits are welcome;
per-producer acceptance tasks are not. Preserve DRAGSTER and all M4-12–14 frozen
evidence. No speculative abstractions or generic validation-cache project.

Frontend, rendering/audio, other tracks/modes, menus, editor/networking and
publication are excluded. Output-side presentation is excluded only after its
lack of feedback into the accepted simulation is authenticated. Do not discard
reached gameplay dependencies by calling them presentation.

## Acceptance

- Both riders: every declared future-relevant state byte matches each update
  from end-1649 through the complete frozen outcome, including escape, downstream
  markers and finish plus 200 post-finish updates (or the disclosed fallback).
  No later dynamic captured inputs; complete reached producer/read inventory.
- Primary reference repeats in fresh processes. Authenticate ROM/core/content,
  expanded input timeline and whole-WRAM identities for supplemental audits.
  Follow the new preflight and lightweight validation ledger in BUILD_AND_VALIDATION;
  implement the missing automatic safeguards in this task, not during preparation.
- Fresh Sol chooses two material untuned variations after candidate freeze.
  Qualify and freeze each original twice before native evaluation; both must
  escape and reach the same outcome, including their own finish continuation
  for full-race acceptance. Extend case horizons where needed. Inert changes or
  cases trapped in the old section do not qualify. A failed case used for tuning
  becomes a regression and requires a fresh untuned replacement.
- Fresh-process restores before/during/after escape, new modes, lap/finish
  transitions and late continuation match uninterrupted native/reference runs.
  Malformed newly serialized state is rejected. Native execution with ROM,
  repository and reference access denied passes, with meaningful negative controls.
- Preserve all accepted differential/restores and DRAGSTER/content/replay/
  presentation gates; complete debug/sanitizer, independent review and exact
  merged checks. Hosted macOS/Linux CI is distinct from private Linux differential
  evidence. Missing/skipped prerequisites are not passes.

Run focused checks before initial review, broad gates after corrections and
appropriate revalidation thereafter. Ledger reuse cannot replace independent
review, fresh withheld evidence, sanitizer coverage or required merge/CI checks.
The primary fixes findings and requests re-review without user relay.

## Closeout

Use consolidated closeout: prepare reviewed code/evidence and tracked handoff
before the final main push; keep remote acceptance conditional until observed.
Push accepted integration to the existing private origin, verify exact refs and
final-tip CI. Record final SHA, commands/results, CI, time and shared usage in
ignored `artifacts/m4-15-integration/closeout.json`. If missing, recover status
with git refs/remote and `gh run list --commit <sha>` / `gh run view <id>`.
Do not make a second documentation-only push to transcribe success.

Report actual gameplay outcome, exact domain, review findings, unavoidable versus
avoidable work and remaining product limits. Completion of this task alone does
not establish all M4 gates or authorize a milestone tag. Stop after acceptance
or a genuine resource/access boundary; no M4-16 dispatch.

## Execution checkpoint

- Dispatch: `931b997f136ffe0a99f0012bf8e070cfd8a7b46f`, fetched and equal to origin/main.
- Start: 2026-09-13T20:40:37.922111+00:00; OpenAI Astra/medium primary.
- Weekly usage: 38% used / 62% remaining; reset epoch 1789899841; zero reset credits. Preserve final 20%; no reset/purchase/provider switch.
- Branch `codex/m4-15-race-completion`, checkout `.worktrees/m4-15-race-completion`.
- Next: reproduce sustained primary with fresh original processes and native restores, then original-only strategy exploration.

- Startup doctor and app-debug build passed. Fresh sustained original rows match `dce7c14b80c66e4bd831cff3733c6cba02d1c4c40cefd96ae2760bf6d4fa74de`; native primary and all declared restores pass (`artifacts/m4-15/baseline-compare.json`). Fresh extraction authenticates all static files.
- Original exploration: continuous Right to 11999 never finishes player; opponent first sets $0F01 at 6488. Player X remains 9200..14281 and transition count decreases in the initial approach. Hypothesis: Right travels against the initial track direction; test continuous Left before inventing obstacle-jump strategies. Original-only `zoom_zoo_race_explore` records observations and new constant transitions; not an acceptance contract.

### Reference outcome freeze (2026-09-13T20:50:29.805008+00:00)

Original-only strategy families: Right through 11999 remains in old section (no player finish); Left through 11999 reaches count 23 and stalls at direction reversal; marker-guided Left/Right finishes player 6484/opponent 6488; adding marker-selected B also finishes (6485/6488). Fixed primary has 21 direction segments and no reactive native control. Four fresh fixed captures match all 5,076 WRAM observations through 6724; C/D additionally retain matching cartridge RAM and the additive 517-byte projection. Freeze rows `241dc277bf823ca9b78614c66d91d6ed7713322affe922855c1dedac64b27686`. Full original instruction audit is running with timeline preflight; native tuning has not begun.

Outcome choice: complete three-lap race, both authenticated finish flags, entire 240-update player finish display (236 after opponent). This is stable post-finish simulation; subsequent result-screen load/rendering remains outside this native domain. Original checkpoint/finish source `$818050-$8182B6` decrements lap count and writes lap times/finish flags; dynamic audit authentication pending. No fallback selected.

Initial additive inventory: preserve 423-byte prefix; per-rider lap/checkpoint/start latch/display countdown/finish flags and five stored timer words, both ten-slot SRAM lap arrays and totals, provisional `$1225/$1227`, finish delay. New audit-discovered corrections must be additive and disclosed. Quota checkpoint: 39% weekly used, no reset.

### Additive camera inventory correction

The full audit authenticates 92,743,600 instructions and all 5,076 WRAM states. First native movement divergence after the player leading-event queue routing is frame3003: `$1225` written by `$82AD7A` at3002 causes the three-unit speed adjustment at `$82A705-A71C`. This is gameplay feedback from camera visibility, not excludable presentation. Before camera tuning, v2 freeze adds six camera words ($041D/$0421/$04F9/$04FD/$0553/$1513), total529 bytes, retaining the original517-byte freeze unchanged. Native accepts only the new task format; old M4-12/14 contracts stay unchanged.

- 20:58 UTC checkpoint: weekly41% used. Native movement first difference3219 exposed progress publication ordering: full audit shows limiter reads player127 at sequence1650, player progress writes128 at2212, opponent limiter then reads128 at2869. Move publication after each rider motion/pose, preserving per-rider order. Before lap-display tuning, v3 adds20 original checkpoint-seen bytes ($114D-$1160): first opponent checkpoint suppresses its display countdown to2 at `$81CA71`, dependent on shared first-crossing flags. All prior freezes remain unchanged. Current code is unreviewed/incomplete.

### Native candidate checkpoint

21:09 UTC usage44% weekly used (start38%). Original full race established; native565-byte state matches all5,076 rows end1649–6724, both finishes6484/6488, full240 player post-finish updates. Focused21 CTests and8 Python checks passed. Source/field recovery and deliberate inventory extensions are in R-0034. Final candidate primary/restores and accepted sustained baseline will run on the frozen code before automatic fresh Sol review. Preliminary restore report is superseded because source changed during its run; validator now rejects source/binary changes. No quota reset/purchase.

### Independent review dispatch

Fresh OpenAI Sol/medium `m415_review`, no inherited conversation, isolated `../m4-15-review` at `e038a5b1a3fe9e1064024eb51b1749400319431e`; weekly45% used. Candidate primary passes565 bytes,5075 updates and465 fresh restores; accepted sustained primary and denied-ROM/repository execution with negative controls pass. Reviewer owns two material untuned original race variations and review report; primary will handle findings and re-review automatically. Broad gates are prepared but not started.

### 45-minute reassessment

At21:27 UTC, full primary completes and review has exposed two material throttle divergences on independently frozen completing variations. Continue recovery and re-review; weekly47% used at21:23 versus38% start, ample review reserve. Both cases become retained regressions, and fresh withheld completion is required. No resource/user boundary or fallback. Source inspection identifies the inverted-tile zero-velocity early return before throttle accumulation at `$82A9CC/$82AA29`; correction is pending verification.

### Corrected validation checkpoint —21:36 UTC

Weekly49% used versus38% startup. Independent review found throttle early return, checkpoint tile bounds, and finish pose selector/relation validation. Corrected gameplay passes primary and both disclosed complete traces/restores in debug. All28 M4-12–14 debug/sanitizer differential/restore commands pass; fixed broad gates pass 405 checks each (no skips), plus DRAGSTER/content/replay/winner-loser presentation. First presentation attempt failed because prior-worktree fixtures were outside permitted roots; task-local copies authenticate and pass. Final relation fix and fresh withheld cases remain pending.

### Reviewed closeout

Independent review evidence commits `467bde9` and `e42c47a` approve candidate `e5e88a8` (native behavior `6faff68`, followed by the reviewed metadata correction); see [M4-15-review](M4-15-review.md). All three failed cases remain regressions; two fresh material replacements pass their complete original outcome and restores. Final candidate matrices are `artifacts/m4-15/final-gates-final`, `regression-matrix-final`, `race-matrix-final` and `withheld-sanitize` (one-step command path correction is recorded separately). Both builds pass 405 synthetic checks with no skips. Native autonomy passes denied repository/ROM access, negative controls and missing-static-input rejection.

Consolidated handoff: merge reviewed task branch to synchronized main, run exact merged app-debug build/synthetic and all six complete race cases with restores, push the existing private origin/main, verify its exact SHA and wait for exact-tip macOS/Linux hosted CI. Store observed results in ignored `artifacts/m4-15-integration/closeout.json`; this tracked statement is conditional, not a remote pass. No second documentation-only push or M4 milestone tag. Stop after acceptance; no M4-16 dispatch.

Unavoidable work included original strategy discovery, authenticated full instruction audit, additive future-state recovery, independent variations and complete restores. Avoidable work included a preliminary source-mutated restore run, starting broad checks before all state-safety review findings arrived, two incorrect fixture/reference paths, and a rejected prior-mode jump hypothesis. These failed attempts are retained and not reported as passes. No expectation was weakened, no provider switch/reset/purchase occurred. Final clock and shared weekly usage are in the closeout artifact; the initial sample was 38% at 20:40:37 UTC.

Local closeout at 22:01 UTC: all eight primary/disclosed race commands pass on both builds, plus both withheld sanitizer cases. Each case has 451–466 fresh restores; the independent reviewer separately repeated all six debug cases. Weekly usage was 53% at 21:57:56 UTC, versus 38% startup; no resource boundary was reached. Exact merged checks and remote/CI verification continue below this tracked checkpoint.
