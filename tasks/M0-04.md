# M0-04 — Replay manifest and comparator foundation

## Assignment

- Status: accepted (integrated 11 September 2026)
- Milestone: M0
- Coordinator: primary agent
- Worker/session/runtime/model: primary agent (Claude Code, Fable 5.1), single interactive session, claimed 11 September 2026
- Reviewer: fresh independent session, own clone
- Dependencies and evidence of acceptance: M0-02 (`6b48da5`, tooling and reports); M0-03 (`e94834e`, reference adapter: `python3 tools/project.py reference ...`, script schema 1, samples schema 1, state sidecars; see D-0001 and R-0002)
- Base commit: `0b402e2` (`main` after M0-03 integration)
- Branch and isolated worktree: `task/M0-04-replay-comparator` in the primary checkout (single sequential worker; no second worktree)
- Owned paths and shared interfaces: `tools/unirally_lab/replay/` (new), `tools/unirally_lab/compare/` (new), `tests/manifests/replay/` (new), `docs/research/R-0003-*`; may extend the reference samples/script schemas only with a version bump and an update to `tests/tooling/test_reference.py`; must not change `src/lab`, the `test --suite synthetic` contract or the pinned core/patch without a new decision record
- Claim/lease/heartbeat/checkpoint location: this file; checkpoint commits at least every 10 minutes
- Session time limit, concurrency allocation and actual spend authorization if relevant: bounded sessions of 45 minutes; one worker; no service purchases

## Outcome and boundaries

Version replay manifests (scenario ID, ROM/core identity, initial procedure or state sidecar, both controllers' inputs with timing units and injection point, sampled-field schema, regeneration command); compare repeated reference runs; deliberately perturb a known active input (for this ROM, Start at frame 300 of a cold start is known to change work RAM on that frame, R-0002 finding 6) and produce a detectable difference; emit a first-divergence report (frame, prior sample, inputs, field values, the trace window from the samples); fail clearly on missing inputs and on timeout using the existing exit codes. State fields may initially be raw diagnostic ranges of work RAM; no gameplay interpretation.

Not in scope: native game code, gameplay fields (M1), scheduler (M0-05).

## Inputs and prerequisites

- ROM as in M0-03 (`local/rom-location.txt`, manifest `tests/manifests/rom/unirally-pal.json`).
- Built pinned core: `python3 tools/project.py reference build`.
- Save states are valid reference origins only when `reference restore-check` at that save point passes all three required checks (D-0001).
- Known baseline failures: none. Known limits: `restore_av_identical` is informational and fails after any restore; saving at boot-sequence points 29–96 (with holes) perturbs the run (R-0002 finding 5).

## Acceptance

| Criterion | Command or experiment | Expected result | Required artifact |
| --- | --- | --- | --- |
| Manifest schema | Versioned manifest validated by a ROM-free test | Invalid manifests rejected with exit 3 | Tests, schema doc |
| Repeated reference runs compare equal | Comparator on two fresh-process runs of the same manifest | No divergence reported, exit 0 | Report |
| Perturbation detected | Same manifest with the Start-at-300 input removed or moved | First divergence reported at the expected frame, exit 1 | Report with divergence |
| Missing input and timeout | Manifest naming an absent state/ROM; unreachable run under `--timeout` | Exit 2 with `missing`; exit 4 with `timeout` | Reports |
| Fresh-process comparison | Reference samples produced by separate worker processes | Comparator never runs both sides in one process | Code and report |

## Evidence and attempts

| Attempt | Hypothesis | Experiment | Observation | Next decision |
| --- | --- | --- | --- | --- |
| 1 | The M0-03 baseline reproduces on `main` at `0b402e2` before any change | `test --suite synthetic`; `reference build`; `reference verify --script tests/manifests/reference/boot-300.json` | 70/70 synthetic checks; incremental core build 0.4 s; three fresh processes gave sample digest `f210eecacf63cbeb…`, final state `df4fb430e731dfe1…` (`artifacts/m0-04/baseline-*.json`) | Extend the worker (fields, stop frame, WRAM dump) with a samples schema bump |
| 2 | The worker can capture declared work RAM ranges, stop at a chosen frame and dump WRAM without changing the sample digest | Samples schema 2: `--fields`, `--stop-after-frame`, `--wram-dump-out`, `process` identity, trace read before the final serialize; direct worker runs of `boot-300` | Sample digest unchanged (`f210eecacf63cbeb…`); a 256-byte range appears as hex on every sampled frame; `--stop-after-frame 149` runs 150 frames and the WRAM dump's digest equals frame 149's sampled WRAM digest; trace count at the end of frame 299 is 4,368,039 versus 4,368,044 after the final serialize | Build manifests, comparator and commands on it |
| 3 | A versioned manifest can derive the reference script and both perturbations diverge at frame 300 | Manifest schema 1 (`tools/unirally_lab/replay/manifest.py`), comparator (`tools/unirally_lab/compare/samples.py`), `replay validate/run/compare`; tracked manifests `boot-start-600`, `-start-removed`, `-start-moved-310`, `unreachable` | `compare` of two fresh processes (distinct PIDs): identical on 600 frames, exit 0, sample digest `583d1ec544ec61a2…`, final state `1a6fbf6d24c532ff…`. Against the removed and the moved variant: first divergence at frame 300 in `wram_sha256` and `wram_0000_0200`, registers identical, exit 1; localization re-run to frame 300 finds exactly one differing WRAM byte, `$7E0073` `0x10`/`0x00`, identical instruction counts (4,381,713) and trace windows. `unreachable` under `--timeout 3`: `timeout`, exit 4 (`run` and `compare`). Absent ROM: `missing`, exit 2. Invalid or absent manifest: exit 3 | Add a state origin with restore-check evidence and expected digests |
| 4 | A state validated by `restore-check` is accepted as an origin, an absent state or absent evidence is `missing`, and expected digests anchor a manifest | `reference restore-check --script boot-start-600.json --save-after 150 --artifacts local/states/boot-start-600-150` (all three required checks passed; state `893f08b82864a5cd…`); manifest `boot-start-600-from-state-150`; scratch manifests naming an absent state and an absent report; `expected` digests recorded in all four scenario manifests, a scratch copy with a wrong digest | Two fresh restores identical on frames 151–599 (449 frames, digest `a5a669d2671de7ab…`), final state equal to the cold-start final state, exit 0; absent state → `origin_available: missing`, exit 2 (detail carries the regeneration command); absent report → `origin_restore_check: missing`, exit 2; wrong expected digest → `sample_digest_matches_expected: failed`, exit 1; correct digest passes | Write ROM-free tests and records |
| 5 | The contract is covered ROM-free | `tests/tooling/test_replay.py` (manifest schema, derivation, comparator, origin resolution, command error paths) and schema-2 additions to `test_reference.py` | Synthetic suite 101/101 (was 70) | Re-run all evidence on the clean candidate |
| 6 | Evidence holds at the candidate `c4dd501` with a clean tree | All commands of attempts 1–4 re-run into `artifacts/m0-04/final/` | Every report records commit `c4dd501`, `dirty` false; exit codes 0/1/4/2/3 exactly as in the acceptance table; digests unchanged (see handoff) | Submit for review |
| 7 | (Review 1 returned) Every manifest defect is reported as exit 3, and the synthetic suite reports every failing test | Reviewer: an out-of-range `wram_range` escaped `validate_fields` as a `ScriptError` traceback (exit 1, no report) on all three subcommands, and three subtests of `test_invalid_manifests_are_rejected` had been erroring while the suite reported 101/101, because `pytests.py` never recorded subtest failures and `test` ignored the runner's exit status (M0-02 defect, first triggered here). Worker: probe suite with failing subtests under the old runner reproduced the loss | Fixed in two commits: `33eb6a4` (runner records each failing subtest, `python_tooling_tests` summary check fails on a non-zero runner exit or any failed record; tests in `test_native_reports.py`; outside M0-04's owned paths, authorized by the coordinator as a bounded correction of a pre-existing defect that made M0-04's evidence untrue) and `972a2eb` (`validate_manifest` converts `ScriptError` and malformed nesting into `ManifestError`; CLI tests assert no traceback). Minor findings fixed in `972a2eb`: the restore-check report must have resumed at `after_frame + 1` (measured `start_frame`, so an edited sidecar and manifest are refused); localization re-runs are optional checks so a timeout there cannot mask a divergence; `av_identical` is required for cold-start comparisons | Re-run all evidence on the clean head |
| 8 | Evidence holds at `972a2eb` with the truthful suite | All commands of attempt 6 plus `reference restore-check --script boot-300-every-5.json --save-after 70` re-run into `artifacts/m0-04/final2/` | Synthetic 106/106 (98 Python tests, 98 records, runner exit 0); exit codes unchanged (0/1/4/2/3); digests unchanged; `restore-check` at 70 still fails only `save_does_not_perturb` with first differing frame 71 (M0-03 behaviour intact under samples schema 2) | Second review |

## Handoff

- Current base/head commit and uncommitted state: base `0b402e2` (main). Branch `task/M0-04-replay-comparator`; review-1 candidate `623b360` (code `c4dd501`); review-2 candidate: code `972a2eb` (after the runner fix `33eb6a4`), record in the commit after it (see `git log`); no uncommitted state. Ignored: `artifacts/m0-04/` (baseline, development and `final/` reports with samples, derived scripts, WRAM dumps, divergence reports and worker logs; `scratch/` manifests for the negative cases) and `local/states/boot-start-600-150/` (state after frame 150, sidecar and restore-check report).
- Verified findings: R-0003 findings 1–7 and attempts 1–8. Acceptance evidence at `972a2eb` (reports in `artifacts/m0-04/final2/`, SHA-256 prefixes; the superseded `c4dd501` set is kept in `artifacts/m0-04/final/`): manifest schema → `tests/tooling/test_replay.py` (40 invalid manifests rejected as `ManifestError`; CLI exit 3 without traceback for invalid, out-of-range and malformed manifests on all three subcommands), `synthetic.json` `2b771dbd` 106/106 (98 Python tests, runner exit 0), `invalid.json` `44f01d2c` exit 3, `validate.json` `da5bde6d` exit 0; repeated runs → `repeat.json` `eed54e8b` exit 0 (`fields_identical` on 600 frames, `av_identical` required, `fresh_processes` with two worker PIDs), `repeat3.json` `a670e190` exit 0, `from-state.json` `ddce5b07` exit 0; perturbation → `perturb-removed.json` `a71c58b0` and `perturb-moved.json` `41d8279f`, both exit 1 with `first divergence at frame 300`, `divergence.json` in each artifacts directory (one differing WRAM byte `0x00073`); missing and timeout → `missing-rom.json` `151dd5a2` exit 2, `absent-state.json` `371d63e9` exit 2, `absent-restore-check.json` `10da9b57` exit 2, `timeout.json` `564d863e` and `timeout-compare.json` `7a2d115f` exit 4; fresh-process comparison → `replay/commands.py::_run_side` only ever calls `reference.commands._run_worker` (a bounded subprocess), every compare report's `fresh_processes` check lists the worker PIDs and the comparator's PID, and review 1 confirmed the check fails when the PIDs coincide; expected digests → `run-expected.json` `b4d99ffc` exit 0, `run-wrong-expected.json` `9011ad2c` exit 1. M0-03 baseline unchanged: `verify.json` `c8c1e3de` (`f210eecacf63cbeb…`, `df4fb430e731dfe1…`), `restore-70.json` `60b8a511` (exit 1, only `save_does_not_perturb`, first differing frame 71).
- Current hypothesis and failed approaches: none open for M0-04. Design choices, not failures: fields are a capture concern passed to the worker separately (`--fields`), so a state's sidecar (`script_sha256`) stays valid whatever fields a manifest declares; localization re-runs with `--stop-after-frame` rather than a truncated script for the same reason; `expected` digests are optional so a new manifest can be run once before its digests are recorded.
- Commands executed, outcomes and report hashes: as in attempt 8; every command is `python3 tools/project.py <args> --report artifacts/m0-04/final2/<name>.json --task M0-04`, with `--artifacts artifacts/m0-04/final2/<name>` for runs. The state was produced by the `regeneration_command` in `tests/manifests/replay/boot-start-600-from-state-150.json` (restore-check report `local/states/boot-start-600-150/restore-check.json`, SHA-256 `566a2cb4c652dad5…`).
- Unavailable/skipped checks: Linux run of the core (CI is ROM-free); no perturbation other than Start on port 0 at 300–305 / 310–315 was measured; a divergence confined to unsampled state that reconverges before the last frame is not detected (R-0002 limits apply); `replay compare` between a state origin and a cold start is reported as a frame-set failure by design (the frame sets differ); the divergence report lists at most 64 differing bytes per field (the count is complete).
- Exact next experiment/command: reviewer on a clean clone with the ROM: `python3 tools/project.py bootstrap`, `build --preset lab-debug`, `reference build`, `python3 tools/project.py test --suite synthetic` (106 checks; `python_tooling_tests` detail `98 tests run, 98 records, 0 failed; runner exit 0`), `replay compare --manifest tests/manifests/replay/boot-start-600.json --rom <ROM>` (exit 0, digest `583d1ec544ec61a2…`), `replay compare --manifest tests/manifests/replay/boot-start-600.json --against tests/manifests/replay/boot-start-600-start-removed.json --rom <ROM>` (exit 1, frame 300, one byte `0x00073`), `replay run --manifest tests/manifests/replay/unreachable.json --timeout 3 --rom <ROM>` (exit 4), `replay run --manifest tests/manifests/replay/boot-start-600-from-state-150.json --rom <ROM>` (exit 2 until the state is regenerated with the manifest's `regeneration_command`, then exit 0 with digest `a5a669d2671de7ab…`); then an own perturbation manifest (other frame, port 1, or a second button) checked against a manual diff of the samples files.
- Remaining dependencies: none for M0-04. M0-05 can dispatch from the replay manifests and reports.
- Elapsed work and provider usage when known: about 1 h 30 min for the first candidate and 30 min for the review-1 fixes, plus one reviewer session of 10 min; usage not metered.

## Review and integration

- Reviewer and independent reproduction/withheld-case results: review 1 (fresh independent session, own clone, 11 September 2026) of `623b360`: reproduced doctor/bootstrap/build, two `reference build`s from GitHub, every handoff prediction (exit codes, digests `583d1ec5…`/`a5a669d2…`, frame 300, byte `0x00073`, `verify` and `restore-check` verdicts unchanged under samples schema 2) and inspected `divergence.json`. Withheld cases, all agreeing with the reviewer's own frame-by-frame comparison script: Start on port 1 (frame 300, bytes `0x00073` and `0x00075`), Start+A at 400–403 (identical for 600 frames), Start+B+Y at 450–453 (frame 450 in all three fields, 18 range bytes, 5 registers, differing final states), Start at 200–205 (identical to no input), sparse `sample_every` 7 and 5 (frames 301 and 300), a range excluding `0x73` (detected through `wram_sha256`, localized to `0x00073`); error paths; a monkeypatched `_run_worker` returning equal PIDs made `fresh_processes` fail. Returned with two material findings (M1 escaped `ScriptError`; M2 subtest failures dropped by the M0-02 runner and runner exit ignored), three minor (restore-check evidence not tied to the save point; localization timeout could become exit 4; `av_identical` optional for cold starts) and notes (no content in the diff; tests exercise the code).
- Review 2 (fresh independent session, own clone, 11 September 2026) of `64de8e8`: reproduced each review-1 failure mode on a worktree at `c4dd501` (traceback exit 1; subtest records lost by the old runner; an edited sidecar and manifest accepted) and confirmed each fixed at `64de8e8` (exit 3 without traceback on all three subcommands for three malformed manifests; failing subtests recorded and an injected failing subtest makes the suite exit 1 with `python_tooling_tests: failed`; the edited save point refused with `report's save point 150 != manifest after_frame 151`; a forced localization timeout leaves the verdict at exit 1; `av_identical` required for cold starts, optional after a restore). Re-verified every acceptance prediction (two core builds, 106/106, digests, exit codes, `restore-check` at 70). Withheld cases, all agreeing with the reviewer's own frame-by-frame script: `up` on port 0 at 300–305 (frame 300, bytes `0x73`, `0x1f1`, `0x1f9`, 5 registers, final states differ, instruction counts 4,381,713 vs 4,381,715), Start with L+R at 500–502 (frame 500, one byte `0x72`), a two-controller combination at 350 (bytes `0x72`, `0x74`, `0x75`), five small ranges including `0x0073` length 1, `--runs 3`, `--no-localize`. Verdict: approve; no material or minor findings; notes: (a) `cmd_compare`'s post-run wiring has no ROM-free test (a stubbed `_run_worker` test would make it regression-safe), (b) the handoff should mention `bootstrap` and `build --preset lab-debug` before `test`, (c) the only baseline-affecting change is the trace read before the final serialize, documented in R-0003 finding 6.
- Required changes or acceptance rationale: review-1 findings addressed in `33eb6a4` and `972a2eb` (attempt 7), evidence re-recorded (attempt 8) and confirmed by review 2. Note (b) is folded into the handoff above; note (a) is the bounded follow-up assigned to [M0-05](M0-05.md); the review-2 withheld observations are recorded in R-0003's independent-check section as reviewer observations. The acceptance table is met: versioned manifests validated ROM-free with exit 3 on rejection; repeated fresh-process runs compare equal (exit 0); the Start-at-300 perturbation (removed and moved) is detected at frame 300 (exit 1) with a first-divergence report carrying the prior sample, inputs, field values, a one-byte work RAM localization and trace windows; missing ROM, state or evidence exit 2 with `missing` and the unreachable manifest exits 4 with `timeout`; every reference execution is a separate worker process and the report records the PIDs.
- Exact merge candidate and required-check results: `4deb09d` (branch head after review 2: the reviewed `64de8e8` plus this review record; fast-forward of `main` from `0b402e2`). Required checks on the reviewed commit `64de8e8`: ROM-free CI run 34555306067 green on `ubuntu-24.04` and `macos-15` (106 checks, truthful runner); local `test --suite synthetic` 106/106 with `source.dirty` false at `972a2eb` (`artifacts/m0-04/final2/synthetic.json`); ROM-dependent evidence at `972a2eb` (the last code change; `64de8e8` touches records only) listed in the handoff; review 2 reproduced it on its own clone. Earlier candidates: `c4dd501` run 34554155218 and `623b360` run 34554212300 were green but their suite hid three erroring subtests (finding M2) and are not evidence.
- Integrated commit and evidence location: `main` fast-forwarded to `4deb09d`; the integration commit after it on `main` records acceptance, updates `docs/STATE.md` and readies M0-05; the CI run on `main` is recorded in `docs/STATE.md`'s history once pushed. Evidence: this record, R-0003, ignored `artifacts/m0-04/final2/` and `local/states/boot-start-600-150/`, CI artifacts on run 34555306067, the two reviewers' reports summarised above.
- Scope still unverified: Linux run of the core; perturbations beyond the buttons and frames listed; a divergence confined to unsampled state that reconverges before the last frame; native comparison (M2).
