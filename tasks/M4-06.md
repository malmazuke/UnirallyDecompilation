# M4-06 — ZOOM ZOO first vertical-contact episode closure

## Assignment

- Status: claimed 13 September 2026 after bounded Astra planning
- Milestone: M4 — Original game coverage
- Planning consultation: bounded OpenAI `gpt-6-astra`/high, read-only at
  accepted M4-05 head `51131a5`
- Task provider/model: OpenAI, Sol/medium worker in an isolated worktree
- Provider usage: the user waived percentage guardrails for the unattended M4
  goal and authorized at most one weekly usage reset only if an actual provider
  limit is reached; no money, paid credits or provider switching is authorized
- Reviewer: fresh sequential OpenAI Sol/medium reviewer with an undisclosed
  adjacent onset or one-frame release around the slope episode
- Dependencies: accepted M4-03 content/read contract, M4-04 field/seed freeze,
  M4-05 contact capture/component, R-0011 contact equations, supported PAL ROM
  and pinned core
- Base/branch/worktree: `51131a5`,
  `task/M4-06-zoom-zoo-vertical-contact`,
  `.worktrees/m4-06-zoom-zoo-vertical-contact`
- Owned paths: additive vertical-contact research tooling/tests/manifests,
  `docs/research/R-0024-zoom-zoo-vertical-contact.md`, this task and review
  records
- Frozen paths: `src/`, existing reference manifests/projections, the M4-05
  verifier and frozen capture, serialization, Classic pack/profile/start,
  frontend/presentation, core and locks; coordinator owns registry/state

## Outcome and boundary

Independently reconstruct every ordered player/opponent contact call on frames
1650--1682, the complete vertical-contact episode before the first observed
direction/special call at frame 1683. Inputs may be captured incoming motion,
pose and raw samples plus independently extracted authenticated static content.
Captured preprocessing, reduction, response and publication values are targets,
never computational inputs.

This is captured-argument research, not autonomous recurrence, native ZOOM ZOO,
a general contact implementation or Classic-pack expansion. Direction/special
geometry, later track paths, finish, AI, camera, presentation and audio remain
out of scope.

## Planning observations and hypothesis

Read-only planning reproduced accepted M4-05 access SHA-256
`267ecfbf7223a3c2a9e6844d620feda0c41adb98fd9a5280d10320d3682fab67`.
The old component rejects frame 1662 as an unrecovered slope even though its
lossy selected summary remains angle -1: points 4 and 6 carry angle -2 with
penetration bytes 250 and 254. Frame 1665 enters with vertical velocity 65490
(-46 signed), which the old response component rejects. Frame 1666 records
reflection 1 and support loss, frame 1667 reacquires support, frame 1677 reaches
another response guard, and frame 1683 first reaches direction/special geometry.
These are captured observations, not recovered equations.

The hypothesis is that the first continuous vertical-contact episode is one
bounded reconstructable family. A failure to close any dependency narrows the
claim; it must not be filled with a plausible default.

## Acceptance

| Criterion | Experiment | Expected result |
| --- | --- | --- |
| Accepted foundation | Reproduce M4-05 capture, both component verifies and 8 focused tests; M4-03 contract and M4-04 pair | Exact accepted identities and outcomes |
| Complete dependency inventory | Inspect all 66 ordered calls and 660 point paths without short-circuiting at the old first failure | Signed comparisons, slope coefficient selection, reducer order, support loss/reacquisition, negative incoming velocity, scratch and publications are accounted for or explicitly unresolved |
| Reference integrity | Fresh-process bounded captures with complete order and no overflow/truncation/unresolved claimed access | Repeatable evidence; live scratch inputs distinguished from overwritten values; new static coefficients have authenticated provenance |
| Independent reconstruction | Compute from captured incoming arguments and independent content | All 660 ordered preprocessing tuples, meaningful axis selectors, reducers, response fields and caller publications compare exactly on frames 1650--1682 |
| Mutation resistance | Preserve lossy summaries while altering nonselected tuples; mutate order, response/publication, state and identities | Contradictions fail or change an independently computed result |
| Worker variation | Preregister first Right at 1653, retain other events and capture through 1700 twice | Exact through 1649 and first input divergence 1650; actual first directional boundary and preceding vertical episode recorded without preclaimed timing |
| Independent review | Reviewer-owned adjacent onset or one-frame release that exercises a new signed/slope/upward path | Reference independently captured; original arithmetic/order checked; irrelevant variation reported honestly |
| Regression | Focused, debug/sanitize, M4-03/M4-04/M4-05, Classic pack and DRAGSTER restore gates | Frozen behavior and identities unchanged |

## Experiment order

1. Reproduce the accepted foundation before adding equations.
2. Inventory every path and ordered access for frames 1650--1682 before
   selecting coefficients or implementing the component.
3. Freeze repeatable reference evidence separately from computation.
4. Reconstruct every call and compare all ordered preprocessing and downstream
   values, clearly labelled as captured-argument evaluation.
5. Preregister and run the worker first-Right-at-1653 variation twice.
6. Provide clean-source reports and exact identities for fresh review.

Signed-byte penetration versus the `0xA0` empty sentinel, reducer tie/order,
negative-velocity arithmetic, phase-dependent publication and pose/reflection
dependencies are priority risks. The first unsupported producer or missing
watch remains a recorded boundary, not permission to expand the core patch.

## Handoff

- Claim base: accepted M4-05 `51131a5001ed11b8d26f6a89e0e88fdc7175f72e`.
- First action: reproduce M4-05 identities, then inventory the full 66-call
  episode before implementing any missing equation.
- Worker variation preregistration: move the first primary-controller Right
  onset from frame 1650 to frame 1653, retain the release at 3299 and every
  other event. Predict exact state through frame 1649 and first controller
  difference at 1650; do not predict contact timing or the directional boundary.
- Runtime needs: supported private PAL ROM, pinned core, ignored artifact space;
  no network, publication, deployment or spending.
- Checkpoint after dependency inventory and at least every ten minutes. Only the
  coordinator accepts after independent review and integration evidence.
