# M3-03 — Minimal frontend and controls

## Assignment

- Status: claimed on 12 September 2026
- Milestone: M3, prerequisite to playable-slice acceptance
- Coordinator: OpenAI Codex Sol/medium (`/root`)
- Worker: OpenAI Codex Sol/medium, one child in an isolated worktree
- Reviewer: fresh sequential OpenAI Codex Sol/medium session
- Dependencies: accepted M3-02 presentation, M3-02A Classic pack and M3-01 full-race simulation
- Starting commit: `759ed9e0b130819d09392b61946c27e433f44438`
- Branch/worktree: `task/M3-03-minimal-frontend` at `.worktrees/m3-03-minimal-frontend`
- Owned paths: `src/app/`, frontend CMake/dependency wiring, focused app tests,
  additive `tools/project.py` frontend/package commands, this task and its
  handoff/research/review records; accepted core/pack interfaces are frozen
- Model/usage: OpenAI Sol/medium default. Account-wide weekly bucket sampled at
  24% used on 12 September 2026. For this unattended run the user removed the
  project percentage stop/review reserve through M3, but explicitly excluded
  weekly reserve use: no reset-credit redemption or purchase. No publication or
  deployment is authorized. Retain 45-minute checkpoints and durable updates.
- Fixture access: worktree-local ignored links/copies to the existing pinned
  toolchain, supported ROM locator, reference tools, generated Classic pack and
  canonical states. Original/generated content remains ignored.

## Outcome and boundaries

Provide the smallest maintainable SDL3 desktop program that runs the accepted
CRAWLER/DRAGSTER native slice. It validates and consumes the Classic pack,
advances the native simulation at PAL 50 Hz, samples keyboard or one gamepad
into the existing two-controller input masks once per update, and displays the
accepted 256x224 presentation with deterministic integer scaling.

On first launch, absence of a valid pack leads to an explicit supported-ROM
selection/argument flow, exact ROM identity verification and atomic local pack
creation. Once created, the same application must relaunch from the pack with
the ROM removed. Cancel, missing ROM, wrong ROM, corrupt pack, renderer failure
and SDL failure must not become successful launches. Audio is intentionally
absent in M3 and must be declared in help/about/startup output.

Not in scope: menus beyond the minimal launch/error flow, audio implementation,
other tracks/riders/modes, interpolation that affects simulation, remapping UI,
configuration persistence beyond the pack path, packaging/release publishing,
online play or original CPU execution. M3-04 owns real sustained-play and
clean-checkout milestone acceptance.

## Required contract freeze

Before UI implementation, record and test:

1. The SDL3 version/source/checksum and local, non-global build arrangement on
   macOS and Linux; headless core/test builds must remain window-independent.
2. The exact 50 Hz monotonic-clock scheduler: maximum catch-up work, pause/
   resume behavior and how delayed display frames affect input/update/render.
3. Keyboard/gamepad button mapping to the accepted SNES masks, including
   simultaneous buttons, release, disconnect and focus loss.
4. The application pack path, explicit ROM input/selection precedence, atomic
   extraction handoff and all exit/error outcomes.
5. The display contract: 256x224 source, aspect-preserving integer scale,
   nearest-neighbor sampling and resize behavior. Presentation output must not
   alter canonical gameplay state.

## Acceptance

- A tracked command builds the desktop target reproducibly without global
  package installation; ROM-free debug/sanitizer CI and headless tests remain
  available when a display is absent.
- Authored tests exercise PAL scheduler boundaries with a fake clock, including
  no early update, exact deadline, bounded catch-up and long-stall recovery.
- Authored tests exercise keyboard/gamepad mask mapping, simultaneous and
  released inputs, disconnect/focus-loss clearing and two-port ownership where
  exposed. One input snapshot reaches exactly one simulation update.
- The app validates every Classic pack identity through the accepted native
  reader before gameplay. Missing-pack first launch exact-gates the supported
  ROM and creates the pack atomically; wrong/cancelled/missing input and corrupt
  pack fail clearly. A later launch passes with the ROM absent.
- The accepted 256x224 renderer is displayed with deterministic nearest-neighbor
  integer scaling; resize/display cadence changes do not alter the canonical
  state sequence for a frozen live-input script.
- Audio omission is explicit in tracked help/startup documentation and no audio
  success is claimed.
- Debug and sanitizer suites pass locally; macOS/Linux hosted CI is green;
  repository hygiene finds no ROM, extracted assets, generated packs, states or
  captures; a fresh independent reviewer approves the exact behavioral commit.

## First experiment

Inventory SDL3 availability on the development host and current CMake/bootstrap
mechanisms. Choose a pinned dependency approach that works in isolated build
directories without affecting headless CI. Before opening a real window, add a
pure scheduler/input adapter with fake-time tests and drive a short pack-backed
native run from a deterministic input sequence; prove its canonical states and
render digests are independent of display cadence.

Only the coordinator marks this task accepted after independent review and
integration evidence.

## Handoff

Claim commit, worker commit(s), exact commands/results, dependency identities,
known limitations and next experiment are appended here before every yield.

### Worker review-candidate handoff — 12 September 2026

- Worktree/branch: `.worktrees/m3-03-minimal-frontend` on
  `task/M3-03-minimal-frontend`, starting at claim commit `32b637d`. Worker was
  OpenAI Codex Sol/medium; no child or frontier consultation was used. The task
  remained within OpenAI and no reset credit, purchase, publication or deploy
  action occurred.
- [R-0016](../docs/research/R-0016-minimal-frontend.md) was frozen before SDL
  UI implementation. It separates source observations from the scheduler,
  input, first-launch, scaling, camera and omission decisions. SDL 3.4.10 is
  pinned to upstream source archive SHA-256
  `12b34280...f2785`; CMake reads the tracked lock, FetchContent verifies it,
  and builds it only in `app-debug`/`app-sanitize`. Headless presets leave SDL
  disabled and window-independent.
- `src/app/frontend.*` implements the pure 20,000,000 ns scheduler (four-update
  maximum and long-stall debt drop), complete 12-button keyboard/gamepad mask
  mappings for two ports, state clearing and integer viewport/camera helpers.
  The authored test covers early/exact/batched/stalled/pause/resume deadlines,
  simultaneous/released/disconnected/focus-cleared input, all mappings,
  controller ownership, 1x/3x viewports, one snapshot per update, identical
  canonical state sequences under two display cadences, 256x224 output and
  renderer state invariance.
- `src/app/sdl_main.cpp` validates the C++ pack before SDL/gameplay, initializes
  video and gamepad only (no audio), uses an RGB24 streaming texture with SDL
  nearest scaling, centers integer viewports after output resize, samples once
  per due PAL update and renders once per batch. Focus loss pauses and clears;
  gain resumes at a fresh deadline. SDL/window/renderer/texture errors fail the
  process. The first two connected standard gamepads own ports 0/1; the
  accepted slice consumes port 0 only.
- M3-02 recovered only five atlas pose pairs. Rather than changing that
  accepted core contract or inventing frames, the app holds the last supported
  render while other semantic poses advance and reports the omission once. Its
  first frame uses a presentation-only copy with the opening recovered pair;
  canonical gameplay is never changed. This is the main visible limitation for
  review and M3-04 real-play assessment.
- `frontend run` validates an existing pack without opening `--rom`; if the
  pack is absent it requires an explicit ROM argument, exact-gates it, uses the
  accepted atomic writer, revalidates, and launches. Existing corruption is
  never overwritten. Authored tooling tests cover first launch, ROM-absent
  relaunch, cancellation/empty input, missing/wrong ROM, corrupt pack and app
  failure. Help, README and startup output explicitly state that audio is not
  implemented.

Exact successful validation commands on the uncommitted candidate were:

```text
python3 tools/project.py build --preset app-debug --report artifacts/m3-03-final-app-debug-build.json --timeout 600 --task M3-03
python3 tools/project.py test --suite synthetic --preset app-debug --artifacts artifacts/m3-03-final-app-debug-synthetic-2 --report artifacts/m3-03-final-app-debug-synthetic-2/report.json --timeout 600 --test-timeout 120 --task M3-03
python3 tools/project.py build --preset app-sanitize --report artifacts/m3-03-final-app-sanitize-build.json --timeout 600 --task M3-03
python3 tools/project.py test --suite synthetic --preset app-sanitize --artifacts artifacts/m3-03-final-app-sanitize-synthetic --report artifacts/m3-03-final-app-sanitize-synthetic/report.json --timeout 600 --test-timeout 120 --task M3-03
SDL_VIDEODRIVER=dummy python3 tools/project.py frontend run --pack artifacts/m3-03-first-launch.pack --preset app-sanitize --hidden --updates 20 --timeout 30 --report artifacts/m3-03-app-sanitize-smoke.json --task M3-03
python3 tools/project.py native finish-check --manifest tests/manifests/native/full-race-continuous.case.json --content-pack artifacts/m3-03-first-launch.pack --save-frame 3213 --save-frame 3453 --preset lab-debug --artifacts artifacts/m3-03-finish-debug --report artifacts/m3-03-finish-debug/report.json --timeout 600 --task M3-03
python3 tools/project.py native presentation-check --manifest tests/manifests/presentation/classic-crawler-dragster-v1.json --fixtures artifacts/m3-03-private-fixtures --content-pack artifacts/m3-03-first-launch.pack --preset lab-debug --artifacts artifacts/m3-03-presentation-debug-2 --report artifacts/m3-03-presentation-debug-2/report.json --timeout 600 --task M3-03
git diff --check
```

Debug synthetic passed 281 Python tests, 20 CTests and three fresh-process lab
runs (304 passed report checks). Sanitizer synthetic's closing output passed
the same inventory; however its report retained two transient failures in
shared `lab-failure-probe` mutation tests, so it is not claimed as a clean
pass. Both tests passed immediately in focused reproduction. A clean exact-head
sanitizer rerun is required after commit. The private finish gate passed all 11
checks including restores at 3213/3453. The private presentation gate passed
the unchanged seven mismatch counts: 36, 697, 279, 445, 653, 962 and 961. The
first-launch pack has the supported ROM identity and 25 entries; both its
creation launch and a later ROM-unconsulted launch passed under SDL dummy.

Report SHA-256 values before the final exact-head rerun: debug build
`c7ec14d0...e76f`, debug suite `c9af7df8...27e7`, sanitizer build
`3c051796...9bfb`, sanitizer suite (retained failure)
`5f9fb693...5822`, first launch `07b38f1e...9816`, pack-only launch
`ecdcf930...7cf`, sanitizer SDL smoke `12037808...1c44`, finish
`966a3be3...f353`, presentation `c198053f...76a5`. An earlier presentation
invocation using a fixture path outside the worktree correctly failed its
authorization check; copying the same hashed fixtures below ignored
`artifacts/` produced the passing result above. An initial Python suite failed
only because the new test imported `tools.unirally_lab` instead of the runner's
`unirally_lab` path; the corrected full debug suite is the one reported.

Next: commit the coherent candidate, rerun sanitizer on that exact head until
the shared mutation tests are clean, record final hashes/commit here, inspect
the staged diff for private content, then submit for independent review. Do not
mark accepted; hosted macOS/Linux CI and real sustained play remain coordinator
/ M3-04 evidence.

### Final worker checkpoint

- Exact behavioral candidate: `6489c644a327e4eeb54b04822b2aa92e8869c2e0`
  (`Add minimal SDL3 frontend and launch flow`). This follow-up changes only the
  task handoff.
- Exact-candidate app-debug build and complete synthetic suite passed. Report
  SHA-256 values: build `1901807d...2aee`, suite
  `9815172d...a805` (281 Python tests, 20 CTests, three fresh processes; 304
  passed checks, no failure/skip).
- Exact-candidate app-sanitize build and complete synthetic suite passed on the
  clean rerun. Report SHA-256 values: build `9b32ea5d...3fa3`, suite
  `60eb2cdf...321` (the same 304 passed checks, no failure/skip). This supersedes
  only the earlier retained transient broad-run failure as candidate evidence;
  that failure remains documented above.
- The staged behavioral diff contained only the 17 listed source/test/doc/lock
  paths. `artifacts/`, `build/`, `local/`, packs, ROM locator, fixture states,
  images and Python caches remained ignored. `git diff --check` passed.
- Submit `6489c64` plus this handoff for fresh independent review. The reviewer
  should particularly challenge scheduler overflow/long-stall boundaries,
  focus/gamepad ownership, invalid existing-pack precedence, Linux SDL source
  configuration without a global SDL install, and whether the explicit held-
  pose presentation limitation is acceptable for M3-03 rather than M3-04.

### Returned-review correction checkpoint — 13 September 2026

- Starting point was preserved review commit
  `8ccb0377ae0f9a244b7bb11638f875be6a625342`. The correction keeps the
  accepted scheduler, input, pack, canonical state, cases and visual thresholds
  unchanged. It addresses every finding in `M3-03-review.md`; the review
  response and the observation/decision split are updated there and in
  [R-0016](../docs/research/R-0016-minimal-frontend.md).
- `LivePresentation` retains only the last recovered rider pose pair. Every
  redraw renders the current gameplay state, camera, scroll and HUD from a
  temporary copy with only the rider pose fields substituted. The authored
  regression proves distinct unsupported current states produce distinct fresh
  frames, exactly matching direct render of those states with only the pose
  pair changed, while canonical inputs remain byte-identical.
- The frontend command rejects non-finite and nonpositive timeouts before pack
  access or child creation. NaN and both infinities are covered by requested
  failed-report and zero-child-call assertions. `SDL_RenderPresent` now has a
  checked boolean result and a renderer-specific failure. Hosted ROM-free CI
  now compiles/tests `app-debug` on macOS and Ubuntu and `app-sanitize` on
  Ubuntu, including pack-free executable help smokes and 600-second command
  bounds; all existing headless jobs remain.
- Pre-commit local `app-debug` and `app-sanitize` synthetic suites each passed
  282 Python tests, 20 CTests and repeatability. The sanitizer dummy smoke
  passed. A 2,146-update continuous-right dummy launch rendered 2,145 frames,
  used rider fallback on 1,419, and recorded zero identical consecutive
  fallback redraws while Racing. The review's exact NaN command shape exited 3,
  wrote its failed report, and left no matching child process.
- The private finish regression passed all 11 checks and both continuation
  boundaries. The private presentation gate retained the seven accepted
  mismatch counts: 36, 697, 279, 445, 653, 962 and 961. An attempted parallel
  invocation of those two commands collided in their shared `lab-debug` build
  directory and produced an invalid intermediate archive; the presentation
  check was rerun serially and passed. This is invocation evidence, not a
  product failure, and the failed ignored artifact was retained with a
  `.failed-parallel` suffix.
- Next: freeze the coherent correction commit, rerun both complete app suites,
  dummy launches and private gates against that exact SHA, record report hashes
  in a handoff-only commit, then submit for fresh sequential independent
  re-review and hosted CI. This checkpoint does not mark M3-03 accepted.

### Returned-review correction handoff

- Exact behavioral correction candidate:
  `3509ae99ed5102d8d7e2afdbea9d7e4ecbeda4da` (`Correct M3-03 review
  findings`). This handoff commit changes documentation only. Review should
  compare from preserved review commit `8ccb037` and must remain independent;
  hosted CI and fresh sequential re-review are still required before the
  coordinator may accept M3-03.
- Exact-candidate `app-debug` and `app-sanitize` builds and complete synthetic
  suites passed. Each suite ran 282 Python tests, 20 CTests and three fresh
  processes: 305 required report checks, with no failure or skip. The new
  native fallback-frame regression and the NaN/infinity process-suppression
  regression are included in both runs.
- Exact-candidate SDL dummy debug and sanitizer pack-only smokes passed without
  opening a ROM. The 2,146-update fixed-right debug run passed in 43.029 s,
  rendered 2,145 frames, used rider fallback on 1,419 frames, and recorded zero
  identical consecutive fallback redraws while Racing. The all-phase count was
  383 identical redraws with a longest run of 228, retained as an explicit
  diagnostic rather than conflated with the former unsupported-pose race
  freeze.
- The exact NaN CLI mutation exited 3, emitted the requested failed report at
  the candidate SHA, and a subsequent process search found no matching app.
  Authored tests additionally cover positive and negative infinity and assert
  that the child runner is never called.
- Exact-candidate private finish checking passed all 11 checks, including
  fresh-process identity and restore continuation at frames 3213 and 3453.
  Private presentation checking passed unchanged mismatch counts 36, 697, 279,
  445, 653, 962 and 961 under the accepted limits.

Exact successful command families were:

```text
python3 tools/project.py build --preset app-debug --report artifacts/m3-03-correction-exact-app-debug-build.json --timeout 600 --task M3-03
python3 tools/project.py test --suite synthetic --preset app-debug --artifacts artifacts/m3-03-correction-exact-app-debug-synthetic --report artifacts/m3-03-correction-exact-app-debug-synthetic/report.json --timeout 600 --test-timeout 120 --task M3-03
python3 tools/project.py build --preset app-sanitize --report artifacts/m3-03-correction-exact-app-sanitize-build.json --timeout 600 --task M3-03
python3 tools/project.py test --suite synthetic --preset app-sanitize --artifacts artifacts/m3-03-correction-exact-app-sanitize-synthetic --report artifacts/m3-03-correction-exact-app-sanitize-synthetic/report.json --timeout 600 --test-timeout 120 --task M3-03
SDL_VIDEODRIVER=dummy python3 tools/project.py frontend run --pack artifacts/m3-03-first-launch.pack --preset app-debug --hidden --updates 20 --timeout 30 --report artifacts/m3-03-correction-exact-debug-smoke.json --task M3-03
SDL_VIDEODRIVER=dummy python3 tools/project.py frontend run --pack artifacts/m3-03-first-launch.pack --preset app-sanitize --hidden --updates 20 --fixed-controller-mask 128 --timeout 30 --report artifacts/m3-03-correction-exact-sanitize-smoke.json --task M3-03
SDL_VIDEODRIVER=dummy python3 tools/project.py frontend run --pack artifacts/m3-03-first-launch.pack --preset app-debug --hidden --updates 2146 --fixed-controller-mask 128 --timeout 90 --report artifacts/m3-03-correction-exact-continuous-right.json --task M3-03
SDL_VIDEODRIVER=dummy python3 tools/project.py frontend run --pack artifacts/m3-03-first-launch.pack --preset app-debug --hidden --timeout nan --report artifacts/m3-03-correction-exact-timeout-nan.json --task M3-03
python3 tools/project.py native finish-check --manifest tests/manifests/native/full-race-continuous.case.json --content-pack artifacts/m3-03-first-launch.pack --save-frame 3213 --save-frame 3453 --preset lab-debug --artifacts artifacts/m3-03-correction-exact-finish-debug --report artifacts/m3-03-correction-exact-finish-debug/report.json --timeout 600 --task M3-03
python3 tools/project.py native presentation-check --manifest tests/manifests/presentation/classic-crawler-dragster-v1.json --fixtures artifacts/m3-03-private-fixtures --content-pack artifacts/m3-03-first-launch.pack --preset lab-debug --artifacts artifacts/m3-03-correction-exact-presentation-debug --report artifacts/m3-03-correction-exact-presentation-debug/report.json --timeout 600 --task M3-03
```

Report SHA-256 values:

| Evidence | SHA-256 |
| --- | --- |
| app-debug build | `6c600f8e27bf3b01f9e1c9ecb1f6ec49298b55a809b085f530466143857b2705` |
| app-debug synthetic | `68110ff512fc9aeb48a62bb834463b6b275e5d7f0269ef27c6ffe2447f60b878` |
| app-sanitize build | `c70ba65326c854e2b66e307a660e3a98c2c5dfa0a57bfe848491e451b5fd575a` |
| app-sanitize synthetic | `5bfea84de649432d0c0032762520e70a41e3315198ade63124e85593cd4ec432` |
| dummy debug | `3465c408c76e79c3a16e6c2aee77442d41b1a82d34bd6a2a2a0a6a816aaf66cb` |
| dummy sanitizer | `fcd4089fac6dac91cf4dc804ed051f87f0025747e5206f24a8a1af3fd76ed24b` |
| continuous right | `02256ef3f7794ac491516ca507433dc22f5dee34823491574b8a2995887d5e48` |
| rejected NaN | `b6dfa6f1dff6077da62010a9987dfb04ded206a96bd12d2eb2052ab57e253099` |
| private finish | `37e6ff6fe83edb48d84960f04c7e723d39af5ebf5d16ffb848e4b67dc9be7e0b` |
| private presentation | `81c251333b02897570b50e4aeee7609523cec57c2d684b941e0a93779de0e275` |

All reports name `3509ae99ed5102d8d7e2afdbea9d7e4ecbeda4da` as their
source commit. The deliberately rejected NaN report has overall status
`failed`; that is the required mutation outcome. The other nine reports have
overall status `passed`.

### Correction-round-2 checkpoint — 13 September 2026

- Started from `6ff9e6a7d50a27cf42bb29928483b81f8d00679a`, which
  preserves fresh re-review commit `c117d42` through the coordinator's task
  head. The three returned findings and their reproductions are recorded in
  `M3-03-review.md`; M3-03 remains unapproved.
- Added a narrowly presentation-only `RiderArtPose` override to the renderer
  while preserving the M3-02 call. Fallback history now reaches only atlas
  selection/rider validation. The same-state/off-screen regression uses
  effectful authored content and two recovered histories, then separately
  proves visible art retains history only within rider rectangles. Canonical
  state is unchanged throughout.
- Added frontend-specific normalization for separated negative infinity before
  argparse and a real subprocess test for all four non-finite spellings. Each
  launches with a sentinel executable and requires exit 3, a failed report and
  no sentinel file. Generic CLI parsing remains unchanged.
- Added an Ubuntu-only, 300-second-bounded APT preparation step for the explicit
  X11 headers listed by pinned SDL's Linux documentation. Headless CI remains
  before this step, the real desktop backend stays enabled, and SDL itself is
  still pinned and built locally rather than globally installed.
- Pre-commit `app-debug` and `app-sanitize` suites each passed 283 Python tests,
  20 CTests and three fresh processes. The four real non-finite probes all
  exited 3, wrote reports and left no matching app process. Next: inspect and
  commit the coherent correction, then rerun all exact-head suites, dummy
  smokes, private finish/presentation and boundary probes before a handoff-only
  evidence commit. Fresh review and hosted CI remain required.

### Correction-round-2 handoff

- Exact behavioral correction candidate:
  `457ba885f82856b5b328243ecffcacce03312bf5` (`Resolve M3-03 second
  review findings`). This handoff changes documentation only. The worktree was
  clean before pushing the two commits; fresh independent review and hosted CI
  are still required, and M3-03 is not marked accepted.
- Exact-candidate `app-debug` and `app-sanitize` builds and complete synthetic
  suites passed. Each suite ran 283 Python tests, 20 CTests and three fresh
  processes: 306 required report checks with no failure or skip. These include
  the same-state/off-screen history isolation, visible-rider bounds, canonical
  invariance and subprocess CLI boundary regressions.
- Debug and sanitizer pack-only SDL dummy smokes passed without opening a ROM.
  The exact 2,146-update fixed-right launch rendered 2,146 frames, used fallback
  on 1,420, and recorded zero identical fallback redraws while Racing. The
  private finish gate passed all 11 checks; presentation mismatch counts remain
  36, 697, 279, 445, 653, 962 and 961.
- Real CLI probes for `nan`, `inf`, separated `-inf` and equals-form `-inf`
  each exited 3, wrote the requested failed report at the exact candidate SHA,
  and left no matching frontend process. The subprocess regression uses an
  executable sentinel and independently requires that no child starts.
- The Ubuntu X11 package preparation can only be validated on hosted Linux.
  The workflow retains its 30-minute job cap, adds 300-second bounds to both
  APT operations and retains 600-second bounds on each SDL app build/test.

Exact successful command families were:

```text
python3 tools/project.py build --preset app-debug --report artifacts/m3-03-r2-exact-debug-build.json --timeout 600 --task M3-03
python3 tools/project.py test --suite synthetic --preset app-debug --artifacts artifacts/m3-03-r2-exact-debug-synthetic --report artifacts/m3-03-r2-exact-debug-synthetic/report.json --timeout 600 --test-timeout 120 --task M3-03
python3 tools/project.py build --preset app-sanitize --report artifacts/m3-03-r2-exact-sanitize-build.json --timeout 600 --task M3-03
python3 tools/project.py test --suite synthetic --preset app-sanitize --artifacts artifacts/m3-03-r2-exact-sanitize-synthetic --report artifacts/m3-03-r2-exact-sanitize-synthetic/report.json --timeout 600 --test-timeout 120 --task M3-03
SDL_VIDEODRIVER=dummy python3 tools/project.py frontend run --pack artifacts/m3-03-first-launch.pack --preset app-debug --hidden --updates 20 --timeout 30 --report artifacts/m3-03-r2-exact-dummy-debug.json --task M3-03
SDL_VIDEODRIVER=dummy python3 tools/project.py frontend run --pack artifacts/m3-03-first-launch.pack --preset app-sanitize --hidden --updates 20 --fixed-controller-mask 128 --timeout 30 --report artifacts/m3-03-r2-exact-dummy-sanitize.json --task M3-03
SDL_VIDEODRIVER=dummy python3 tools/project.py frontend run --pack artifacts/m3-03-first-launch.pack --preset app-debug --hidden --updates 2146 --fixed-controller-mask 128 --timeout 90 --report artifacts/m3-03-r2-exact-continuous-right.json --task M3-03
python3 tools/project.py frontend run --pack artifacts/m3-03-first-launch.pack --preset app-debug --hidden --timeout nan --report artifacts/m3-03-r2-exact-timeout-nan.json --task M3-03
python3 tools/project.py frontend run --pack artifacts/m3-03-first-launch.pack --preset app-debug --hidden --timeout inf --report artifacts/m3-03-r2-exact-timeout-inf.json --task M3-03
python3 tools/project.py frontend run --pack artifacts/m3-03-first-launch.pack --preset app-debug --hidden --timeout -inf --report artifacts/m3-03-r2-exact-timeout-negative-inf.json --task M3-03
python3 tools/project.py frontend run --pack artifacts/m3-03-first-launch.pack --preset app-debug --hidden --timeout=-inf --report artifacts/m3-03-r2-exact-timeout-equals-negative-inf.json --task M3-03
python3 tools/project.py native finish-check --manifest tests/manifests/native/full-race-continuous.case.json --content-pack artifacts/m3-03-first-launch.pack --save-frame 3213 --save-frame 3453 --preset lab-debug --artifacts artifacts/m3-03-r2-exact-finish --report artifacts/m3-03-r2-exact-finish/report.json --timeout 600 --task M3-03
python3 tools/project.py native presentation-check --manifest tests/manifests/presentation/classic-crawler-dragster-v1.json --fixtures artifacts/m3-03-private-fixtures --content-pack artifacts/m3-03-first-launch.pack --preset lab-debug --artifacts artifacts/m3-03-r2-exact-presentation --report artifacts/m3-03-r2-exact-presentation/report.json --timeout 600 --task M3-03
```

Report SHA-256 values:

| Evidence | SHA-256 |
| --- | --- |
| app-debug build | `e4bf096d9b70dc6b6228eb8036f97dd9eaf6f2969efb864ee81250d094d88ca7` |
| app-debug synthetic | `1c4be645a6648636ef8665969e4a0a79466a32fd1779e7112acfdbcff522d363` |
| app-sanitize build | `9d60725d395717721f297991404b10425823bc5e846865cb168b1fcba258700f` |
| app-sanitize synthetic | `6b74f13e2606a1d6d00c3de88319b802e07bb1555bb8c6e97b2c218212617f7e` |
| dummy debug | `6281ff909d81b677fb860f8cb5f2472f65e36c1871368018bee93e5f6d4b89d4` |
| dummy sanitizer | `eedfe5a210c10af754a1e3b69315d9e3f1a9a154c421bc0e0a328bacfd2b231d` |
| continuous right | `bca0af98842f8030a5697e32597b6330b33bb9277f20a5263f9bb962925132aa` |
| rejected NaN | `cc32d06b213b061f0d11b4a67008a925d32f242e86ce52b41f5c8e5a776d3056` |
| rejected positive infinity | `686a865568f2be211f5c5ef9e77a4a9440e5997886b6a2d3b36a23024bf04872` |
| rejected separated negative infinity | `f353c33d5976bdcb0aa0b516192b92855bb815e449b0ba472184b45c525618b9` |
| rejected equals negative infinity | `1dd8edfaef59809e40354552273dd7608a3f3f5a6329565eb93e93e9ad8463a1` |
| private finish | `8333566d92df76d7c23e614a4319fd6e8fbc2a21bfdbb1932388fbd29679572f` |
| private presentation | `0419b0819a63422af82fb9c1233586ae6d3bf9a7fa9cfae3e59e189025810708` |

Every report names `457ba885f82856b5b328243ecffcacce03312bf5` as its
source commit. The four timeout reports have required overall status `failed`;
the other nine reports have status `passed`.

Post-push hosted workflow run `34700625467` passed at handoff SHA
`6b4ac12cf7d9c1097a5cf7e41c3f159be34898ee`. macOS 15 completed the
headless path, app-debug build/test and executable help smoke in 2m59s. Ubuntu
24.04 completed the headless path, bounded X11 dependency preparation,
app-debug build/test/help and the full lab/app sanitizer step in 4m38s. The
only annotations were GitHub's unrelated Node 20 deprecation notices for
official checkout/cache/upload actions. Hosted evidence is now complete;
fresh independent review remains required before acceptance.

### Final path-collision correction checkpoint — 13 September 2026

- Started from task head `9db1011`, which preserves the final independent
  review. The correction is limited to frontend CLI preflight, its authored
  tests and documentation; gameplay, renderer, SDL, scheduler, pack/state
  formats, cases and thresholds are unchanged.
- Report output is compared against ROM, pack, rules and explicit/default
  executable paths by normalized absolute spelling, non-strict resolution and
  existing-file identity. A collision exits 3 before any input read,
  extraction, child or report write. Not writing the report is deliberate
  because that write is the destructive operation being refused.
- Focused authored coverage passed seven frontend tests. Direct cases cover
  lexical, normalized absent, symlink and hardlink forms plus rules/executable
  inputs; subprocess ROM/pack reproductions require byte preservation, no
  first-launch pack on refusal and no executable sentinel. Next: freeze the
  behavioral correction, rerun exact app-debug/app-sanitize suites plus real
  first-launch and pack-only smokes, record hashes in a handoff-only commit,
  push clean and submit for fresh narrow review. M3-03 remains unapproved.

### Final path-collision correction handoff

- Exact behavioral correction candidate:
  `8682b176f99db4da82b18fb28a0dc74c1e7efdeb` (`Guard frontend reports
  from input aliases`). This handoff commit changes documentation only.
- Exact-candidate `app-debug` and `app-sanitize` builds and complete synthetic
  suites passed. Each ran 285 Python tests, 20 CTests and three fresh processes:
  308 required report checks with no failure or skip. Seven frontend tooling
  tests include direct and subprocess path-collision coverage.
- A real first-launch SDL dummy run used an ignored disposable exact PAL ROM,
  created the expected 25-entry pack and passed. A sanitizer pack-only relaunch
  validated that pack, did not open a ROM and passed. The source ROM was copied,
  never operated on directly.
- Exact command-level collision probes then used `--report` equal to the
  disposable ROM and generated pack. Both exited 3 with the expected refusal.
  The ROM remained 2,097,152 bytes at SHA-256
  `a1105819d48c04d680c8292bbfa9abbce05224f1bc231afd66af43b7e0a1fd4e`;
  the pack remained at SHA-256
  `5c1fc5b00747621ccd2c0a1f6f34cf6ba290c8808e6bb1d92b8c0ad4b0df1529`.
  The refused first-launch pack was absent afterward and no matching frontend
  process remained. No report exists for either refusal by design because the
  requested report paths were the protected inputs.

Exact successful commands were:

```text
python3 tools/project.py build --preset app-debug --report artifacts/m3-03-collision-exact-debug-build.json --timeout 600 --task M3-03
python3 tools/project.py test --suite synthetic --preset app-debug --artifacts artifacts/m3-03-collision-exact-debug-synthetic --report artifacts/m3-03-collision-exact-debug-synthetic/report.json --timeout 600 --test-timeout 120 --task M3-03
python3 tools/project.py build --preset app-sanitize --report artifacts/m3-03-collision-exact-sanitize-build.json --timeout 600 --task M3-03
python3 tools/project.py test --suite synthetic --preset app-sanitize --artifacts artifacts/m3-03-collision-exact-sanitize-synthetic --report artifacts/m3-03-collision-exact-sanitize-synthetic/report.json --timeout 600 --test-timeout 120 --task M3-03
SDL_VIDEODRIVER=dummy python3 tools/project.py frontend run --pack artifacts/m3-03-collision-exact.pack --rom artifacts/m3-03-collision-exact-rom.sfc --preset app-debug --hidden --updates 20 --timeout 30 --report artifacts/m3-03-collision-exact-first-launch.json --task M3-03
SDL_VIDEODRIVER=dummy python3 tools/project.py frontend run --pack artifacts/m3-03-collision-exact.pack --preset app-sanitize --hidden --updates 20 --fixed-controller-mask 128 --timeout 30 --report artifacts/m3-03-collision-exact-pack-only.json --task M3-03
python3 tools/project.py frontend run --pack artifacts/m3-03-collision-refused.pack --rom artifacts/m3-03-collision-exact-rom.sfc --executable /usr/bin/true --updates 1 --timeout 30 --report artifacts/m3-03-collision-exact-rom.sfc --task M3-03
python3 tools/project.py frontend run --pack artifacts/m3-03-collision-exact.pack --executable /usr/bin/true --updates 1 --timeout 30 --report artifacts/m3-03-collision-exact.pack --task M3-03
```

Report SHA-256 values:

| Evidence | SHA-256 |
| --- | --- |
| app-debug build | `1e7a4d704772d74fa18c9c1d8114ecbcb96f17b59da067d6604a88f59d5fc676` |
| app-debug synthetic | `bd9a5c17f3b7faff90ec81a559b79b49f5377369c7c9f056a04f047e1116b1b7` |
| app-sanitize build | `278f748605aeb6f4aeb0a3aa31a712976dbd8ef0326cd65f4413ecbb243c3a00` |
| app-sanitize synthetic | `166ac9f7322cd81e9897610a4c007c5238f1ea30b7733f0143e3aa266a4462ce` |
| first launch | `ad9f03f3c09cb16854a52b2f49a8bdc3d21eb4433abac74a1fea9cb2bb5edd91` |
| pack-only relaunch | `1d9e2c1ac95927da29c3b1ac373129f11685821c3d1a688460e0af2a9489138d` |

All six reports name `8682b176f99db4da82b18fb28a0dc74c1e7efdeb` as their
source commit and have overall status `passed`. The existing hosted run
`34700625467` remains applicable because this correction touches only the
Python frontend guard/tests/docs and both local app presets compile and pass.
Fresh narrow independent review is the remaining requirement; M3-03 is not
marked accepted.

### Path-semantics correction checkpoint — 13 September 2026

- Started from task head `cfc2b04`, which preserves the collision-guard
  re-review. Its quoted-literal-tilde destructive miss and symlink-parent/`..`
  false refusal are recorded in `M3-03-review.md`; M3-03 remains unapproved.
- Canonicalized pack, optional ROM, rules, explicit/default executable and
  optional report exactly once with non-strict symlink-aware resolution before
  report construction or any input read. Those same `Path` objects now drive
  every collision check, read, pack write, child launch and report write.
- Removed Python-side user expansion. A quoted leading `~` is deliberately
  literal; an unquoted tilde has already been expanded by the shell. Equality
  catches identical/nonexistent resolved targets and `samefile` catches
  existing symlink/hardlink identity.
- Focused authored coverage passed nine frontend tests. A direct spy and real
  subprocess both require the quoted-tilde alias to exit 3, preserve the pack,
  construct no report and start no child. A subprocess symlink-parent/`..`
  case requires the distinct resolved report to be accepted and written while
  the pack remains byte-identical. Existing ordinary, nonexistent, symlink and
  hardlink alias regressions remain green.
- Next: freeze the coherent correction, run exact app-debug/app-sanitize suites
  and Classic first-launch/pack-only dummy smokes, append hashes in a handoff
  commit, push clean and submit for one fresh narrow review.
