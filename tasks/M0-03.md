# M0-03 — Automated reference adapter spike

## Assignment

- Status: review (resubmitted 11 September 2026 after the first review returned it; not accepted)
- Milestone: M0
- Coordinator: primary agent
- Worker/session/runtime/model: primary agent (Claude Code, Fable 5.1), single interactive session, claimed 10 September 2026
- Reviewer: fresh independent session, own clone
- Dependencies and evidence of acceptance: M0-00 (`bbce3e4`), M0-01 (`37f9f24`, ROM identity in `tests/manifests/rom/unirally-pal.json`); M0-02 tooling (`ac059dd`) for reports and the isolated toolchain
- Base commit: `243020a` (`main` after M0-02 integration)
- Branch and isolated worktree: `task/M0-03-reference-adapter` in the primary checkout (single sequential worker; no second worktree)
- Owned paths and shared interfaces: `tools/unirally_lab/reference/`, `tools/locks/` entries for the emulator, `docs/decisions/D-0001-*`, `docs/research/R-0002-*`; reuses the report schema and exit codes; must not change `src/lab` or the `test --suite synthetic` contract
- Claim/lease/heartbeat/checkpoint location: this file; checkpoint commits at least every 10 minutes
- Session time limit, concurrency allocation and actual spend authorization if relevant: bounded sessions of 45 minutes; one worker; no service purchases; a pinned emulator source checkout or release archive must be recorded by URL, revision and digest in `tools/locks/`

## Outcome and boundaries

Compare integration effort for a pinned bsnes core and Mesen Community Edition for headless programmatic execution of the identified PAL ROM. Implement, for the selected candidate: load and identify the ROM bytes; deterministic initial SRAM and reset; bounded input delivery at a defined point; capture of memory, relevant registers and a trace window without a GUI; save/restore. Repeat a cold-start run three times in fresh processes with identical sampled outputs. Record the decision in `docs/decisions/D-0001-reference-emulator.md`.

Not in scope: any gameplay interpretation, comparator, replay manifest schema (M0-04), or building the emulator into the release.

## Inputs and prerequisites

- ROM SHA-256 `a1105819d48c04d680c8292bbfa9abbce05224f1bc231afd66af43b7e0a1fd4e` via ignored `local/rom-location.txt`; verify with `python3 tools/project.py rom inspect --expect tests/manifests/rom/unirally-pal.json`.
- Isolated toolchain from `python3 tools/project.py bootstrap`; host C++ compiler (doctor reports it).
- Emulator sources or binaries: to be fetched under ignored `local/` with recorded provenance; any patch to the emulator lives in tracked `tools/` as a diff.
- Known baseline failures: none.

## Acceptance

| Criterion | Command or experiment | Expected result | Required artifact |
| --- | --- | --- | --- |
| Candidate comparison | Written attempt log per candidate with commands and outcomes | Effort, blockers and headless capabilities stated with evidence | Decision record |
| ROM identity in adapter | Adapter reports the loaded ROM SHA-256 | Equals the manifest | Report |
| Deterministic cold start | Three fresh-process runs of the same bounded input script | Identical sampled memory/register digests | Reports with digests |
| Bounded execution | Runs with a deliberately unreachable condition | Timeout reported as `timeout`, exit 4 | Report |
| Save/restore | Restore a state, continue N updates, compare with the uninterrupted run | Identical digests | Reports |
| Pinned core | Lock entry with URL/revision/digest and build steps | Reproducible on a second clean checkout | `tools/locks/` |

## Evidence and attempts

| Attempt | Hypothesis | Experiment | Observation | Next decision |
| --- | --- | --- | --- | --- |
| 1 | Both candidates build headless on this host from a pinned checkout | `git clone` both; bsnes `make -C bsnes target=libretro binary=library local=false -j12`; Mesen `make core -j12` | bsnes `7d5aa1e` built `out/bsnes_libretro.dylib` in 8 s wall. Mesen `20f497c` failed: its makefile passes `$(realpath ./)` include paths unquoted and the project path contains a space; rebuilt in 39 s wall (361 s CPU) with `CXXFLAGS` overridden to relative include paths. SDL2 2.32.10 from Homebrew satisfied Mesen's link dependency; .NET was not needed for the core library. | Evaluate both through Python ctypes; record the Mesen path limitation in the lock |
| 2 | bsnes libretro core runs the PAL ROM deterministically from Python without a GUI | Scratch ctypes driver: environment/video/audio/input callbacks, core option `bsnes_entropy=None`, 300 frames, three fresh processes | Region reported PAL; 300 frames in 0.49 s; per-frame video and audio digests identical across the three processes. Final `retro_serialize` digests differed: exactly 8 bytes at offsets 542–549, inside the serialized `Random` state (seeded from `clock()` even with entropy `None`). `retro_get_memory_data` returns null in this target, so WRAM is not reachable through the libretro API. | Patch the libretro target with laboratory exports (WRAM, cartridge RAM, CPU registers, reseed, instruction trace ring) |
| 3 | With the patch, memory and registers sample deterministically and save/restore is exact | `unirally.cpp` exports plus a one-line instruction hook in `CPU::main`; three fresh 300-frame runs; run B serializes after frame 149 and continues; run C restores that state in a fresh process and continues to 300 | WRAM 131072 B, cartridge RAM 8192 B. Three runs: every per-frame WRAM digest, register set, video and audio digest identical; 4,368,042 instructions traced; serialized states byte-identical after `unirally_reseed(0,0)`. A vs B identical on all 300 frames, so serializing (which runs the cores to a synchronization point) did not perturb this run. First C attempt diverged at frame 150 because the probe re-ran the saved frame (probe off-by-one, not an emulator effect); rerun recorded below. | Fix the probe and rerun C |
| 4 | After a fresh-process restore, bsnes continues identically to the uninterrupted run | Probe start index corrected; C and a second restore C2 run frames 150–299 from the state saved after frame 149 | WRAM digests and CPU registers identical to run B on every frame 150–299; C and C2 identical to each other; final serialized states of A, B, C and C2 all identical. Differences remain only in the video digest of frame 150 (the first frame rendered after the restore) and in the resampled audio digests, i.e. the output streams are not part of the serialized state. | Treat memory/registers as the comparison domain; A/V after a restore are not bit-exact and must not be compared across a restore boundary |
| 5 | Inputs delivered through the libretro input callback reach the game at the intended frame | 600-frame runs: no input; Start held frames 200–205 (twice); Start held frames 300–305 | Start at 300 produces the first WRAM difference at frame 300 itself; the repeated Start-at-200 runs are identical to each other and identical to the no-input run for all 600 frames (the game does not react to Start at that point of its boot sequence; no claim about why). | Bounded input delivery is demonstrated at frame granularity |
| 6 | The Mesen core library can be driven headlessly through its C interop and the debugger step API | ctypes probe: `InitDll`, `InitializeEmu` with null window handles and no audio/video/input, `SetSnesConfig` (struct layout verified with a compiled `offsetof` check), `ConsoleMode` flag plus `Pause()` before `LoadRom` so the debugger breaks on the first instruction, `Step(PpuFrame)` per frame | Loads the ROM; `SnesPrgRom` contents hash to the manifest SHA-256; reports PAL (50.007 Hz, 312 scanlines); initial state identical across processes. First runs segfaulted in Mesen's emulation thread (`Debugger::SleepUntilResume` → `NotificationManager::SendNotification`) when the probe exited without `Stop()`; a stall was traced to a race between `IsExecutionStopped()` and `_waitForBreakResume` when a new step request lands inside Mesen's break window; polling `IsPaused()` too closes it. With that fix three 300-frame runs completed, but per-frame samples were not identical across fresh processes (analysis in attempt 7), save/restore (`SaveStateFile`/`LoadStateFile`, 65680-byte state) reproduced the saved run exactly, and each frame took 20 ms (the frame limiter stays active under the debugger). | Analyse the cross-process difference; try scanline-boundary steps and the maximum-speed flag |
| 7 | Mesen's cross-process differences are sampling artefacts, not emulation differences | Field-level comparison of the three PpuFrame-step runs; three more runs with `Step(240, SpecificScanline)` and the `MaximumSpeed` flag | PpuFrame mode: WRAM and registers identical on all 300 frames; only the `master_clock` value read through `GetTimingInfo` differed, on 2 of 300 frames, by 60 clocks. Scanline-boundary mode: all fields identical across three processes (218 distinct WRAM digests, frame counts 1..300). Elapsed stayed 6.1 s per 300 frames with the speed flag: each debugger step costs about 20 ms of thread hand-off (Mesen sleeps in 10 ms slices while broken). | Choose bsnes as primary (D-0001); keep the Mesen probe under `experiments/` |
| 8 | An adapter with fresh-process workers reproduces the probe results through `project.py` | `reference build/run/verify/restore-check` implemented; `verify --runs 3`, `restore-check --save-after 149`, `run unreachable.json --timeout 3`, `run boot-start-600.json` | First `verify` failed: run 1 differed from runs 2–3 from frame 228, and `restore-check` reported saving as perturbing. Cause: the worker now unloads the game, so the core wrote `Unirally (Europe).srm` (8192 B) into the shared save directory and later runs loaded it as initial cartridge RAM. Fix: private empty save directory per run, initial WRAM/cartridge-RAM digests folded into the sample digest. After the fix: `cold_start_identical` passed (sample digest `f210eecacf63cbeb…`, final state `8e0af6be66113714…`), `save_does_not_perturb`, `restore_matches_saved_state` and `restore_continuation_identical` passed, the unreachable script reported `timeout` with exit 4, and the Start-at-300 run completed 600 frames in 1.3 s. | Reproduce from a clean clone |
| 9 | The pinned core is reproducible from the lock on a clean checkout | `git clone` of this repository at `db8020c` into scratch (no `local/`), `reference build` (clones bsnes from GitHub, applies the patch, builds with `-j4`), `reference verify --rom …` | Build passed in 9.2 s; verify gave the same sample digest `f210eecacf63cbeb…`, A/V digest `edfa4f8e92b24dc0…` and final state `8e0af6be66113714…` as the primary checkout. The library SHA-256 differs between the two builds (`b00ed0c0…` vs `4f87bed7…`; `-g` embeds absolute paths), so bit-identical binaries are not claimed. | Submit for review |
| 10 | (Review 1 returned) `restore_matches_saved_state` measured its own definition and restore fidelity holds at every save point | Reviewer's sweep of `--save-after` over 20 points, then a worker sweep of 36 points on `boot-300` and 6 on `boot-start-600` with both bsnes synchronization methods (new export `unirally_set_serialization_method`) | Reviewer: check failed at 12 of 20 points; save point 50 diverged game-visibly after restore; 290/299 differed only in the final state. Worker: the failing check compared the frame-end sample with the post-`runToSave` state. Redefined against a post-serialize sample stored in the sidecar: passes at all 42 points under both methods. `Strict`: restore exact at 42/42, saving perturbs the run at 7/36 `boot-300` points (frames 45–90) and 0/6 `boot-start-600` points; `Fast`: saving never perturbs, restore diverges at 2/9 (50, 290). | Default to `Strict`; document the verified domain; require `save_does_not_perturb` for stored states (D-0001) |
| 11 | (Review 1 minor findings) ctypes string lifetime, sidecar validation, patch-digest fragility | Keep encoded option/directory bytes alive for the core's lifetime; `validate_state_sidecar` refuses states from another core build, ROM, script or method and states leaving no frames; patch regenerated with pinned diff options (`--full-index`, explicit prefixes, no renames) recorded in the lock | Six new ROM-free tests; synthetic suite 62/62; `reference build` re-verifies the regenerated patch (`04267fc82746…`) | Rerun all evidence at the final code |
| 12 | Evidence at the resubmitted candidate | `verify --runs 3`, `restore-check --save-after 149`, `run unreachable.json --timeout 3`, `run boot-start-600.json`, `test --suite synthetic` | Sample digest `f210eecacf63cbeb…` unchanged; final state under `Strict` `8b0a653c8b93c0fb…`; all required checks passed; timeout exit 4; 62/62. | Clean-clone reproduction, CI, re-review |

## Handoff

- Current base/head commit and uncommitted state: base `243020a` (main). Branch `task/M0-03-reference-adapter`; first review candidate `bda194a`; review fixes in the commit after this record (see `git log`); no uncommitted state after it. Ignored: `local/emulators/{bsnes,MesenCE}` checkouts and builds, `artifacts/m0-03/` reports including the `sweep/` directory (42 Strict and 9 Fast restore-check reports).
- Verified findings: R-0002 and attempts 1–12. Acceptance evidence at the resubmitted candidate (reports under ignored `artifacts/m0-03/`, SHA-256 prefixes): candidate comparison → D-0001 with attempts 1, 6, 7; ROM identity in adapter → `verify.json` `6d398c4a` check `rom_identity_in_adapter`; deterministic cold start → same report, `cold_start_identical` (3 processes, digest `f210eecacf63cbeb…`); bounded execution → `timeout.json` `e9498775` (outcome `timeout`, exit 4); save/restore → `restore.json` `c1181848` plus the sweep reports; pinned core → `tools/locks/emulators.json`, `build.json` `d026dde5`, clean-clone reports listed in the review section. Synthetic suite: `synthetic.json` `6635aaaf`, 62/62 passed.
- Current hypothesis and failed approaches: open question, not blocking: why saving inside frames 45–90 of the boot sequence perturbs the continuing run under `Strict` (hypothesis: APU upload handshake). Failed/superseded approaches: WRAM via `retro_get_memory_data` (null in this target); raw `retro_serialize` digests before reseeding; the frame-end-sample definition of `restore_matches_saved_state`; bsnes `Fast` synchronization as the default (silent restore divergence); Mesen `Step(PpuFrame)` completion by `IsExecutionStopped()` alone; Mesen without `Stop()` before exit.
- Commands executed, outcomes and report hashes: as in attempt 12, all with default options; sweep: `reference restore-check --script <script> --save-after N --serialization-method <Fast|Strict> --artifacts artifacts/m0-03/sweep/<method>-N --report artifacts/m0-03/sweep/<method>-N.json`.
- Unavailable/skipped checks: Linux build/run of the core (CI is ROM-free); cycle-accurate PPU profile; Windows. The `.worktrees/native` stale worktree from M0-02 is untouched.
- Exact next experiment/command: re-reviewer reproduces on a clean clone: `python3 tools/project.py reference build`, `reference verify --script tests/manifests/reference/boot-300.json --rom <ROM>`, `reference restore-check --script tests/manifests/reference/boot-300.json --save-after 50 --rom <ROM>` (expect `save_does_not_perturb` failed, the other two restore checks passed, exit 1) and `--save-after 275` (expect exit 0).
- Remaining dependencies: none for M0-03. M0-04 depends on samples schema 1, script schema 1 and the state sidecar fields (`post_serialize`, identity digests, `serialization_method`).
- Elapsed work and provider usage when known: about 1 h 45 min for the first candidate and about 45 min for the review fixes, plus one reviewer session of about 12 min; usage not metered.

## Review and integration

- Reviewer and independent reproduction/withheld-case results: review 1 (fresh independent session, own clone, 11 September 2026) of `bda194a`: reproduced doctor/bootstrap/build/synthetic (56/56), ROM identity, `reference build` from GitHub, `verify` (same digests), `restore-check` at 149 and the timeout case; withheld cases: 450 frames with `sample_every` 7, input windows on port 1 and port 0 at other frames (first difference at the intended frame), error paths, and a `--save-after` sweep. Returned with two material findings (the restore check's definition; restore divergence at other save points, so the record generalised from one point) and minor findings (ctypes string lifetime, unvalidated state sidecar, patch-digest fragility), plus notes (trace hook scope, unknown environment commands, video digest of the padded buffer).
- Required changes or acceptance rationale: all review-1 findings addressed in the resubmitted candidate (attempts 10–11); the verified save/restore domain is now stated in R-0002 and D-0001 instead of generalised. Re-review pending.
- Exact merge candidate and required-check results: first candidate `bda194a` had ROM-free CI run 34473092681 green on `ubuntu-24.04` and `macos-15`. Resubmitted candidate `88bd676`: ROM-free CI run 34520244357 green on `ubuntu-24.04` and `macos-15` (62 checks); clean clone at `88bd676` (no `local/`): `reference build` cloned bsnes from GitHub, applied the regenerated patch and built in 8.7 s (`clean2-build.json` `577e2a50…`), `verify` reproduced sample digest `f210eecacf63cbeb…` and final state `8b0a653c8b93c0fb…` (`clean2-verify.json` `9eff3449…`), `restore-check --save-after 50` exited 1 with only `save_does_not_perturb` failed (`clean2-restore-50.json` `5cccff17…`) and `--save-after 275` exited 0 (`clean2-restore-275.json` `4489a678…`), as predicted in the handoff.
- Integrated commit and evidence location: pending.
- Scope still unverified: Linux core build; cycle-accurate PPU; the cause of the frames 45–90 save perturbation; any gameplay interpretation (M1).
