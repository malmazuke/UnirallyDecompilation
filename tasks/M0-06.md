# M0-06 — M0 acceptance report

## Assignment

- Status: in review (submitted 11 September 2026; the registry row is the coordinator's to move)
- Milestone: M0
- Coordinator: primary agent
- Worker/session/runtime/model: fresh session of Claude Opus 5, run as a subagent of the coordinator's Claude Code (Fable 5.1) session and dispatched with the portable prompt in `docs/AGENT_WORKFLOW.md` and nothing else; claimed 11 September 2026 13:40 AEST, with the registry row updated in the same commit (M0-05 gap G1)
- Reviewer: fresh independent session, own clone
- Dependencies and evidence of acceptance: M0-00 through M0-05 accepted (see [the registry](README.md)); evidence records R-0001 to R-0004 and decision D-0001
- Base commit: `3794dc2` (`main` after M0-05 integration)
- Branch and isolated worktree: `task/M0-06-acceptance-report` in the linked worktree `.worktrees/m0-06` (ignored path; the coordinator's checkout stays on `main`). Fixture arrangement: the coordinator placed `local/rom-location.txt` in the worktree; nothing else was copied, so the worker bootstraps and builds the core from the records. The M0-05 evidence directory is `.worktrees/m0-05/artifacts/m0-05/` on this host if the report needs to cite a file digest from it. The coordinator pushes the candidate branch to origin for CI
- Owned paths and shared interfaces: `docs/research/R-0005-*` (the acceptance report), this record; `tests/tooling/test_replay.py` only for the one follow-up test named below; must not change any command, schema, manifest, the pinned core or `src/lab`
- Claim/lease/heartbeat/checkpoint location: this file and the registry row; checkpoint commits at least every 10 minutes
- Session time limit, concurrency allocation and actual spend authorization if relevant: bounded sessions of 45 minutes; one worker; no service purchases

## Outcome and boundaries

Assemble the M0 acceptance report: for every M0 gate in [the project plan](../docs/PROJECT_PLAN.md) and [build and validation](../docs/BUILD_AND_VALIDATION.md) (clean setup, repeated reference capture, deliberately changed input, comparator/report failure path, timeout and missing-prerequisite handling, restart after interruption, a failed check reported accurately, a task resumed from its persisted record) the evidence that meets it, with the machine/tool/ROM/core identities and the exact reproducible commands, taken from R-0001 to R-0004 and the task records; any gate without evidence is listed as unmet and blocks acceptance rather than being argued around. Re-run the recorded checks once on the report's own commit so the report describes a state that was actually observed. Update the next-stage estimates in the project plan from the observed effort of M0-01 to M0-05 (elapsed work is in each task's handoff). One bounded test follow-up from the M0-05 review: cover the `divergence_localized: failed` branch of `replay compare` (`consistent_with_first_runs` false) with a stubbed-worker case, so that forcing it true in `_localize` is caught.

Not in scope: new commands, gameplay work (M1), a scheduler, changes to any format.

## Inputs and prerequisites

- ROM as in M0-03 (`local/rom-location.txt`, manifest `tests/manifests/rom/unirally-pal.json`), built pinned core (`reference build`), replay manifests in `tests/manifests/replay/`.
- Runtime needs (M0-05 gap G5): network access for the pinned wheels and the core clone; about 15 s of core build; the ROM path arrangement above.
- Known baseline failures: none. Known limits: the state-origin manifest needs its local state regenerated (its `regeneration_command`); `av_identical` is informational after a restore; saving at boot-sequence points 29–96 perturbs the run (R-0002); CI is ROM-free (G7).

## Acceptance

| Criterion | Command or experiment | Expected result | Required artifact |
| --- | --- | --- | --- |
| Every M0 gate has evidence or is listed as unmet | Gate-by-gate table citing the record, commit, command and report digest | No gate is described as met without a cited observation; unmet gates block M0 acceptance | `docs/research/R-0005-*` |
| Identities and commands are reproducible | Re-run of the recorded checks on the report's commit | Same exit codes and digests as the cited records (`583d1ec544ec61a2…`, `f210eecacf63cbeb…`, frame 300, byte `0x00073`, 117+ synthetic checks) | Reports |
| Follow-up test | Stubbed-worker case for `divergence_localized: failed` | Suite grows by at least one check, green on both CI platforms; forcing `consistent_with_first_runs` true is caught | Test, CI run |
| Estimates updated from observation | Project plan next-stage estimates revised from the elapsed work in the M0 task handoffs | Estimates cite the observed figures; no calendar or cost promise beyond them | Plan diff |
| Independent review of the exact candidate | Reviewer reproduces on the candidate commit; CI on that commit | Approve, or specific reproducible findings addressed | Record |

## Evidence and attempts

| Attempt | Hypothesis | Experiment | Observation | Next decision |
| --- | --- | --- | --- | --- |
| 1 | A fresh session reproduces the M0 baseline from the records alone, with nothing in the worktree but `local/rom-location.txt` | `doctor`, `bootstrap`, `build --preset lab-debug`, `test --suite synthetic`, `reference build` | Pinned CMake 3.31.10 and Ninja 1.13.2 fetched and verified in 2.2 s; native build 0.9 s; suite 117/117 (`109 tests run, 109 records, 0 failed; runner exit 0`) at the claim commit `a7ed341`; the pinned core cloned from GitHub, patched (`a719f5ffe222`) and built in 14.6 s. Nothing had to be guessed or asked for; the M0-05 handoff's runtime-needs line (G5) covered the network access and the build time | Baseline holds; write the follow-up test before re-running the evidence, so the candidate is one commit |
| 2 | The `divergence_localized: failed` branch can be covered with the stubbed worker, and the test catches the mutation M0-05 review 1 left uncaught | `StubWorker` gains `localize_defects`: a localization re-run that completes but stops one frame before the requested `--stop-after-frame` (`short`), or whose work RAM dump on disk is not the one its samples file hashed (`dump_mismatch`, the shape of the stale/orphaned file of G2/G3). New test `test_an_inconsistent_localization_is_failed_not_passed` | Both cases give exit 1 from the *field* comparison with `divergence_localized: failed` and not required; `short` yields an **empty** work RAM diff (0 differing bytes) that must not read as a pass, `dump_mismatch` the usual 1 byte. `test_replay.py` 38 → 39 tests; suite 117 → 118 checks, 109 → 110 Python tests. Three mutations of the `consistent` expression (forced true — the reviewer's uncaught one — and either clause dropped) are each caught by the named test (`artifacts/m0-06/mutation-probes.py`); the unmutated file is green | Commit the code change, then re-run every recorded check on that commit |
| 3 | Every recorded M0 check reproduces on the candidate with a clean tree | 18 commands through `artifacts/m0-06/evidence.sh` into `artifacts/m0-06/final/`, plus the interruption and failing-test experiments | Every exit code and digest as recorded, nothing adjusted: 118/118 synthetic; `f210eecacf63cbeb…` ×3 fresh processes; `583d1ec544ec61a2…` / `1a6fbf6d24c532ff…` in two fresh workers; both perturbations first diverge at frame 300 and localize to the single byte `0x00073`; exits 4/4 on timeout, 2/2/2 on missing ROM, ROM path and state, 3 on an unreadable manifest, 1 on a wrong `expected` digest and 1 on `save_does_not_perturb` at save point 70 (first differing frame 71). Every report records `abb8c60`, `dirty` false. Full table in [R-0005](../docs/research/R-0005-m0-acceptance.md) | Assemble the gate table |
| 4 | A killed run still leaves no partial verdict, and a failing test is still named | Worker `SIGKILL` during `replay compare` (live workers matched by the `--samples-out` path in their argv, R-0004 G2) then a re-run into the same directory; a deliberate failing-test file in a scratch detached worktree, removed and pruned afterwards | Worker killed: exit 1, `run1: failed`, `fields_identical: skipped`, no `comparisons`, no `divergence` key, **no samples file on disk**; re-run exit 0 with `583d1ec544ec61a2…` in two fresh workers. Failing test: exit 1, 4 failed of 122 checks, each named, the subtest with its parameters, the passing sibling still passing, `python_tooling_tests: 114 tests run, 114 records, 3 failed; runner exit 1` | Write the report and the plan estimates |
| 5 | Every M0 gate has an observation, or is listed as unmet | Gate-by-gate table over the M0 rows of the project plan and build/validation, plus the six spike requirements | 11 of 12 gates met with cited, re-run observations. **One outstanding: CI has not run on `abb8c60`** (the coordinator pushes the branch), so the Linux side of the synthetic gate is unobserved on this candidate and M0 acceptance is blocked until it is green. Limits recorded rather than argued around: the pinned core has never been built or run on Linux; ROM-bound evidence is one host; no automated trusted-fixture runner exists (G7) | Submit for review |

## Handoff

- Current base/head commit and uncommitted state: base `a7ed341` (the coordinator's claim commit on `task/M0-06-acceptance-report`). The only code change is **`abb8c60`** (`tests/tooling/test_replay.py`); the records commit follows it. All ROM-bound evidence was produced at `abb8c60` with a clean tree. No uncommitted state. Ignored: `artifacts/m0-06/` (`final/` reports, samples, derived scripts, work RAM dumps, divergence reports, worker logs; the drivers `evidence.sh`, `interrupt.py`, `mutation-probes.py`), `local/toolchain/`, `local/emulators/bsnes/`, `build/`.
- Verified findings: attempts 1–5 and [R-0005](../docs/research/R-0005-m0-acceptance.md). Acceptance evidence at `abb8c60` (`artifacts/m0-06/final/`, report SHA-256 prefixes): every M0 gate has evidence or is listed as unmet → R-0005's gate table; identities and commands reproducible → `doctor` `1226774e`, `bootstrap-repeat` `50921936`, `build` `8f272257`, `synthetic` `610c5a44` (118/118, `110 tests run, 110 records, 0 failed`), `rom-expect` `5b7d3790`, `rom-absent` `a4b047c4` (exit 2), `reference-build-repeat` `e00528cd`, `verify` `bb8c1ac9` (`f210eecacf63cbeb…`, `df4fb430e731dfe1…`, A/V `edfa4f8e92b24dc0…`), `restore-70` `cbbc6271` (exit 1, only the required `save_does_not_perturb` failed, first differing frame 71), `validate` `a5c7b1eb`, `repeat` `6dd2ceb9` (`583d1ec544ec61a2…` / `1a6fbf6d24c532ff…`, pids 16578/16592 vs comparator 16573), `perturb-removed` `d4544275` and `perturb-moved` `fbb758f5` (exit 1, frame 300, one byte `0x00073`), `timeout-run` `34665668` and `timeout-compare` `64494ce8` (exit 4), `absent-state` `e984a5f7` and `absent-rom` `efbf7f3b` (exit 2), `invalid-manifest` `4ddeb473` (exit 3), `wrong-expected-report` `89eb964e` (exit 1); follow-up test → `synthetic` above plus `tests/tooling/test_replay.py::StubbedCompareTests::test_an_inconsistent_localization_is_failed_not_passed`; interruption and failed-check reporting → `interrupt-worker-killed` `8ba48aa5` (exit 1, no samples file), `interrupt-rerun` `ef174bb6` (exit 0), `failing-test` `64315af9` (exit 1, 4 failed of 122, `dirty` true by design); estimates → the effort table in R-0005 and the new subsection in [the project plan](../docs/PROJECT_PLAN.md). The commits after `abb8c60` touch only `docs/` and `tasks/` (`git diff --stat abb8c60..HEAD`), so the suite result at `abb8c60` stands for the branch head; a confirming re-run on the head gave 118/118 with `dirty` false.
- Current hypothesis and failed approaches: none open. Design choices, not failures: the two localization defects are modelled at the stub rather than by changing `_localize`, since M0-06 must not change command behaviour; the `short` case is kept because it is the one that produces an *empty* diff, which is the failure mode a passing `divergence_localized` would hide; the report cites R-0001 to R-0004 instead of restating them, so a corrected finding has one home.
- Commands executed, outcomes and report hashes: `bash artifacts/m0-06/evidence.sh` (the 18 commands and their expected exit codes are in the script), `python3 artifacts/m0-06/interrupt.py`, `python3 artifacts/m0-06/mutation-probes.py`, and the scratch-worktree failing-test run. Every project command is `python3 tools/project.py <args> --report artifacts/m0-06/final/<name>.json --task M0-06`.
- Unavailable/skipped checks: **CI on the candidate has not run** — the coordinator pushes the branch, so "green on both CI platforms" for the new test is outstanding and M0 acceptance is blocked on it. Not re-run today (cited from the records instead): the parent-kill orphan case, `replay compare --runs 3`, the M0-02 clean-clone sequence and the five clean-clone core builds. Not available on this host: `.worktrees/m0-05/` no longer exists, so no M0-05 artifact digest could be re-hashed — R-0005 cites M0-05's recorded digests instead. No Linux run of the pinned core; no automated trusted-fixture runner (G7); no cross-provider resumption.
- Exact next experiment/command: reviewer on a clean clone with the ROM, at the branch head: `python3 tools/project.py doctor && python3 tools/project.py bootstrap && python3 tools/project.py build --preset lab-debug && python3 tools/project.py reference build && python3 tools/project.py test --suite synthetic` (118 checks; `110 tests run, 110 records, 0 failed; runner exit 0`); `python3 -m unittest discover -s tests/tooling -t tests/tooling -p test_replay.py` (39 tests, no ROM and no core needed); force `consistent = True` in `_localize` in `tools/unirally_lab/replay/commands.py` and confirm `test_an_inconsistent_localization_is_failed_not_passed` fails in both subtests, then drop either clause of that expression and confirm the matching subtest fails; `bash` the equivalent of `artifacts/m0-06/evidence.sh` (the script is ignored, so the reviewer re-issues the commands from R-0005's table) and check each exit code and digest against R-0005; then audit the gate table against the cited records — in particular, look for a gate marked met whose citation does not contain the observation claimed for it, and for an M0 requirement in `docs/BUILD_AND_VALIDATION.md` or `docs/PROJECT_PLAN.md` that the table omits. Runtime needs: network access for the pinned wheels and the core clone, about 15 s for the core build, the ROM path in `local/rom-location.txt`; the full evidence set takes about 3 minutes.
- Remaining dependencies: the coordinator's push of the candidate branch so CI runs on the exact commit; the coordinator's registry row (still `claimed`) and `docs/STATE.md` update on acceptance. Small observation for the coordinator, outside this task's paths: `.worktrees/native` and its branch `task/M0-02-native` no longer exist on this host, so `docs/STATE.md`'s note that the stale worktree "remains" is out of date (`git worktree prune` was run here after removing this task's scratch worktree; the directory and branch were already gone).
- Elapsed work and provider usage when known: about 50 minutes in one session (baseline reproduction ~5 min, the follow-up test and its mutation probes ~15 min, the evidence re-run and the interruption/failing-test experiments ~10 min, the report, plan and records ~20 min); usage not metered.

## Review and integration

Pending. Submitted for independent review at the branch head; not accepted by the worker.
