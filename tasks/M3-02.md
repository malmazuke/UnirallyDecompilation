# M3-02 — Native presentation contract

## Assignment

- Status: second corrected review candidate — identity seam addressed; not accepted
- Milestone: M3, prerequisite to the SDL frontend
- Coordinator: OpenAI Codex Sol/medium (`/root`)
- Worker: OpenAI Codex Sol/medium `/root/m3_02_worker`, dispatched from claim commit `88c0c81`
- Reviewer: fresh sequential OpenAI Codex Sol/medium session after a candidate is submitted
- Provider/model: preserve the provider selected by the user; OpenAI default is Sol/medium
- Dependencies: accepted M3-02A pack/start contract, accepted R-0008 decode and M3-01 full-race state
- Base commit: `ed4505181bcbe6524b6f6645171a6ca92ccca7d9`
- Branch/worktree: `task/M3-02-native-presentation` at `.worktrees/m3-02-native-presentation`
- Owned paths: presentation extraction rules and logical IDs, native offscreen presentation code/tests, task/research records; coordinate shared pack/CLI/docs changes through the integrator
- Session policy: 45-minute checkpoints, one worker, final 20% quota reserve, no spending/publication/deployment
- Claim guardrails: account-wide OpenAI weekly window sampled at 4% used / 96% remaining on 12 September 2026 at 19:24 AEST, reset 19 September 2026 at 19:29 AEST; stop discretionary implementation at 24% used or when only the final 20% reserve remains. The account-wide delta may include unrelated work. One reset credit exists but is not authorized for redemption. No live `reference/worker.py` process was present at claim.
- Current-run override: the user first raised this correction run's ceiling to
  50% at 21% used, then explicitly removed the project percentage stop/reserve
  for the active objective through all remaining M3 work. This is session-only:
  no reset, purchase, publication or deployment is authorized, and future M3
  tasks retain evidence-first commits and independent review under D-0004.
- Worker checkpoint: first 45-minute interval ends at 20:10 AEST. Durable task/evidence updates are due at least every ten minutes; the first action is reference and pack-baseline reproduction before freezing the presentation contract.

## Outcome and boundaries

Define and implement the deterministic, headless presentation boundary for the
accepted CRAWLER/DRAGSTER slice. Recover the rider pose-to-frame mapping and the
BG1 column gather from decoded track data, add the minimum background, rider,
HUD and stable-result assets to a versioned Classic pack profile behind logical
IDs, and render reference-checkable native frames without ROM offsets or a ROM
at runtime.

SDL windowing, live controls and PAL scheduling belong to M3-03. Audio, menus,
other tracks/riders, high-resolution replacements, editor/mod APIs and public
distribution are out of scope. Do not change accepted gameplay state or its
serialization.

## Acceptance

- Freeze presentation fields, sampling phase, logical IDs and expected frame
  cases before native rendering changes.
- Reproduce the BG1 tile-map column gather from the decoded `$7F` block rather
  than replaying the original upload sequence.
- Map the accepted semantic rider pose/orientation state to the original rider
  frame/tile data for preregistered race, finish-delay and result cases.
- Extend extraction deterministically; the new pack remains exact-ROM gated,
  atomic, inspectable, mutation-tested and contains no tracked original bytes.
- Render the minimum BG/rider/HUD/result composition headlessly and compare
  native-resolution output against frozen reference frames with explicit,
  predeclared omissions or per-region mismatch limits.
- Prove gameplay canonical hashes are unchanged with presentation enabled and
  that the renderer runs from the validated pack with the ROM absent.
- Add authored ROM-free parser/renderer boundary tests; pass debug and
  sanitizer suites, private reference checks, repository hygiene audit and an
  independent withheld frame/corruption review.

## First experiment

Regenerate or reuse the accepted R-0008 port/access evidence for frames 1600,
2000 and 2400. At the frame-1334 load boundary, relate `$0234`, `$0236` and
`$0559` to `$7F:000F + X` and the staged 32-byte BG1 columns, then decode that
region into coordinates. After the shared schema is frozen, trace which sprite
ROM reads/DMA rows are selected by the native pose fields at those frozen
frames. Record observations separately from interpretation.

Only the coordinator marks this task accepted after independent review and
integration evidence.

## Evidence and attempts

| Attempt | Hypothesis | Experiment | Observation | Next decision |
| --- | --- | --- | --- | --- |
| 0 | Accepted presentation and pack baselines remain reproducible at the claim commit | Re-ran R-0008 frames 1600/2400 and M3-02A pack-backed continuous finish/restores | Exact prior mismatch counts (490 and 791) and all twelve native finish checks pass | Freeze the presentation boundary before renderer edits |
| 1 | `$81:B31C/$B3D3` is a deterministic strided gather from the decoded track entry | Fresh access capture 1328–1340 with builder pcs/parameters and staging buffers watched; compare every watched load with decoded content | 347/347 loaded words equal decoded `$7F:000F+X`; frame 1334 is 33/33, with observed `(start,stride)` parameter pairs `(16,2)` and `(0,8)` over base `$0559=$8000` | Implement the 32-byte column expansion and bounded strided gather; keep scratch meanings provisional |
| 2 | Existing semantic pose index is the presentation animation key | Compared canonical native states with original `$0FF3/$0FF5` and per-frame sprite DMA inventories | Exact pose keys agree at preregistered race frames; finish provenance names the tile DMA rows through frame 3453 | Implement an explicit supported-pose map; reject unknown poses rather than inventing animation |
| 3 | Stable-result graphics are loaded during the result transition | Filtered the accepted finish provenance for non-OAM DMA over frames 3454–3679 | ROM palette at 3529, two ROM VRAM payloads at 3556/3559, and a constructed 2,048-byte WRAM map upload at 3561; stable 3679 has only OAM DMA | Recover the frame-3560 16-byte template expansion rather than packing a captured WRAM map |
| 4 | The result layout is a deterministic overlay on a uniform tile map | Fresh capture 3559–3561 with `$0200..$09FF` series and transition images | `$83:8B85` fills 1,024 words with `$004C`; frame 3560 overlays 444 bytes in `$0290..$0771`; final map SHA `2d6e563a...0129` persists through stable result | Trace `$80:C431` and sibling writers back to font/layout tables and semantic result fields |
| 5 | Semantic pose changes choose tile payloads while OAM bases remain fixed | Corrected OAM-shadow series at tracked `$138B` after rejecting `$0A00` DMA staging as the wrong layer | Player/opponent are entries 98/99, 64×64, tile bases 0/136, attributes `$66/$68`; frame-2000 opponent wraps to X -26 | Encode pose-specific tile upload slices behind the atlas ID; do not pack captured OAM |
| 6 | The 30×16 decoded region is a complete static BG1 tilemap | Asset-backed SNES tile/palette render at frame 3213 with freshly captured scroll tables | 33,561/50,176 pixels differ (66.89%), failing the frozen 3% limit | Reject hypothesis; port `$81:B270` rolling metatile selector/gather before comparing again |
| 7 | Rolling BG1 columns are selected directly from decoded track by camera coarse/fine position | Fresh access capture 1599–3454 watching `$81:B270/$B366`, `$00..$08`, `$040D/$040F` | Four updating frozen frames read five selector planes at `$5831 + 2*floor(scroll_x/64) + n*$800`; fine X is `(floor(scroll_x/16)+1)&3`; access SHA `39fa19e2...392d` | Check VRAM ring-column phase, then replace the rejected static map expansion |
| 8 | The logical rider atlas and semantic state suffice for original OAM composition | Fresh capture watching VMADD/CGRAM plus the corrected OAM shadow | 108 atlas tiles associate with five exact destination inventories; X=`position_x-camera_x-832`, Y=`position_y-752`; rolling BG/palette/riders reduce frame 3213 to 445/50,176 (0.89%) | Add automated checks for every frozen case and finish the result-screen composition |
| 9 | The first all-case manual reduction satisfies every frozen limit | Native pack-backed renders for frames 1600/2000/2400/3213/3453 | 2000=1.39%, 2400=0.56%, 3213=0.89% pass; 1600=20.39% (windowed `G`) and 3453=11.27% (winner overlay) fail | Keep failures; reconstruct overlays and stable result before automated gate |
| 10 | The two failed effects are deterministic channel-6 window compositions, not missing bitmap art | Captured frames 3452–3454 with `$4360..$436A` and window/colour registers watched; checked `$82:D572` setup and decoded the selected ROM tables | Mode 4 writes WH0..WH3 and XORs the two inclusive windows. The selected 898-byte tables are `$15:8A89` at 1600 and `$15:BF36` at 3453; exact source SHA-256 values are `33f19dae...b29` and `b6fddc69...df20` | Additively freeze both logical entries before the bounded consumer commit; do not change any comparison limit |
| 11 | The four packed result pieces can be placed directly from the transition DMAs | Rendered the stable 3679 reference from a gameplay-identical frame-3678 restore using the first placement hypothesis | The prior diagnostic/placeholder differs at 57,280/57,344 pixels (99.89%), failing 15%. The observed mode-3 registers and final constructed map are closed, but the retained tile-76 background and the two transition payloads still need exact BG association | Retain the failure; associate map tile IDs with the palette/tile payloads before another stable-result claim |
| 12 | The stable screen retains earlier CPU-copied VRAM content omitted from the first result inventory | Captured `$82:B296` for frames 3528–3562 and partitioned its contiguous ROM-source runs at each observed VMADD write | Four final-state source runs total 41,536 bytes: 8,192 at VRAM word `$6000`, 2,816 at `$0000`, 8,960 at `$2000`, and 21,568 at `$31A0`; the later 1,920/3,072-byte transfers overwrite portions in observed order | Additively freeze the retained base-VRAM entry, then replay exact ordering and mode-3 composition |
| 13 | The frame-3529 palette remains the stable result palette | Associated all `$82:B296` runs with their destination port, not only VRAM | A later 216-byte CGRAM copy from `$04:8700` replaces indices 0–107 at frames 3557–3558; its first colour is the stable `$39CE` gray, while the earlier packed palette starts with transition green `$43F0` | Freeze the stable 216-byte palette separately before consuming it; retain the transition palette provenance |
| 14 | Mode-3 BG1 uses direct colour because stable `CGWSEL=$02` has bit 1 set | Retained BG1's three-bit tilemap palette group, implemented bsnes' exact direct-colour formula with authored checks, and compared frame 3679 | Rejected: pinned bsnes `io.cpp` maps bit 0 to direct colour and bit 1 to subscreen blend. Enabling direct colour worsens 54,532/57,344 (95.10%) to 57,344/57,344 (100%) | Keep direct colour disabled for this captured state; recover the remaining CGRAM indices and render the observed result OAM/subscreen composition |
| 15 | The 216-byte stable result palette is the complete stable CGRAM change | Captured every execution of `$82:B18A/$B19D/$B1A3` over frames 3454–3561 and replayed CGADD plus paired CGDATA stores | Rejected: after indices 0–107 load, retained 112–127, frame-3560 128–191 and frame-3561 208–255 values remain. The 128–191 sources already occur in `presentation.classic.palette.v1`; four required 32-byte sources do not | Additively freeze the four missing writer sources as one 128-byte logical entry before its renderer consumer |
| 16 | The residual is caused by an incorrect retained result scroll or full-brightness output | Captured the exact BG1 scroll stores at frame 3559 and inspected all stable INIDISP writes | Scroll hypothesis rejected: `$80:F553` writes 82 then `$80:F63E` later writes 78, so the existing final vertical scroll 78 is correct; horizontal scroll is zero. INIDISP instead finishes its fade at brightness 14 on frame 3568 | Apply exact per-channel 14/15 rounding; retain the remaining structural BG mismatch for a bounded tile-address/source association check |
| 17 | The remaining structural mismatch is retained-VRAM and writer chronology, not tile-origin arithmetic | Bounded read-only Astra consultation, then a fresh capture watching both `$00:210E` and `$80:210E` merged by frame/sequence | Frame 3546 resets VMADD to word `$3D80` after 8,000 bytes; the final banked scroll writes are sequences 798/799 and set 82; the exact header is `PLAYER     TIME`. The earlier attempt 16 ordering inference from separate per-PC arrays is rejected. These changes produce the predicted 961/57,344 (1.676%) at frame 3679 | Preserve the 15% gate and omit the eleven captured result OBJ descriptors; all six frozen cases now pass |
| 18 | Presentation becomes visible before the gameplay phase enum changes | Reproduced reviewer frame 3678, inspected its canonical semantic fields, and added it as an identity-bound withheld visual case | Original result is visible at `ResultLoading`/`PlayerWon` update 225; native gameplay reaches `ResultScreen` at 226. A presentation-only 225 boundary gives 962/57,344 (1.678%) without changing serialization | Retain update 224 as race presentation; enforce the seven-case contract with the tracked command |
| 19 | Recording a supplied manifest hash authenticates its presentation identity | Re-review changed only the declared source ROM SHA-256 to zero and reran the valid pack/fixtures | Rejected: all seven cases passed because top-level contract identities were ignored | Pin every top-level identity and cross-check source/profile/start/rules/logical entries against the validated pack before build/render |

The two channel-6 tables are an additive internal prerequisite discovered by
attempt 9 after the initial logical-ID freeze. Before committing a consumer,
the machine contract and extraction rules now freeze
`presentation.effect.go-window.v1` and
`presentation.effect.winner-window.v1`, each exactly 898 bytes. This preserves
the already declared effect scope and limits; it does not replace or relax a
reference case. The evidence capture passed with `access.json` SHA-256
`3a6a0b56141b36da...`, and the first uncommitted reduction using the frozen
entries measures frame 1600 at 36/26,656 (0.135%, limit 2%) in its declared
rectangle and frame 3453 at 653/50,176 (1.301%, limit 3%).

## Worker checkpoint — 19:40 AEST

- Durable head before this checkpoint: `d79d030`; working baseline and capture artifacts are ignored.
- Contract/research freeze: `tests/manifests/presentation/classic-crawler-dragster-v1.json` and [R-0015](../docs/research/R-0015-native-presentation.md), written before native presentation code.
- Required checks so far: no skip/failure. Reference execution remains one macOS host.
- Next experiment: bind the supported pose keys to deterministic pack assets and add the ROM-free gather/renderer boundary first.

## Worker checkpoint — 20:00 AEST

- Implementation checkpoint `9c2bcb3` adds exact BG1 gather/expansion,
  explicit supported-pose selection, a validated-pack-only headless runner and
  ROM-free boundary/determinism/gameplay-state invariance coverage.
- Debug and sanitizer builds plus focused presentation CTests pass. A private
  ROM-absent run from the frozen pack and frame-3213 canonical state emitted
  `artifacts/m3-02-headless-3213.ppm`, SHA-256 `8c975f8f...ca651`.
- The synthetic suite ran 275 Python records plus 19 native/repeatability
  checks with one failure: `test_native_prepare` assumes the temporary fixture
  is below the worktree, while the coordinator-provided ignored `local`
  symlink resolves outside it. This is not reported as a pass.
- After the coordinator replaced that symlink with an ignored worktree-local
  fixture directory, the exact synthetic suite rerun passed all 275 Python
  records, all 19 native/repeatability checks, and three-process determinism in
  32.5 seconds. The earlier failure remains recorded rather than rewritten.
- This checkpoint is not a review candidate: presentation logical entries are
  absent from the pack and no native visual comparison meets the preregistered
  limits. Next is the deterministic asset extension and provenance-backed PPU
  composition; do not mark accepted.

## Review-candidate handoff — 12 September 2026

- Candidate implementation/evidence commit: `eda0173` (`Complete native
  presentation reference candidate`), based on durable checkpoint `cc35f53`.
- The twenty-five-entry Classic presentation pack is deterministic and
  exact-ROM gated. Its parser/extraction mutation tests reject altered schema,
  identities, payloads, duplicates, missing entries and extra entries. A
  separately corrupted candidate was rejected with `Classic pack
  extraction-rules identity is unsupported`.
- The automated private six-frame comparison passes all unchanged limits:
  1600 0.135%, withheld 2000 1.389%, 2400 0.556%, 3213 0.887%, 3453 1.301%,
  and stable result 3679 1.676%. Report SHA-256 is
  `41a5b36945fc67ec6bd890ffb547fff6aeaeb01dd5a0d23c79f71109cfe13857`.
- Moving the ignored ROM locator aside and invoking the runner only with the
  validated pack and canonical result state succeeds; its PPM SHA-256 is
  `b5cf1100f278a74c7690662bfd4600e458e2693547b9a92b84133715ff75fbbb`.
- `native finish-check` with save boundaries 3213 and 3453 passes two fresh
  processes, gameplay/finish equality, both prefixes/suffixes and both restored
  continuations under `lab-debug` and `lab-sanitize`.
- Full `test --suite synthetic` passes 275 Python checks, 19 native checks and
  three-process repeatability under both presets. Focused presentation/content
  pack CTests pass 2/2. Generated packs, captures, images, states and reports
  remain ignored. Independent review is still required; do not mark accepted.

Exact closing commands were:

```text
python3 tools/project.py access capture --manifest tests/manifests/replay/race-crawler-dragster-12000-continuous-right-fields.json --out artifacts/m3-02-result-scroll-merged --from-frame 3559 --to-frame 3559 --watch-address 0x00210E --watch-address 0x80210E --ring 262144 --task M3-02
ctest --test-dir build/lab-debug -R 'presentation|content_pack' --output-on-failure
python3 tools/project.py build --preset lab-debug --report artifacts/m3-02-final-build-debug.json --task M3-02
python3 tools/project.py test --suite synthetic --preset lab-debug --artifacts artifacts/m3-02-final-synthetic-debug --report artifacts/m3-02-final-synthetic-debug/report.json --timeout 600 --test-timeout 120 --task M3-02
python3 tools/project.py native finish-check --manifest tests/manifests/native/full-race-continuous.case.json --content-pack artifacts/m3-02-presentation.pack --save-frame 3213 --save-frame 3453 --preset lab-debug --artifacts artifacts/m3-02-final-finish-debug2 --report artifacts/m3-02-final-finish-debug2/report.json --timeout 600 --task M3-02
python3 tools/project.py build --preset lab-sanitize --report artifacts/m3-02-final-build-sanitize.json --task M3-02
python3 tools/project.py test --suite synthetic --preset lab-sanitize --artifacts artifacts/m3-02-final-synthetic-sanitize --report artifacts/m3-02-final-synthetic-sanitize/report.json --timeout 600 --test-timeout 120 --task M3-02
python3 tools/project.py native finish-check --manifest tests/manifests/native/full-race-continuous.case.json --content-pack artifacts/m3-02-presentation.pack --save-frame 3213 --save-frame 3453 --preset lab-sanitize --artifacts artifacts/m3-02-final-finish-sanitize --report artifacts/m3-02-final-finish-sanitize/report.json --timeout 600 --task M3-02
git diff --check
```

The respective closing report SHA-256 values are `ea477166...15f3d`,
`67129f45...d66de`, `03e10321...af93`, `a17dfac5...2830`,
`fe2aa32a...8765` and `b07caf07...6b15`. The private comparison driver read
the frozen presentation manifest, six canonical states, their captured camera
and scroll inputs, and six unchanged PNG references, invoked the pack-only
headless runner once per case, and emitted the identity-and-threshold report
recorded above.

## Returned-review correction handoff — 12 September 2026

Correction implementation and response commit: `6797f83`. Clean exact-commit
reports are: debug/sanitizer synthetic `8c58c2fc...7347` /
`7d7fb637...7a69`; debug/sanitizer presentation `4a47f4cc...1397` /
`781275a2...2c2b`; debug/sanitizer finish/restore `bf5f6e3c...5d43` /
`3d992aee...673b`; and deliberate threshold failure `a3731991...24cf`.

All five findings in [the independent review](M3-02-review.md) are addressed:

- Frame 3678 is now a withheld, SHA-bound case. Its semantic presentation
  boundary is player-win `ResultLoading` update 225; gameplay remains unchanged.
- The frozen reflection field is consumed as a supported-combination guard:
  all observed pairs have `reflected=true`, and contradictory `false` states
  are rejected. The exact frame-2000 mutation exits 1.
- Camera arithmetic is wide and safely clipped. `INT32_MIN` and `INT32_MAX`
  pass under sanitizers; scroll 32768 exits 1 before narrowing.
- `native presentation-check` is a tracked manifest-driven command with
  structured reports and three ROM-free failure/seam tests. Its debug and
  sanitizer runs pass all seven cases; a tightened-threshold mutation fails.
- Pack documentation now states exactly 25 total entries: 13 gameplay and 12
  presentation.

Both complete synthetic suites pass 278 Python plus 19 native checks and
three repeatability processes (300 required records total). Pack-backed finish/restore
checks pass in debug and sanitizer at frames 1600, 2000, 2400, 3213, 3453 and
3678. A corrupted pack exits 1. With the ROM locator absent, frame 3678 renders
from the pack alone with PPM SHA-256 `b5cf1100...fbbb`. The correction remains
a review candidate pending independent re-review; it is not accepted.

Exact candidate commands (repeat with `lab-sanitize` and a fresh artifact
suffix for the sanitizer variants) were:

```text
python3 tools/project.py test --suite synthetic --preset lab-debug --artifacts artifacts/m3-02-candidate-synthetic-debug --report artifacts/m3-02-candidate-synthetic-debug/report.json --timeout 600 --test-timeout 120 --task M3-02
python3 tools/project.py native presentation-check --manifest tests/manifests/presentation/classic-crawler-dragster-v1.json --fixtures artifacts/m3-02-presentation-fixtures --content-pack artifacts/m3-02-presentation.pack --preset lab-debug --artifacts artifacts/m3-02-candidate-visual-debug --report artifacts/m3-02-candidate-visual-debug/report.json --timeout 120 --task M3-02
python3 tools/project.py native finish-check --manifest tests/manifests/native/full-race-continuous.case.json --content-pack artifacts/m3-02-presentation.pack --save-frame 1600 --save-frame 2000 --save-frame 2400 --save-frame 3213 --save-frame 3453 --save-frame 3678 --preset lab-debug --artifacts artifacts/m3-02-candidate-finish-debug --report artifacts/m3-02-candidate-finish-debug/report.json --timeout 600 --task M3-02
git diff --check 65b49fa..HEAD
```

## Second correction — contract identity seam

The tracked presentation contract now explicitly binds its Classic pack
profile, semantic start and extraction-rules SHA-256. `load_contract` rejects
any change to the supported source, presentation profile, sampling schema/
phase, state-field order, logical-entry order, omission list or pixel contract,
as well as missing/unknown top-level fields. After pack validation, the command
requires its source/profile/start/rules identities and all presentation logical
entries to agree with those declarations before build or rendering.
The canonical seven-case array is also digest-pinned; it cannot be shortened,
reordered or replaced by attacker-chosen self-consistent fixture identities.

The ROM-free mutation test covers all formerly unconsumed identity fields
and four pack-side identities. Each manifest mutation exits 3 with a failed
report and no `visual_results`; the reviewer's exact all-zero source mutation
does the same. The valid tracked manifest continues to pass the same seven
visual cases and limits. This remains a review candidate, not an acceptance.

Identity correction commit `887e4bb` has clean exact-commit evidence. Debug
and sanitizer synthetic reports (`6f9109a6...4a76`, `503e0cae...36c1`) each
pass 278 Python records, 19 CTests and three repeatability processes. Debug and
sanitizer presentation reports (`b1b632d3...6fff`, `3a8ce350...3caa`) pass all
seven unchanged cases. Pack inspection (`973f4564...d09c`) validates all 25
entries; frame-3678 continuation (`a7631bf9...884b`) passes; and the ROM-absent
seven-case report (`340d9cfb...d8fa`) passes. The exact all-zero source
mutation exits 3 before build/render with failed report `cf95e7ef...929c` and
no `visual_results`.
