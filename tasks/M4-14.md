# M4-14 — Sustained native ZOOM ZOO traversal

## Assignment

- Status: ready/unclaimed for the next user-started Astra/medium session.
  This preparation does not launch gameplay work.
- Milestone: M4 (incomplete); primary directly owns investigation, native
  implementation and coordination under [D-0006](../docs/decisions/D-0006-capability-driven-work.md).
- Primary: OpenAI `gpt-6-astra`/medium, standard service. Automatic independent
  reviewer: fresh `gpt-5.6-sol`/medium, explicit candidate/isolated checkout,
  `fork_turns: "none"`. One active child maximum; no routine implementation child.
- Budget: [D-0004](../docs/decisions/D-0004-model-and-usage-budget.md#m4-14-sustained-traversal-extension):
  waive the 20-point task cap, preserve last 20% weekly review/recovery reserve.
  Record fresh usage; no reset/purchase/provider switch authorized.
- Accepted ancestor: `01cb7b62ff6ef6de513b842f72f37212853be36f`. Record the exact
  synchronized main tip containing this preparation as dispatch base; verify
  ancestry and create `task/M4-14-sustained-traversal` in isolated
  `.worktrees/m4-14-sustained-traversal`. Record actual names if occupied.
- Owned: reached native motion/contact/mode/control/pose/AI/progress/finish
  dependencies, content/extraction/state/serialization, comparison/capture tooling,
  tests, additive contracts and evidence/source guide; primary also owns registry
  and integration. Reviewer owns independent cases and report in its checkout.
- Frozen: accepted DRAGSTER and M4-12/M4-13 expectations, case/ROM/core/content
  identities. Add necessary state/content versions without relabeling old data.
  A core/lock change needs documented necessity and independent validation.
- Excluded: frontend/presentation/audio, other game modes, editor/networking,
  public release/deployment and speculative frameworks. Any gameplay transition
  actually reached by this traversal, including finish if encountered, is an
  internal dependency rather than grounds to shorten the domain.

## Required outcome — keep the larger target

Run both riders autonomously from the authentic end-1649 seed, static content
and **continuous Right** for controller 0, with controller 1 neutral, through
at least **end-3299: 1,650 updates / 33 PAL seconds**. Preserve the accepted
pre-seed reference input history. No earlier B-jump substitute, shortened native
prefix, later seed or captured-state repair can satisfy this assignment.

Before native tuning, freeze repeatable original reference output, exact state
inventory, controller script, horizon H >= 3299, landing/mode entry and exit
criteria, and a measurable definition of recovery/resumed track progress based
on authenticated state/writers. The run must include **200 subsequent updates
of resumed progress after that recovery**; extend H from reference observation
if needed. Do not define recovery retroactively to fit a native result. If the
reference does not satisfy the criterion by 3299, investigate/extend the same
continuous-Right scenario rather than choosing a convenient shorter one.

New steeper contact, mode, pose, boost/camera, geometry, AI or progress producers
belong inside this task. Small experiments, commits and functions are encouraged;
separate task acceptance per routine or field is not. Checkpoints record progress,
not permission requests or new assignments. A budget stop leaves this capability
incomplete; useful partial research must not be relabelled as acceptance.

## Starting evidence — established versus exploratory

[M4-13](M4-13.md) and [R-0032](../docs/research/R-0032-player-landing-recovery.md)
accept both riders through 1849, including B-jump landing/recovery. The existing
`update_zoom_zoo` in movement.cpp rejects updates after 1849, so **no native
first divergence at 1991 has yet been demonstrated**. The vertical-contact
component currently restricts recovered coefficient magnitudes to eight.

The prior agent's follow-up reports exploratory continuous-Right observations:
player landing near 1991, unsupported count 9 -> 0, horizontal velocity 483 -> -6,
surface angle -8 -> -16; `$0B93` becomes 1 and `$0B97` becomes signed -16 at
1992, and `$0BAB` pulses at 1991/2185. Private source:
`.worktrees/m4-13-player-landing/artifacts/m4-13/explore-right.json`.
These later observations are not yet repeat-frozen/writer-audited acceptance
facts. Reproduce and authenticate them before deriving new behavior.

Existing [contact mapping](../docs/research/R-0011-contact.md) associates `$0B93`
with scratch `$0F23` mode selection. The role/producers/reset rules of `$0B97`
and `$0BAB` remain provisional; do not name crash/tumble mechanics from their
correlation with landing. `$132B` is a separate previously authenticated landing
matrix option; its earlier zero value does not prove it stays zero later.
Camera involvement at 1991 has not been established. Recover each actual reached
read/writer and state lifecycle; removing guard checks alone is not recovery.

## Startup and execution

Read NEXT_SESSION, STATE, AGENTS, AGENT_WORKFLOW, this task and D-0004/D-0006.
Consult PROJECT_PLAN for scope, BUILD_AND_VALIDATION for implemented recipes,
R-0032 and M4-13 review for bounded behavior, and R-0030 as needed for static
content and seed provenance. Do not reread every historical task by default.

Verify clean synchronized main, ancestry, model/effort, clock and actual quota;
prepare real local/artifacts roots and verified caches under the
[documented layout](../docs/BUILD_AND_VALIDATION.md#worktree-cache-and-report-layout).
Reproduce an accepted M4-13 primary comparison/restores plus the M4-12 primary
using the [current laboratory](../docs/BUILD_AND_VALIDATION.md#m4-13-player-landing-laboratory)
and linked recipes. Private reports/captures live in the M4-12/M4-13 task and
review worktrees; hashes, not paths, establish identity. Fresh outputs are required.

Extend the reference/native harness deliberately: both accepted laboratories
are bounded to the old horizon. Do not claim arbitrary-frame commands exist.
Keep full state projections across the longer run, using targeted instruction
windows to investigate first divergences instead of repeatedly collecting huge
whole-run instruction traces. Preserve completeness/ordering/source evidence
for every claimed producer and any supplemental capture. Freeze additive state
or content inventories before tuning against them; keep prior expectations intact.

Implement and compare early. Retain integer widths, ordering, timing and readable
native updates under D-0003. Inventory every future-affecting field even if the
current 395-byte serialization omits it. No per-frame state injection, ROM CPU
fallback, scripted motion, fabricated constants or selective comparison that
hides newly changing fields. Unsupported guards may remain only outside the
accepted new domain; reached transitions must be recovered and validated.

## Acceptance

| Criterion | Required evidence |
| --- | --- |
| Sustained primary | Every declared state byte for both riders exact from end-1649 through H >= 3299, plus 200 updates after authenticated recovery/resumed progress. Primary stays continuous Right; original references repeat exactly. |
| Dependency closure | All reached mode/angle/contact/control/pose/AI/progress and relevant camera/boost state produced natively and serialized. Static inputs authenticated; later dynamic captured inputs zero. |
| Independent generalization | Fresh Sol reviewer selects two untuned input variations after candidate freeze. Both run at least the primary H, exercise the newly recovered later transitions materially and include 200 updates after their own reference-defined recovery (extend a case horizon if needed). Repeat/freeze reference before native evaluation. Inert cases or those avoiding the new mechanics do not qualify. |
| Restores and autonomy | Fresh-process restores before/during/after landing/mode transitions and a later traversal boundary reproduce uninterrupted native state and reference through each full horizon. Demonstrate native runtime works with ROM/repository/reference access denied; negative controls expose accidental fallback. |
| Regression and quality | Preserve all accepted M4-12/M4-13 primary/cases/restores and DRAGSTER gates. Focused new arithmetic/order/width/branch tests; human-readable functions and navigable source evidence. |
| Integration | Fresh Sol approval of corrected candidate, complete local debug/sanitizer and frozen private gates, exact merge checks, private remote ref and final-tip hosted macOS/Linux CI verified. Explicitly distinguish synthetic Linux CI from private Linux differential execution. |

Review failures used for tuning become disclosed regressions and need fresh
untuned replacement cases. Primary automatically handles findings/re-review and
integration; no user relay. Do focused checks before initial review, then broad
validation after initial findings are resolved, overlapping focused re-review
where useful. Subsequent changes still require appropriate revalidation.

## Completion and continuity

Record native horizon reached, first divergence, unresolved coupled dependencies,
new branches/modes, next experiment and quota at ten-minute checkpoints and before
expensive work. Reassess approaches at 45 minutes without terminating the task,
rotating agents or reducing its target. Respect resource/access boundaries.

Use [consolidated closeout](../docs/AGENT_WORKFLOW.md#consolidated-closeout): finish
review/source/local gates and tracked handoff before the final main push, leave
remote acceptance explicitly conditional, then store actual ref/CI/time/usage in
`artifacts/m4-14-integration/closeout.json`. Final user report names that evidence.
Do not create another full CI cycle merely to transcribe the first success.
No preliminary task-branch push is required by this local review workflow.

Report sustained coverage and recovered transitions, remaining product limits,
measured time/usage and review findings; no full-game claim from a finite run.
End after this capability is accepted or a genuine resource/access boundary.
Do not automatically dispatch M4-15. Preparation handoff: no claim, new reference
capture or native change; fresh startup and domain freeze remain implementation work.
