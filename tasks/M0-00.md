# M0-00 — Establish source control and durable tasks

- Status: accepted
- Coordinator/worker: current primary agent
- Scope: local Git baseline, user-supplied origin, tracked task records, private-input exclusions.
- Remote: `git@github.com:malmazuke/UnirallyDecompilation.git` (private; empty on inspection).
- Acceptance: reviewed planning baseline committed and pushed; local inputs excluded; next work recorded with ownership and dependencies.
- Checks: `git status`, staged diff inspection, Markdown link validation, `git check-ignore` for local inputs.
- Exclusions: ROM publication, releases, deployment and paid services.

## Handoff

Repository initialized on `main`; private origin configured. Planning baseline `7ce358e` committed and pushed. Staged diff and whitespace checks passed; ignore checks confirmed `local/`, `artifacts/` and `.worktrees/` exclusions. Planning links were validated. Task records and registry exist. Subsequent implementation is paused at the user's request.
