# M4-08 — ZOOM ZOO position integration and residue recurrence

## Assignment

- Status: accepted 13 September 2026 after independent approval and green
  macOS/Linux CI
- Milestone: M4 — Original game coverage
- Planning: bounded OpenAI `gpt-6-astra`/high, read-only at accepted M4-07
  head `ba43872`
- Worker: OpenAI Sol/medium in isolated worktree; fresh sequential Sol/medium
  reviewer owns a private one-frame release in frames 1663--1665
- Usage: user-waived percentage guardrails; at most one weekly reset only after
  an actual provider block; no spending/provider switch
- Dependencies: accepted M4-03--M4-07, R-0011, authenticated end-1649 seed,
  supported PAL ROM and pinned core
- Base/branch/worktree: `ba43872`, `task/M4-08-zoom-zoo-position`,
  `.worktrees/m4-08-zoom-zoo-position`
- Owned: additive position-integration capture/research tooling, tests,
  manifests and replay variation; `docs/research/R-0026-zoom-zoo-position.md`;
  task/review records and additive command inventory
- Frozen: `src/`, accepted captures/modules/manifests/projections,
  serialization, Classic pack/profile/start, frontend, core and locks;
  coordinator owns registry/state

## Outcome and boundary

Recover and independently evaluate `$82:A627--A6F7` for both riders' 102 calls
on frames 1650--1700: signed /32 displacement, persistent residue recurrence,
track masking and exercised slope-contact Y adjustment. Propagate all four
residues from the authenticated end-1649 seed rather than reseeding per call.

Position, velocity and contact-state inputs remain captured external boundaries.
This closes one upstream dependency; it is not autonomous movement, live native
ZOOM ZOO, a wrap-crossing claim, or Classic-pack/profile expansion.

## Planning observations and hypothesis

Accepted access `267ecfbf...fab67` records frame 1662 player y 1561 at
`$82:A6B9` then 1565 at `$82:A6EB` (+4), after surface `0xFFFF` and guard reads.
Frame 1665 records y 1558 at `$82:A69B` then 1557 at `$82:A6F5` (-1), with
incoming vy `0xFFD2` and surface `0xFFFD`. The window reaches 20 positive/+4
stores at `$82:A6EB` and 14 negative/-1 stores at `$82:A6F5`.

Native `integrate_motion` has signed displacement/residue division but omits
the slope Y adjustment and `$0D4F` mask. R-0011 supplies a prior DRAGSTER-domain
hypothesis, not proof here. The existing capture leaves `$82:A6E9`'s
direct-page read unresolved; the worker must establish ordered last-write or
register provenance in a dedicated additive capture.

The hypothesis is that R-0011's equations transfer with the reached slope
predicates/mask and that four residue chains close from one seed. Missing
producer evidence narrows the contract and may not become a default.

## Acceptance

| Criterion | Required evidence |
| --- | --- |
| Foundation | Reproduce accepted M4 identities and exact 1650--1700 contact capture/component |
| Producer capture | Two fresh runs; all ordered calls, inputs/producers, guards, residues, intermediates, mask and final publications complete; resolve `$82:A6E9` dependency |
| Seed and equations | Authenticate end-1649 four-residue seed; derive widths, wrapped sums, signed division/remainder order and slope predicates from instructions |
| Stateful component | Compute 102 calls while propagating four residues without per-call oracle replacement; exact intermediates, x/y and residue publications; external captured inputs explicit |
| Worker variation | Release Right only at frame 1662, restore 1663; exact through 1661/first controller divergence 1662, with branch/timing/reconvergence not preclaimed; two fresh runs |
| Reviewer variation | Different private release in 1663--1665, independent capture and arithmetic inspection; relevance described honestly |
| Integrity | Residue reseed, rider/call swap/loss, bad seed, missing slope adjustment, mask/order, identity and incomplete evidence mutations fail/change result |
| Regression | Focused/debug/sanitize and frozen M4 contract/contact, pack and DRAGSTER restore gates pass |

## Handoff

- Claim base: accepted M4-07 `ba43872aeda8da6bf0ac37148380e480c950697e`.
- First action: reproduce foundations, then preregister exact producer watches
  before observing new results. Resolve the old `$82:A6E9` gap explicitly.
- Worker variation preregistration: one-frame Right release at 1662 and restore
  at 1663, every other event unchanged. Predict agreement through 1661 and first
  controller divergence 1662 only.
- Use only ignored private fixtures/artifacts. Record non-passes exactly. Only
  the coordinator accepts after independent review, integration, hosted CI and
  private-remote verification.

### Worker candidate

- Preregistration commit `93503d05d34b4446541f223e8c50c74b8bea68df`
  froze the full routine watch set and the release-Right-only-at-1662 replay
  before any new M4-08 observation. Implementation commit
  `e590babdb3d3408f69c29808029cb78fdc61df82` adds only the research component,
  manifests, replay result, focused tests and additive command inventory.
- Two fresh primary captures agree at access SHA `a53e6617...15c2f` and WRAM
  series `53c03d22...d13d`; two variation captures agree at `95cfe960...d426`
  and `a747635d...ca20`. All four have zero unresolved stores/non-ROM PCs and
  no overflow. Unresolved reads are reported, never used as defaults.
- The authenticated end-1649 seed is player `(9200,1561,-1,22)`, opponent
  `(8248,1452,-24,-1)`. Four recurrent residues close every one of 102 ordered
  calls in both scenarios. Primary rows/report are `aaf2eb99...1bfe` /
  `e6a787e8...1254`; variation rows/report are `5ace936f...3028` /
  `33c85f3a...4b70`.
- `$82:A6E9` is resolved by ordered same-call provenance: integrated Y from
  `$82:A6B9` or `$82:A69B`, accumulator 4 entering the ADC, and producer+4 at
  `$82:A6EB`. No guessed/default value remains. The component preserves
  16-bit wrapped sums/additions, signed magnitude `/32`, sign-restored
  quotient/remainder, post-add X mask and original slope-tail guard order.
- The worker replay passes twice with sample/final/A/V hashes
  `b56fe4c2...0661`, `1b603f9b...965d`, `25f54d56...2634`. Primary comparison
  intentionally diverges at 1662. Controller `$0313` differs only at 1662;
  player component first differs at 1662, first branch divergence is 1678,
  opponent stays exact and player does not reconverge by frame 1700. Branch
  differences occur at 1678--1681 and 1685; this timing was not preclaimed.
- Focused M4-05--M4-08 checks pass 37/37. Clean debug and sanitizer at
  `e590bab` each pass 350 Python tests, 20 CTest checks and three-process
  repeatability `0be347c529fadda9`, with report hashes
  `bc16a377...39af` and `a6d5ac71...1a46`. Frozen contract, contact, vertical,
  reflected, 25-entry pack and DRAGSTER finish/four-restores gates all pass;
  exact report hashes are recorded in R-0026.
- Initial missing ROM/toolchain attempts were correctly reported as missing;
  the initial synthetic run's five native-report skips and missing build are
  not passes. The zero-placeholder replay failed only its four expected digest
  checks while both fresh runs agreed. Primary-versus-variation replay exits 1
  by design. Private source bytes/captures/reports remain ignored.
- `src/`, accepted tooling/captures/manifests/projections/replays,
  serialization, pack/profile/start, frontend, core, locks and coordinator
  records are unchanged. Status remains review. A reviewer still owns the
  different private one-frame release in 1663--1665 and coordinator alone may
  accept/integrate.

### Review and integration acceptance

- Independent review `a2e0125` approved without a material finding. Its private
  frame-1664 release was captured twice identically and independently evaluated
  for all 102 recurrent calls from the same end-1649 seed. Controller/component
  first differ at frame 1664, branch path first differs at 1676, the opponent
  remains exact and the player does not reconverge by 1700. Reseeding 99 calls,
  removing `$82:A6E9` provenance, and seed/order mutations fail as required.
- Coordinator integration `6de3dd5` reproduces exact component
  `e6a787e8...1254`. Focused checks pass 37/37; app-debug passes 350/350 Python
  checks, all 20 CTest checks and repeatability (`f6f1f653...bab0`). The worker
  variation replay passes 24/24 with exact A/V (`555ac83b...4e14`), Classic
  pack passes 3/3 (`b2839fb0...b9e3`) and DRAGSTER finish/restores pass 18/18
  (`e012d15c...929e`).
- Hosted run `34741431794` passes macOS 15 and Ubuntu 24.04, including Linux
  SDL sanitizers. M4-08 is accepted as one stateful upstream producer; captured
  velocity/contact inputs remain explicit and native ZOOM ZOO is not claimed.
