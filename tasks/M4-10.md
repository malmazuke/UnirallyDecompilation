# M4-10 — ZOOM ZOO inter-contact response-B producer and recurrence

## Assignment

- Status: claimed 13 September 2026 after bounded Astra planning audit
- Milestone: M4 — Original game coverage
- Planning: OpenAI `gpt-6-astra`/high, read-only at accepted M4-09 `2d1130e`
- Worker/reviewer: isolated Sol/medium worker, then fresh Sol/medium reviewer
  with a private one-frame Right release at frame 1669 or 1670
- Usage: user-waived percentage guardrails; at most one weekly reset after an
  actual provider block; no spending/provider switch
- Dependencies: accepted M4-03--M4-09, R-0011, supported PAL ROM/pinned core,
  authenticated end-1649 seed
- Base/branch/worktree: `2d1130e`, `task/M4-10-zoom-zoo-response-b`,
  `.worktrees/m4-10-zoom-zoo-response-b`
- Owned: additive response-B capture/evaluator/composition adapter/tests,
  manifests/replay, `docs/research/R-0028-zoom-zoo-response-b.md`, task/review
  records and additive command inventory
- Frozen: `src/`, accepted tooling/captures/manifests/replays/projections,
  serialization, pack/profile/start, frontend, core/locks; coordinator owns
  state/registry

## Outcome and boundary

Remove both riders' `response_b` values from M4-09 per-call captured inputs.
Seed them once at end-1649, recover every reached intervening producer, and feed
the computed values into the accepted position/contact composition. Other
velocity, pose and contact fields remain explicit captured inputs. This extends
the bounded recurrence from eight words to ten; it does not establish autonomous
ZOOM ZOO gameplay.

## Planning evidence and hypothesis

The accepted M4-09 evidence disproves blind contact-output feedback: the
opponent publishes `response_b = 254` at frame 1661 but enters the next contact
with zero at frame 1662. R-0011 maps scratch `$0F57` into rider words
`$0BB7/$0BB9` and identifies an active-phase rotation at `$82:A49F--A5F9`,
including word clears, preserved values and low-byte writes that preserve the
high byte. Its meaning was intentionally left provisional. Pose processing
consumes response B before collision, so phase and writer ordering must be
observed rather than inferred. The production DRAGSTER specialization's 2/0
range cannot be generalized to ZOOM ZOO's observed 254.

## Acceptance

| Criterion | Required evidence |
| --- | --- |
| Combined capture | Two fresh end-1649--1700 windows covering persistent words, scratch `$0F57`, overlaps, rider/phase, actual writers and guards/widths, pose consumption, contact entry/output/publication; identify the observed 254-to-0 transition without inferred zeros |
| Original arithmetic | Derive byte versus word writes, high-byte preservation, signed/wrapped behavior and active/inactive phase handling from instructions and ordered traces |
| Recurrence | From one authenticated end-1649 seed plus an explicit inventory of external producer inputs, compute both riders' response-B entry/output for all 102 calls; later captured response-B values are comparison targets only |
| Composition | Feed computed response B into the accepted M4-09 composition; retain exact 102 calls, 1,020 sample words/source offsets and final eight position/residue words; compare pose-consumed response without claiming recurrent pose |
| Worker variation | Release Right only at frame 1668 and restore 1669; predict exact through 1667 and controller divergence at 1668 only; reproduce twice and report actual downstream relevance |
| Reviewer variation | Independently choose frame 1669 or 1670; capture/order audit and honest relevance assessment |
| Integrity | Seed/rider swaps, omitted writer, wrong phase, per-call reseed, captured substitution, ordering, byte/word width, high-byte preservation and clear mutations reject or change the result |
| Regression | Focused/debug/sanitize and frozen M4/replay/pack/native gates pass on the exact candidate |

## Handoff

- Claim base: `2d1130e2d95847395c560bbfa81f1271f6bbad34`.
- First action: regenerate any ignored accepted artifacts needed by the task,
  reproduce the M4-09 focused foundation, and preregister the complete watch set,
  frame-1668 input variation and writer/phase hypotheses before new results.
- Disk exhaustion is a missing prerequisite, never a pass; generated private
  captures remain ignored and may be deleted only when reproducible and no longer
  needed for the active task.
- Only coordinator acceptance follows independent review, integration, hosted CI
  and verified private-origin synchronization.

### Preregistration checkpoint

- At claim `087660809f66c4c8d5a2468421ef306b0ff3a7f0`, before any new
  M4-10 result, the worker reproduced the accepted focused foundation 46/46.
  [The additive inventory](../docs/inventory/M4-10-response-b.md) freezes the
  combined watch set, producer/phase/width hypotheses, response-B order and
  exact worker-variation prediction.
- The new replay releases Right only at frame 1668 and restores it at 1669;
  its zero expected digests are intentional preregistration placeholders. New
  captures and result-dependent authored manifests follow in a later commit.

### Worker candidate

- Preregistration commit: `5562690b64ac42a023e5ae5a29a0d9d61e60ec84`.
  Implementation/evidence commit:
  `3d9a083bb2bcc6a4df25c103e03304dd7f95654e`.
- Two fresh primary captures agree at access `f8488406...25bcd` and series
  `53c03d22...d13d`; two fresh variation captures agree at
  `e2e52693...e6f54` and `1f5f269e...99420`. All are complete,
  nontruncated, have zero unresolved stores/non-ROM PCs and a maximum ring
  delta of 18,640/262,144.
- [R-0028](../docs/research/R-0028-zoom-zoo-response-b.md) authenticates the
  347-byte `$82:A49F--A5F9` producer, its ordered motion/pose/contact
  publications, 16-bit predicates/clears and 8-bit replacement semantics.
  Active phase 1 selects the player and phase 0 the opponent; inactive calls
  preserve. Unreached short-circuited inputs remain unavailable, not inferred.
- The end-1649 seed is player 0/opponent 254. All 102 calls recur exactly:
  51 inactive preserves, 42 low-angle/count word clears, six reached opponent
  `0xFE + control` low-byte writes and three zero-pair word clears. The
  frame-1662 opponent word clear proves the accepted 254-to-0 transition.
  Pose consumption and all motion/contact inputs/publications match.
- Computed response B feeds the accepted M4-09 composition without changing
  its 102 calls, 1,020 sample words/source offsets or final eight recurrent
  words. Primary response rows are `0a6ba3b5...337ba`; both response words
  finish at zero.
- The worker variation satisfies the preregistration: exact through 1667 and
  controller divergence at 1668 only. Full player composition first differs
  at 1668 and samples at 1672; opponent stays exact and player does not
  reconverge by 1700. Response-B values never differ, an honest negative
  relevance result; its producer evidence row first differs at 1669.
- Focused tests pass 51/51. Final app-debug and app-sanitize runs each pass 364
  Python tests, 20 CTests and three-process repeatability. The finalized replay
  passes 24/24; the frozen contract and pack each pass 3/3; DRAGSTER finish and
  restores at 1600/3213/3453/3678 pass 18/18. Exact commands and report hashes
  are in R-0028.
- Non-passes from intentional zero replay digests, an unsupported replay
  option, the first incomplete watch set, missing isolated toolchain and one
  wrong native report location are recorded in R-0028; every affected gate was
  corrected and rerun without weakening an expectation.
- No `src/`, accepted evidence/tooling/manifests/replays/projections,
  serialization, pack/profile/start/frontend, core/lock or coordinator-owned
  file changed. Private ROM bytes, content, captures and reports remain ignored.
  Independent review remains; only the coordinator may accept and integrate.
