# M4-05 — ZOOM ZOO contact-path audit and first unsupported branch

## Assignment

- Status: claimed 13 September 2026 after bounded Astra planning
- Milestone: M4 — Original game coverage
- Planning consultation: bounded OpenAI `gpt-6-astra`/high completed read-only
  at accepted M4-04 head `b6f87d0`; it found and corrected a material premise
- Task provider/model: OpenAI, Sol/medium worker in an isolated worktree
- Provider usage: the user waived percentage guardrails for the unattended M4
  goal and authorized at most one weekly usage reset only if an actual provider
  limit is reached; no money, paid credits or provider switching is authorized
- Reviewer: fresh sequential OpenAI Sol/medium reviewer with an undisclosed
  adjacent first-Right onset variation
- Dependencies: accepted M4-03 content/read contract, accepted M4-04 riding
  freeze, R-0011 contact contract, supported PAL ROM and pinned core
- Base/branch/worktree: `b6f87d0`, `task/M4-05-zoom-zoo-contact`,
  `.worktrees/m4-05-zoom-zoo-contact`
- Owned paths: additive ZOOM ZOO contact research tooling/tests/manifests,
  `docs/research/R-0023-zoom-zoo-contact.md`, a narrow evidence correction to
  R-0022, this task and review records
- Frozen paths: native movement/contact/sampling, serialization, accepted
  projections/replays/content expectations, core/locks, Classic pack/profile/
  start, frontend and presentation

## Outcome and boundaries

Correctly classify both riders' contact paths for frames 1650--1700 (at most
102 calls), locate the first genuinely exercised condition outside the accepted
native flat-contact domain, and recover only that branch plus the state needed
through the preceding and following calls. Produce a captured-argument research
component that independently reconstructs preprocessing, reduction and changed
persistent outputs from authenticated static content.

This is not autonomous movement, native ZOOM ZOO, a general contact format or
pack expansion. AI, finish, camera, presentation, audio and later contact
branches remain out of scope.

## Planning correction

M4-04's capture/projection is valid, but R-0022's frame-1650 blocker
interpretation is not. Native `flat_contact.cpp` and the original R-0011
contract both test `(descriptor & 0x03FF) == 0` before the direction guard.
Thus player points 8--0 at `0x5800` and all opponent points at `0x7800` are
marker-only. Player point 9 is `0x1AE6`, whose `0xC001` guard mask is zero.
No frame-1650 word proves the directional branch. Correct the prose without
changing any frozen value or expectation. Width 256/stride 512, the absent
pack, and mask `0x3FFF` remain separate observed integration limits; mask
presence alone does not prove wrap is exercised.

## Acceptance

| Criterion | Experiment | Expected result |
| --- | --- | --- |
| Corrected blocker | Reproduce frame-1650 raw words and original/native predicate order | Marker handling agrees; the false `0x5800` direction attribution is corrected without changing frozen data |
| Complete bounded classification | Compactly capture both riders for 1650--1700 | All 102 calls are complete, ordered by rider/call and classified in actual guard order; first incompatible frame/rider/predicate is reproducible |
| Branch contract | Narrow ordered trace around first incompatible call plus preceding/following call | Inputs, static reads, scratch, persistent outputs, widths, signedness, wrapping and publication order are exact through return |
| Independent reconstruction | Research component consumes captured incoming arguments and independently extracted authenticated content | Preprocessing, reduction and changed persistent outputs match exactly; no captured result is replayed as computation |
| Recurrence/integrity | Mutate call presence/order/rider/descriptor classification/state/identity and incomplete evidence | Wrong, omitted, duplicated, swapped, truncated or defaulted dependencies fail |
| Variation/review | Shift first Right onset by a preregistered worker amount; reviewer owns an undisclosed adjacent onset | Exact pre-input agreement; branch result is predicted from actual inputs, with unchanged/reconvergent response recorded honestly |
| Regression | Focused and synthetic suites plus accepted M4-03/M4-04 identities and DRAGSTER native restore | No frozen expectation, pack or native behavior changes |

## Experiment order

1. Reproduce M4-04's start capture hash `1243dbe5...a269`, frame-1650 raw
   words and marker-before-direction order. Inspect the accepted compact contact
   capture infrastructure before adding code.
2. Capture 1650--1700 with missing penetration/angle pairs `$0230`--`$0243`,
   axis selectors `$0290`--`$02A3`, postprocessed descriptors `$02C0`--`$02D3`,
   relevant tile bytes/flags and reducer/response outputs. Preserve rider and
   call boundaries because scratch is shared.
3. Classify guards in execution order and narrow the first actual unsupported
   call. Trace from incoming samples through reducer, response and caller
   publication, including adjacent calls for read-before-write state.
4. Before a worker variation, preregister a small first-Right shift and predict
   exact pre-input agreement. Do not promise the same contact frame or path.
5. Freeze identities and component equations without changing production
   native or pack code.

PC snapshots are pre-instruction and separate arrays do not prove cross-PC
order. Missing or unresolved dependencies stay explicit rather than being
initialized plausibly.

## Handoff

- Current base/head: claim begins from accepted M4-04 `b6f87d0`.
- Verified correction: frame 1650 has one player `0x1AE6`, nine player
  `0x5800`, and ten opponent `0x7800` descriptors; marker predicates precede
  the direction guard, so no directional path is established there.
- First action: reproduce that classification, then capture/classify every call
  through frame 1700 before selecting the first actual incompatible branch.
- Runtime needs: supported private PAL ROM, pinned core, ignored artifact space;
  no network, publication, deployment or spending.
- Checkpoint after correction, bounded classification, branch reconstruction
  and at least every ten minutes. Only the coordinator accepts after review.
