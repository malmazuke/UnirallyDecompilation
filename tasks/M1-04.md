# M1-04 — M1 acceptance report

## Assignment

- Status: review (submitted by the worker, 12 September 2026; claimed by the coordinator at 03:05 AEST with the registry row updated in the same commit, M0-05 gap G1)
- Milestone: M1
- Coordinator: primary agent (Claude Code, Fable 5.1)
- Worker/session/runtime/model: fresh session of Claude Fable 5.1, run as a subagent of the coordinator's session and dispatched with the portable prompt in `docs/AGENT_WORKFLOW.md` and nothing else
- Reviewer: fresh independent session, own clone (dispatched by the coordinator after submission)
- Dependencies and evidence of acceptance: M1-01 ([R-0006](../docs/research/R-0006-observed-code-map.md)), M1-02 ([R-0007](../docs/research/R-0007-player-state.md), [D-0002](../docs/decisions/D-0002-data-access-observation.md)) and M1-03 ([R-0008](../docs/research/R-0008-track-decode.md)) accepted (see [the registry](README.md)); M0 accepted ([R-0005](../docs/research/R-0005-m0-acceptance.md))
- Base commit: the claim commit on `task/M1-04-acceptance-report` (its parent is `607cb94`, `main` after the M1-03 integration and its CI note)
- Branch and isolated worktree: `task/M1-04-acceptance-report` in the linked worktree `.worktrees/m1-04` (ignored path; the coordinator's checkout stays on `main`). Fixture arrangement as M1-02/M1-03: the coordinator placed `local/rom-location.txt` in the worktree and copied the ignored `local/toolchain`, `local/emulators` and `local/cache` directories from the main checkout; nothing else was copied. The M1 evidence directories on this host are `.worktrees/m1-01/artifacts/m1-01/`, `.worktrees/m1-02/artifacts/m1-02/` and `.worktrees/m1-03/artifacts/m1-03/` if the report needs to cite a file digest from them. The coordinator pushes the candidate branch to origin for CI
- Owned paths and shared interfaces: `docs/research/R-0009-*` (the acceptance report); `docs/PROJECT_PLAN.md` only for the next-stage estimate paragraph; `tests/tooling/test_access.py`, `tests/tooling/test_content.py`, `tests/tooling/test_coverage.py` and `tools/unirally_lab/coverage/derive.py` only for the follow-up checks named below; this record. Must not change any command's behaviour or output, any schema, manifest, tracked map or content manifest, the pinned core, `src/lab`, `docs/STATE.md` or the registry (coordinator-owned)
- Claim/lease/heartbeat/checkpoint location: this file and the registry row; checkpoint commits at least every 10 minutes
- Session time limit, concurrency allocation and actual spend authorization if relevant: about 1 h of worker time (M0-06 took about 50 min); one worker; no service purchases

## Outcome and boundaries

Assemble the M1 acceptance report R-0009: for the M1 gate in [the project plan](../docs/PROJECT_PLAN.md) ("validated addresses and meanings for the selected sequence; state sampling point defined; each critical finding has an experiment; unknowns remain explicit"), the M1 rows of [the registry's work order](README.md) (M1-01 to M1-03 acceptance criteria) and the M1 row of the test progression in [build and validation](../docs/BUILD_AND_VALIDATION.md) ("evidence-linked arithmetic/decoding checks; primary and withheld replays; fresh-process repeatability"), the evidence that meets each, with the machine/tool/ROM/core identities and the exact reproducible commands, taken from R-0006 to R-0008, D-0002 and the task records; any gate without evidence is listed as unmet and blocks acceptance rather than being argued around. Re-run the recorded checks once on the report's own commit so the report describes a state that was actually observed: the suite; `replay compare` on `boot-start-600` and `race-crawler-dragster-3000`; the perturbation `--against` the Right-removed variant; `coverage capture` of the boot scenario (sample digest unchanged; coverage digest `1e6c02747d7c1003…` once the host's `library_sha256` is substituted); the windowed `access capture` of the M1-02 handoff (`464049b37dcdb267…`) and `access query` on `$7E0313`; the fields manifest compare and its `--against`; `content decode` on the content manifest (all ten digests) and `content compare` at frame 1600 (490 of 26,656). State plainly what M1 does not establish (the list in `docs/STATE.md` and the three records' limits: the physics arithmetic, the tile-map gather, the geometry regions and bank $20/$21 tables, lean, the Y button, other tracks and riders, Linux, VRAM contents, the D-0002 residual). Update the next-stage estimate paragraph in the project plan from the observed M1 effort (about 30, 53 and 55 min of worker sessions and three reviews of 10.5, 15.5 and 14.7 min, each returning a material finding, against the M0-06 estimate of roughly 12 h and 6 to 15 rounds), and give an M2-01/M2-02 estimate with the spread as the honest figure and no calendar or cost promise.

Follow-up checks (tests only, no command behaviour change; the open notes in `docs/STATE.md`): a `test_access.py` case with an unresolved *store* (a `STA (dp)` whose pointer was changed by a read-modify-write instruction in the same frame) asserting `unresolved_stores` and `unresolved_store_pcs`; a `test_content.py` RNC stream that uses a symbol at or above 9 so the base-table constant is exercised by decoding; in `test_coverage.py` the two synthetic cases from M1-01 review 1 (an unknown edge from a ROM site; a direct `per_frame_summary` case) and `nmi_vector_once_per_frame` in `derive.py` tightened to require exactly one entry in every frame of the tail from `once_per_frame_from` (both tracked maps must still pass unchanged; if either would change, leave the check as is and record why). The coverage-side `library_sha256` note stays open (it changes the coverage identity and the tracked maps).

Not in scope: new commands, native code (M2), changes to any format, manifest or tracked map, the M2 task records (coordinator), a scheduler.

## Inputs and prerequisites

- ROM as in M0-03 (`local/rom-location.txt`, manifest `tests/manifests/rom/unirally-pal.json`); built pinned core (`reference build`, commit `7d5aa1e656b9`, patch `a719f5ffe222…`); the manifests under `tests/manifests/replay/`, `tests/manifests/fields/` and `tests/manifests/content/`; the tracked maps under `docs/map/`.
- Runtime needs: the cached wheels and core checkout copied into the worktree (network only if the cache misses); about 10 s of core build if not cached; the whole set of re-run checks takes a few minutes (a windowed access capture 23 s, a worker dump 13 s, `content decode` under a second, the suite about 30 s); artifacts under ignored `artifacts/m1-04/`.
- Baseline to reproduce first: `doctor`, `bootstrap`, `build --preset lab-debug`, `test --suite synthetic` (191 checks), `reference build`.
- Known baseline failures: none. Known limits: reference execution on one macOS host; CI is ROM-free; the tracked maps embed the core `library_sha256` (open note); the M1-01 worktree's evidence directory may be pruned by the user at any time, so cite digests from the records rather than re-hashing files there.

## Acceptance

| Criterion | Command or experiment | Expected result | Required artifact |
| --- | --- | --- | --- |
| Every M1 gate has evidence or is listed as unmet | Gate-by-gate table citing the record, commit, command and report digest | No gate is described as met without a cited observation; unmet gates block M1 acceptance | `docs/research/R-0009-*` |
| Identities and commands are reproducible | Re-run of the recorded checks on the report's commit | Same exit codes and digests as the cited records (`583d1ec544ec61a2…`, `72f618f2f7e3416e…`, frame 1500 byte `$7E0313`, `464049b37dcdb267…`, `joy1h_image` at 1500, the ten content digests, 490 of 26,656) | Reports under `artifacts/m1-04/` with digests in the handoff |
| Unknowns are explicit | A section of R-0009 | Everything M1 leaves open is listed with the record that says so; nothing open is described as established | R-0009 |
| Follow-up checks | The four test additions and the tightened NMI check | Suite grows by at least four checks, green on both CI platforms; the tracked maps still pass `coverage map` checks unchanged; the "unresolved stores not counted" and "base table repeating above 128" mutations are caught by a decoding test | Tests, CI run |
| Estimates updated from observation | Project plan next-stage paragraph revised from the M1 handoffs' harness-measured figures | Estimates cite the observed figures and the spread; no calendar or cost promise | Plan diff |
| Independent review of the exact candidate | Reviewer reproduces on the candidate commit from its own clone; CI on that commit | Approve, or specific reproducible findings addressed | Record |

## Evidence and attempts

| Attempt | Hypothesis | Experiment | Observation | Next decision |
| --- | --- | --- | --- | --- |
| 1 (03:06 AEST) | The baseline reproduces in this worktree from the copied caches | `doctor`, `bootstrap`, `build --preset lab-debug`, `test --suite synthetic`, `reference build` (reports in ignored `artifacts/m1-04/baseline/`) | exit 0 each; both tools `source=cache`; 191/191 checks at `9680119`, `dirty` false; `patch_applied a719f5ffe222`, incremental core build 0.4 s; `system_ninja` missing (optional) | Re-run the recorded M1 checks on the claim commit before changing anything |
| 2 (03:08–03:13) | Every recorded M1 check holds on `9680119` | `artifacts/m1-04/evidence.sh` (replay, coverage, access, fields) and `evidence-part2.sh` (ports capture, windowed capture, three worker dumps, content decode/provenance/compare) | every exit code and digest as recorded: `583d1ec544ec61a2…`, `72f618f2f7e3416e…`, frame 1500 byte `0x00313` with `divergence.json` `ccc37b2e1457aec2…`, boot coverage `1e6c02747d7c1003…` after substituting the tracked map's `library_sha256` (`35d646d4…`) for this host's (`77811621…`), `464049b37dcdb267…`, writer `$80:87F2`/reader `$82:AADF`, `joy1h_image` at 1500, ports record `11bb8622e7db6637…`, windowed `76f1154941145446…`, dumps `aebeb3b30a678651…`/`d236520df0c5d843…`/`2dd530a270892496…`, all ten content digests with 33,814/33,815, 39,705 transfers and 2,708 block moves, 490 of 26,656 (ignored `artifacts/m1-04/rerun-9680119/`) | Session interrupted by the provider's usage limit at about 03:15 while reading the test files; nothing lost (all reports on disk, verified on resume at 07:08) |
| 3 (07:09–07:14) | The four open test notes can be closed without changing any command's output | five ROM-free tests (`UnresolvedStoreTests` ×2, `test_symbols_at_or_above_nine_use_the_base_table`, `test_unknown_edge_from_a_rom_site`, `test_per_frame_summary`) and the `per_frame_summary` rule in `coverage/derive.py` (`once_per_frame_from` only when no frame holds more than one entry); commit `1363871`; `artifacts/m1-04/mutation-probes.sh` | suite 191 → 196 (188 Python tests); probes: unresolved stores not counted **caught**, base table repeating above 128 **caught** by the new stream test and missed by the two older stream tests, unknown ROM-site edge called a branch **caught**, the `all(n <= 1)` guard removed **caught**, `!= 1` → `< 1` inside the guarded loop survives (equivalent under the guard); race coverage capture + map on `1363871`: NMI 0/1/2+ in 371/2629/0 frames, once per frame from 1329, map identical to the tracked one except the two library-digest lines, coverage `a075220e58ecd355…` after substitution | Re-run the whole check set on `1363871` |
| 4 (07:15–07:20) | Every recorded check holds on the candidate `1363871` | `evidence.sh` (now also the race coverage capture and map) and `evidence-part2.sh`; `test --suite synthetic` on the clean tree | identical to attempt 2 in every exit code and digest (reports in ignored `artifacts/m1-04/final/`, all recording `1363871`, `dirty` false; the table in R-0009); suite 196/196, report `841fed36…` | Write R-0009, the plan paragraph and this handoff citing `1363871` |
| 5 (07:20–07:25:49) | — | R-0009 (gate table, follow-up checks, effort table, unknowns), the project plan's next-stage estimate, this record | see the handoff | Submit for review |

## Handoff

- Current base/head commit and uncommitted state: base `9680119` (the claim commit); `1363871` adds the follow-up checks (the only code change: `tests/tooling/test_access.py`, `test_content.py`, `test_coverage.py`, `tools/unirally_lab/coverage/derive.py`); the candidate head is the commit that adds R-0009, the plan paragraph and this handoff, followed by the submission amendment that records the suite on it; worktree clean after it. Nothing else changed: no command behaviour, schema, manifest, tracked map, content manifest, core, patch, lock, `src/lab`, `docs/STATE.md` or registry (`git diff 9680119 --stat`).
- Verified findings: [R-0009](../docs/research/R-0009-m1-acceptance.md): every recorded M1 check reproduced on `9680119` and on `1363871` (17 commands, every exit code and digest as recorded); gates 1–10 of the M1 gate table met with the narrowings stated there (the trick interpretation and the camera rule are explicit unknowns; save/restore continuation belongs to M2-02 and is not claimed); gate 12 (CI on the candidate, independent review) outstanding and blocking. Follow-up checks: suite 191 → 196, both tracked maps regenerate identical except the two library-digest lines, 5 of 6 mutation probes caught (the survivor equivalent under the new rule). Unknowns listed in R-0009 with their records. Project plan: the next-stage paragraph revised from the observed M1 figures (about 2 h 18 min of worker sessions, 0 fix rounds, 3 reviews of 10.5/15.5/14.7 min each with a material finding, against the 12 h / 6–15 rounds estimate) with an M2-01/M2-02 estimate of roughly 1–5 h + 1–2 h and 1–5 + 1–2 review rounds, spread as the figure, no calendar or cost.
- Current hypothesis and failed approaches: none open. Decision recorded in R-0009: the once-per-frame tightening voids `once_per_frame_from` on any doubled frame and keeps the tail rule; a minimum tail length (arbitrary) and "from the first NMI" (fails the race map's 1208–1328 gap) were considered and rejected, so a short tail after a late gap still passes with the gap listed — recorded, not argued around. The coverage-side `library_sha256` note stays open (it changes the coverage identity and both tracked maps).
- Commands executed, outcomes and report hashes (reports under ignored `artifacts/m1-04/`, SHA-256 prefixes): baseline on `9680119`: `baseline/doctor.json`, `bootstrap.json`, `build.json`, `test.json` (191), `reference-build.json` (exit 0 each; digests at the end of this line); claim-commit re-run `rerun-9680119/`: `boot-compare` `54e42ad0`, `race-compare` `6bf31ab1`, `race-perturb` `37da106b` (exit 1), `boot-coverage` `ebac4a93`, `boot-map` `694931d1`, `access-window` `64c970f9`, `access-query` `3efe222b`, `fields-compare` `662d8c31`, `fields-against` `c0832f3f` (exit 1), `race-ports` `40f03d9f`, `w1600` `d48102cd`, `content-decode` `98e19ca6`, `content-provenance` `3e80d96b`, `content-compare-1600` `3d585540`; candidate re-run `final/` on `1363871`: `test` `841fed36` (196), `boot-compare` `175c5c71`, `race-compare` `fef3df53`, `race-perturb` `c3b6313f` (exit 1), `boot-coverage` `3c5aa4bb`, `boot-map` `e607e185`, `race-coverage` `40d85f89`, `race-map` `e41ab62d`, `access-window` `761457ff`, `access-query` `8ad223c2`, `fields-compare` `b831e0bd`, `fields-against` `f139af0a` (exit 1), `race-ports` `226d7ef5`, `w1600` `863154f9`, `content-decode` `940aa3d3`, `content-provenance` `13e00e42`, `content-compare-1600` `a02fa829`; artifact digests: `final/access-window/access.json` `464049b37dcdb267…`, `final/race-ports/access.json` `11bb8622e7db6637…`, `final/w1600/access.json` `76f1154941145446…`, dumps 1243/1333/1600 `aebeb3b30a678651…`/`d236520df0c5d843…`/`2dd530a270892496…`, `final/race-perturb/divergence.json` `ccc37b2e1457aec2…`, `final/boot-coverage/coverage.json` `161dc4acf757c5ec…` (→ `1e6c02747d7c1003…` substituted), `final/race-coverage/coverage.json` `158fdad1e3d9d611…` (→ `a075220e58ecd355…`), `final/race-ports/frames/frame-01600.png` `36e7d50410c86a97…`; suite on the record commit `6da94d3`: 196/196, report `final/test-record-commit.json` `852f641b`; baseline report digests `doctor.json` `f91e22ff`, `bootstrap.json` `3435fc31`, `build.json` `99f9726a`, `test.json` `f412b57f`, `reference-build.json` `2f37e791`.
- Unavailable/skipped checks: CI on both platforms for the candidate (the coordinator pushes; the ROM-free suite is what CI runs); independent review; a Linux run of the core (M0 limit); the M1-02 whole-run access record (`bc799832ffa6666e…`, 90 s) and the frame-2400 rendering comparison were not re-run (the windowed and ports records and the frame-1600 comparison were; R-0008's 2400 figure is cited from the record); `reference verify`/`restore-check` (M0 checks, unchanged since R-0005, not part of the M1 set).
- Exact next experiment/command: reviewer, from a fresh clone at the candidate: `python3 tools/project.py doctor && python3 tools/project.py bootstrap && python3 tools/project.py build --preset lab-debug && python3 tools/project.py test --suite synthetic` (196 checks), `python3 tools/project.py reference build`, then the commands of R-0009's re-run table (the two drivers are ignored files; the commands are listed verbatim in the table and in the M1-02/M1-03 handoffs and the content record), checking each digest against the table; audit every gate's citation against R-0006 to R-0008, D-0002 and the task records; run the mutation probes (the six one-line edits are in the R-0009 follow-up table); trace the effort figures to the three handoffs' elapsed-work lines. A withheld case the reviewer could add: any other recorded M1 command (for example the frame-2400 comparison, 791 of 50,176, or the whole-run access record `bc799832ffa6666e…`).
- Remaining dependencies: none for review. For M1 acceptance: CI green on both platforms on the candidate and the review's approval, then the coordinator's registry/state update and tag.
- Runtime needs (network, build time, fixtures, memory): none beyond M1-03 (cached wheels and core checkout were in the worktree; core build incremental 0.4 s, about 10 s from clean); the ROM via `local/rom-location.txt`; the whole re-run about 4 min of wall clock (ports capture 86 s, windowed captures 8–24 s, compares 3–23 s, dumps 3 s each, content commands under 1 s, suite 27 s); `artifacts/m1-04/` about 320 MiB (two whole-run access records of 22 MiB each with samples, coverage files, dumps and frames).
- Elapsed work and provider usage when known: session start **03:06:03 AEST** (`date`), 12 September 2026; baseline and the claim-commit re-run complete by 03:13:09; the session was interrupted by the provider's usage limit at about 03:15 while reading the test files (no command in flight, nothing lost); resumed **07:08:34 AEST** (`date`) after the coordinator's notice; follow-up checks committed 07:14, the candidate re-run 07:15–07:20, records written after; record commit `6da94d3` at 07:25:49 AEST, the suite on it, and this submission amendment at **07:26:27 AEST** (`date`). Wall clock of commands actually run: about 12 min (two re-runs of 4 min, three suites, one race capture). The harness-measured session duration is the authoritative figure (M1-01 note); the two segments are about 9 min before the interruption and from 07:08 to the submission commit after it.

## Review and integration

- Reviewer and independent reproduction/withheld-case results:
- Required changes or acceptance rationale:
- Exact merge candidate and required-check results:
- Integrated commit and evidence location:
- Scope still unverified:
