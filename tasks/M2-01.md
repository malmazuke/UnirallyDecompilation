# M2-01 — Native movement experiment

## Assignment

- Status: blocked on M2-01A (12 September 2026 coordinator reassessment); verified sampler/progress components submitted for review, not gameplay acceptance
- Milestone: M2
- Coordinator: primary agent (Codex desktop)
- Worker/session/runtime/model: fresh Codex collaboration worker `native_movement`, assigned by the coordinator; reads the portable repository record. Note for the record: sessions up to M1-04's submission were Claude Fable 5.1; M1-04's review and integration were Claude Opus 5
- Reviewer: fresh independent session, own clone (dispatched by the coordinator after submission)
- Dependencies and evidence of acceptance: M1 accepted ([R-0009](../docs/research/R-0009-m1-acceptance.md), tag `m1`); the player-state schema [docs/state/player-state.md](../docs/state/player-state.md) and its declarations `tests/manifests/fields/player-state-v1.json` ([R-0007](../docs/research/R-0007-player-state.md)); the access record and its tooling ([D-0002](../docs/decisions/D-0002-data-access-observation.md)); the decoded per-tile tables ([R-0008](../docs/research/R-0008-track-decode.md)); the comparator ([R-0003](../docs/research/R-0003-replay-comparator.md))
- Base commit: the coordinator claim commit containing this assignment, descended from `a0b5c9931f98b23eb2a8cf1824463efb49023060` (`main`, after `m1`); its exact hash is supplied in the dispatch and recorded by the worker
- Branch and isolated worktree: `codex/M2-01-native-movement` in `.worktrees/m2-01` (created at dispatch, with the same fixture arrangement as M1-02 to M1-04: `local/rom-location.txt` plus the copied ignored `local/toolchain`, `local/emulators` and `local/cache`)
- Owned paths and shared interfaces: new `src/core/` (the native simulation) and its `CMakeLists.txt` wiring in the top-level file; new `tests/native/` (C++ checks); new `tools/unirally_lab/native/` (the native runner and the native-vs-reference comparison) with a `native` command group registered in `tools/project.py`; new `tests/tooling/test_native.py`; new `tests/manifests/native/` (the frozen primary and withheld cases); `docs/research/R-0010-*`; `docs/BUILD_AND_VALIDATION.md` command-table rows and the native-side manifest items it marks as proposals; this record. May read but must not change: `tools/unirally_lab/access/`, `coverage/`, `content/`, `reference/`, `replay/`, `tools/locks/`, every existing manifest, tracked map and content manifest, `docs/state/player-state.md` (the field interface is frozen for this task; a needed change goes through the coordinator), `docs/STATE.md`, the registry
- Claim/lease/heartbeat/checkpoint location: claimed 2026-09-11T22:23Z; this file and the registry row; checkpoint commits at least every 10 minutes and before every long capture
- Session time limit, concurrency allocation and actual spend authorization if relevant: about 3 h of worker time for a first candidate (the plan's estimate for this task is 1 to 5 h and 1 to 5 review rounds); one worker; no service purchases. If the arithmetic cannot be read without decoding the bank $20/$21 tables, stop at that boundary and submit what is established, as the estimate anticipates

## Active continuation (coordinator, 12 September 2026)

M2-01A contact and motion research is dispatched in parallel; its internal dependency does not end the user request. Meanwhile the coordinator prepares the independent native comparison/reporting layer in `.worktrees/m2-01-comparator`, branch `codex/M2-01-comparator`, from this continuation claim commit (after `ba71142`). Owns new `tools/unirally_lab/native/compare.py` and `tests/tooling/test_native_compare.py`, plus a separate `tasks/M2-01-comparator-handoff.md`. No gameplay routine or required expectation is changed by this work. This unblocked subpart does not accept native movement; it is merged under review and connected to the real native runner when the required mechanics are recovered. Root source checkout remains the coordinator-owned integration tree.

## Outcome and boundaries

Deliver a native C++20 implementation of the riding update of the validated race scenario that reproduces the required movement projection below exactly, per PAL frame, against the frozen reference, with the arithmetic read from the ROM rather than fitted to the observations.

**Scope fixed before implementation.** Required movement projection: `joy1l_image`, `joy1h_image`, `axis_v`, `axis_h`, the player u16 component of `pos_x`, `displacement_x`, `throttle`, `speed`, and the five timer digits. Freeze explicit byte projections in the new native manifests without changing the existing field schema: `pos_x` also includes the excluded opponent, and `timer` spans padding/interleaved bytes as well as digits. Keep all eleven original ranges in reference evidence; report opponent position, `jump_height`, `camera_x` and non-digit timer bytes as diagnostic-only, not reproduced. They cannot count as native agreement. This narrows M2-01 to riding and does not establish the full M2 movement/trick gate. Any further reduction needs a coordinator decision before implementation, not a passing report with omitted failed fields.

**Freeze first (before any tuning).** Write the primary case and at least two withheld input variations into `tests/manifests/native/` and record their expected per-frame field values from the *reference* (not from the native candidate) in a dedicated reference-freeze commit after the administrative claim/baseline work, before any native routine computes a field. The primary case is the riding phase of `race-crawler-dragster-3000` from frame 1534. The withheld variations must be chosen after looking at the game's behaviour and must not be used while developing the routine: at least one different release frame (M1-02's reviewer used 2500, M1-02's worker 2000, so pick another) and at least one input pattern not yet recorded anywhere in the repository (for example an alternating Right/no-Right cadence, or Left applied while moving). The reviewer owns at least one further withheld case. A case whose expected values come from the candidate rather than the reference is a defect, not evidence.

**Read the arithmetic, do not fit it.** Begin at `$82:8AB1`–`$82:8DF8`, but follow its actual control/data dependencies: the known speed writer is outside that range at `$82:8E9A` (R-0007 finding 10 gives its reads and writes; R-0006's map gives its ranges and entry points; `coverage map --detail` regenerates a per-address listing under ignored `artifacts/`). Disassemble it locally (the listing stays untracked, per the M1 tracked-content rule), and implement each step with the operand widths, signedness, wrapping and ordering the instructions show. Record in R-0010, per field, the instruction sequence that produces it and the exact integer expression implemented, so a reader can check the code against the ROM without re-deriving it. Where the routine reads a table (the decoded per-tile table of R-0008, or the undecoded ROM tables in banks $20/$21), read the table through the content tooling or record the read values from the access record and state which of the two the native code uses. **Do not invent a mechanism for anything the trace does not show**: an unexplained step is recorded as unexplained and its field is reported as not reproduced, not approximated with a constant that happens to match.

**Compare per frame.** `python3 tools/project.py native compare --manifest <native case> [--from-frame N] [--to-frame N] --report <json>` runs the native simulation over the case's frames and compares every required projected field against the reference values, listing all diagnostic-only fields separately, emitting a first-divergence report in the shape `replay compare` uses (frame, field, native value, reference value, the prior frame's values and the inputs of both frames). Exit codes follow the repository convention. Initialize the native state once from the reference at the end of frame 1533; frame 1534 is the first computed update. Additional future-affecting initial state must have documented provenance. Never feed per-frame expected gameplay state or captured dynamic table-read values back into the update; static extracted content is allowed, with identity/provenance. The native side must be driven by the same input series the replay manifest declares, and its state must be serializable so that M2-02 can restore and continue it.

Deliverables: a compact `src/core/README.md` explaining update flow, units, evidence and validation commands; readable native code following [D-0003](../docs/decisions/D-0003-human-readable-native-code.md); `src/core/` with the update and its state type (explicit widths, no undefined overflow, no host byte-order or padding assumptions in the serialization); `tools/unirally_lab/native/` and the `native` command group; the frozen cases under `tests/manifests/native/`; C++ and Python tests, including a case that exercises the first-divergence report on a deliberately wrong native value; R-0010 with the per-field arithmetic and the results; the command-table rows; this record's handoff.

Not in scope: rendering, audio, the menu path, the opponent's AI and position, tricks and jumps (unvalidated in M1), other tracks or riders, save/restore continuation (M2-02), a playable build (M3), any change to the reference side or to the field schema.

Tracked-content rule: no ROM bytes and no disassembly listing in tracked files. Constants the native code needs are written into the source as named values with the ROM file offset they came from in a comment, and R-0010 records the offsets; a table of more than a few entries is loaded through the content tooling from the ROM at runtime, never pasted into the repository.

## Coordinator scope amendment (12 September 2026)

Required player speed causally reads opponent progress (R-0010 finding 2). The coordinator authorises the minimum track-grid gather/progress transition and opponent motion that feed required player output, with unrelated opponent features still excluded. No projection, frozen output or no-oracle rule changes. First investigate this prerequisite for at most 45 minutes before reassessment; preserve the evidence as a separate prerequisite if a substantial unresolved format is reached.

The later bounded contact investigation showed the required opponent motion
includes jumps/contact/orientation unvalidated in M1. The coordinator stopped
further native expansion and assigned research prerequisite **M2-01A**. The
scope amendment does not grant gameplay acceptance to the isolated components;
the frozen required projection and no-oracle rules remain unchanged.

## Inputs and prerequisites

- ROM as in M0-03; built pinned core; the manifests of `tests/manifests/replay/` and `tests/manifests/fields/`; the content manifest and tooling of M1-03; the access tooling of M1-02.
- Runtime needs: as M1-02 to M1-04; an `access capture` over the riding phase for the physics routine's reads and writes (about 25 s windowed), `coverage map --detail` for the per-address listing, the ROM for the disassembly.
- Baseline to reproduce first: `doctor`, `bootstrap`, `build --preset lab-debug`, `test --suite synthetic` (196 checks), `reference build`, `replay compare --manifest tests/manifests/replay/race-crawler-dragster-3000-fields.json` (exit 0).
- Known baseline failures: none. Known limits carried forward: the unknowns list of [R-0009](../docs/research/R-0009-m1-acceptance.md), in particular that the physics arithmetic has not been read, the bank $20/$21 tables are undecoded, and the camera rule and the trick states are open; reference execution on one macOS host; CI is ROM-free, so authored native unit tests must run without the ROM. Frozen reference values alone do not make native comparison ROM-free: extracted content may still be required. Keep such content ignored, record hashes and regeneration commands, and report missing content as exit 2; never paste original tables into tracked fixtures to satisfy CI.

## Acceptance

| Criterion | Command or experiment | Expected result | Required artifact |
| --- | --- | --- | --- |
| Cases frozen before tuning | The commit history of the branch | The manifests with reference-derived expected values are committed before the first commit that computes a field natively; the withheld cases are not referenced by the development commits | Branch history, manifests |
| Readable native source | Code review against D-0003 and `src/core/README.md` | Domain names, documented units/arithmetic, explicit dependencies and evidence links; research translations isolated and justified | Review, source guide |
| Exact agreement on the primary case | `native compare` over the riding phase | Every required projected field agrees with the reference in every frame of the case; diagnostic-only fields are explicitly excluded from the agreement claim | Report, R-0010 |
| Exact agreement on withheld cases | `native compare` on at least two withheld variations, run for the first time after the routine was fixed | Same agreement, or a first-divergence report that is investigated and recorded rather than tuned away | Reports |
| Arithmetic read from the ROM | R-0010's per-field table | Each reproduced field cites the instruction sequence and the exact integer expression; each constant cites its ROM file offset; unexplained steps are listed as unexplained | R-0010 |
| Divergence reporting works | A deliberately wrong native value | Exit 1 with the first divergence's frame, field, both values, the prior frame and the inputs; a test covers it | Test, report |
| Determinism and portability of the native side | Repeated runs in fresh processes; the sanitizer preset | Identical field series and state hash; no sanitizer diagnostic | Reports |
| ROM-free tests and CI | `test --suite synthetic` locally and on both CI platforms | Suite grows by the native tests; green on `ubuntu-24.04` and `macos-15` | Tests, CI run |
| Independent review of the exact candidate | Reviewer reproduces on the candidate from its own clone with a withheld case of its own | Approve, or specific reproducible findings addressed | Record |

## Coordinator handover audit (12 September 2026)

The prior M1 acceptance remains intact. Before native work, corrected four handover ambiguities: the administrative claim precedes the reference-freeze commit; the routine range must extend to dependencies such as the known speed writer `$82:8E9A`; the fixed movement projection excludes explicitly out-of-scope systems and timer padding; expected outputs do not remove the native content prerequisite. Clarified the initial sample boundary and prohibited oracle-driven updates. Existing field-manifest descriptions include known stale camera and speed prose; the corrected player-state research is authoritative for interpretation, while the schema/addresses remain frozen. Added D-0003 readability to acceptance. No reference expectation or existing manifest changed.

## Evidence and attempts

| Attempt | Hypothesis | Experiment | Observation | Next decision |
| --- | --- | --- | --- | --- |
| Coordinator baseline | Incoming records reproduce before implementation | `doctor --report artifacts/m2-01-coordinator/doctor.json`; `replay compare --manifest tests/manifests/replay/race-crawler-dragster-3000-fields.json --report artifacts/m2-01-coordinator/baseline-fields.json` from main at `a0b5c99` | Both exit 0; two fresh workers reproduce sample digest `72f618f2f7e3416e…`, all declared fields, final state and A/V; optional system Ninja absent, isolated Ninja available. Only the coordinator's own reference worker was live during the process audit | Dispatch isolated worker with corrected scope and D-0003 |

## Handoff

- Submitted scope: frozen references, reviewed native sampling, native progress recurrence with explicit alternating phase and 15-byte serialization, authored tests, and the precise remaining contact/pose/opponent-motion boundary. **M2-01 is blocked, not completed or accepted.** The coordinator owns the new M2-01A work order and integration decision.
- Tested component/regression commit: `131ecd03eccdd809f76bc094b8a1791578672e04`, clean during final 211-check suite and sanitizer checks; production source remains `ac586beb2f32a7837e4cbc42335d6173f53f108e`. This handoff adds this task, R-0010, build/validation documentation and the reviewer-authored progress ordering regression; production source is unchanged; obtain its exact submission hash from `git log -1` or the coordinator submission. Dispatched base `a9f86e590e4be0d76369a975ece2d556883aa51f`; worktree `.worktrees/m2-01`, branch `codex/M2-01-native-movement`.
- Freeze provenance: dedicated `784dffcdba0c73b68ab09c6843eea6876cc250ca` precedes all native computation; all three reference cases reproduced in two fresh processes. Existing withheld release-2347 and cadence-17 expected series remain unopened during implementation and native withheld comparisons remain unrun. Coordinator independently reproduced primary projections without the freeze utility.
- Verified findings: the bank-21 pose/template expansion, grid sampler and alternating progress recurrence match their narrow primary component contracts. Player speed first depends observably on opponent progress at frame 1632. The opponent jump latch at 1578, counter progression 1580–1596 and contact-like state progression 1580–1588/clear 1617 are distinct events. Full identities, provenance, addresses, ordered observations, exact capture/watch lists and next commands are in [R-0010](../docs/research/R-0010-native-movement.md).
- Final commands/results: final authored ordering test passes original and kills the observe-before-advance mutant (expected exit 1); clean-source full synthetic suite 211/211; sanitizer build plus four native CTests pass without diagnostics; fresh debug and sanitizer probes agree on all 29,320 sample words and 11,728 progress fields. R-0010 records commands, report paths and full SHA-256 values. Incoming positions/poses still come from a primary capture, so component agreement is not an autonomous movement result.
- Failed approaches and corrections: updating both progress counters each frame caused 159 mismatches, first at 1577; recovered serialized phase scheduling removed them. A proposed 4 KiB template extent was too small and expanded before probing. Initial `lab-sanitized` typo exited 3; correct `lab-sanitize` passed. Earlier mutable-source checks are superseded by clean `ac586beb` checks. No expectation was regenerated to fit native output.
- Reviewer fixes: sampler review of `611c396` reproduced its capture, 29,320 words in debug/sanitizer and 206 checks. All four findings are addressed: execution/protocol failure exit1, strict primary identity validation, authored wrapped-sign boundary, and current status/handoff. Sampler checkpoint CI 34654922992 passed Ubuntu/macOS; independent follow-up review of `ac586beb` reproduced all 211 checks and both probes in debug/sanitizer, approving component integration; the coordinator will run final candidate CI. That earlier CI does not validate the later progress component.
- Unavailable/skipped acceptance: autonomous native movement and its command group, required player field arithmetic, full-state movement hashes, movement first-divergence regression, primary/withheld native comparisons and reviewer-owned movement case are unimplemented/unrun. Timer and input axes are not implemented. No complete M2 gate or M2-02 claim.
- Exact next experiment: follow the R-0010 focused primary capture for 1576–1618. Correlate `$81:8F98` entry/return with scratch-array preprocessing and per-frame ordered stores, distinguish 1578 trigger from 1580–1596 counter updates, then recover the next pose and opponent-input producer before any native implementation. M2-01A preregisters its own research variation without inspecting existing withheld movement outputs.
- Runtime needs: existing private ROM/pinned local core, ignored content and captures; focused capture 9 s, full sampling capture about 50 s; primary WRAM series 26,112,000 bytes. Tools and assets are local and ignored; no account/purchase needed. The report regenerates from the source manifest and listed watches rather than relying on copied binaries.
- Elapsed/provider usage: worker began 2026-09-11T22:25:13Z, final stable checks and contact capture complete by 22:49Z; provider usage unavailable. Checkpoints were made throughout; no uncommitted native code remains.

## Review and integration

- **Component integration only; M2-01 remains blocked and unaccepted.** No primary/withheld autonomous native comparison or full movement serialization exists. Next ready task: [M2-01A](M2-01A.md), whose research contract was also checked by the reviewer.
- Worker submission: `baeae9b0fae6eb1da74675c6999c526342763217`, clean. Production source is `ac586beb2f32a7837e4cbc42335d6173f53f108e`; `131ecd03eccdd809f76bc094b8a1791578672e04` adds the promoted ordering regression and records; `baeae9b` records its successful clean-source checks. Frozen expectations unchanged since `784dffc`.
- Independent review in its own clone `.worktrees/m2-01-sampling-review`: `611c396` sampler capture reproduced exactly (`bebe75c349332a55…`), 29,320 words in debug/sanitizer and 206 checks. Four findings were fixed: failure taxonomy, reference identity, wrapped-sign regression and stale records. Follow-up on `ac586be` independently passed 211 checks and all 11,728 progress values in debug/sanitizer (`acb7a65ca8306dcf99311aca6d2cd4e2ff9144e1a5c4ed06ad465a976e869bb8`); no blocking implementation finding. Approval extended to `131ecd0` after the promoted progress-ordering test passed original code and rejected the observe-before-advance mutation. No frozen withheld movement series was read.
- Durable review reports are ignored under that clone: `artifacts/sampling-review/REVIEW.md`, `artifacts/progress-review/REVIEW.md`, `artifacts/final-component-review/SIGNOFF.md`. Their reproduction commands and source/artifact hashes are preserved in R-0010 and this handoff; fresh agents regenerate captures from manifests rather than requiring those directories.
- Coordinator integration candidate: worker submission plus this record, D-0003/project policy, corrected STATE/registry, and M2-01A. Coordinator requires `build --preset lab-debug`, `test --suite synthetic` (211), both component probes and private-repository CI on Linux/macOS before fast-forwarding main. Integration reports are under `artifacts/m2-01-coordinator/integration/`; GitHub checks attach to the component-integration branch/PR. Earlier CI 34654922992 proves only `611c396`, not the final progress checkpoint. The reviewed production source and frozen expectations are unchanged by coordinator records.
- **Integrated 12 September 2026:** main fast-forwarded to `83e898557de39d06637b8fac6f8d76b288a3acf8`, the exact clean integration candidate, after independent component approval and all required checks. [PR #1](https://github.com/malmazuke/UnirallyDecompilation/pull/1) records the scope. [CI run 34655948639](https://github.com/malmazuke/UnirallyDecompilation/actions/runs/34655948639) passed Ubuntu/macOS, including Linux sanitizers, on that candidate. This subsequent entry records the completed integration; it changes no production source, test or expectation.
- Coordinator checks at `83e8985`, all exit 0, `dirty=false`, no source change during runs: `build --preset lab-debug` report SHA-256 `9f67c1938529ea37f478de57d9ebb340f4d07d4a86d2cc00ba24a34684095c8a`; `test --suite synthetic` **211/211**, report `abec02d68825a0d81057205991ab6f00c1ff4015892a4551deb95dfe19e9f50a`; sampling **29,320/29,320**, report `d7c079923eff43f022332f60236e0979cb453d132c7f9da6af5f9a90753847e2`; progress **11,728/11,728**, report `99e79f3118d86f140436a2c384c8a3972956f32bddd9a9c858d7b2e201088fa4`. Files are `artifacts/m2-01-coordinator/integration/{build,synthetic,sampling,progress}.json`. Probes used the reviewer's independently regenerated captures/content with the root candidate's binaries and manifests; their native output digests equal the independent review's. Command argv and input hashes are in those reports; R-0010 supplies portable regeneration commands.
- Repository process limit: the GitHub branch-protection query returned HTTP 403 (unavailable under the current private-repository plan); no setting or subscription was changed. Independent review and exact-candidate CI were enforced manually before the fast-forward. This does not assert server-enforced branch protection.
- Accepted scope of the component checkpoint: sampling/marker-progress behavior only in the tested domain, explicit 15-byte progress serialization, authored ROM-free checks, and the readable source guide. It does not satisfy the M2-01 or M2 milestone gate. Remaining contact, pose and opponent-motion closure is the explicit M2-01A prerequisite, not an excuse to inject future reference state into a native simulation.

### Coordinator continuation: input and timer component

While contact and motion research proceeds, the coordinator owns an isolated
`codex/M2-01-input-timer` worktree `.worktrees/m2-01-input-timer` from this claim
commit. Scope: new `src/core/input_timer.*`, `tests/native/input_timer*`,
`tools/unirally_lab/native/input_timer*`, `docs/research/R-0010-input-timer.md`,
`tasks/M2-01-input-timer-handoff.md`, and their CMake registration only. Recover
joypad images/axis precedence and the five race-timer digits from original
instructions, expose timer enable as an explicit input until motion/start
research establishes its producer, and validate isolated arithmetic. This
component cannot claim autonomous movement or supply future state to it.
Expectations/field declarations remain frozen. Independent review is required.

### Native comparison command continuation

The coordinator owns `tools/unirally_lab/native/commands.py`, `protocol.py`,
new native case descriptors, `tests/tooling/test_native_cli.py`, and registration
in `tools/project.py`, in isolated `.worktrees/m2-01-native-cli` on
`codex/M2-01-native-cli`. Base is this claim plus the independently reviewed
comparison/input-timer dependency. The motion worker supplies a native runner
accepting one canonical seed, static content directory, and contiguous
`frame port0_mask port1_mask` input rows only. It emits a version header and
initial/update rows containing all 13 projected fields and canonical state hex.
The coordinator validates complete output, reports first divergence, hashes
canonical bytes and checks fresh-process determinism. No expected output or
per-frame original state may be passed into the native process. Standard exit
codes and report provenance apply; stub protocol tests make no gameplay claim.
