"""Content decode (M1-03): provenance of the race content from the access record,
the decompressor the ROM uses, raw-copy decoding by manifest, and the runtime
rendering comparison against frame images.

No ROM bytes, decoded tiles, palettes, tile maps or images are tracked; the
manifests in ``tests/manifests/content/`` carry offsets, sizes, digests and
identifiers only.
"""
