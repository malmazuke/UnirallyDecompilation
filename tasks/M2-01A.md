# M2-01A — Close the riding contact and opponent-motion dependency

## Assignment

- Status: claimed at 2026-09-11T23:06:00+00:00; two independent research workers, coordinator integrates their contracts before M2-01 resumes.
- Milestone: M2; prerequisite for resuming M2-01, not a replacement for its gameplay acceptance.
- Coordinator: Codex primary agent. Contact worker `contact_contract`; opponent/pose worker `motion_contract`; independent reviewer assigned after submission.
- Dependencies: accepted M1; the independently reviewed sampling/progress component checkpoint identified in M2-01's handoff and R-0010. M2-01 itself is not accepted and is not a prerequisite, avoiding a task dependency cycle.
- Starting source: component submission `baeae9b0fae6eb1da74675c6999c526342763217` plus the coordinator integration records on main. Base commit, worker, branch, isolated checkout and claim time: coordinator pins the exact integrated main commit at dispatch. Use a fresh isolated checkout and the existing ignored fixture/cache arrangement.
- Owned paths: this task; new `docs/research/R-0011-*`; new experiment commands/tests under `tools/unirally_lab/native/` and `tests/tooling/test_native*.py` where needed; new manifests under `tests/manifests/native/contact-research/`. Existing frozen movement expectations, reference adapter/locks, field declarations, sampling/progress source, STATE and registry are read-only. Request a coordinator scope amendment for a demonstrated tooling defect or small isolated native component; do not grow a register interpreter.
- Session allocation: two workers on disjoint paths in separate worktrees, 45-minute checkpoint/reassessment intervals; no user-imposed total run budget and no service purchases. Checkpoint at least every ten minutes and before long captures. Independent review in its own checkout follows submission.

## Active dispatch and ownership

- Dispatch base: this claim commit, descended from integrated `26280f354b74c9e0d83e5f5797561159fd726c62`; the exact claim hash is supplied in each dispatch. No live reference worker was present at claim.
- `contact_contract`: branch `codex/M2-01A-contact`, worktree `.worktrees/m2-01a-contact`. Owns `docs/research/R-0011-contact.md`, `tasks/M2-01A-contact-handoff.md`, new `tools/unirally_lab/native/contact_research/`, `tests/tooling/test_native_contact*.py`, and new manifests under `tests/manifests/native/contact-research/contact/`. Recover post-gather response `$81:8F98` and its required persistent state/content, callers and actual branches. Do not modify opponent/pose worker files.
- `motion_contract`: branch `codex/M2-01A-motion`, worktree `.worktrees/m2-01a-motion`. Owns `docs/research/R-0011-motion.md`, `tasks/M2-01A-motion-handoff.md`, new `tools/unirally_lab/native/motion_research/`, `tests/tooling/test_native_motion*.py`, and new manifests under `tests/manifests/native/contact-research/motion/`. Recover opponent input/jump scheduling and pose/orientation/displacement update supplying collision input. Do not modify contact worker files.
- Coordinator owns this parent task, registry, STATE and combined contract/decisions. Existing source/interfaces/frozen expectations remain read-only for both workers until a coordinator amendment. Each worker retains its own ignored local caches/captures; prior M2-01 artifacts may be read, not modified.
- Read-only interface exchanged between workers: original addresses, widths, sampling/update phase and dependency chain. Semantic names remain provisional until evidence supports them. A contact output needed by pose or vice versa is reported promptly, not replaced by a per-frame oracle in eventual native code.
- Each worker writes its own handoff with base/head, actual commands/results/hashes, new facts versus hypotheses, failed approaches and next experiment. Checkpoint every ten minutes. User clarification of 2026-09-11T23:06:00+00:00: the previous coordinator stopping at this internal dependency was an error; this task proceeds autonomously and M2-01 follows it without requiring a new user message.

## Why this prerequisite exists

M2-01's frozen riding sequence is not causally independent of the opponent. At frame 1632 the player's speed cap changes because it reads opponent progress (R-0010, independently reproduced). Native sampling and marker progress have been recovered, but their component probes still receive original positions and poses. Those inputs cannot be replayed into an autonomous simulation.

The original post-gather response starts at `$81:8F98` and includes changes to position, velocity and persistent contact-related state. The opponent also jumps during the primary sequence: the recorded investigation locates an AI flag writer at `$83:E122`, postprocessing at `$83:E21F` and a jump trigger at `$82:A914`. These observations do not by themselves explain the mechanism. M1 did not validate jumps or the relevant pose/contact rules, and M2-01 excludes unvalidated trick/jump reconstruction. This work closes that prerequisite explicitly instead of substituting an always-accelerating opponent or relaxing required player fields.

## Outcome and boundaries

Deliver an evidence-backed, implementable contract for the minimum contact response, pose selection and opponent input/motion that determine the frozen primary riding outputs. Recover arithmetic and state dependencies from the original; do not fit formulas to the expected movement series. Distinguish observed addresses, hypotheses about their meaning, and chosen native interface names.

Start with `$81:8F98` and follow the executed primary branches to their actual return. Inventory incoming and outgoing state, scratch temporaries, content reads, callers and update order. Link corrected rider position/velocity publication and persistent state to their exact writers. Explain the opponent jump's trigger, frame schedule and subsequent contact transition; determine how pose selection feeds later collision sampling. Include phase, timers, counters and RNG only if their causal role is demonstrated. A list of every address touched is not yet an independent state inventory: distinguish overwritten scratch from data carried into the next update.

Use the original primary input path plus a separate research variation selected and preregistered before capture. Leave M2-01's two frozen withheld movement series unopened. Inspect both riders where their dependency differs. Prefer a small isolated experiment with captured arguments to establish a function contract, clearly labelled as such; it cannot satisfy autonomous gameplay acceptance.

Do not implement a full AI system, rendering, audio, unrelated tricks, other tracks or an emulator-shaped native runtime. Do not change existing expectations to accommodate an observed failure. No new native gameplay agreement is required or implied by this research task. If two bounded sessions leave the contract incomplete, checkpoint the exact missing dependency and narrower next experiment; do not mark the task accepted.

## Baseline and first experiment

Read AGENTS.md, STATE, AGENT_WORKFLOW, BUILD_AND_VALIDATION, M2-01 and R-0010. Inspect the actual checkpoint's handoff for the recorded current commands, source/input hashes and unavailable checks; reproduce a recorded component check before continuing. Run `doctor`, `bootstrap`, `build --preset lab-debug`, `test --suite synthetic` and the unchanged primary fields replay as applicable.

The first investigation is a window around the primary opponent jump and subsequent landing/contact response. Recover the exact watch PCs/addresses and captured contract from the M2-01 handoff; begin with frames 1576–1618 so the trigger at 1578, later updates and the observed branch at 1617 are not conflated. Write a predicted ordering before capture. Retain all command arguments and artifact hashes. Widen the window only when the observed lifetime of a required state demands it.

## Acceptance

| Criterion | Evidence |
| --- | --- |
| Implementable state contract | R-0011 inventories each persistent input/output with width, interpretation confidence, update phase, source PCs and how it reaches a required M2-01 field; scratch variables are distinguished |
| Contact response | Each executed branch required by the primary sequence has its predicate, arithmetic, relevant state/content and output ordering explained; unexecuted branches stay explicitly unsupported |
| Opponent jump and pose closure | Trigger source, timing, recurring state, response and pose feedback are traced; any random or input dependency is either recovered or explicitly blocks acceptance |
| Independent experiment | A preregistered research variation tests a causal prediction, with original-derived expectations and precise tested domain; no access to frozen M2-01 withheld outputs |
| Reproducible evidence | Exact source/ROM/core identity, capture commands, digests, results and ignored-artifact regeneration; missing/skipped checks remain such |
| Human-readable implementation plan | A small proposed semantic state/update interface grounded in the contract, following D-0003; no speculative mod API or undocumented register-shaped design |
| Independent review | Fresh reviewer reproduces the critical claim and runs a separate boundary or variation; exact candidate passes affected checks; coordinator integrates and records acceptance |

## Handoff

Fill on claim and at every checkpoint: exact base/head and dirty state; commands/results/hashes; verified findings versus open hypotheses; failed approaches; exact next command; fixture paths and runtime needs; elapsed time and usage when known.

## Review and integration

Pending. Accepting this task supplies a prerequisite to M2-01; it cannot accept M2 or substitute for native per-frame primary/withheld agreement.

### Coordinator speed dependency assignment

The motion worker identifies speed clamp/boost decay as an independent bounded
contract. Coordinator owns new `docs/research/R-0011-speed.md`,
`tasks/M2-01A-speed-handoff.md`, `tools/unirally_lab/native/speed_research/`, and
`tests/tooling/test_native_speed*.py` in isolated `.worktrees/m2-01a-speed`,
branch `codex/M2-01A-speed` from this claim commit. Recover `$82:A6F8–A8C8`,
including boost/cap state, tables and update counter, using captured arguments
and fresh focused evidence. Motion worker retains reward generation/AI; contact
worker retains contact. No frozen expectations or existing sources change.
This is a research prerequisite, not autonomous gameplay acceptance.

### Contact implementation continuation

Independent reviewer reproduced research head280e282, its primary/focused
captures, all checked preprocessing/response relations, and a separate delayed
Right variation. No blocking primary-domain finding remains; final report is
being recorded. The coordinator authorizes the existing contact worker to
continue in its isolated branch with new `src/core/flat_contact.*`, new
`tests/native/contact*`, and `tools/unirally_lab/native/contact_research/` probe
support. CMake registration may be added in its branch and reconciled during
integration. Preserve existing sampler/progress interfaces and frozen references.

Implement the semantic interface in R-0011-contact, including explicit
unsupported-branch guards based on algorithm predicates. A landing coordinate
or frame tuple must never select behavior. Component replay takes captured
arguments for validation only; eventual movement must supply native state.
Native code requires independent review and exact integration checks before
acceptance. This continuation does not accept M2-01A's remaining motion/speed
contract or M2-01 gameplay.
