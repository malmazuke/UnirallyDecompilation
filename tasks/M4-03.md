# M4-03 — ZOOM ZOO decoded content and physics contract

## Assignment

- Status: claimed 13 September 2026 after the bounded Astra planning checkpoint
- Milestone: M4 — Original game coverage
- Planning consultation: bounded OpenAI Astra/high before claim because this
  task freezes a content identity and decides which recovered structures are
  safe inputs to later native work; order discriminating experiments and
  challenge reuse of the DRAGSTER layout
- Task provider/model: OpenAI, Sol/medium coordinator/worker in an isolated
  worktree after the planning checkpoint
- Provider quota: sampled before planning on 13 September 2026 at 55% weekly
  used / 45% remaining. The user then explicitly waived the percentage
  guardrail for an unattended M4 run and authorized at most one weekly usage
  reset if an actual provider limit is reached; this does not authorize money,
  paid credits or provider switching. Account-wide movement may include
  unrelated work.
- Reviewer: fresh sequential OpenAI Sol/medium reviewer with one withheld
  decoded-address or collision sample
- Dependencies: accepted M4-02 and [R-0020](../docs/research/R-0020-zoom-zoo-reference.md);
  accepted M3/tag `m3`; supported PAL ROM and pinned reference core locally
  available
- Owned paths: additive ZOOM ZOO content manifests/decoder contracts, bounded
  authored tooling/tests if required, research/task/review records
- Frozen paths/interfaces: accepted replay/map expectations, ROM/core identity,
  DRAGSTER content/native behavior, Classic pack and frontend
- Base commit, branch/worktree and heartbeat: `d7ce69d`,
  `task/M4-03-zoom-zoo-contract`, `.worktrees/m4-03-zoom-zoo-contract`;
  durable checkpoint after loader/layout, gather, collision/contract and at
  least every ten minutes

## Outcome and boundaries

Turn M4-02's identity-bound but opaque 50,665-byte ZOOM ZOO `0xC3` decode into
the smallest evidence-backed content/physics contract needed for later native
work. Locate and validate decoded layout regions, tile-set selection and rolling
BG1 gather against original runtime reads, and classify the bounded collision/
geometry inputs exercised by the frozen path.

Do not implement native ZOOM ZOO, expand the Classic pack, complete the race,
generalize a format from two tracks, recover presentation exhaustively or change
accepted DRAGSTER expectations. Unknown words remain opaque and decoded/original
bytes stay ignored.

## Acceptance

| Criterion | Experiment | Expected result |
| --- | --- | --- |
| Identity and layout gate | Decode the accepted `0xC3` object twice; compare header/region boundaries with ordered loader reads and writes | Exact packed/decoded identities remain stable; every claimed region has an observed consumer and unclaimed gaps remain opaque |
| Tile-set selection | Trace 25 load-time cursor iterations and resulting ROM-to-VRAM transfers | Selected entries, source pieces, order and destinations reproduce the accepted 406-transfer load without treating envelopes as continuous reads |
| Rolling gather | Reconstruct selected BG1 staging columns/rows from decoded content and runtime scroll/index state at preregistered riding frames | Authored reconstruction equals original staging bytes at all declared samples and does not replay captured uploads as input |
| Collision/physics boundary | Capture decoded-table reads and geometry consumers around at least two preregistered track positions/input conditions | Width, address derivation, cadence and values are exact for the tested domain; indistinguishable meanings remain provisional |
| Content contract | Freeze a reference-only manifest/schema binding accepted regions and hashes, with mutation failures | Fresh regeneration is exact; malformed bounds, wrong source identity and one changed region fail without tracking payload bytes |
| Regression/review | ROM-free suite, frozen ZOOM ZOO replay/map, selected M3 checks, hygiene and independent withheld sample | Accepted identities remain unchanged; no ROM/decoded payload/trace is tracked; reviewer reproduces the contract from commands alone |

## First experiment

Before claim, run the bounded Astra planning consultation and record its
recommendations plus coordinator decisions. Then reproduce the accepted M4-02
decode/access baseline and make a narrow capture around the `0xC3` decode tail
and first rolling gather. Predeclare two riding frames with distinct scroll/
index state, derive their expected staging addresses from observed reads, and
compare reconstructed bytes with the runtime buffer. If the DRAGSTER layout does
not transfer, record the mismatch and identify the smallest additional header/
consumer trace; do not force old offsets onto ZOOM ZOO.

Only the coordinator marks this task accepted after independent review.

## Astra planning checkpoint

Bounded OpenAI `gpt-6-astra`/high consultation completed read-only before claim
on 13 September 2026 against accepted M4-02 head `d7ce69d`. Coordinator
decisions:

- Treat DRAGSTER's selector base `0x5831`, plane stride `0x0800`, five planes,
  definition base `0x800F` and sampler bases `0x000F`/`0x800F` as hypotheses,
  not a general decoder. Supplement R-0008 with the later R-0010/R-0015
  recovery before judging reuse.
- Decode the accepted object twice first, then capture frames 1286-1291 around
  `$82:E1A7` and `$82:E2CD`-`$82:E346`. Compare immutable decoded bytes with
  completed runtime memory and explain every mutation; do not copy DRAGSTER's
  one-byte cursor exception or assume whether the terminator is included in the
  reported 25 iterations.
- Resolve each selected `$82:B7DD` tile entry and `$17:A000`/`$17:C4E4` table
  source, pairing ordered source pieces and destinations with all 406 observed
  transfers. Source envelopes and the tempting `406 / 2 = 203 tiles`
  interpretation are not evidence by themselves.
- Capture the first gather narrowly at frames 1376-1384, including
  `$81:B270`, `$81:B31C`, `$81:B366`, `$81:B3CF`/`$81:B3D3`, inputs `$0234`,
  `$0236`, `$0559`, selector bytes and staging `$0437`-`$0478`. Establish
  selector derivation and compare upload frame N with end-of-frame N-1 staging.
- Predeclare frames 1700 and 2220 as candidate Right-only and Right+Up samples.
  First verify distinct position/selector state; if a builder is absent, use a
  recorded next-builder rule rather than silently substituting a convenient
  frame. Reconstruct bytes from decoded content and runtime inputs without
  replaying captured uploads.
- At the same samples inspect both riders' incoming geometry arguments and
  consumers `$81:8D97`/`$81:8D9C`, `$81:8EF1`/`$81:8EF6`, `$81:8AA1`-
  `$81:8AC0`, `$81:8B66`/`$81:8B6A`, `$81:8CBB`/`$81:8CDD`. Derive widths,
  stride, address arithmetic, cadence and values from pre-collision inputs;
  unfamiliar meanings and edge branches remain provisional.
- Add a narrow reference-contract validator because the existing generic
  manifest path does not reconcile negative or contradictory descriptive
  source/runtime bounds and RNC header metadata. Require mutations of both
  payload-derived region identities and descriptive bounds/source identities.
- Reject incomplete PC logs, missing samples, ring overflow and unresolved
  accesses on claimed dependencies. Because PC snapshots are pre-instruction
  and lack cross-PC sequence numbers, observe loaded values at a consumer or
  store and prove cardinality/order rather than joining rows by frame alone.

The task remains a reference-only content/read-boundary contract. It does not
generalize two tracks, add native or Classic-pack support, absorb contact/AI/
camera/finish behavior, or assign semantic names to indistinguishable values.

## Handoff

- Status: candidate ready for independent review; not accepted.
- Current base/head: worker started from `9dacdf800325947e9141887934282a494910710d` on
  `task/M4-03-zoom-zoo-contract` in the declared isolated worktree.
- Checkpoint 13 September 2026 09:15 AEST: read the project workflow, validation
  contract and required R-0008/R-0010/R-0015/R-0020 evidence. The supported ROM
  exact-gates 10/10 identity fields. Two independent `content decode` executions
  reproduce the 50,665-byte object at SHA-256
  `db6770152e399f9d16fc6937b5d56588a8f67e825ae70d6578b77d36053fdd28`; their
  tracked-shape `decode.json` outputs are also byte-identical at SHA-256
  `bb15b647290826f28cd9e4ee75ac15e318623bef1e0ba04f69006f410b2c395b`.
- Checkpoint 13 September 2026 09:31 AEST: five fresh, complete, non-overflowed
  targeted captures retain sample digest `791a7863...` and final state
  `2a843314...`. The load window records 25 cursor iterations: 24 selected ids
  at decoded `0xC5CF..0xC5E6`, the consumed `0xFF` at `0xC5E7`, and one-byte
  cursor mutation `0xC5CF -> 0xC5E8`; decoded byte `0xC5E8` remains opaque.
  Directory/table resolution yields 203 tiles, 6,496 ordered table bytes and
  203 ordered flag bytes; the LoROM-wrap-aware ordered 406-transfer contract
  hashes to `6d28534e...`. Contrary to DRAGSTER, the observed ZOOM ZOO sampler
  width/stride are 256/512. Decoded-only reconstruction matches 66 staging
  bytes at Right-only frame 1700 and 32 bytes at the recorded next builder
  frame 2224 for the preregistered Right+Up frame 2220. Both riders' ten sample
  words match at frames 1700 and 2220 (40 words total). A new narrow validator
  passes from the supported ROM, and seven focused ROM-free schema, source,
  bounds, region, gather and addressing mutation tests pass. Research record,
  broader regression/hygiene and final handoff remain in progress.
- Verified starting facts: selector `0xC3` points to a 6,599-byte RNC object at
  ROM file offset `0x0C0183`, decoded length 50,665; its tail drives 25 cursor
  iterations and 406 observed 64-byte ROM-to-VRAM transfers. Accepted access/
  provenance commands and identities are tracked in R-0020.
- Current interpretation: the instruction family is shared with DRAGSTER, but
  the observed 256/512 coarse width/stride and ZOOM ZOO-specific ordered load
  require this separate bounded contract. No two-track generalization is made.
- Next action: independent review and a reviewer-owned decoded-address or
  collision sample, followed by coordinator integration only if approved.
- Runtime needs: supported private PAL ROM, pinned local reference core and
  ignored capture space; no network, publication, deployment or spending.
- Candidate scope: additive
  `tests/manifests/content/zoom-zoo-reference-contract.json`, the narrow
  `content zoom-zoo-contract` validator, seven ROM-free mutation/addressing
  tests, [R-0021](../docs/research/R-0021-zoom-zoo-content-contract.md), and
  this task handoff. No native gameplay source, Classic pack, frontend,
  accepted replay/map expectation, ROM/core lock, `docs/STATE.md` or
  `tasks/README.md` changed. The exact candidate commit is returned to the
  coordinator after the final clean-source reruns.
- Loader/layout result: the decoded object and four claimed region identities
  are exact. Runtime changes only decoded byte 11 (`0xCF -> 0xE8`) by the end
  of frame 1291. Twenty-four ordered ids plus the consumed terminator account
  for 25 cursor executions, 203 tiles, 406 ordered 64-byte transfers, 6,496
  ordered per-tile table bytes and 203 flag bytes. The trailing decoded byte at
  `0xC5E8` remains opaque. The validator independently re-derives directory
  sources, LoROM bank wrapping, transfer destinations and all payload hashes
  from the supported ROM.
- Gather/collision result: decoded-only gathers match 66 bytes at Right-only
  frame 1700 and 32 bytes at frame 2224, the first recorded builder after the
  preregistered Right+Up frame 2220. Both riders' ten collision samples match
  at 1700 and 2220 (40/40 words). The observed coarse width/byte stride are
  256/512, directly rejecting blind reuse of DRAGSTER's 1024/2048 values.
  Returned word meanings, table-byte meanings and all unsampled branches stay
  provisional.
- Exact evidence: the five complete, non-truncated, non-overflowed capture
  hashes are `bc733d1c39bda507...`, `36aeac919fa7f48b...`,
  `02f497194d481e37...`, `fa3e5e2d4352e793...` and
  `ecb45a173dafca37...`; R-0021 tracks the exact successful commands and full
  hashes. `content zoom-zoo-contract` passes with 24 entries, 406 transfers,
  98 gather bytes and 40 collision words (report `1c2c77499144393d...`).
- Regressions before the final clean-source rerun: focused tests pass 7/7;
  app-debug and app-sanitize each pass 322/322. Two fresh ZOOM ZOO replays
  retain `791a7863...` / `2a843314...` and exact A/V (report
  `9981877214bfc230...`). Coverage regenerates `476de96fa13b2cff...`; its map
  and summary are byte-identical to the tracked files (reports
  `c47c2b652a798890...` / `da4b2f2d385e7d30...`). DRAGSTER's ten content
  identities pass (`c8ca39f37c05babb...`), the unchanged 25-entry Classic
  pack passes (`ffbb4b367b7ebbdec...`), and pack-backed full-race/restores at
  1600/3213/3453/3678 pass all 18 checks (`72d822fbe8a21b75...`).
- Retained failures: the first native regression used unsupported preset
  `app-debug` and exited 3 before execution. Two following attempts correctly
  rejected report paths outside their requested fresh artifact directories;
  the corrected single `lab-debug` command with four save frames passed. No
  missing or failed check is reported as a pass.
- Reviewer start: use a fresh sequential OpenAI Sol/medium checkout. Re-run the
  tracked validator, both decoded-content commands, the load capture and one
  independently chosen collision or gather sample not frozen above. Require
  complete/no-overflow capture status, derive its source addresses from runtime
  inputs, and compare with the immutable decode rather than captured upload
  bytes. Then run the seven ROM-free tests, inspect tracked-file hygiene and
  confirm all M3/frozen identities remain unchanged. Review sets status only to
  approved/changes-requested; the coordinator alone accepts and integrates.
- Model/usage: OpenAI Sol/medium throughout. The user waived percentage
  guardrails for this unattended M4 goal. No reset was needed or redeemed, no
  money was spent and no provider switch occurred.
- Final clean-source checkpoint 13 September 2026 10:02 AEST: implementation
  commit `b296b994ff773c0cffd69039fe9dd69a5d54df30` passes the following exact
  commands. Each generated report records that same source at start/finish and
  reports `source_changed_during_run: false`:
  - `python3 tools/project.py content zoom-zoo-contract --contract tests/manifests/content/zoom-zoo-reference-contract.json --report artifacts/m4-03/final-contract/report.json`;
    passed 3/3, report SHA-256
    `53f83b0a89ff65980a6abc1ce66a403d4d9271f50a3fa05c8294ef177d738b3a`.
  - `python3 tools/project.py test --suite synthetic --preset app-debug --artifacts artifacts/m4-03/final-app-debug/artifacts --report artifacts/m4-03/final-app-debug/report.json --task M4-03`;
    passed 322/322, report SHA-256
    `10776fd2237d575c62aace71205f5fe525f6d72a6baa2590e48009e89a82933c`.
  - `python3 tools/project.py replay compare --manifest tests/manifests/replay/race-crawler-zoom-zoo-3300.json --runs 2 --artifacts artifacts/m4-03/final-zoom-replay/runs --report artifacts/m4-03/final-zoom-replay/report.json --task M4-03`;
    passed 24/24 with exact A/V and frozen sample/final identities, report
    SHA-256
    `77ac768ae7e158caec85531de8c43961de5d53848ec839653443e9056517b24f`.
  - `python3 tools/project.py coverage capture --manifest tests/manifests/replay/race-crawler-zoom-zoo-3300.json --out artifacts/m4-03/final-zoom-coverage/capture --report artifacts/m4-03/final-zoom-coverage/report.json --task M4-03`;
    passed 19/19, report SHA-256
    `4563f8c1858b4fa83a0538f5d3785bfc97e4fc3e51b551f9c80e61bbc19f3bcc`.
  - `python3 tools/project.py coverage map --coverage artifacts/m4-03/final-zoom-coverage/capture/coverage.json --out artifacts/m4-03/final-zoom-coverage/map.json --summary artifacts/m4-03/final-zoom-coverage/map.md --detail artifacts/m4-03/final-zoom-coverage/detail.json --baseline docs/map/race-crawler-dragster-3000.map.json --scenario race-crawler-zoom-zoo-3300 --report artifacts/m4-03/final-zoom-coverage/map-report.json --task M4-03`;
    passed all 7 required and 3 optional checks, report SHA-256
    `6daf976fdd14ac64ab047d2389d114c173a9880eb5e024dae069dc2913516f78`.
  - `python3 tools/project.py native finish-check --manifest tests/manifests/native/full-race-continuous.case.json --content-pack /Users/markfeaver/Projects/Unirally\ Decompilation/local/classic-crawler-dragster.pack --save-frame 1600 --save-frame 3213 --save-frame 3453 --save-frame 3678 --preset lab-debug --artifacts artifacts/m4-03/final-native --report artifacts/m4-03/final-native/report.json --task M4-03`;
    passed 18/18, report SHA-256
    `d4b07b22d0db0198151824c3ff1d616cf4263435e8823d118660044742ccd974`.
  `git diff --check HEAD^ HEAD` passes and `git status --short --branch` shows
  only the branch header after this handoff checkpoint is committed. Generated
  ROM-derived bytes, traces, reports and capture outputs remain ignored.
