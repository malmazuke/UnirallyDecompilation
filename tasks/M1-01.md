# M1-01 — Observed code/data map for the selected scenarios

## Assignment

- Status: review (submitted 11 September 2026 by the attempt-2 worker; parts A and B delivered; claimed 14:55 AEST, registry row updated in the claim commit, M0-05 gap G1)
- Milestone: M1
- Coordinator: primary agent (Claude Code, Fable 5.1)
- Worker/session/runtime/model: fresh session of Claude Fable 5.1, run as a subagent of the coordinator's session and dispatched with the portable prompt in `docs/AGENT_WORKFLOW.md` and nothing else
- Reviewer: fresh independent session, own clone (dispatched by the coordinator after submission)
- Dependencies and evidence of acceptance: M0 accepted (tag `m0`, [R-0005](../docs/research/R-0005-m0-acceptance.md)); the replay comparator and manifest schema 1 ([R-0003](../docs/research/R-0003-replay-comparator.md)); the reference adapter's instruction trace ring ([R-0002](../docs/research/R-0002-reference-adapter-determinism.md) finding 7)
- Base commit: the claim commit on `task/M1-01-code-map` (its parent is `241e288`, `main` after M0-06 integration)
- Branch and isolated worktree: `task/M1-01-code-map` in the linked worktree `.worktrees/m1-01` (ignored path; the coordinator's checkout stays on `main`). Fixture arrangement: the coordinator placed `local/rom-location.txt` in the worktree; nothing else was copied, so the worker bootstraps and builds the core from the records. The coordinator pushes the candidate branch to origin for CI
- Owned paths and shared interfaces: new package `tools/unirally_lab/coverage/`; new tests `tests/tooling/test_coverage.py`; `tools/project.py` only to register the new `coverage` command group; `tools/unirally_lab/reference/worker.py` and `bsnes.py` only for **additive** options (per-frame trace drain, frame image dump) that leave every existing output, exit code and digest unchanged; new manifests `tests/manifests/reference/<new scenario>.json` and `tests/manifests/replay/<new scenario>.json`; new map files under `docs/map/`; `docs/research/R-0006-*` (the evidence record); this record. Must not change: the pinned core or its tracked patch (see the capture decision below), `tools/locks/`, any existing manifest, the samples/report schemas, `src/lab`, `docs/STATE.md` or the registry (coordinator-owned)
- Claim/lease/heartbeat/checkpoint location: this file and the registry row; checkpoint commits at least every 10 minutes and before every long capture
- Session time limit, concurrency allocation and actual spend authorization if relevant: about 3 h of worker time for a first candidate (the M1 basis in R-0005 is roughly 12 h for M1-01 to M1-03, and M0-03 is the analogue: a task that meets a real unknown may absorb more); one worker; no service purchases. If part B below is not reached within the budget, submit part A for review with part B's state recorded, rather than a partial part B

## Outcome and boundaries

Deliver a trace-driven map of what the ROM actually executes in two scenarios, with processor-mode context and entry points, and the tool that produces it reproducibly. Unexecuted bytes stay unclassified; nothing is inferred from static analysis alone.

**Part A — coverage capture and the map of the validated scenario.**

1. A whole-run instruction-coverage capture. Decision (coordinator, 11 September 2026): the pinned core and its tracked patch stay unchanged, because every replay manifest and `tools/locks/emulators.json` pin the patch digest `a719f5ffe222…`, so a patch change would be a baseline change for every recorded digest. Instead the existing ring (`trace_entries`, up to 1,000,000 entries of 20 bytes; the hook already records pc, p and e per executed instruction) is **drained once per frame** from Python: after each `retro_run`, read `trace_total`, take the newest `delta` entries of the window and accumulate them. About 14,000 instructions execute per frame in this ROM (8,514,634 over 600 frames, R-0002 finding 7), so a ring of 100,000 or more cannot overflow within a frame; the capture must still check `delta <= capacity` every frame and fail the run (exit 1, reason recorded) if it ever does not hold. Enabling the ring does not alter emulation by construction (the hook only copies registers), and the acceptance table verifies it anyway: the sample digest of `boot-start-600` must be unchanged under capture. Only if this approach proves unusable (measured, for example more than about ten minutes for 600 frames after obvious optimisation such as `struct.iter_unpack` over the raw buffer) may the worker fall back to a core patch extension, and then the lock and every manifest's `patch_sha256` change in one explained commit with every recorded digest re-run and shown unchanged, and the reviewer confirms that re-run.
2. Per executed 24-bit address: execution count, the set of processor modes observed (E, M, X flags from `p` and `e`), the first frame at which it executed, and the ROM file offset it maps to under the LoROM rule (a cartridge address reached through more than one bank is recorded with each bank and one offset). Code executing from work RAM or anywhere outside the ROM is listed separately, not dropped.
3. Opcode lengths under the observed M/X flags classify the operand bytes of executed instructions, so the map distinguishes executed opcode bytes, their operand bytes, and everything else (**unclassified**). Absolute and long operands of executed instructions give a list of *statically referenced* addresses (candidate data, tables or jump targets); indexed and indirect targets are not resolved in this task. Reads and writes are not tracked (the trace does not carry them); a data-access map is a later task if M1-02 or M1-03 needs one.
4. Entry points: the six native-mode and six emulation-mode vectors from the ROM header with their targets and whether each target executed; every executed instruction whose predecessor in the trace is not its sequential successor (subroutine and jump targets, interrupt handlers, returns) with the edge's kind (JSR, JSL, JMP, JML, RTS, RTL, RTI, BRK/COP, interrupt entry, branch), its mode and count. The interrupt entry is not an executed instruction (R-0002 finding 7), so NMI/IRQ handlers are recognised by the target being a vector target.
5. The map for `boot-start-600` (the only validated scenario at dispatch), as a tracked derived file under `docs/map/` plus a short Markdown summary (per-bank executed byte counts, mode mix, entry-point count, how many bytes of the 2 MiB are executed, and the coverage limits), and the evidence record R-0006.

**Part B — a gameplay-reaching scenario.** The boot scenario cannot show where rider state or track data live, and M1-02 and M1-03 depend on this task. Decision (coordinator): the selected gameplay scenario is a cold start driven from the title screen into the **first race of the single-player mode** with a short segment of riding (a few seconds of accelerate/lean input), bounded to at most 3,000 PAL frames (60 s). The worker discovers the menu path by experiment: add a frame-image dump option to the worker (write the XRGB8888 frame at chosen frame numbers as PNG through `zlib`; no new dependency) and look at the images, then encode the discovered presses as a reference script and a replay manifest with the same fields as `boot-start-600`. The scenario is validated the M0-04 way: two fresh runs identical on every declared field (`replay compare` exit 0, expected digests recorded in the manifest), and one perturbation of a riding-phase input (for example removing the accelerate press) produces a first divergence during the riding phase, which shows the inputs reach gameplay code. Then capture coverage for it and produce its map; the summary must list what the race scenario executes that the boot scenario does not (new ranges, new entry points), which is the hand-over to M1-02 and M1-03. Menu choices that the worker makes (mode, track, player) are recorded in the manifest description with their rationale; a wrong but recorded choice is acceptable.

Not in scope: assigning any meaning to a work RAM address (M1-02), decoding any asset or track (M1-03), a data-access (read/write) map, a native simulation, a disassembly listing in tracked files, changes to the pinned core (except the recorded fallback), a Linux run of the core, a scheduler.

Tracked-content rule: tracked map files carry addresses, ranges, offsets, counts, modes, frame numbers, edge kinds and the twelve vector values; they must not carry ROM bytes, opcode bytes or a disassembly (those stay in ignored `artifacts/`). Keep each tracked map file under about 1 MiB by using ranges; per-address detail stays in ignored artifacts and is regenerated by the recorded command.

## Inputs and prerequisites

- ROM as in M0-03 (`local/rom-location.txt`, manifest `tests/manifests/rom/unirally-pal.json`, SHA-256 `a1105819…`); built pinned core (`reference build`, commit `7d5aa1e656b9`, patch `a719f5ffe222…`); replay manifests in `tests/manifests/replay/`.
- Runtime needs: network access for the pinned wheels and the core clone; about 15 s of core build; the ROM path arrangement above; memory for a 1,000,000-entry ring is 20 MiB, for per-address accumulators over the 16 MiB address space up to about 100 MiB (this host has 64 GiB); a 3,000-frame capture is expected to take well under a minute of emulation plus the Python drain.
- Baseline to reproduce first: `doctor`, `bootstrap`, `build --preset lab-debug`, `test --suite synthetic` (118 checks), `reference build`, `replay compare --manifest tests/manifests/replay/boot-start-600.json` (exit 0, sample digest `583d1ec544ec61a2…`).
- Known baseline failures: none. Known limits carried from M0: reference execution on one macOS host; CI is ROM-free, so the new tests must exercise the capture, the decoder and the map generation with stubbed trace data (as `test_replay.py` does with the stubbed worker); no lease store (G4); an orphaned worker survives a parent kill (G2), so a re-run into the same artifacts directory is not safe until live `reference/worker.py` processes matching the `--samples-out` path are checked.
- Reference material for the 65816: the opcode length depends on the M and X flags for immediate operands and on E for nothing that affects length; the worker writes its own 256-entry table and tests it ROM-free. Do not copy tables from other emulators' sources into the repository without recording provenance and licence.

## Acceptance

| Criterion | Command or experiment | Expected result | Required artifact |
| --- | --- | --- | --- |
| Capture does not perturb the run | `coverage capture` (intended interface, see below) on `tests/manifests/replay/boot-start-600.json` | Sample digest `583d1ec544ec61a2…` and final state `1a6fbf6d24c532ff…` as in the manifest; instruction total equals the sum of per-frame deltas and matches the recorded 8,514,634 (or the documented difference for the final serialize); no frame's delta exceeds the ring capacity | Capture report, coverage file digest |
| Capture is repeatable | Two fresh-process captures of the same manifest | Byte-identical coverage files (same digest) | Two report digests |
| Map is derived reproducibly | `coverage map` from the coverage file and the ROM | Same tracked map file from two captures; the map records the ROM SHA-256, core identity, scenario id, frames and the generating command | `docs/map/boot-start-600.*` |
| Mode context and entry points | Inspection of the map plus a spot check of at least three executed addresses against the trace window in the samples file (the 64-entry window at the end of the run must be a suffix of the captured trace) | Every executed range lists its observed E/M/X modes; addresses executed in more than one mode are listed; the reset vector target is the first executed instruction; the NMI vector target appears once per frame after NMIs are enabled (count and first frame recorded) | R-0006 |
| Unexecuted bytes stay unclassified | Summary totals | executed opcode bytes + operand bytes + unclassified = 2,097,152; no byte is labelled data or code without an executed instruction or vector behind it | Summary in `docs/map/` |
| ROM-free tests | `test --suite synthetic` locally and on both CI platforms | Suite grows by tests for the opcode-length table (all 256 opcodes in each M/X combination, at least the immediate-width cases checked against hand-derived expectations), the per-frame drain against a stub core (including the overflow failure), the LoROM address mapping and the edge classification on a synthetic trace; green on `ubuntu-24.04` and `macos-15` | Tests, CI run |
| Gameplay scenario validated (part B) | `replay compare --manifest tests/manifests/replay/<race scenario>.json`; a perturbed copy of the manifest via `replay compare --against` | Exit 0 with the expected digests written into the manifest; the perturbed riding-phase input gives a first divergence at or after the frame the input is applied, localized to work RAM | Manifests, two reports |
| Gameplay scenario mapped (part B) | `coverage capture` and `coverage map` on the race manifest | Tracked map and summary; the summary lists ranges and entry points executed in the race scenario but not in the boot scenario | `docs/map/<race scenario>.*` |
| Independent review of the exact candidate | Reviewer reproduces on the candidate commit from its own clone; CI on that commit | Approve, or specific reproducible findings addressed | Record |

Intended interface (not yet implemented at dispatch; the worker may adjust names but records the final ones here and in `docs/BUILD_AND_VALIDATION.md`'s command table): `python3 tools/project.py coverage capture --manifest <replay manifest> --out <dir> [--ring N] [--frame-image N ...] --report <json>` runs one fresh worker process with the per-frame drain and writes the coverage file, the usual samples and the report; `python3 tools/project.py coverage map --coverage <file> --out <map file> --summary <markdown> --report <json>` derives the tracked map, reading the ROM at `local/rom-location.txt` for opcode decoding. Exit codes follow the repository convention (0/1/2/3/4). Reports use schema 1 with `--task M1-01`.

## Evidence and attempts

| Attempt | Hypothesis | Experiment | Observation | Next decision |
| --- | --- | --- | --- | --- |
| 0 | (dispatch attempt 1, 11 September 2026 14:57 AEST) | Worker subagent started with the portable prompt | Terminated by the provider's session usage limit before any command ran; the worktree stayed clean at `8e8420e` with no live worker process (checked 17:01 AEST) | Re-dispatched as attempt 2 from the same worktree after the limit reset; nothing to recover |
| 1 | Baseline reproduces from the records (attempt 2, 17:02 AEST) | `doctor`, `bootstrap`, `build --preset lab-debug`, `test --suite synthetic`, `reference build`, `replay compare --manifest tests/manifests/replay/boot-start-600.json` | All exit 0; 118 checks; sample digest `583d1ec544ec61a2…`, final state `1a6fbf6d24c532ff…`, 8,514,628 instructions at the end of frame 599 (8,514,634 after the final serialize). The worktree's ignored `local/` already held the toolchain cache and the built core checkout (the coordinator's fixture arrangement copied more than `rom-location.txt`), so the core build step reported 0.1 s | Proceed with part A |
| 2 | The unchanged ring drained once per frame from Python captures the whole run without perturbing it and in acceptable time | Worker option `--coverage-out` (ring 262,144, whole-ring copy per frame, newest `delta` entries accumulated as (pc, mode, data bank) sites and consecutive-site pairs); run on the boot script directly | Sample digest and final state unchanged; per-frame deltas sum to 8,514,628; largest frame 18,346; 4.6 s for 600 frames (1.3 s without); first site `$00:8858` in mode Emx = the emulation reset vector | No core patch needed; build `coverage capture|map` on it |
| 3 | The trace entry is 20 bytes (task text) | `struct.Struct("<IHHHHHBBBBHH").size` and `unirally_trace_entry_size` | 22 bytes (pc 4, five 16-bit registers 10, four bytes B/P/E/reserved, two 16-bit counters 4); the core already checks this at load | Drain unpacks 22-byte entries; the task's 20 was an approximation |
| 4 | Two captures of one manifest are byte-identical | `coverage capture` twice on `boot-start-600` | First pair differed only in the derived script's path inside the coverage identity | Identity written without host paths (digests and sizes only); after that identical (`1e6c02747d7c1003…`) |
| 5 | The map derives reproducibly and passes its whole-run consistency checks | `coverage map` from both captures | Identical map files; opcode 2,085 + operand 2,563 + unclassified 2,092,504 = 2,097,152; 0 overlapping bytes; 5 unknown edges, all from work RAM sites `$00:0199`/`$00:019C`; NMI target `$00:8587` once per frame from 250 | Part A complete; spot checks in R-0006 finding 4 |
| 6 | The single-player race is reachable by Start presses on default menu entries within 3,000 frames | Exploration scripts with `--frame-image` dumps (`artifacts/m1-01/explore1`–`explore4`) | Start at 300 → main menu (1P selected at 599); 620 → PICK YOUR UNI (MIKE); 750 → PICK TOUR (CRAWLER); 900 → PICK TRACK (DRAGSTER; the tour also lists ZOOM ZOO, BOWL, SWITCHER, MONSTER); 1050 → NOW PLAYING MIKE vs BRONSEN on DRAGSTER (Race/Exit); 1200 → race on screen by 1400 with the timer at 0:00:0 | Decision: rider MIKE, tour CRAWLER, track DRAGSTER, all defaults so no cursor movement is needed; recorded in the manifest description |
| 7 | One of B, Y, Right, A, X, Up is the accelerate input | Six parallel worker runs holding one button from 1500 (or 1700 for B/Y) and comparing WRAM and frame-image digests against a no-input run (`explore5-*`) | All six change WRAM from the first held frame; only Right changes the video output while the rider is stationary: the rider moves along the track, passes the opponent (`+0:00:1` at 2400) and the `PULL A STUNT` hint appears; Up while riding tilts the rider (frame 2230 of the race capture) | Scenario inputs: Right 1500–2999, Up 2200–2259 |
| 8 | The race scenario is a valid manifest (identical fresh runs) and its riding input reaches gameplay code | `replay compare --manifest race-crawler-dragster-3000.json`; `replay compare --against race-crawler-dragster-3000-right-removed.json` | Exit 0: 3000 frames identical on all fields, final state `0f3cc38bd5d64401…`, sample digest `72f618f2f7e3416e…` (written into the manifest). Perturbation: exit 1, first divergence at frame 1500 in `wram_sha256`, localized to one byte `$7E0313` (`0x01` vs `0x00`), no register difference | Part B validated |
| 9 | The race capture and map behave like the boot ones | Two `coverage capture` runs, two `coverage map --baseline docs/map/boot-start-600.map.json` derivations | Byte-identical coverage (`a075220e58ecd355…`) and maps; 49,465,866 instructions, largest frame 19,410; 14,727 + 20,173 + 2,062,252 = 2,097,152; 10 unknown edges, all from the same two work RAM sites; NMI once per frame from 250 except frames 1208–1328 (race load) and again from 1329; 30,252 bytes in 633 ranges and 1,809 entry points executed only in the race | Part B complete |
| 10 | The "once per frame" claim needs per-frame evidence, and the tracked race map must stay under 1 MiB | First race map: `nmi_vector_once_per_frame` failed from totals alone (2,629 executions over 2,750 frames) and the map was 1,065,816 bytes | Added `--coverage-watch` (per-frame counts of the interrupt vector targets, passed by `coverage capture` from the ROM manifest's vectors) and a per-frame summary in the vectors table; dropped derivable `rom_offset`/`region` from entry points and references and tightened the JSON; the race map is 793,963 bytes; the summary lists only the 781 new entry points reached by a call, jump, return or interrupt in full | All captures and maps regenerated on the final code |

## Handoff

- Current base/head commit and uncommitted state: base `72bf3ba`; candidate head is the commit that adds this handoff and R-0006 on `task/M1-01-code-map` (the coordinator records the hash on push); worktree clean after it. Commits in order: `ae7a749` opcode table, `d4c9705` worker options, `5f6e2ff` commands and derivation, `210d156` boot map, `6d9baf8` tests, `4ce4c6c` race manifests, `f13f3a4` per-frame watch and slimmer map, `669d373` regenerated maps.
- Verified findings: [R-0006](../docs/research/R-0006-observed-code-map.md) findings 1–13. Final interface: `python3 tools/project.py coverage capture --manifest <replay manifest> --out <dir> [--ring N] [--frame-image N ...] [--rom P] [--expect M] [--timeout S] --report <json> --task M1-01` and `python3 tools/project.py coverage map --coverage <dir>/coverage.json --out <map.json> [--summary <md>] [--detail <json>] [--baseline <map.json>] [--rom P] --report <json>`; recorded in `docs/BUILD_AND_VALIDATION.md`. Worker additions: `--coverage-out`, `--coverage-ring`, `--coverage-watch`, `--frame-image`, `--frame-image-dir`.
- Current hypothesis and failed approaches: none open. Not tried: the core-patch fallback (unnecessary: 4.5 s / 25 s captures). `nmi_vector_once_per_frame` from aggregate counts was replaced by per-frame watching (attempt 10).
- Commands executed, outcomes and report hashes (reports under ignored `artifacts/m1-01/`, SHA-256 prefixes): baseline `baseline/test.json` `bfc3854883dd93d3…` (118 checks, exit 0), `baseline/compare.json` `243b5599f1113e8c…` (exit 0); captures `cap1-report.json` `d3f2d3a298edb711…`, `cap2-report.json` `0b7d91a4b29330cc…` (exit 0, coverage `1e6c02747d7c1003…` both), `race-cap1-report.json` `656e815121627c89…`, `race-cap2-report.json` `cab3a6dfe81213db…` (exit 0, coverage `a075220e58ecd355…` both); maps `map1-report.json` `6265b258362a2c56…`, `map2-report.json` `bead64b119290770…`, `race-map1-report.json` `d45ee085dd039cbb…`, `race-map2-report.json` `eddd426f1518225d…` (exit 0; tracked map digests `21c0526c5c51ad67…` boot, `e2c9da7c1509e350…` race); `race-compare.json` `a43154c607d7436a…` (exit 0); `race-perturb.json` `a0cf3d62b15afa19…` with `race-perturb/divergence.json` `ccc37b2e1457aec2…` (exit 1, frame 1500, `$7E0313`); suite on the final tree `test-final.json` `3377c92981a2ce24…` (148 checks, exit 0, 25.2 s).
- Unavailable/skipped checks: CI on both platforms for the candidate (the coordinator pushes; the ROM-free suite is what CI runs); independent review; a Linux run of the core (M0 limit); `first_instruction_is_reset_vector` and `reset_vector_is_first_instruction` are skipped by design for state-origin manifests (none captured here).
- Exact next experiment/command: reviewer: from a fresh clone at the candidate, `python3 tools/project.py doctor && python3 tools/project.py bootstrap && python3 tools/project.py build --preset lab-debug && python3 tools/project.py test --suite synthetic` (148 checks), `python3 tools/project.py reference build`, then `python3 tools/project.py coverage capture --manifest tests/manifests/replay/boot-start-600.json --out artifacts/coverage/boot-start-600 --report artifacts/coverage/boot-capture.json` (coverage SHA-256 `1e6c02747d7c1003…`, sample digest `583d1ec544ec61a2…`), `python3 tools/project.py coverage map --coverage artifacts/coverage/boot-start-600/coverage.json --out /tmp/boot.map.json --summary /tmp/boot.md` and `cmp /tmp/boot.map.json docs/map/boot-start-600.map.json`; the same for `race-crawler-dragster-3000` with `--baseline docs/map/boot-start-600.map.json` (coverage `a075220e58ecd355…`); `python3 tools/project.py replay compare --manifest tests/manifests/replay/race-crawler-dragster-3000.json --against tests/manifests/replay/race-crawler-dragster-3000-right-removed.json` (exit 1, frame 1500, one byte `$7E0313`). A withheld case the reviewer could add: another button or frame for the perturbation, or `--frame-image` at chosen frames to confirm the menu path.
- Remaining dependencies: none for review. For M1-02/M1-03: the hand-over lists in R-0006 finding 12 and the `compared_to` section of the race map.
- Runtime needs (network, build time, fixtures, memory): network for the pinned wheels and the core clone on a fresh host (both were already cached in this worktree); core build about 15 s when not cached; the ROM via `local/rom-location.txt`; a capture holds a 262,144-entry ring (5.6 MiB) plus Python accumulators well under 200 MiB; 4.6 s per boot capture, 25 s per race capture, 23 s for the perturbation compare with localization, 22 s for the suite.
- Elapsed work and provider usage when known: about 2 h 05 min of worker session (17:02–19:07 AEST, one session, no interruption); provider usage not visible to the worker.

## Review and integration

- Reviewer and independent reproduction/withheld-case results:
- Required changes or acceptance rationale:
- Exact merge candidate and required-check results:
- Integrated commit and evidence location:
- Scope still unverified:
