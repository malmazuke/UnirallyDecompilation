# M4-04 — ZOOM ZOO riding reference and continuation-boundary freeze

## Assignment

- Status: accepted 13 September 2026 after one returned seed-integrity finding,
  approved focused re-review and green macOS/Linux hosted validation
- Milestone: M4 — Original game coverage
- Coordinator: current OpenAI coordinator
- Planning consultation: bounded OpenAI `gpt-6-astra`/high completed read-only
  before claim against accepted M4-03 head `31e2dae`
- Task provider/model: OpenAI, Sol/medium worker in an isolated worktree
- Provider usage: the user waived percentage guardrails for the unattended M4
  goal and authorized at most one weekly usage reset only if an actual provider
  limit is reached; no money, paid credits or provider switching is authorized
- Reviewer: fresh sequential OpenAI Sol/medium reviewer with a withheld
  one-frame Up-entry or Up-release boundary
- Dependencies: accepted M4-02 replay and M4-03 content/read contract; accepted
  M3/tag `m3`; supported PAL ROM and pinned core available locally
- Base/branch/worktree: `31e2dae`, `task/M4-04-zoom-zoo-riding-freeze`,
  `.worktrees/m4-04-zoom-zoo-riding-freeze`
- Owned paths: additive field-bearing ZOOM ZOO replay/native reference
  manifests and projections, a dedicated additive freeze/verifier and focused
  tests if needed, `docs/research/R-0022-zoom-zoo-riding-reference.md`, this
  task and its review records
- Frozen paths: existing M3/M4 manifests and expectations, native update and
  canonical serialization, Classic pack/profile/start, frontend/presentation

## Outcome and boundaries

Freeze reproducible field-bearing reference expectations and a documented
candidate continuation boundary for the accepted 3,300-frame ZOOM ZOO primary
and release-2500--2599 paths. Use end-of-frame 1649 as the proposed initial
observation and cover updates 1650--3299, preserving the opponent's accumulated
state. Determine the first observed native-domain blocker from original
evidence; this research task can be accepted without recovering every newly
encountered mechanic.

Do not implement native ZOOM ZOO, revise canonical serialization, alter the
existing DRAGSTER freezer/seed, expand the Classic pack, recover rendering/
camera/audio/result behavior, or generalize contact, AI or track formats.

## Why this prerequisite is required

M4-03 proves immutable content, two gathers and four collision calls, but stops
before contact response, pose recurrence, opponent AI and finish. The current
native path still supplies width 1024 where ZOOM ZOO observes 256, rejects
several non-flat/directional/horizontal contact branches, fixes opponent input
to 2 and uses DRAGSTER's finish threshold. Existing freeze/prepare tooling also
assumes the DRAGSTER frame-1533 seed and transient state. Changing hashes or
adding `0xC3` to the pack would conceal those dependencies.

## Acceptance

| Criterion | Experiment | Expected result |
| --- | --- | --- |
| Exact field-bearing freeze | Run both new cases twice in distinct fresh processes | Complete contiguous frames, exact declared values, final state and A/V; controller schedules and underlying accepted whole-state identities remain unchanged |
| Perturbation boundary | Compare primary and release-2500--2599 cases | Exact through 2499, input divergence at 2500 and subsequent original motion response |
| State mapping | Audit end-1649 WRAM and cartridge RAM plus producer/consumer windows | Each required field records address, width, integer interpretation, sampling phase and observed producer/consumer; both riders' x/y, velocities, pose/reflection, contact and progress plus input/throttle/timer are present; uncertain fields remain provisional |
| Continuation candidate | Repeat end-1649 observation and inventory read-before-write dependencies | Candidate start includes actual SRAM-dependent state and an explicit mapping; no unsupported complete-future-state claim |
| First native blocker | Inspect existing 1700/2220 evidence, then narrow windows only as needed | First exercised incompatible contact/pose/AI/wrap condition is identified at exact frames/PCs, or bounded readiness guards are stated |
| Freeze/verifier integrity | Mutate ROM/core/options/script, projection metadata/row width, frames and seed/capture identities | Wrong, missing, duplicate, incomplete or contradictory inputs fail; output refuses overwrite |
| Regression/review | ROM-free focused and synthetic suites, M4-02/M4-03 identities, DRAGSTER pack/full-race restore and withheld boundary | Frozen behavior is unchanged; reviewer independently shifts one Up boundary and observes the precise response or lack of response |

## Experiment order

1. Reproduce M4-03 and inspect the existing 1700/2220 captures for actual
   descriptor values, orientation/reflection, velocities, width/stride and
   `$0D4F`; record an immediate blocker if present without inferring from art.
2. Capture and independently repeat end-of-frame 1649 WRAM and cartridge RAM.
   Audit positions/velocities/residues, contact, pose/displacement history,
   progress, both update phases, countdown/timer, AI, reward queues/cursors/
   weight and learned-feature total. Never default an unproven field to zero.
3. Add dedicated field-bearing replay manifests with controller schedules
   byte-identical to the accepted cases. Freeze two fresh runs per case and
   separately prove the underlying accepted whole-WRAM, final-state and A/V
   identities are unchanged despite the new declared-field digest.
4. Capture narrow ordered windows at 1649--1651, 1671--1673, Up entry/exit and
   2499--2501/2599--2601. Follow only producers exposed by these windows or the
   projected series; reject incomplete logs, overflow and unresolved claimed
   ordering.
5. Commit the reference projections and provenance before any later native
   task. End with either bounded native-readiness guards or one precisely named
   prerequisite routine/branch.

PC snapshots are pre-instruction and separate arrays do not prove cross-PC
order. Confirm that no finish/result transition occurs in the interval; if it
does, preserve it and bound the riding projection before that transition.

## Astra planning checkpoint

The consultation selected this reference freeze instead of native or pack
work. It found the accepted replay currently exposes only whole-WRAM/register
diagnostics and `$0000..01FF`, so it cannot feed the 13-field native freezer;
the current freezer assumes 3,000 frames and frame 1533, while seed preparation
authenticates one DRAGSTER state. M4-03's sampler agreement is therefore
necessary but not sufficient for autonomous movement. Pack expansion remains a
later separate profile/compatibility decision after an accepted static
inventory and semantic start exist.

The requested planning reference `R-0010-native-riding-routine.md` did not
exist; the audit used `R-0010-native-movement.md` and `R-0011-motion.md`.

## Handoff

- Current base/head: claim begins from accepted M4-03 `31e2dae`.
- Verified input facts: accepted ZOOM ZOO replay sample/final identities are
  `791a7863...` / `2a843314...`; M4-03 observes width/stride 256/512 and exact
  bounded content reads, but does not close response/state recurrence.
- First action: reproduce M4-03, inspect the ignored existing captures, then
  predeclare and repeat the end-1649 state observation before adding fields.
- Runtime needs: supported private PAL ROM, pinned core, ignored artifact space;
  no network, publication, deployment or spending.
- Checkpoint after state mapping, field freeze, blocker inventory and at least
  every ten minutes. Only the coordinator marks accepted after review.

### Worker checkpoint 1 — baseline and predeclared field surface

- Actual isolated starting head is claim commit `22d951c` (whose accepted
  dependency parent is `31e2dae`); branch and worktree match the assignment.
- ROM inspection passes all 10 identity fields. A fresh pinned-core build
  passes lock/checkout/patch/export checks. Two fresh accepted-primary runs
  retain sample `791a7863...`, final state `2a843314...` and exact A/V; the
  M4-03 ROM-derived contract reconstructs 50,665 decoded bytes, 24 entries,
  406 transfers, 98 gather bytes and 40 collision words, and its 10 ROM-free
  mutation tests pass.
- Two additive manifests now predeclare end-frame fields without changing
  either accepted controller schedule or whole-state expectation. The field
  surface includes both riders' position, velocity/residue, pose/reflection,
  contact and progress state; input, throttle, timer, reward/AI continuation,
  `$0D4F`, and finish guards. Initial executions retained the accepted whole-
  state/A/V identities; final executions after the finish-guard addition are
  running under `artifacts/m4-04/{primary,release}-final/`.
- The dedicated reference-only freezer/capture verifier and seven initial
  ROM-free mutation checks are authored. Two direct preliminary end-1649
  observations agree at WRAM SHA-256 `9c80a52a...d805e` and cartridge-RAM
  SHA-256 `cdd747a3...867`; they will be repeated against the final manifest
  identity before freezing.
- Existing M4-03 observations already expose a candidate first continuation
  blocker: native `update_movement` passes DRAGSTER width 1024 at the sampler,
  while observed ZOOM ZOO uses width 256/stride 512 and `$0D4F=0x3FFF`.
  The first post-boundary update and producer/consumer PC still require the
  declared narrow 1649--1651 ordered capture before this is a final claim.

### Worker candidate handoff — reference freeze and first blocker

- Scope stayed reference-only. Added two field-bearing replay manifests, two
  compact native-reference projections, the dedicated `freeze_zoom_zoo`
  capture/freezer/verifier, the six-window capture wrapper, ten ROM-free
  mutation tests and R-0022. Existing replay manifests, native update and
  serialization, Classic pack/profile/start, frontend, `docs/STATE.md` and
  `tasks/README.md` are unchanged.
- Both field-bearing cases ran twice in distinct fresh processes and passed
  24/24 checks. Primary retained accepted sample/final
  `791a786370913136e21ed34c841e9a3d892852bf6804e2e0aef21e1df6557b68` /
  `2a843314d19e60d13dab746541d43ffa9ad84adfe6f95b9cc6d3c45bd9a16aeb`
  and A/V `dc7ca8ff6db1c2bbef17efd639c74f0d238a93f920e3b8f72b1c5ad80b1be22a`.
  Release retained `c253de4bce372be26b0457fa870b72c7a1acddb953a3ecf9ee187006ad55d63e` /
  `00bf36ed74988b785f96c9e21116b2920915d85a81896b275255961d0330127d`
  and A/V `5f5388daccf78c191cb5875475ffbcfb2b06db8c829665f9260da4c83f3b012e`.
  Reports hash to `e67b89bed64a640c8f4375d9d7693b5b741088b745d580cc95ed888ccffb6d20`
  and `2cb37d334b6a6ce73644f1416f94e81f06ea5f7b68e42d4d98b483006db9a1a2`.
- Direct end-1649 repeats agree at WRAM
  `9c80a52a47706929c2a4a08a012799eb1163c3a6ccfd985e9be3cacae78d805e`,
  cartridge RAM
  `cdd747a31846cfa03de238c622ea3154b396ef9d9d4541e2d6854b15866b7867`
  and reward queue
  `66687aadf862bd776c8fc18b8e9f8e20089714856ee233b3902a591d0d5f2925`.
  Their distinct-process reports hash to `bd926492...d20f` and
  `4b963547...7e67`; the projection carries the explicit 124-field inventory
  and actual SRAM-dependent learned total rather than an assumed zero.
- Tracked projections cover exactly frames 1649--3299. Their files hash to
  `130e4631a7785b3839a3ba9a8b3d59d15a0773711450fdc769632c57a3d92766`
  and `90adf082f2410251dff4ec50431dfe557a2552fb94d0f6b9737a0d0285fba247`;
  their canonical rows hash to `84bed2b1...18aae` and `5558632a...40949`.
  Pair verification proves exact fields through 2499, then at frame 2500
  observes the input plus x/residue/vx/vy/throttle response. Neither case has
  a finish/result transition through 3299.
- All six three-frame access captures are complete/nontruncated with no ring
  overflow, unresolved store or PC outside ROM. Ordered watched accesses at
  frame 1650 establish input -> motion -> pose -> contact publication. Their
  access hashes are `1243dbe5...a269`, `00c6d5d1...1b94`,
  `270089cb...2eb`, `5a518f37...305a`, `115c7009...e8a6` and
  `69910902...0b3`.
- The first observed native blocker is update 1650's track sample: original
  call snapshots use width 256/stride 512 and preserve wrap mask `$0D4F =
  0x3FFF`, while native supplies width 1024 and has no ZOOM ZOO content. The
  next observed prerequisite is point-zero descriptor `0x5800`, whose
  directional bit is outside the current flat-contact guard. R-0022 records
  the exact PCs/sequence evidence and does not assign unrecovered semantics.
- Focused ROM-free tests pass 10/10. `lab-debug` and `lab-sanitize` synthetic
  runs pass 335/335 each, including 312 Python checks, native component tests
  and three-process deterministic hashes; reports are
  `4b6d5e0bbb9bdab92054718fe4944dc166e12519c14dbe9a987d3bd66fa77327`
  and `f38af770e78291a83cbf62cb81f5b58aad2d071503ba7bdd40bccf661c94de72`.
  The unchanged 25-entry Classic pack passes 3/3
  (`c85b57fe...0bd`), and DRAGSTER full-race/restores at
  1600/3213/3453/3678 pass 18/18 (`ad7c8214...5f39`).
- Retained failures/missing prerequisites: the first baseline replay attempt
  correctly exited 2 because the pinned reference core had not yet been built;
  the first dirty-source synthetic attempt ran all 312 Python checks but
  correctly exited 2 because the isolated native toolchain/build was absent.
  Both prerequisites were then built. A later exploratory
  `python3 tools/project.py pack --help` used a nonexistent top-level command
  and exited 2; the implemented `content pack-inspect` command passed. None is
  reported as a pass. After tightening the core/A/V mutation gate, one focused
  rerun had one assertion-message mismatch (`core options` versus the expanded
  `core binary, options` diagnostic); the expectation was corrected and the
  complete focused suite then passed.
- Reviewer start: fresh sequential OpenAI Sol/medium checkout at the candidate
  commit. Verify both projections and their pair, run the ten ROM-free tests,
  independently shift one withheld one-frame Up entry or release boundary,
  and confirm exact pre-boundary agreement plus the observed response or lack
  of response. Then run both field-bearing replays twice, both synthetic
  presets, Classic pack inspection and the four-boundary DRAGSTER restore.
  Review sets approved/changes-requested only; the coordinator owns acceptance.
- Model/usage: OpenAI Sol/medium throughout. The user waived percentage
  guardrails. No provider block occurred, so no reset was requested or
  redeemed; no money was spent and no provider switch occurred.

### Clean-source candidate checkpoint

- Exact implementation candidate: `7de0cd1018b04d944b40412454f5489e9aa09c6b`.
  Every JSON report below records this clean commit at start and finish with
  `source_changed_during_run: false`.
- `python3 -m unittest tests.tooling.test_zoom_zoo_freeze
  tests.tooling.test_zoom_zoo_windows -v` passes 10/10. Both standalone
  projection verifies pass, and `verify-pair` reports first divergence 2500,
  the seven expected input/motion fields and `finish_transition: false`.
- `python3 tools/project.py replay compare --manifest
  tests/manifests/replay/race-crawler-zoom-zoo-3300-riding-fields.json --runs 2
  --artifacts artifacts/m4-04/clean-primary/runs --report
  artifacts/m4-04/clean-primary/report.json --task M4-04` passes 24/24; report
  SHA-256 `283dd92b2fa5df48fa8670d582d8a96918bdd32f5d4f6373cfacd9fabc4a4258`.
- The same command for
  `race-crawler-zoom-zoo-3300-release-2500-2599-riding-fields.json` using
  `clean-release` passes 24/24; report SHA-256
  `cfff15b8802320d8080b1950496cfb294f48b240ecde7aafd3f74807f87654f5`.
- `python3 tools/project.py test --suite synthetic --preset lab-debug
  --artifacts artifacts/m4-04/clean-debug/artifacts --report
  artifacts/m4-04/clean-debug/report.json --task M4-04` and its
  `lab-sanitize` / `clean-sanitize` counterpart each pass 335/335. Reports are
  `bba85377ba8b06ab734b690ac873833313e207f4bbbf9e359f0bf1a05c5ed3dc`
  and `da0928bddee0d601b9962fd12114bf2114aa78188e363ec8d88d07397334dd85`.
- `python3 tools/project.py content pack-inspect --pack
  '/Users/markfeaver/Projects/Unirally Decompilation/local/classic-crawler-dragster.pack'
  --report artifacts/m4-04/clean-pack-report.json --task M4-04` passes 3/3;
  report SHA-256
  `815ca17d97d4396a24c5768a8cca91e50b6bfd871af1209bc2d653c30dfa462e`.
- `python3 tools/project.py native finish-check --manifest
  tests/manifests/native/full-race-continuous.case.json --content-pack
  '/Users/markfeaver/Projects/Unirally Decompilation/local/classic-crawler-dragster.pack'
  --save-frame 1600 --save-frame 3213 --save-frame 3453 --save-frame 3678
  --preset lab-debug --artifacts artifacts/m4-04/clean-dragster-native --report
  artifacts/m4-04/clean-dragster-native/report.json --task M4-04` passes 18/18;
  report SHA-256
  `b6e3e97752ceb2563e4b29582983a5629b42c0be8045ae002d4ec5e58dffdb88`.
- `git diff --check` passes, and the candidate worktree was clean before this
  handoff-only task-record update. ROMs, memory dumps, access logs, generated
  samples and reports remain ignored. Status remains review, not accepted.

### Review correction checkpoint — complete seed binding

- Independent review record `tasks/M4-04-review.md` is preserved in this
  branch. Its changes-requested finding was reproduced before correction:
  standalone command verification exited 0 after changing `seed.values.player_x`
  from 9200 to 65535, the queue digest to 64 zeroes, or `manifest_identity` to
  64 zeroes. The reviewed pair escape likewise relied on synchronously changing
  both projection copies.
- The verifier now canonicalizes the complete seed object with sorted JSON keys
  and compact separators and exact-binds SHA-256
  `d16d7576d556ac09adf842a890d68498db858c3f1c771fcfa499d181a398bc45`.
  This covers all 124 claimed values and inventory records, the WRAM/SRAM and
  queue hashes, ROM/core/replay/script provenance and sample identity. Existing
  projection rows and projection files are unchanged.
- The added command-boundary mutation test changes a seed value, queue hash and
  provenance identity independently in both documents. Both standalone
  `verify` and synchronous `verify-pair` must exit nonzero with the precise
  complete-seed diagnostic for all three mutations.
- Scope remains the reference verifier, focused test and documentation only.
  No gameplay behavior, native code, serialization, content pack/profile,
  frontend or coordinator-owned status file changed. Status remains review,
  pending a fresh independent correction review.

### Clean-source review-correction handoff

- Exact correction candidate: `970dcb83956750cb93d5427d6b912bc8bad0675b`;
  review record commit is preserved immediately before it as `c5ee5fd`.
- `python3 -m unittest tests.tooling.test_zoom_zoo_freeze
  tests.tooling.test_zoom_zoo_windows -v` passes 11/11. This includes six
  command-layer rejection checks: standalone and synchronous pair verification
  for each seed-value, queue-hash and provenance-identity mutation. Both
  unchanged tracked projections verify, and their pair still reports only the
  expected frame-2500 seven-field response with no finish transition.
- `python3 tools/project.py test --suite synthetic --preset lab-debug
  --artifacts artifacts/m4-04-correction/clean-debug/artifacts --report
  artifacts/m4-04-correction/clean-debug/report.json --task M4-04` passes
  336/336 at the clean correction commit; report SHA-256
  `2df8dac007d1ebec1a33f8a585ea75867091a0358c63fb1b38dc9f81485f0ae4`.
- The same command with `lab-sanitize` and `clean-sanitize` passes 336/336;
  report SHA-256
  `f0348c74f8f22e2ae87c64692e32353edaeae344d7b3e83215f753a19f98ca7b`.
  Both reports bind clean source `970dcb8` at start and finish with
  `source_changed_during_run: false`.
- The pre-fix focused regression intentionally failed three subtests because
  the public command returned 0 for each contradictory standalone seed. This
  is the requested reproduction, not a candidate pass. After the correction,
  all focused and affected gates above pass. No new provider failure, reset,
  spending or provider switch occurred. Status remains review, not accepted.

## Review and integration

- [Independent review](M4-04-review.md) reproduced both field replays,
  projections, seed captures, regressions and the observed native blocker. Its
  withheld one-frame Up-release extension first diverged at frame 2260 only in
  `$0313`/`$0315`, then complete WRAM and all declared fields reconverged at
  2261 with identical final state and A/V. Review returned one integrity defect:
  synchronized seed-value/queue/provenance edits could pass verification.
- Correction `970dcb8` exact-binds the complete canonical 124-field seed object
  at SHA-256 `d16d7576...bc45` and adds standalone/paired mutations.
  [Focused re-review](M4-04-rereview.md) independently rejected those three
  mutations plus an extra value and missing inventory row, then approved
  corrected candidate `ac2bee7`.
- Reviewed head `1f34bde` merged to `main` as
  `4f7b248d735cb75d84e37f31604ccbe19ae77ae9`. On that exact merge, focused
  tests pass 11/11; both projections and their pair pass with first divergence
  2500 in the seven expected input/motion fields and no finish; app-debug passes
  336/336; both field-bearing replays pass 24/24 with exact A/V; Classic pack
  inspection passes 3/3; and DRAGSTER full-race/restores pass 18/18. Report
  SHA-256 values are `0c4f7488...9d64`, `d8b68e72...3a79`,
  `a993717c...3e72`, `7831f6a9...f16b` and `6ded54fd...709`.
- Private `origin/main` was synchronized to the merge candidate. Hosted run
  `34730516561` passed macOS 15 and Ubuntu 24.04, including Linux SDL
  sanitizers. The acceptance update is pushed separately and verified.
- Accepted scope is a reference-only 1650--3299 riding projection and repeated
  end-1649 seed for the primary/release schedules. It identifies width/stride
  256/512, wrap mask `0x3FFF` and the first directional descriptor blocker.
  Native ZOOM ZOO, descriptor semantics/response, pack expansion, finish,
  presentation, AI generalization and other modes remain unsupported.
