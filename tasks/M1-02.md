# M1-02 — Validated player-state schema and update timing

## Assignment

- Status: accepted (integrated 11 September 2026 after one independent review; claimed by the coordinator 18:50 AEST with the registry row updated in the same commit, M0-05 gap G1; submitted by the worker at 22:09 AEST after one usage-limit interruption)
- Milestone: M1
- Coordinator: primary agent (Claude Code, Fable 5.1)
- Worker/session/runtime/model: fresh session of Claude Fable 5.1, run as a subagent of the coordinator's session and dispatched with the portable prompt in `docs/AGENT_WORKFLOW.md` and nothing else
- Reviewer: fresh independent session, own clone (dispatched by the coordinator after submission)
- Dependencies and evidence of acceptance: M1-01 accepted ([R-0006](../docs/research/R-0006-observed-code-map.md); the race scenario `race-crawler-dragster-3000`, its map and the `compared_to` hand-over); the replay comparator and manifest schema 1 ([R-0003](../docs/research/R-0003-replay-comparator.md)); the capture-method decision [D-0002](../docs/decisions/D-0002-data-access-observation.md)
- Base commit: the claim commit on `task/M1-02-player-state` (its parent is `0801782`, `main` after the M1-01 integration)
- Branch and isolated worktree: `task/M1-02-player-state` in the linked worktree `.worktrees/m1-02` (ignored path; the coordinator's checkout stays on `main`). Fixture arrangement: the coordinator placed `local/rom-location.txt` in the worktree and copied the ignored `local/toolchain`, `local/emulators` and `local/cache` directories from the main checkout so that bootstrap and the core build read from the cache instead of the network; nothing else was copied. The coordinator pushes the candidate branch to origin for CI
- Owned paths and shared interfaces: new package `tools/unirally_lab/access/`; new tests `tests/tooling/test_access.py`; `tools/project.py` only to register the new `access` command group; `tools/unirally_lab/reference/worker.py`, `bsnes.py` and `tools/unirally_lab/coverage/drain.py` only for **additive** options that leave every existing output, exit code and digest unchanged; new replay manifests `tests/manifests/replay/race-crawler-dragster-3000-<variant>.json` and matching reference scripts (the existing `race-crawler-dragster-3000.json` and `-right-removed.json` are frozen); new `tests/manifests/fields/`; new `docs/state/`; `docs/research/R-0007-*` (the evidence record); `docs/BUILD_AND_VALIDATION.md` command-table rows for the new commands; `docs/map/*` only to regenerate the two tracked maps if the library-digest note below is done; this record. Must not change: the pinned core or its tracked patch (D-0002), `tools/locks/`, any existing manifest, the samples/report/coverage/map schemas except additively, `src/lab`, `docs/STATE.md`, the registry or `tasks/M1-03.md` (coordinator-owned)
- Claim/lease/heartbeat/checkpoint location: this file and the registry row; checkpoint commits at least every 10 minutes and before every long capture; part A is committed on its own before part B starts, because M1-03 is dispatched from that commit
- Session time limit, concurrency allocation and actual spend authorization if relevant: about 3 h of worker time for a first candidate (part A about 1 h); one worker; no service purchases. If part B is not complete within the budget, submit part A plus the completed part-B findings for review with the remaining state recorded, rather than an unvalidated schema

## Outcome and boundaries

Deliver a validated first schema of the player state in the race scenario and the timing at which it updates, with the tool that shows which instruction reads or writes which address and what it stores.

**Part A — trace-derived access record (D-0002).** The pinned core and patch stay unchanged. During M1-01's per-frame drain of the unchanged trace ring, decode each executed instruction's opcode and operand bytes from the ROM (for pcs in ROM) and, from the registers the same entry carries (A, X, Y, S, D, DBR, P, E), compute:

1. The effective 24-bit address of every direct-page, absolute, long, indexed, stack-relative, push/pull and block-move access, classified as read, write, read-modify-write, stack or block move, with the width (8/16 bit from M/X, or 16/24 for pushes of PEA/PEI/PER/PHD/PHB/PHK) and, for stores and pushes, the exact value stored (from the register in the entry). For block moves (MVN/MVP), per re-execution the source `X` and destination `DBR:Y`; the source bank comes from the operand bytes, which for the work RAM routine at `$00:0199` are read from the end-of-frame work RAM and labelled as such. Loads may take their loaded value from the next entry's register (optional).
2. The residual, counted per (pc, addressing mode) and never guessed: indirect modes (`(dp)`, `[dp]`, `(dp,X)`, `(dp),Y`, `[dp],Y`, `(abs)`, `(abs,X)`, `(sr),Y`) unless resolved from the end-of-frame work RAM when the record shows the pointer bytes were not written in that frame (then labelled `end_of_frame_resolution`); accesses by code executing from work RAM; DMA engine transfers (record the DMA parameter stores to `$43x0`–`$43x6`, `$420B` and `$420C` with their values instead).
3. Aggregation per (pc, mode, effective address, kind): count, first frame, last frame; per address the set of writer and reader pcs; a compact per-frame value log for a list of watched addresses and a register log at watched pcs (frame, A, X, Y, S, D, DBR, P, E per execution). An optional raw work RAM series (a declared range every N frames, written as a binary file plus an index) for differencing between runs.
4. Commands (intended interface; the worker may adjust names and records the final ones here and in `docs/BUILD_AND_VALIDATION.md`): `python3 tools/project.py access capture --manifest <replay manifest> --out <dir> [--from-frame N] [--to-frame N] [--watch-address A ...] [--watch-pc P ...] [--wram-series-range START LENGTH] [--wram-series-every N] --report <json>` and `python3 tools/project.py access query --access <dir>/access.json (--address A | --pc P) [--kind write|read|...]` listing the matching records. Exit codes 0/1/2/3/4 as in the repository convention; reports use schema 1 with `--task M1-02`. The capture checks, like `coverage capture`, that the run's sample digest and final state equal the manifest's.
5. ROM-free tests: the addressing-mode and access-kind classification of all 256 opcodes with the effective address checked against hand-derived expectations for every mode (including the direct-page page wrap in emulation mode when `DL` is zero, the 16-bit `D` offset with its bank-0 wrap, the bank crossing of absolute indexed and absolute-long indexed against the bank-0 wrap of direct-page indexed, stack pushes decrementing `S`, MVN/MVP direction), store widths under M/X, the residual counting, the streaming derivation over a stub ring on a synthetic ROM image (as `test_coverage.py` does), the work RAM series writer and the CLI exit codes 2 and 3.
6. Open notes carried from M1-01 review 1 (last in part A; leave for the handoff if the budget is short): record the core `library_sha256` outside the coverage-digest input so that a regenerated map on another build is byte-identical (regenerate both tracked maps and show they differ only in the expected lines); add the two synthetic cases (an unknown edge from a ROM site; the per-frame vector summary); make `nmi_vector_once_per_frame` require the whole tail from `once_per_frame_from`, not just some frame.

**Part B — the player state and its timing.** Every claim below is tied to a controlled-input experiment whose prediction is written into this record *before* the run (attempts table), and to the writer instruction(s) found in the access record.

1. Candidate discovery by differencing. From the validated race scenario, variants that keep frames 0–1499 identical and change only the riding input: the existing `-right-removed`; Right released at frame 2000 (the reviewer's withheld case in M1-01); Left instead of Right; Down instead of Up; and each of B, Y, A, X, L, R held from frame 1500 together with Right (trick or jump candidates; the M1-01 exploration found B, Y, A, X and Up change work RAM from the first held frame). Each variant is a replay manifest validated by two identical fresh runs (`replay compare` exit 0, digests written into the manifest) and compared against the baseline with `--against`. Work RAM series over the ranges that the race map's 968 static work RAM references cluster in (pages `$00:02`–`$00:16` and `$7E:20`, plus whatever the divergences point to) give the per-frame time series of every differing byte.
2. Interpretations to validate, each with a pre-registered prediction and exact integer arithmetic over at least 500 riding frames: **position along the track** (monotonic while riding right; its per-frame increment equals the speed field in the fixed-point split found; reversed or slowed by Left), **speed** (grows while Right is held, decays after release at 2000, with the increment/decrement constants read from the writer's arithmetic), **race timer** (increments on a fixed frame cadence that matches the on-screen tenths in `--frame-image` dumps; this is the PAL-cadence evidence, do not assume 50 or 60 updates per second), **lean or tilt** (Up changes it and it returns after release), and **one trick or jump state** if a button produces one (a candidate that produces no visible or work RAM effect beyond the joypad image is recorded as unknown, which is acceptable). The **joypad image** bytes (`$7E0313` and neighbours) are identified as such, with the instruction that reads `$4218`/`$4219` and stores them. Since the race is MIKE vs BRONSEN, check whether the opponent's state has the same layout at a fixed stride (the opponent moves in the `-right-removed` run while the player does not); a second instance with the same writers is strong support for the layout.
3. Writers and timing. For each validated field: the writer pc(s) with mode and kind, the per-frame execution count of the primary writer over the riding phase (from the per-frame register log or the coverage watch), and the order within one frame of the joypad read, the field updates and the sample point (end of `retro_run`, R-0003). State whether one game update is one PAL frame in this scenario, how the 1208–1328 NMI-off loading phase and the first riding frames behave, and define the **capture phase** for M2: which frame's inputs the sample at the end of frame N reflects (M0-04 showed `$7E0073` following Start and M1-01 `$7E0313` following Right in the same frame).
4. Deliverables: `docs/state/player-state.md` (schema v1: per field the address, width, signedness, units or fixed-point split, instances, writer pcs, the validating experiment, confidence and open questions; a section of what is unknown); `tests/manifests/fields/player-state-v1.json` declaring the validated fields as named work RAM ranges in the replay manifest `fields` format; a replay manifest with the race scenario's inputs and those fields declared (`race-crawler-dragster-3000-fields.json` or similar), validated by two identical fresh runs, whose `--against` comparison with the Right-removed variant names the field that diverges first; the evidence record R-0007; the ROM-free tests; this record's handoff.

Not in scope: decoding any track, tile or sprite (M1-03), the meaning of opponent AI beyond identifying its state instance, other tracks, tours, riders or modes, a native simulation or any constant tuning, a core patch change (D-0002 fallback is a separate task), changes to the frozen manifests, the samples/coverage/map schemas or `docs/STATE.md`.

Tracked-content rule: tracked files carry addresses, widths, counts, frames, register values at stores to hardware registers and field descriptions; no ROM bytes, opcode bytes, disassembly listings or work RAM dumps (those stay in ignored `artifacts/`). Values of work RAM bytes may be quoted in R-0007 and `docs/state/` where they support a claim (a timer series, a position series).

## Inputs and prerequisites

- ROM as in M0-03 (`local/rom-location.txt`, manifest `tests/manifests/rom/unirally-pal.json`, SHA-256 `a1105819…`); built pinned core (`reference build`, commit `7d5aa1e656b9`, patch `a719f5ffe222…`, unchanged); replay manifests in `tests/manifests/replay/`; the race map `docs/map/race-crawler-dragster-3000.map.json` (static references, `compared_to`), regenerable per-address detail via `coverage map --detail`.
- Runtime needs: the cached wheels and core checkout copied into the worktree (network only if the cache misses); about 15 s of core build if not cached; the ROM path arrangement above; a race capture with the access derivation is expected to take a few minutes (M1-01's drain alone: 25 s; restrict with `--from-frame` where the whole run is not needed); a work RAM series of 16 KiB × 1500 frames is 24 MiB per run; keep artifacts under ignored `artifacts/m1-02/`.
- Baseline to reproduce first: `doctor`, `bootstrap`, `build --preset lab-debug`, `test --suite synthetic` (148 checks), `reference build`, `replay compare --manifest tests/manifests/replay/race-crawler-dragster-3000.json` (exit 0, sample digest `72f618f2f7e3416e…`, final state `0f3cc38bd5d64401…`), `replay compare --manifest … --against tests/manifests/replay/race-crawler-dragster-3000-right-removed.json` (exit 1, first divergence at frame 1500, one byte `$7E0313`).
- Known baseline failures: none. Known limits carried forward: reference execution on one macOS host; CI is ROM-free, so the new tests must exercise the decoder and the derivation with a synthetic ROM and stub ring; tracked maps embed the core `library_sha256` (M1-01 review note, part A item 6); an orphaned worker survives a parent kill (R-0004 G2), so check for live `reference/worker.py` processes before re-running into the same artifacts directory.
- Reference material for the 65816 addressing modes: write the table for this project from the WDC instruction set description, as M1-01 did for lengths (`tools/unirally_lab/coverage/opcodes.py`); do not copy tables from other emulators' sources without recording provenance and licence.

## Acceptance

| Criterion | Command or experiment | Expected result | Required artifact |
| --- | --- | --- | --- |
| Existing behaviour unchanged | `test --suite synthetic`; `replay compare` on `boot-start-600` and on `race-crawler-dragster-3000`; `coverage capture` on `boot-start-600` | All existing checks pass; sample digests `583d1ec544ec61a2…` and `72f618f2f7e3416e…` unchanged; coverage digest unchanged unless part A item 6 changes the identity, in which case the two tracked maps are regenerated and the diff is exactly the explained lines | Reports |
| Access derivation is correct on synthetic input | ROM-free tests on a synthetic ROM image and stub ring | Every addressing mode's effective address, kind, width and stored value match hand-derived expectations; the residual is counted per (pc, mode) | `tests/tooling/test_access.py` |
| Access derivation agrees with the real run | `access capture` on the race manifest; spot checks | Sample digest and final state equal the manifest's; the writer of `$7E0313` is a store whose value follows the Right bit and whose source is a read of `$4218`/`$4219`; three further addresses from the race map's static references show the same writers the map's referencing instructions imply; the derivation's instruction total equals the coverage total | R-0007, report |
| Capture is repeatable | Two fresh-process `access capture` runs of one manifest | Byte-identical access files (same digest) | Two report digests |
| Fields validated with controlled inputs | The variant manifests, each `replay compare` exit 0 with digests recorded, and the `--against` comparisons | Position, speed and timer validated: each has a pre-registered prediction recorded before the run, an exact integer relation that holds over at least 500 riding frames, at least one writer pc; lean and trick/jump are validated the same way or recorded as unknown with the experiments that failed to find them; the joypad image is identified with its reader of `$4218`/`$4219` | `docs/state/player-state.md`, R-0007, manifests |
| Timing and capture phase defined | Per-frame writer counts and the in-frame order from the register log | The update cadence per PAL frame is stated from counts, not assumed; the order joypad read → update → sample point is shown from the trace for one riding frame; the timer's frame cadence matches the on-screen tenths in frame images at two chosen frames | R-0007 |
| Field manifest works with the comparator | `replay compare --manifest <fields manifest>`; `--against` the Right-removed variant | Exit 0 with digests recorded; exit 1 whose first divergence names a declared field | Manifest, two reports |
| ROM-free tests and CI | `test --suite synthetic` locally and on both CI platforms | Suite grows by the access tests (and the two M1-01 synthetic cases if item 6 is done); green on `ubuntu-24.04` and `macos-15` | Tests, CI run |
| Independent review of the exact candidate | Reviewer reproduces on the candidate commit from its own clone with a withheld input variant of its own; CI on that commit | Approve, or specific reproducible findings addressed | Record |

## Evidence and attempts

| Attempt | Hypothesis | Experiment | Observation | Next decision |
| --- | --- | --- | --- | --- |
| 1 | Baseline reproduces from the records (worker session start 18:53 AEST, 11 September 2026) | `doctor`, `bootstrap`, `build --preset lab-debug`, `test --suite synthetic`, `reference build`, `replay compare` on the race manifest, `replay compare --against` the Right-removed variant | All as recorded: 148 checks; core build 9.2 s from the copied checkout; race sample digest `72f618f2f7e3416e…`, final state `0f3cc38bd5d64401…`, exit 0; perturbation exit 1, first divergence at frame 1500, one byte `$7E0313` (`0x01`/`0x00`); reports under `artifacts/m1-02/baseline/` | Proceed with part A |
| 2 | Part A: the effective address, kind, width and stored value of every instruction can be derived from the ring entry's registers and the ROM bytes without a core change | `tools/unirally_lab/access/` (decoder `modes.py`, streaming derivation `derive.py`, commands), additive worker options; first capture of the race manifest over frames 1500–1520 watching `$7E:0313` | Sample digest and final state unchanged under capture; 378,961 instructions and 188,953 accesses in 21 frames (about 12 µs per instruction of derivation); the only writer of `$7E0313` is `$80:87F2` (STA abs, 8-bit, value `0x01` in every riding frame), its only reader `$82:AADF`; `$4218` is read at `$80:87E9` and `$4219` at `$80:87EF`, nine bytes before the store (Right is bit 0 of `$4219`, the stored value); 538 unresolved accesses (0.3 %), all `[dp]` at `$83:F27E`, `$83:F286`, `$83:F24D`; 0 unresolved stores; 28 DMA triggers per frame with channel parameters shadowed | Part A works on the real run; write the ROM-free tests, then part B |
| 3 | Pre-registered (before any variant run): `right-released-2000` is identical to the baseline through frame 1999 and diverges first at 2000 in `$7E0313` (`0x01` → `0x00`); a speed field then decays over the following frames while a position field keeps advancing more slowly; final state differs | `replay compare` (two fresh runs, exit 0), then `--against` the baseline | Two fresh runs identical (exit 0; sample digest `a99d5fcbcd223ed1…`, final state `325aba5494d2478d…`, 15.1 s). Against the baseline: exit 1, first divergence at frame 2000, six bytes `$0313` (`0x01`→`0x00`), `$0319` (2→1), `$0401`, `$04BB` (223→222), `$0BEB`/`$0BEC` (432→0); final states differ. Series: `speed` (`$04BB`) then falls by exactly 1 per frame for 100 frames while `pos_x` keeps advancing 14–15 per frame; `throttle` is 0 from 2000 | Prediction held (speed decays, position continues); the throttle drop is immediate |
| 4 | Pre-registered: `left-instead` diverges at 1500 with `$7E0313` = `0x02` (Left is bit 1 of `$4219`) instead of `0x01`; the position field advances less than in the baseline or in the opposite direction | as above | Exit 0 (`b1b8717932a5d01a…`); against the baseline exit 1 at frame 1500 in `$0313` = `0x02` (registers also differ); `throttle` ramps −16 per frame to −528; `pos_x` 1088 → 990 at 1540 and only 1133 at 2999 | Prediction held; Left moves the rider backwards then barely forwards |
| 5 | Pre-registered: `down-instead-of-up` is identical through 2199 and diverges at 2200 with `$7E0313` = `0x05` (Right, Down) instead of `0x09` (Right, Up); a lean field takes the opposite sign of the baseline's during 2200–2259 and returns afterwards | as above | Exit 0 (`8340e6a9c09d7d8a…`, final state identical to the baseline's); against the baseline exit 1 at 2200 in `$0313` (`0x05` vs `0x09`) and `$0315` (`0x02` vs `0x00`), both back at 2260; whole-WRAM dumps at the end of frame 2230 differ in exactly those two bytes | Prediction on `$0313` held; no lean state exists: `$0315` is the derived vertical axis (0 up, 1 neutral, 2 down); lean recorded as unknown |
| 6 | Pre-registered: `b-held` and `y-held` diverge at 1500 with `$7E0313` = `0x81` / `0x41` (bits 7/6 of `$4219`) | as above | Exit 0 for both (`11074c77741db2b5…`, `c9a0aa46f5c0d3a2…`); against the baseline exit 1 at 1500: `b-held` one byte `$0313` = `0x81`; `y-held` 39 bytes including `$0313` = `0x41` | Prediction held; B and Y investigated in attempt 8 |
| 7 | Pre-registered: `a-held`, `x-held`, `l-held`, `r-held` diverge at 1500 in the byte that images `$4218` (predicted `$7E0312`, read at `$80:87E9`) with values `0x80` / `0x40` / `0x20` / `0x10` | as above | Exit 0 for all four; against the baseline exit 1 at 1500 in `$0311` = `0x80`/`0x40`/`0x20`/`0x10` (A/X/L/R); L and R also set `$0329`/`$032D` = 1 | Values as predicted, address off by one: the `$4218` image is `$7E0311`, written by `$80:87EC` |
| 8 | Pre-registered: at least one of B, Y, A, X, L, R produces a work RAM change beyond the joypad image while riding (a jump or trick state) and a visible change in the frame image at 1600; a button that changes nothing but the joypad image is recorded as unknown | the `--against` divergence bytes and frame images at 1520 and 1600 | B: from frame 1534 (the GO) `$0331` = 1 and `$033D` (u16) rises 1079 → 1111 → 1145 while `pos_x` equals the baseline's in every frame; frame image at 1600 unchanged in position. Y: the rider never leaves the start (`pos_x` 1088 and `speed` 0 for the whole run while `throttle` saturates at 432 from 1500; the frame image at 1600 shows the rider on the start pad). L, R: nothing beyond `$0311` and one flag byte through frame 1599 | B is a jump-like state (`jump_height`, meaning partly interpreted); Y is recorded as unknown; no trick state beyond these |
| 9 | Pre-registered from the work RAM series, before the derivation run (attempts 3–8 observed, see below): **position** is the 16-bit word `$7E0415` (player; `$7E0417` the opponent at stride 2; frozen at 1088 without Right, moving from frame 1534), and for every riding frame `pos[f+1] − pos[f]` equals the signed word `$7E0BBB` sampled at the end of frame f+1 (the displacement applied in that frame); the **speed magnitude** is the 8.8 word `$7E04BA` whose integer byte `$04BB` drops by exactly 1 per frame (256 in 8.8) after the release at 2000; the **throttle** `$7E0BEB` (signed) ramps by +16 per frame while Right is held, −16 with Left, and is 0 from the first frame after release; the **timer** counts 5 frames per tenth (`$0E29` frames, `$0E25` tenths, `$0E21` seconds, `$0E1D` tens of seconds) from frame 1535 and matches the on-screen `0:01:3` at 1600 and `0:17:3` at 2400; the **joypad image** is `$7E0311` (JOY1L, `$4218`) and `$7E0313` (JOY1H, `$4219`) with derived axis bytes `$0315` (0 up, 1 neutral, 2 down) and `$0319` (0 left, 1 neutral, 2 right). The access record over 1500–2100 will show one store per frame for each of these from a ROM pc, the joypad stores after the `$4218`/`$4219` reads and before the position update in the same frame, and two fresh captures will be byte-identical | `access capture --from-frame 1500 --to-frame 2100` with the addresses watched, twice | Two fresh captures over 1500–2100 byte-identical (`464049b37dcdb267…`, 23 s each; a first pair `9bfab961…`/`13f3dc4a…` differed only in the regeneration command's output path, corrected). Every primary writer stores exactly once per frame in 601/601 frames (`$80:87EC`, `$80:87F2`, `$82:AB36`, `$82:AB52`, `$82:8DB9`, `$82:92A7`, `$82:8DF8`, `$82:8DBF`, `$81:AE87`); `pos[f+1] − pos[f]` = the displacement stored in frame f+1 in 565/565 frames; the timer frames counter `$0E29` 1,2,3,4 then 0 with the tenth +1 (`$81:C6C9`/`$81:C6ED`/`$81:C6FF`), origin 1534; joypad stores at sequence 566–568 of frame 1600 after the `$4218`/`$4219` reads, physics stores at 2191–2211. The speed prediction was wrong in its address: `$04BA` has no resolved writer (touched only through `[dp]` pointers advanced by `INC`); the speed is the s16 word at `$04BB` written once per riding frame by `$82:8E9A` (1671 stores over 1329–2999), decaying by 1 per frame after release as predicted | Fields validated; schema written; the whole-run capture (attempt 10) for the exact instruction total |
| 10 | The whole run derives in acceptable time and its instruction total equals the coverage total | `access capture` over frames 0–2999 with the same watches | 88.5 s; 49,465,866 instructions = the samples' trace total = the M1-01 coverage total; 25,914,832 accesses, 255,898 distinct keys, 189,993 ROM bytes read in 690 ranges, 39,705 DMA triggers; 61,477 unresolved accesses (0.24 %, 0 stores) at `[dp]`/`[dp],Y` sites whose pointers are advanced by `INC dp`; record `bc799832ffa6666e…` | Residual recorded in R-0007 finding 3; no D-0002 fallback needed for the validated fields |
| 11 | The validated fields work with the comparator | `race-crawler-dragster-3000-fields.json` (eleven `wram_range` fields from `tests/manifests/fields/player-state-v1.json`): `replay compare`, then `--against` `race-crawler-dragster-3000-right-removed-fields.json` | Exit 0, digests `72f618f2f7e3416e…`/`0f3cc38bd5d64401…` (identical to the baseline's: the digest covers work RAM and registers, not the declarations); against the Right-removed variant exit 1, first divergence at frame 1500 in `joy1h_image` (`0x01` vs `0x00`), one byte | Part B complete |
| 12 | (interruption) | The worker session was cut by the provider's usage limit at about 21:5x AEST after attempt 9's captures and analysis; resumed 22:02 AEST from the coordinator's message | On resume the worktree was at `b146aae` with the variant digests and the regeneration-command fix uncommitted; no worker process was alive; the three derivation reports and their access files were on disk with matching digests, so nothing was re-run | Committed the pending work and finished part B |

Pre-register each part-B prediction as its own row before running the experiment. Link concise evidence records and full local artifacts; do not paste an entire transcript.

## Handoff

- Current base/head commit and uncommitted state: base `4f593d6` (the claim commit); commits in order `e9b2a4f` (part A code), `e80d573` (tests, series), `7b34c54` (variant manifests, predictions, command rows), `b146aae` (field predictions), `04382c0` (variant digests, canonical regeneration command); the candidate head is the commit that adds this handoff, `docs/state/player-state.md`, `tests/manifests/fields/player-state-v1.json`, the two fields manifests and R-0007; worktree clean after it.
- Verified findings: [R-0007](../docs/research/R-0007-player-state.md) findings 1–13 and the schema [docs/state/player-state.md](../docs/state/player-state.md). Final interface: `python3 tools/project.py access capture --manifest <replay manifest> --out <dir> [--from-frame N] [--to-frame N] [--watch-address A ...] [--watch-pc P ...] [--wram-series-range START LENGTH [--wram-series-every N]] [--frame-image N ...] [--ring N] [--rom P] [--timeout S] --report <json> --task M1-02` and `python3 tools/project.py access query --access <dir>/access.json (--address A | --pc P) [--kind read|write|rmw|push|pull|block_read|block_write] [--json] --report <json>`; recorded in `docs/BUILD_AND_VALIDATION.md`. Worker additions: `--access-out`, `--access-ring`, `--access-from-frame`, `--access-to-frame`, `--access-watch-address`, `--access-watch-pc`, `--wram-series-out`, `--wram-series-range`, `--wram-series-every` (the series covers every executed frame on its stride, independent of the derivation window). Existing behaviour unchanged: `boot-start-600` compare `583d1ec544ec61a2…` and coverage capture with the same sample digest (coverage digest `7ddf22432be6a2fa…` on this host's library `a9da0045…`; substituting the tracked map's `library_sha256` reproduces `1e6c02747d7c1003…` exactly); race compare `72f618f2f7e3416e…`.
- Current hypothesis and failed approaches: none open for the validated fields. Not done: part A item 6 (record the library digest outside the coverage-digest input, the two synthetic cases, `nmi_vector_once_per_frame` over the whole tail) — the access record already keeps the library digest out of its identity; the coverage side is left for the next tool task. Not done: the `--watch-pc` register log was implemented and tested but not used for the evidence (the per-access sequence numbers in the watch logs gave the in-frame order). The speed prediction's address was wrong (`$04BA` → `$04BB`, attempt 9); the `$4218` image prediction was off by one (`$0312` → `$0311`, attempt 7). Reference scripts for the variants were not written (the replay manifests derive them).
- Commands executed, outcomes and report hashes (reports under ignored `artifacts/m1-02/`, SHA-256 prefixes): `baseline/test` `88a239df0a984a1e…`; `baseline/race-compare` `a45b02c285d57372…`; `baseline/race-perturb` `9f9b71666a4a24ad…`; `unchanged/boot-compare` `a2a9f8f7d2743080…`; `unchanged/boot-coverage` `34fe9feb90e22e96…`; `probe1-report` `7f27c4e43d23b1a5…`; `derive3-report` `d7931cca42fff271…`; `derive4-report` `d645fcd452769edd…`; `derive-full-report` `0d4d1b83180d1d41…`; `fields/compare` `e2d87d28eaaf6fd6…`; `fields/against` `46f5246ab343e634…`; `test-parta` `ee3ef46b5c2bd112…`; `test-final` `754c7946bba917ab…`; variants: `right-released-2000` compare `a9d8b03debda1e34…` (exit 0) / against `8d3c7005c8405087…` (exit 1, frame 2000); `left-instead` compare `8ea98a622dd89b87…` (exit 0) / against `8afc93c4003507d4…` (exit 1, frame 1500); `down-instead-of-up` compare `8d06352154b16709…` (exit 0) / against `4b359f4bf1077bb2…` (exit 1, frame 2200); `b-held` compare `83cb19c0037c0e15…` (exit 0) / against `38fe45fb0be0711d…` (exit 1, frame 1500); `y-held` compare `65abe9d759226680…` (exit 0) / against `2efff7c913358b14…` (exit 1, frame 1500); `a-held` compare `094fa277cd92533a…` (exit 0) / against `3a72f6eab8503700…` (exit 1, frame 1500); `x-held` compare `319e7843f0f3e23a…` (exit 0) / against `e82e8844dce29049…` (exit 1, frame 1500); `l-held` compare `1da4303b64eb3928…` (exit 0) / against `712078a1fa69bce0…` (exit 1, frame 1500); `r-held` compare `a752c1c3a9d5d5bc…` (exit 0) / against `6e1a81c445b0fd97…` (exit 1, frame 1500); series captures `variants/<name>-series.json` (exit 0 each); access records: window `464049b37dcdb267…` (derive3 = derive4), whole run `bc799832ffa6666e…`; `lean/` whole-WRAM dumps at frame 2230 (worker run directly with `--stop-after-frame 2230 --wram-dump-out`).
- Unavailable/skipped checks: CI on both platforms for the candidate (the coordinator pushes); independent review with a withheld variant; a Linux run of the core; the `--watch-pc` log on the real run; the D-0002 fallback (not needed).
- Exact next experiment/command: reviewer, from a fresh clone at the candidate: `doctor && bootstrap && build --preset lab-debug && test --suite synthetic` (173 checks); `reference build`; `python3 tools/project.py access capture --manifest tests/manifests/replay/race-crawler-dragster-3000.json --out artifacts/access/race --from-frame 1500 --to-frame 2100 --watch-address 0x7E0311 --watch-address 0x7E0313 --watch-address 0x7E0315 --watch-address 0x7E0319 --watch-address 0x7E0415 --watch-address 0x7E0417 --watch-address 0x7E041D --watch-address 0x7E04BA --watch-address 0x7E0BBB --watch-address 0x7E0BEB --watch-address 0x7E0E25 --watch-address 0x7E0E29 --watch-address 0x7E0E21 --watch-address 0x7E033D --watch-address 0x7E0331 --watch-address 0x7E2026 --frame-image 1535 --frame-image 1600 --frame-image 2400` twice (access.json `464049b37dcdb267…` both times; `access query --address 0x7E0313` lists writer `$80:87F2` and reader `$82:AADF`); `replay compare --manifest tests/manifests/replay/race-crawler-dragster-3000-fields.json` (exit 0) and `--against tests/manifests/replay/race-crawler-dragster-3000-right-removed-fields.json` (exit 1, frame 1500, `joy1h_image`); one variant of the reviewer's own (for example Right released at another frame, or Y released before the GO) with a prediction from the schema written before the run.
- Remaining dependencies: none for review. For M2: the schema and `player-state-v1.json`; for M1-03: the whole-run record's ROM read ranges and DMA log (regenerate with the whole-run command, 90 s).
- Runtime needs (network, build time, fixtures, memory): none beyond M1-01 (cached wheels and core checkout were copied into the worktree; core build 9.2 s); the ROM via `local/rom-location.txt`; a windowed capture 23 s, the whole run 90 s and 22 MiB of JSON (well under 1 GiB of memory); each variant 15 s (compare) + 23 s (against) + 8 s (series, 26 MiB); the suite 28 s.
- Elapsed work and provider usage when known: session start 18:53:36 AEST (`date`), interrupted by the provider's usage limit at about 21:5x AEST (the coordinator's figure; the last command of the first session finished at about 19:37, after which the analysis and record writing were in progress), resumed 22:02:24 AEST, ended 22:09:37 AEST (`date` after the submission commit `967fb6e`); this amendment is the final commit. Wall clock of commands actually run: baseline 1 min, part A captures and tests about 10 min, variants 8 min, derivations 3 min, fields 1 min, suites 1.5 min. The harness-measured session duration is the authoritative figure (M1-01 note): the coordinator's harness reported no duration for the first segment (it ended in the provider's usage-limit error, with the last recorded command at about 19:37 and the pre-registration commit `b146aae` made before the cut) and 7 min 54 s (9 tool calls) for the resumed segment. Recorded effort: **about 45 min of worker session in the first segment (18:53 to roughly 19:37, plus record writing) and 8 min in the second**, one interruption with nothing lost. Review 1: 15.5 min.

## Review and integration

- Reviewer and independent reproduction/withheld-case results: review 1 (fresh independent session, Claude Code Fable 5.1, own clone in the scratchpad with only `local/rom-location.txt` provided, 11 September 2026 22:10:54–22:26:20 AEST, 15.5 min) of `ae44893`: bootstrapped from the network and built the core itself (9.2 s); 173 checks; `boot-start-600` and race compares and the Right-removed perturbation unchanged under the new worker; boot coverage capture unchanged (map differs from the tracked one only in `library_sha256` and the coverage digest, `1e6c02747d7c1003…` once substituted); access record window `464049b37dcdb267…` byte-identical in two fresh processes (23 s each), whole run `bc799832ffa6666e…` (83 s) with the instruction total equal to the coverage total and 0 unresolved stores; `access query` on `$7E0313` gives writer `$80:87F2` and reader `$82:AADF`, the `$4218`/`$4219` reads at `$80:87E9`/`$80:87EF`; static references `$00:0300` 9/9, `$00:0302` 7/7, `$00:0313` 2/2; fields manifest exit 0 and `--against` the Right-removed fields variant exit 1 at 1500 in `joy1h_image`; `down-instead-of-up` digests as recorded; every pre-registered prediction row precedes its observation; from its own capture: position/displacement 565/565, throttle ramp to 432 at 1529, timer frames counter incremented by `$81:C6C9` in every frame 1535–2100 and reset by `$81:C6ED` every fifth frame, one store per frame for every primary writer over 601/601 frames (speed writer 772/772 over 1329–2100), in-frame order at 1600 as finding 10, on-screen `0:01:3` at 1600 and `0:17:3` at 2400; diff `4f593d6..ae44893` inspected (no change to the patch, locks, frozen manifests, reference scripts, `src/lab`, `docs/STATE.md`, `docs/map/`, the registry or workflows; worker changes additive and gated on `--access-out`; no ROM bytes, dumps or images tracked; no skips or weakened comparisons); CI green on both platforms. Withheld case `right-released-2500` with seven predictions written before the run: identical fresh runs (`e1c76be7a48f1543…`); first divergence at 2500 in the same six bytes as the release at 2000; speed 469 → 368 by exactly 1 per frame over 2500–2600; position/displacement 549/549 over 2450–2998; timer digits equal to the baseline's in 550/550 frames; one store per frame and joypad < axes < physics order in 201/201 frames; the camera prediction failed (finding 1). Mutations: 9 of 10 caught; the survivor is "unresolved stores not counted". Verdict: return with two required documentation corrections, then approve: (1) `camera_x` was marked validated with `= pos_x − 936` in every frame, but the offset is 936 only when stationary and 875–880 at speed; (2) the axis fields' writers were listed only for the baseline's values (`$82:AB20` writes `axis_v` = 0, `$82:AB59` writes `axis_h` = 1; the Down and Left writers were never captured); (3, minor) speed figures: 479 at 2000, not 481, and 479 → 448 over 2000–2100; (4, non-blocking) `test_access.py` lacks an unresolved-store case.
- Required changes or acceptance rationale: the coordinator folded findings 1–3 into `docs/state/player-state.md` and R-0007 (findings 8 and 9, independent check) in the correction commit after `ae44893`; no code, manifest or digest change was required, and the reviewer confirms the corrected text below. Finding 4 and part A item 6 (coverage-side library digest, the two synthetic cases, the whole-tail NMI check) are recorded in `docs/STATE.md` as open notes for the next tool task. Note from the review: `race-crawler-dragster-3000-right-removed-fields.json` carries no `expected` digests, consistent with the frozen M1-01 `right-removed.json`; a perturbation manifest is compared against, not validated on its own. The acceptance table is met: existing behaviour unchanged (worker and reviewer, separate clones); derivation correct on synthetic input and in agreement with the real run; capture repeatable; position, speed, timer and the joypad images validated with pre-registered predictions and exact relations, lean and a trick recorded as unknown with the failed experiments; the cadence and in-frame order shown from the trace by both; the fields manifest works with the comparator; 25 ROM-free tests, green on both CI platforms; independent review with a withheld variant.
- Exact merge candidate and required-check results: `ae44893` (branch head reviewed; code `e9b2a4f`–`e80d573`, manifests `7b34c54`/`04382c0`, records `967fb6e`/`ae44893`) plus the record-only correction commit after it. Required checks: CI run 34597570911 on `ae44893` green on `ubuntu-24.04` (54 s) and `macos-15` (57 s), 173 checks; local suite 173/173 by worker and reviewer on separate clones; ROM-dependent evidence reproduced by the reviewer as listed above.
- Reviewer confirmation of the corrections: the reviewer fetched `5ba3579` into its clone (22:30:06–22:30:37 AEST), checked every corrected statement against its own captures and approved, with one range of its own making corrected in the integration commit: the speed maximum 495 is held over frames 1775–1913 (its report had listed the first five frames of that plateau).
- Integrated commit and evidence location: `main` fast-forwarded from `0801782` to the integration commit (the reviewed `ae44893`, the correction commit `5ba3579` and this record); pushed to origin on 11 September 2026; the `main` CI run is recorded in a follow-up commit. Evidence: this record, R-0007, `docs/state/player-state.md`, `tests/manifests/fields/player-state-v1.json`, the eleven new manifests, ignored `artifacts/m1-02/` in `.worktrees/m1-02`, the reviewer's report summarised above.
- Scope still unverified: the arithmetic inside the physics routine (`$82:8AB1`–`$82:8DF8`) and the camera lag rule; the writers of `axis_v` = 2 and `axis_h` = 0; the rider's y coordinate, angle, animation and lean; the Y-button mechanism; opponent AI; every other track, rider, tour and mode; a Linux run of the core; VRAM/CGRAM/OAM contents and DMA engine transfers (D-0002 residual).
