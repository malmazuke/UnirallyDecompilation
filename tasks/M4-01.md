# M4-01 — DRAGSTER loser-result presentation

## Assignment

- Status: review; implementation candidate `f438741` validated 13 September
  2026 and awaiting fresh independent review
- Milestone: M4 — Original game coverage
- Coordinator: current OpenAI Sol/medium coordinator
- Task provider: OpenAI
- Planning consultation: bounded OpenAI Astra/high before claim/implementation;
  identify hidden assumptions, order the evidence-freeze experiments and return
  a written recommendation to the Sol coordinator under D-0004
- Worker/session/runtime/model: OpenAI Sol/medium in an isolated worktree after
  the planning checkpoint
- Actual model/reasoning effort: planning consultation `gpt-6-astra`/high;
  coordinator/worker `gpt-5.6-sol`/medium
- Provider quota: sampled before planning at 45% weekly used on 13 September
  2026; the user explicitly removed the percentage stop/reserve for this task.
  No spending or reset redemption is authorized
- Reviewer: fresh sequential OpenAI Sol/medium reviewer with one withheld loser
  result boundary or mutation
- Dependencies: accepted M4-00 inventory at integration `3a3f977`; accepted M3/tag `m3`; supported PAL
  ROM, release-path reference fixtures and valid 25-entry Classic pack available
  locally or regenerated by their tracked commands
- Base commit, branch/worktree and heartbeat: `5c3eec7`,
  `task/M4-01-loser-result`, `.worktrees/m4-01-loser-result`; durable checkpoint
  at each completed evidence/implementation phase and at least every ten minutes
- Owned paths: additive loser-result reference/presentation contract and
  identity files, result-composition source/tests, task/research/review records;
  pack extraction rules only if a pre-implementation provenance experiment
  proves existing entries insufficient
- Frozen interfaces: do not change M3 gameplay/replay expected values, existing
  seven visual cases or thresholds, ROM/core identity, scheduler/input behavior,
  canonical state encodings or accepted winner rendering
- Session limit: 45-minute reassessment and ten-minute durable checkpoints; one
  reference worker at a time; no paid execution

## Outcome and boundaries

Render and present the original player-loss result for the already accepted
PAL/1P/MIKE/CRAWLER/DRAGSTER release-3000 path. Gameplay already reaches an
exact `PlayerLost` stable result at frame 3800; current native result composition
explicitly rejects any outcome other than `PlayerWon`.

Recover the loser screen's semantic composition and add an identity-bound visual
case with a threshold frozen before implementation. The native frontend must
render that state without throwing and without changing canonical gameplay.

This task does not claim general rankings/awards, add menus/progression/audio,
change winner behavior, recover another track/rider/opponent/mode, or substitute
plausible loser text for observed original behavior.

## Inputs and prerequisites

- `tests/manifests/replay/race-crawler-dragster-12000-release-3000-3299-fields.json`
  and `tests/manifests/native/full-race-release.*.json`.
- Original complete loser-result observation at frame 3800 in
  `docs/research/R-0012-complete-race-evidence.md`.
- Existing winner visual contract
  `tests/manifests/presentation/classic-crawler-dragster-v1.json` and ignored
  fixture layout documented by R-0015.
- Current rejection at `src/core/presentation.cpp`'s `build_result_map`.
- Exact PAL ROM and generated pack remain ignored; never track reference PNGs,
  canonical binary states, access traces or extracted bytes.

## Acceptance

| Criterion | Command or experiment | Expected result | Required artifact |
| --- | --- | --- | --- |
| Gameplay baseline remains exact | Run `native finish-check` on the release-3000 case with its accepted restore frames | Exact through frame 3800 with `PlayerLost`; unchanged expected identities | report/hash |
| Original loser composition is observed before code | Capture the reference frame and narrowly targeted VRAM/CGRAM/OAM/access evidence at stable result frame 3800; compare its transfer/layout sequence with winner frame 3679 | Text, row order, time/name fields, palette/window differences and unknowns are recorded; no semantic field is guessed | research record plus ignored artifact hashes |
| Visual contract is frozen independently | Add an identity-bound loser state/reference PNG case before implementation | State and PNG hashes bind the accepted release manifest/core/ROM; whole-frame mismatch limit is predeclared from the existing winner-result policy, not candidate errors | tracked contract; private fixtures ignored |
| Native loser result renders | Extend semantic result composition only as supported by the trace; run `native presentation-check` | New loser case passes its frozen limit; all seven M3 cases retain their exact mismatch counts | report and source evidence links |
| Frontend boundary is safe | Authored pack-backed test renders the stable `PlayerLost` state through `LivePresentation` | No `unsupported Classic result composition` exception; canonical state bytes before/after render are identical | test/report |
| Negative identities remain strict | Mutate outcome/state/fixture identity and any newly interpreted layout input | Contradictory, corrupt or unsupported combinations fail before presenting a plausible screen | tests/report |
| Regression and hygiene | App-debug/app-sanitize suites, release-path gameplay/restore, diff inspection | Required checks pass; no frozen expectation weakened and no ROM/pack/state/PNG/trace tracked | reports and staged-file list |
| Independent review | Reviewer reproduces capture identity and visual result, inspects semantic text/layout and owns one boundary/mutation | No candidate-derived tolerance or invented composition remains | review record |

## First experiment

Before claiming the task, run the bounded Astra planning consultation required
above and record its risks, experiment ordering and coordinator decisions here.
Then reproduce the accepted release-3000 gameplay and regenerate its stable
end-of-frame-3800 state and reference PNG in a new ignored directory. Capture
the narrow result-load/result-screen port/access window using the same method as
R-0015. Diff the final VRAM map, palette and OAM against winner frame 3679, then
write the observed loser layout and freeze its visual contract before touching
`src/core/presentation.cpp`.

## Astra planning checkpoint

Bounded OpenAI `gpt-6-astra`/high consultation completed before claim on 13
September 2026 against base `420d647`; it was read-only. Coordinator decisions:

- Reproduce `full-race-release.case.json` first with restore frames 1600, 3318,
  3558 and 3799. Generate the loser state from that unchanged producer; never
  manufacture it by changing a winner state's outcome.
- Capture ordered result-load evidence from frame 3559 through stable result,
  including the `$0200`–`$09FF` map, final palette, OAM and relevant PPU ports.
  Compare it with winner frame 3679 and widen the capture only for unresolved
  differences. Preserve `(frame, sequence)` order, mirrored register writes,
  retained VRAM, late palette writes and VMADD resets.
- Observe the adjacent loser result boundary around frames 3798–3800. Do not
  transfer the winner's loading counter or assume a uniform 105-frame shift;
  gameplay timing and canonical state remain frozen.
- Freeze an additive loser contract before renderer code, bound to the release
  manifest, ROM/core, exact state/PNG hashes and sampling phase. Retain the
  original seven-case manifest and its authentication. Use the existing result
  policy's `[0, 0, 256, 224]` rectangle and `0.15` limit.
- Add focused semantic assertions for observed title, row order, name/time and
  map placement because the whole-frame tolerance alone can hide an incorrect
  composition. Recover the digit `6` mapping needed by observed `0:35.66`;
  infer no unobserved alphabet, ranking or award behavior.
- Exercise the pack-backed `LivePresentation` seam with the actual release
  state, both fresh and reused presentation instances, and prove canonical bytes
  unchanged. Keep contradictory semantic-state rejection distinct from stale
  fixture/hash rejection.
- Preserve exact existing mismatch counts `36, 697, 279, 445, 653, 962, 961`.
  Run app-debug and app-sanitize serially and leave an adjacent boundary or
  mutation for the independent reviewer.

Strict scope remains stable release-3000 loser composition plus an evidenced
adjacent presentation boundary. No generalized rankings, finish-overlay work,
menus, audio, gameplay/state change, winner cleanup or pack-rule change without
ordered provenance proving a missing payload.

## Evidence and attempts

| Attempt | Hypothesis | Experiment | Observation | Next decision |
| --- | --- | --- | --- | --- |
| 1 | The accepted release path reproduces unchanged | `native finish-check` with restore frames 1600, 3318, 3558 and 3799 | First invocation correctly reported the new worktree's missing toolchain; after pinned bootstrap, two fresh runs and all four continuations passed through frame 3800 | Preserve the exact producer; derive the terminal state from its frame-3800 output |
| 2 | The loser result is not safely inferred from the winner case | Fresh reference access/frame capture over 3528–3800 with map, OAM, result writers and banked/unbanked PPU ports watched | Identities and completeness pass; frames 3798/3799 match and frame 3800 first shows the result; access SHA `afa7abed...5482`, PNG SHA `2441b84c...1afb` | Compare an identically instrumented winner run before freezing |
| 3 | Existing result assets are sufficient and only semantic/time-cycle values differ | Winner capture over 3423–3679; compare copier stream, result map and final palette writes | Both copier streams contain the same 53,432 register/value rows; maps differ at exactly six player-time bytes; loser CGRAM 108–111 has a separately observed cycle phase | Freeze the additive loser contract without changing pack rules |

The pre-implementation evidence and contract are recorded in
[R-0019](../docs/research/R-0019-dragster-loser-result.md). Commit `74112e7`
froze that evidence before implementation commit `f438741`.

## Implementation and candidate validation

Commit `f438741` extends the bounded Classic result compositor to the observed
stable `PlayerLost` state, adds the recovered `6` glyph and the observed loser
palette phase, and preserves the winner's update-225 loading boundary exactly.
It fails closed for contradictory phase/outcome, finish ordering, digit or
centisecond combinations. Focused native tests assert every semantic result-map
word and the exact six winner/loser differences.

The additive tooling profile authenticates the separately frozen loser manifest
and case-array hash. A pack-backed native runner loads the actual release state,
renders it through fresh and reused `LivePresentation` instances, proves the two
frames identical and verifies that serialization before and after presentation
is byte-identical. No pack entry or extraction rule changed.

All following reports were produced from clean candidate commit `f438741`:

| Check | Result | Report SHA-256 |
| --- | --- | --- |
| `app-debug` | 315/315 passed | `948be10071df9d9a29bf5a00e74637262e29c09e0cc490830020371c89000d91` |
| `app-sanitize` | 315/315 passed | `5922bbac7a62c6494c2ab6739a837777163aabe9c32fb736d830071dfd59ba86` |
| winner visual, debug | 8/8 passed; exact mismatch counts `36, 697, 279, 445, 653, 962, 961` retained | `ba15aecd514157b6de6d1363cbc867f95d0d7416fdadee448c8e3f83635a3045` |
| winner visual, sanitize | 8/8 passed with the same exact counts | `ad29a0b12800ae3ff8c8f174e10cf19cf759adb8f5e3dde3ec4fc66f7c68c70a` |
| loser live visual, debug | 2/2 passed; `1073/57344` pixels (`1.871164%`) | `6519ac5fa7199a73ce7746de27c3c673e0f03e2507accff66ba62d072d751229` |
| loser live visual, sanitize | 2/2 passed; same exact count | `a8a21c404461e7ec93052f6f9625ca634769004bfd51e5fb5d945511e43b9476` |
| release gameplay plus restores, debug | 18/18 passed through frame 3800 | `e2adf47c8093cc5127e04b063e939db9c8af0c9a632d80d235f715986887d3a0` |
| release gameplay plus restores, sanitize | 18/18 passed through frame 3800 | `3ad6f83bcf9df83df0f271e9f16f40e2befa8ac4cf049b9affd5d12efe172e34` |

The focused native presentation/frontend tests and all four loser tooling tests
also pass. Independently corrupted copies of the PNG and canonical state each
exit 3 on their hash mismatch before visual presentation; their expected-fail
reports have SHA-256
`2f66703fb40a382d1b9a5e25af16e79879037982bd1c409bf23362da50be4c09`
and `9f28e88744e105f619891005e39c94d9b90d580f67b3722e29cc87b66a6235ac`.

Two prerequisite misses are not passes: the first suite invocation found no
worktree-local toolchain, and `finish-check --save-frame 3800` rejected the
terminal frame because restore saves must be interior. Pinned bootstrap fixed
the former; the terminal state was instead decoded from the exact validated
producer's final row.

## Handoff

- Current base/behavioral head: `5c3eec7` / `f438741` on
  `task/M4-01-loser-result`.
- Verified finding: stable loser composition is the winner map with only the
  observed `0:35.66` tile changes and a separately observed palette-cycle phase;
  the candidate renders it through the real pack-backed frontend without
  changing canonical state.
- Exact next action: commit this handoff, push the task branch, then give the
  exact candidate to a fresh Sol/medium reviewer in an isolated worktree. The
  reviewer owns one withheld boundary or mutation and the review record.
- Unavailable/skipped checks: hosted macOS/Linux CI and independent review are
  pending; no missing local acceptance check is reported as passed.
- Remaining dependency: independent approval of the exact candidate, followed
  by coordinator integration validation and private `origin/main` sync.
- Scope still unverified: generalized result rankings/awards and all other M4
  feature dimensions.

Only the coordinator marks this task accepted after independent review.
