# M4-13 — Native player landing and recovery

## Assignment

- Status: in_progress; claimed 2026-09-13 12:01 UTC by Astra/medium primary.
- Milestone: M4, still incomplete. Policy: [D-0006](../docs/decisions/D-0006-capability-driven-work.md).
- Primary: OpenAI `gpt-6-astra`/medium, standard service; directly owns coupled
  investigation/native implementation and integration. No routine implementation child.
- Review: primary automatically spawns fresh `gpt-5.6-sol`/medium, with
  `fork_turns: "none"`, explicit immutable candidate and isolated checkout.
  One active child maximum; primary handles fixes/re-review without user relay.
- Budget: [D-0004 extension](../docs/decisions/D-0004-model-and-usage-budget.md#m4-13-trial-extension).
  No 20-point task cap; preserve final 20% weekly allowance for review/recovery.
  Sample actual starting usage; no reset, purchase or provider switch authorized.
- Accepted ancestor: `07e7ffa6f35a90e6350665613901e87623ca5af9`. At claim, verify
  ancestry and record the synchronized main tip containing this preparation as
  dispatch base. Create `task/M4-13-player-landing` in isolated
  `.worktrees/m4-13-player-landing`; record alternatives if those names are occupied.
- Owned: task-scoped native movement/contact/control/state/serialization,
  extraction/comparison tooling and tests, additive reference manifests, source
  guide/evidence and task record. Primary also owns registry/state at integration.
  Reviewer owns independent cases and review report in its own checkout.
- Frozen: accepted M4-12 and DRAGSTER projections, cases, content/state identities,
  ROM/core locks and expected results. Add new contracts/versions when necessary;
  do not weaken or rewrite old expectations to obtain a pass.
- Excluded: full-race finish, presentation/frontend dispatch, audio, other modes,
  networking/editor, public release/deployment and speculative infrastructure.

## Outcome and domain selection

Advance both riders autonomously from the accepted authentic end-1649 seed
through a controlled **player support-loss, landing and recovery** sequence,
including at least 100 subsequent updates after the player's landing. The new
primary horizon must also include all of the accepted 1650--1849 interval.
Recover newly reached control, contact, camera or AI dependencies inside this
capability. Preserve existing Right/neutral behavior; additional inputs are in
scope only as needed to produce and reproduce the chosen player sequence.

Before native tuning, use reference-only experiments to select the sequence and
freeze the exact controller script, event definition, landing frame, horizon,
field/dependency inventory, immutable content and reproducible reference outputs.
Identify support loss and landing by authenticated state transitions/writer traces,
not a visual guess or a renamed opponent event. Include the landing response and
subsequent recovery state; freezing only a contact point is insufficient.

The primary chooses the experiment without asking the user. Prefer the smallest
input/horizon change that exercises an actual unsupported player landing path.
Extend the horizon based on reference evidence if the recovery gate needs it;
never shorten it to fit the implementation. Reuse M4-12's 395-byte projection and
add every newly reached future-affecting state field. No later captured runtime
inputs, original CPU execution, scripted motion or fabricated camera constants.
Authentic immutable pre-race extraction is permitted, with provenance; native
runtime consumes only initial state, static content and controllers.

## Startup and evidence

Read NEXT_SESSION, STATE, AGENTS and AGENT_WORKFLOW, this task, D-0004's extension,
D-0006 and [R-0031](../docs/research/R-0031-m4-12-retrospective.md). Then use
[R-0030](../docs/research/R-0030-zoom-zoo-native-trial.md) and
[M4-12 review](M4-12-review.md) for accepted boundaries and rejected branches.
Consult PROJECT_PLAN for scope and BUILD_AND_VALIDATION for actual commands.

Record dispatch/model/effort/time/usage, verify ROM identity, then reproduce the
accepted M4-12 primary comparison and its restores using
[the implemented recipe](../docs/BUILD_AND_VALIDATION.md#m4-12-experimental-native-zoom-zoo-laboratory).
Use fresh output paths. The existing trial harness is fixed to 1650--1849;
implement/document an additive horizon/control/restore extension as needed.
Do not advertise arbitrary-horizon commands before implementing them.

Use [worktree layout](../docs/BUILD_AND_VALIDATION.md#worktree-cache-and-report-layout):
real local/artifacts roots, verified cache links beneath local, separate outputs,
reports inside the designated run directory. Accepted private inputs/reports are
in `.worktrees/m4-12-native-zoom-zoo/artifacts/m4-12` and reviewer cases under
`.worktrees/m4-12-review/artifacts/m4-12-review`; use manifest-based reproduction
if caches are absent. These paths are clues, not fixture identities. Verify hashes.

## Acceptance

| Criterion | Required evidence |
| --- | --- |
| Frozen event/domain | Repeatable original captures authenticate player support loss, landing and recovery, full horizon and all source/seed/content identities. Freeze before native tuning. |
| Native autonomy | Both riders match every declared byte at every update from the same seed through the frozen horizon, including at least 100 post-landing updates. Zero captured runtime state; demonstrate reference/ROM access is unavailable to the native child. |
| Dependency closure | All future-affecting native state and reached producers inventoried, including any newly required camera/control state; exact widths, signed arithmetic and writer order. No silent defaults or fixture-specific repairs. |
| Independent cases | Fresh Sol reviewer selects two untuned input-timing variations, records them before candidate evaluation and captures repeatable references. Both exercise player landing/recovery with materially different timing or state, and run at least 100 updates after their own landing. Freeze each case's horizon from reference before comparing. |
| Restores | Primary and both independent cases restore in fresh native processes before and after their own landing, then match uninterrupted canonical state/reference through each full horizon. Serialize every newly future-affecting field. |
| Regression/quality | Preserve all M4-12 accepted primary/variations/restores and DRAGSTER behavior; focused arithmetic/branch tests and readable source/evidence guide under D-0003. Unsupported out-of-domain paths stay explicit. |
| Integration | Required final native/private/ROM-free gates pass, Sol approves corrected candidate, exact private main ref and hosted CI verified. Report Linux synthetic coverage separately from private native differential coverage. |

A withheld failure used for tuning becomes a regression; retain the failure and
obtain fresh untuned evidence. If a variation misses the event or is inert, record
that result and choose a discriminating case rather than count it as acceptance.
Research alone, an opponent-only landing, or a short successful prefix is not
completion. Resource exhaustion leaves the task incomplete with a durable handoff.

## Execution, review and completion

Use first-divergence experiments and small native changes in one sustained
primary context. Keep coupled producers as internal experiments. Checkpoint
coverage/first divergence/next command/usage every ten minutes and before expensive
work; reassess after 45 minutes without an automatic stop or new agent.

Run focused checks and primary comparison/restores before the first Sol review.
After initial findings are addressed, run complete app-debug/app-sanitize and the
declared private gates (new primary/cases/restores, all accepted M4-12 gates,
DRAGSTER full-race/opponent-first/restores, presentation and content/pack/replay).
Record exact invocations/report identities in one evidence record. Focused
re-review can overlap broad validation; subsequent changes still require appropriate
revalidation. No broad matrix per experiment. No preliminary task-branch push is
required for this local review workflow; final accepted integration is pushed to
private main and its macOS/Linux CI must pass. No CI trigger changes are proposed.

Spawn review automatically in its own checkout at the candidate; give the fresh
Sol/medium agent this task, evidence/input locations, frozen contracts, required
cases and owned `tasks/M4-13-review.md`. Handle findings and re-review, integrate
only after approval, and verify the remote ref. Do not ask the user to launch review.

At closeout report exact event/horizon/fields, branches gained, new versus reused
work, captured inputs remaining, review rounds, measured wall time and shared
usage delta (with attribution caveat). Keep Linux private execution gaps explicit.
Stop after accepted M4-13 or a genuine resource/access boundary. Do not dispatch
M4-14 automatically. Current handoff: no claim, new captures or native changes yet.

## Execution log

- Started 2026-09-13 12:01 UTC, OpenAI Astra/medium primary. Dispatch `60e34e6737f0aa843622d8e998a753e9dbd9629d`, clean main synchronized after fetch; accepted M4-12 ancestry verified. Isolated branch `task/M4-13-player-landing`, checkout `.worktrees/m4-13-player-landing`.
- Fresh weekly usage 15% used / 85% remaining; zero reset credits. Preserve final 20%; no spending/reset authorized. Real local/artifacts roots, read-only linked caches beneath local.
- Next: reproduce frozen M4-12 comparison/restores, select reference-only player landing domain before native tuning.

### 12:12 UTC reference freeze

Fresh M4-12 baseline matches all 395 bytes for 200 updates and restores 1700/1804/1823 (`artifacts/m4-13/baseline-report.json`). New primary holds Right with B 1681–1695, two fresh references identical, rows `9d1b4d9b9f3ed4a1a038d491c8536dbf609b3fc4c4edd86968791a804c05dcc9`. Player full unsupported count reaches nine then resets at 1745; second landing 1762. Freeze end-1649 through 1849 (104 updates after selected landing), existing 395-byte projection/static inventory plus any audit-discovered future fields; restores 1744/1745/1761/1762. No native tuning yet. Continuous Right exploration first lands at 1991 and changes excluded guards, so held jump is the smaller capability domain. Single-frame B was inert for full support loss and is not acceptance evidence. Next: trace player input/landing writers and authenticate $132B before implementing.

### 12:13 UTC candidate checkpoint

Weekly use sampled at 16% (start 15%; account-wide). Primary all 200 updates now match, 395 bytes, restores 1744/1745/1761/1762 pass; M4-12 primary/restores unchanged. Recovered B mapping, low-rate rolling pose branch and landing impulse/quadrant order; see R-0032. Original access trace 3,684,948 instructions, no unresolved stores/conflicts; $132B=0 throughout. Twenty-one CTests and three trial Python checks pass. Next: fresh Sol/medium review with independent timing cases, then broad gates after findings are resolved. No added captured dynamic input or content.

### Independent review correction checkpoint

Fresh Sol reviewer returned `f1cd993` with zero-half-dy landing (early case1760)
and nonnegative first-probe support (late case1746). Both now match the complete
horizon/restores with source-derived fixes; retained as tuned regressions.
Focused boundary tests added. Next: immutable corrected candidate to reviewer,
two fresh untuned cases, and broad final gates alongside re-review.
