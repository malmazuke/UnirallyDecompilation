# M4-07 — ZOOM ZOO `0x4000` vertical-contact family through frame 1700

## Assignment

- Status: claimed 13 September 2026 after bounded Astra planning
- Milestone: M4 — Original game coverage
- Planning consultation: bounded OpenAI `gpt-6-astra`/high, read-only at
  accepted M4-06 head `3a0822a`
- Task provider/model: OpenAI, Sol/medium worker in an isolated worktree
- Provider usage: user-waived percentage guardrails; at most one weekly reset
  only after an actual provider block; no spending or provider switch
- Reviewer: fresh sequential OpenAI Sol/medium with a private preregistered
  one-frame Right release within frames 1682--1686, excluding frame 1684
- Dependencies: accepted M4-03 through M4-06 contracts/evidence, R-0011,
  supported PAL ROM and pinned core
- Base/branch/worktree: `3a0822a`,
  `task/M4-07-zoom-zoo-reflected-vertical`,
  `.worktrees/m4-07-zoom-zoo-reflected-vertical`
- Owned paths: additive reflected-vertical research module/tests/manifests and
  replay variation, `docs/research/R-0025-zoom-zoo-reflected-vertical-contact.md`,
  this task/review records, additive command-inventory note
- Frozen paths: `src/`, M4-03--M4-06 tooling/expectations/replays/projections,
  serialization, Classic pack/profile/start, frontend, core and locks;
  coordinator owns registry/state

## Outcome and boundary

Independently reconstruct the 36 ordered calls/360 points on frames 1683--1700
whose observed player path uses descriptor bit `0x4000`, including mirrored
column selection and positive-slope response. Compose this with accepted M4-06
to verify all 102 calls/1,020 points on frames 1650--1700.

Frame 1700 is a bounded observation cap, not proof that the episode ends there.
This remains captured-argument evaluation. Autonomous recurrence, production
native ZOOM ZOO, horizontal/`0x8000`/bit-zero special geometry, later track
paths, finish, AI, presentation, audio and Classic-pack expansion are excluded.

## Planning evidence and hypothesis

Read-only planning reproduced access SHA-256
`267ecfbf7223a3c2a9e6844d620feda0c41adb98fd9a5280d10320d3682fab67`.
All 18 later player calls set only `0x4000` within mask `0xC001`; active tile
flags and axes are zero, horizontal/special/auxiliary outputs remain zero,
opponent calls remain flat and support is continuous. Player surface outputs
progress from 0 through +8. Frame 1683 point 9 distinguishes a reflected column
(direct angle 1, reflected angle 0, captured 0); frame 1684 enters at vx/vy
483/19 and exits at 483/30 on angle +1.

The planning hypothesis—mirrored column `15-column` plus byte-negated table
angle for `0x4000`—matched all 360 tuples in an exploratory calculation. The
worker must derive instruction widths/order and coefficients 6--8 independently
before adopting it.

## Acceptance

| Criterion | Required evidence |
| --- | --- |
| Foundation | Exact M4-03 contract, M4-04 pair, M4-05/M4-06 focused checks and accepted primary capture identity |
| Repeatable reference | Two distinct fresh captures through 1700; ordered/complete watches and no unresolved claimed dependency |
| Original contract | Trace preprocessing `$81:8B75--8CF3`, reducer, positive-angle coefficient/response at `$81:970B/$81:97E1`, and caller publication; authenticate reached indices 6--8 |
| Exact component | All 360 suffix tuples and downstream fields exact; unchanged 66-call prefix composes to 102/1,020; unobserved descriptor/horizontal paths reject |
| Mutation resistance | Summary-preserving tuple, mirror/angle, descriptor, signed penetration, tie order, coefficient, response/publication, call order/count and identity mutations fail or change computation |
| Worker variation | Preregister one-frame Right release at 1684, exact through 1683 and first controller divergence 1684; two fresh runs, with timing/reconvergence not preclaimed |
| Independent review | Different private one-frame release in 1682--1686 (not 1684), original arithmetic inspection and honest relevance/new-class result |
| Regression | Focused, debug/sanitize, M4-03--M4-06, Classic pack and DRAGSTER finish/restores remain exact |

## Handoff

- Claim base: accepted M4-06 `3a0822a38bac0c664e168a71adccd73aa9cc1123`.
- First action: reproduce the accepted foundation and two primary captures,
  then inventory all 36 calls before implementing equations.
- Worker variation preregistration: retain first Right at 1650, release it for
  frame 1684 only, restore at 1685 and preserve all other events. Predict exact
  state through 1683 and first controller divergence at 1684; do not predict
  contact timing or reconvergence.
- Runtime needs: private supported ROM, pinned core and ignored artifacts only.
  Missing watches or producers stay explicit; do not modify the frozen capture
  merely to fill a value.
- Only the coordinator accepts after clean candidate, fresh independent review,
  integration validation, hosted CI and private-remote synchronization.

### Worker candidate handoff

- Status: ready for fresh sequential review, not accepted. Preregistration and
  pre-equation inventory commit is `37dbd859a3d483fbd31e9c11f447854f6560ad9e`;
  implementation commit is `28cafed5768d8970308de92f22f03ade708b4013`.
  Findings and exact boundaries are in
  [R-0025](../docs/research/R-0025-zoom-zoo-reflected-vertical-contact.md).
- Foundation was reproduced before equations: ROM identity and the exact M4-03
  content contract pass; the M4-04 projection pair reports its accepted frame-
  2500 divergence; M4-05/M4-06 focused tests pass 19/19. Two fresh primary
  captures are byte-identical at access SHA-256 `267ecfbf...fab67`, with
  102 calls/1,020 points, zero unresolved stores and no truncation/overflow.
- The preimplementation inventory covers every suffix call: 36 calls/360
  points, comprising 18 player `0x4000` reflected-vertical calls and 18 flat
  opponent calls. Trace widths/order derive mirrored column selection and
  byte-negated table angle. Positive angles 0..8 use the original signed shift,
  repeated-add multiplication and signed half-angle vx contribution. Reached
  coefficient indices 6--8 are authenticated; the 18-byte index-0--8 payload
  hashes to `f5016c76...3e7a1`.
- The primary component composes unchanged 66/660 prefix and exact 36/360
  suffix to 102/1,020. Report SHA-256 is `aff5212c...b4aed`. Unobserved
  descriptor combinations and horizontal flags reject, as do unsupported
  response domains. ROM-free mutations cover tuples, mirror/angle, descriptor,
  signed arithmetic, ties, coefficients, response/publication dependencies,
  call order/count and identities.
- Worker variation releases Right only at frame 1684 and restores it at 1685.
  Two final fresh runs agree on sample/final/A/V hashes `5685c085...d43eb`,
  `037fee6d...7eb00`, `d25c5f46...e46df`. The preregistered comparison passes
  exact-through-1683/first-divergence-1684 at report SHA-256
  `5a6098f9...aa305`; no timing/reconvergence was preclaimed. Two contact
  captures agree at `4161f1de...7762a9`; their 102/1,020 component report is
  `6550e369...96c9`. They exercise no new contact class through frame 1700.
- The initial foundation attempts without the worktree-local ignored ROM/core
  correctly reported missing prerequisites. The first preregistered replay used
  zero digest placeholders and correctly exited 1 while both processes agreed;
  its report is retained. Primary-versus-variation replay comparison
  intentionally exited 1 at expected frame 1684. The first focused test run
  exposed and corrected an authored fixture penetration expectation, and its
  next run exposed only an over-specific exception regex; neither failed run is
  counted as a pass.
- `src/`, all existing M4 tooling/manifests/replays/projections, serialization,
  pack/profile/start, frontend, core and locks are unchanged. Private ROM,
  decoded content, captures and reports are ignored. No provider block, reset,
  spending or provider switch occurred. Coordinator-owned state/registry files
  are unchanged. Coordinator alone changes status after independent review.
- Clean implementation commit `28cafed`: focused M4-05/M4-06/M4-07 tests pass
  29/29. Debug and sanitize each pass 342 Python tests/342 records, 20 CTest
  checks and three fresh processes with hash `0be347c529fadda9`; reports bind
  clean source with no skipped/missing/failed checks and hash to
  `84c84a33...71801` and `7a394aff...3d004`. The M4-03 contract passes at
  report `81a2e00f...d52bc`; M4-04 pair remains exact. The unchanged 25-entry
  Classic pack passes at `bfb75ef9...d8cd`; DRAGSTER finish plus restores
  1600/3213/3453/3678 pass 18/18 at `63364922...b8c3`.

### Returned-review correction

- Review `251b992b8d4085d6b335fce26f0343dc2d88101b` returned two
  evidence-integrity findings. R1 reproduced: `description` and `limits`
  mutations were formerly accepted. Both are now exact-bound per scenario and
  focused tests reproduce the two former unexpected passes as rejections.
- R2's mask-address finding reproduced: `$81:8BE9` is `LDA $18` and the
  `AND #$4000` begins at `$81:8BEB`. Independent byte decoding found that the
  review's proposed point-x replacement range is itself one byte late at both
  ends. Exact PAL bytes are `$81:8C2A: 80 13` (`BRA`, operand at `8C2B`),
  `$81:8C2C: A9 03` (`LDA #$03`) through `$81:8C3C: 99 31 02`
  (`STA $0231,Y`, ending at `8C3E`), followed by the next instruction at
  `$81:8C3F`. The corrected evidence therefore cites the precise instruction
  range `$81:8C2C--8C3E`, not the review's `$81:8C2D--8C3F`.
- Status remains review. The correction commit, focused/component results and
  clean proportional validation are appended after completion; coordinator
  alone accepts.
