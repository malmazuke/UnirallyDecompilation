# M4-15 — Native ZOOM ZOO race completion

## Assignment

Status: ready/unclaimed. Preparation only; gameplay starts in the next user-started
Astra/medium session. Accepted ancestor: `38c72e889d59b62270be6e26fca713b3fb3aa153`.
At claim, record the actual synchronized main tip containing this preparation,
clock, model/effort, quota, branch and worktree. Use `codex/m4-15-race-completion`
in `.worktrees/m4-15-race-completion` (record alternatives if occupied).

Read NEXT_SESSION, STATE, AGENTS, AGENT_WORKFLOW, D-0004/D-0006, PROJECT_PLAN,
BUILD_AND_VALIDATION and M4-14/R-0033/review. Reproduce the accepted sustained
primary and restores with fresh outputs before new work. Reuse authenticated
local fixtures/caches by hash, not by path alone.

Primary: Astra/medium directly investigates, implements and integrates. Fresh
Sol/medium independent review is automatically launched in an isolated checkout,
with explicit model/effort and no inherited conversation. One active child.
[D-0004](../docs/decisions/D-0004-model-and-usage-budget.md#m4-15-race-completion-extension)
waives the 20-point task cap but preserves the final 20% weekly reserve for
review/recovery. Sample quota at startup and checkpoints; no reset, purchase or
provider switch is authorized. Ten-minute checkpoints and 45-minute reassessments
are continuation points, not smaller task boundaries. A resource stop preserves
an incomplete handoff. Do not automatically dispatch M4-16.

## Outcome and reference feasibility

The intended outcome is **a complete native ZOOM ZOO race from the authentic
end-1649 seed**, escaping the repeatedly visited section and reproducing the
original's authenticated race result and stable post-finish continuation.
Preserve the accepted pre-seed history. No later seed, captured runtime repair,
CPU fallback or longer run trapped in the same section satisfies this target.
This proves completion from the existing seed, not native initialization from
race start, a playable frontend or general coverage of every input.

Reference exploration is the first internal phase of this task. Before native
tuning, explore controller strategies in the identified original PAL ROM using
repeatable scripts. Authenticate track markers, lap/progress and finish writers;
use them to distinguish obstacle escape and new track coverage from temporary
counter increases or elapsed frames. Keep meaningful failed strategies and the
reason for each change. Preserve the original opponent policy unless a justified
scenario decision is recorded before freeze; no altered game content or cheats.

Aim to demonstrate an entire reference race. Freeze the successful controller
timeline, seed/static identities, horizon, state inventory, escape/progress
criteria, race-result semantics and at least 200 post-finish updates in two fresh
matching processes before native implementation. Extend tooling if needed;
the current sustained laboratory's frame limit is not an acceptance boundary.
Reference-discovered state extensions must be frozen additively before tuning
against them; preserve old contracts and disclose later inventory corrections.

A substantial downstream milestone is a fallback only if evidence-led original
exploration cannot establish race completion. Record attempted strategy families,
why they fail, authenticated escape, distinct downstream markers reached and
remaining uncertainty. Before native tuning, freeze that alternative outcome and
its full horizon in two runs, with an explicit rationale subject to independent
review. Time pressure, a native divergence, a difficult new producer or an easy
short prefix do not justify lowering the target. Report any accepted fallback
as partial race progression, never completion; leave the full-race objective
outstanding. If neither reference outcome is established, continue investigation
within this task until a real resource/access boundary; do not accept research
alone as the native capability. No routine user decision is required.

## Implementation scope

Recover every coupled gameplay dependency needed by the frozen outcome: geometry,
contact, motion, control/pose, boost/camera inputs, opponent AI, progress/laps,
finish/result and future-affecting state/serialization. Own necessary source,
extraction/content contracts, harnesses, tests and evidence; coordinator owns
registry/integration. Keep readable functions, explicit integer semantics and
address/experiment links under D-0003. Small experiments and commits are welcome;
per-producer acceptance tasks are not. Preserve DRAGSTER and all M4-12–14 frozen
evidence. No speculative abstractions or generic validation-cache project.

Frontend, rendering/audio, other tracks/modes, menus, editor/networking and
publication are excluded. Output-side presentation is excluded only after its
lack of feedback into the accepted simulation is authenticated. Do not discard
reached gameplay dependencies by calling them presentation.

## Acceptance

- Both riders: every declared future-relevant state byte matches each update
  from end-1649 through the complete frozen outcome, including escape, downstream
  markers and finish plus 200 post-finish updates (or the disclosed fallback).
  No later dynamic captured inputs; complete reached producer/read inventory.
- Primary reference repeats in fresh processes. Authenticate ROM/core/content,
  expanded input timeline and whole-WRAM identities for supplemental audits.
  Follow the new preflight and lightweight validation ledger in BUILD_AND_VALIDATION;
  implement the missing automatic safeguards in this task, not during preparation.
- Fresh Sol chooses two material untuned variations after candidate freeze.
  Qualify and freeze each original twice before native evaluation; both must
  escape and reach the same outcome, including their own finish continuation
  for full-race acceptance. Extend case horizons where needed. Inert changes or
  cases trapped in the old section do not qualify. A failed case used for tuning
  becomes a regression and requires a fresh untuned replacement.
- Fresh-process restores before/during/after escape, new modes, lap/finish
  transitions and late continuation match uninterrupted native/reference runs.
  Malformed newly serialized state is rejected. Native execution with ROM,
  repository and reference access denied passes, with meaningful negative controls.
- Preserve all accepted differential/restores and DRAGSTER/content/replay/
  presentation gates; complete debug/sanitizer, independent review and exact
  merged checks. Hosted macOS/Linux CI is distinct from private Linux differential
  evidence. Missing/skipped prerequisites are not passes.

Run focused checks before initial review, broad gates after corrections and
appropriate revalidation thereafter. Ledger reuse cannot replace independent
review, fresh withheld evidence, sanitizer coverage or required merge/CI checks.
The primary fixes findings and requests re-review without user relay.

## Closeout

Use consolidated closeout: prepare reviewed code/evidence and tracked handoff
before the final main push; keep remote acceptance conditional until observed.
Push accepted integration to the existing private origin, verify exact refs and
final-tip CI. Record final SHA, commands/results, CI, time and shared usage in
ignored `artifacts/m4-15-integration/closeout.json`. If missing, recover status
with git refs/remote and `gh run list --commit <sha>` / `gh run view <id>`.
Do not make a second documentation-only push to transcribe success.

Report actual gameplay outcome, exact domain, review findings, unavoidable versus
avoidable work and remaining product limits. Completion of this task alone does
not establish all M4 gates or authorize a milestone tag. Stop after acceptance
or a genuine resource/access boundary; no M4-16 dispatch.
