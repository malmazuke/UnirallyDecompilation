# M1-03 — Minimal track/asset decode

## Assignment

- Status: claimed (coordinator, 11 September 2026 22:35 AEST; registry row updated in the same commit, M0-05 gap G1). Record written by the coordinator on 11 September 2026 as `planned`; became ready when M1-02 (the trace-derived access record of [D-0002](../docs/decisions/D-0002-data-access-observation.md)) was integrated at `630f472`
- Milestone: M1
- Coordinator: primary agent (Claude Code, Fable 5.1)
- Worker/session/runtime/model: fresh session of Claude Fable 5.1, run as a subagent of the coordinator's session and dispatched with the portable prompt in `docs/AGENT_WORKFLOW.md` and nothing else
- Reviewer: fresh independent session, own clone (dispatched by the coordinator after submission)
- Dependencies and evidence of acceptance: M1-01 accepted ([R-0006](../docs/research/R-0006-observed-code-map.md): 395 static ROM references, no bank above $83 executes, block-move routine at `$00:0199`, DMA channel registers referenced from ROM code); M1-02 part A (the `access capture` tool with stored values at the DMA registers, the block-move source/destination series and the register log at watched pcs); [D-0002](../docs/decisions/D-0002-data-access-observation.md)
- Base commit: the claim commit on `task/M1-03-track-decode` (its parent is `630f472`, `main` after the M1-02 integration). Sequencing decision (coordinator): M1-02 runs first because both tasks need the same additive worker and drain options and the shared files (`worker.py`, `drain.py`, `tools/project.py`) cannot be owned by two concurrent workers. M1-03 may be dispatched from M1-02's committed part-A commit on `task/M1-02-player-state` before M1-02 is integrated, in its own worktree, if that commit passes the suite; it is then integrated after M1-02 (dependencies merge first). Otherwise it starts from `main` after the M1-02 integration
- Branch and isolated worktree: `task/M1-03-track-decode` in the linked worktree `.worktrees/m1-03` (ignored path; the coordinator's checkout stays on `main`). Fixture arrangement as M1-02: the coordinator placed `local/rom-location.txt` in the worktree and copied the ignored `local/toolchain`, `local/emulators` and `local/cache` directories from the main checkout; nothing else was copied. The coordinator pushes the candidate branch to origin for CI
- Owned paths and shared interfaces: new package `tools/unirally_lab/content/` (decoders and the comparison against frame images); new tests `tests/tooling/test_content.py`; `tools/project.py` only to register the new `content` command group; new `docs/content/` (tracked decode records: identifiers, ROM offsets, formats, provenance; no extracted content); `docs/research/R-0008-*`; `docs/BUILD_AND_VALIDATION.md` command-table rows; this record. Must not change: the pinned core or patch, `tools/locks/`, existing manifests, the samples/coverage/map/access schemas except additively, `tools/unirally_lab/access/`, `tools/unirally_lab/coverage/` and `reference/worker.py` except for **additive** options that leave every existing output, exit code and digest unchanged (M1-02 is integrated, so the coordinator allows additive changes there; record each one in the handoff), `docs/state/`, `docs/STATE.md`, the registry
- Claim/lease/heartbeat/checkpoint location: this file and the registry row; checkpoint commits at least every 10 minutes and before every long capture
- Session time limit, concurrency allocation and actual spend authorization if relevant: about 3 h of worker time for a first candidate; one worker; no service purchases. If the runtime comparison (part B) is not reached, submit the provenance findings (part A) for review with the decode state recorded

## Outcome and boundaries

Decode one representative segment of the track the validated race scenario runs on (DRAGSTER in the CRAWLER tour) and the rider frames the scenario draws for MIKE, from the ROM through the loading path the original executes, and show that the decoded data is what the original uses at runtime. Record where the data comes from and how it is transformed. Nothing is extracted into tracked files.

**Part A — provenance: how the data reaches the PPU and work RAM.** Using `access capture` on the race scenario with the DMA registers and the block-move site watched: list every DMA transfer set up during the race load (frames 1200–1400) and during riding — channel, direction, B-bus register (`$2118`/`$2119` VRAM, `$2122` CGRAM, `$2104` OAM, or work RAM), A-bus source (bank, address), size, and the pc that started it (`$420B` write) — and every block move through `$00:0199` (source, destination, length, the operand banks as read from the end-of-frame work RAM, labelled as in D-0002). From these, name the ROM regions (file offsets under the LoROM rule) that supply the DRAGSTER track tiles, tile map, palette and MIKE's sprite tiles, and whether each is copied raw or passes through a decompressor in work RAM first (a transfer whose source is work RAM that was itself filled by a routine reading ROM through indexed loads points at a decompressor; name the routine's entry point from the coverage map and the ROM range it reads). Record the reads of ROM by the physics code during riding (from the access record: reads with sources in banks above $83 or in the referenced ROM addresses of the race map) as the candidate collision or track-geometry data, without decoding them if the budget does not allow.

**Part B — decode and runtime comparison.** Decode one segment: the tiles and tile-map region visible in the frame image at a chosen riding frame (for example frame 1600, before the rider has moved far, and one later frame), and the rider tiles the OAM upload contains, from the ROM offsets of part A, through the identified transformation (raw copy, or a reimplementation of the decompressor with its arithmetic preserved and checked byte-for-byte against the work RAM it produced in the original run). Compare against the original's runtime rendering: render the decoded tiles with the decoded palette at the positions the tile map gives for the visible region and compare pixel-for-pixel against the frame image (`--frame-image`) over a stated rectangle, stating the PPU features the renderer omits (priority, scrolling registers, windowing, sprite priority) and the resulting mismatch count; a byte-for-byte comparison of the decoded data with the work RAM staging buffer (from the work RAM series) where one exists is the stronger check and is preferred where available. If a collision or geometry table is identified in part A, compare one value the physics reads (from the access record's load values) with the decoded table entry at that index.

Deliverables: `docs/content/dragster-segment.md` (identifiers, ROM offsets and sizes, format description, the transformation, the comparison results and their limits; the stable logical identifiers for the track, segment, palette and rider frames independent of ROM offsets, per the project plan); `tests/manifests/content/` (a manifest of the decoded items with ROM digest, offsets, sizes and the expected digest of the decoded bytes, so that a fresh host regenerates and verifies them without any tracked content); the `content` commands (`content provenance`, `content decode`, `content compare` or as adjusted) with ROM-free tests on synthetic data for the decoder and the tile renderer; R-0008; this record's handoff.

Not in scope: other tracks, tours or riders; a general extractor; audio; a renderer beyond what the comparison needs; any native gameplay; a core patch change (D-0002 fallback: if VRAM contents are needed beyond what the frame image and the DMA parameters show, record the need and stop that branch); assigning meaning to work RAM fields (M1-02).

Tracked-content rule: no ROM bytes, decoded tiles, palettes, tile maps or images are tracked; tracked files carry offsets, sizes, formats, digests, identifiers and counts. Decoded content and comparison images stay in ignored `artifacts/`.

## Inputs and prerequisites

- ROM as in M0-03; built pinned core; the race manifest; the race map; M1-02's `access capture` (final interface in the M1-02 handoff: `--watch-address`, `--watch-pc`, `--wram-series-range/--wram-series-every`, `--frame-image`; the record's `dma_log`, `rom_reads`, `rom_read_ranges`, block-move decoding and `access query`) at the base commit; the M1-02 handoff names the whole-run capture command (about 90 s, 22 MiB) and its digest `bc799832ffa6666e…`.
- Runtime needs: as M1-02; a race `access capture` restricted with `--from-frame 1200` where the load phase is the interest; frame images at chosen frames.
- Baseline to reproduce first: `doctor`, `bootstrap`, `build --preset lab-debug`, `test --suite synthetic` (173 checks), `reference build`, `replay compare --manifest tests/manifests/replay/race-crawler-dragster-3000.json` (exit 0, sample digest `72f618f2f7e3416e…`), and the windowed `access capture` command in the M1-02 handoff (exit 0, `access.json` SHA-256 `464049b37dcdb267…`).
- Known baseline failures: none. Known limits carried forward: reference execution on one macOS host; ROM-free CI (new tests use synthetic data); the D-0002 residual (VRAM/CGRAM/OAM not readable, DMA engine transfers seen only through their parameter stores, pointers changed within a frame unresolved); the open notes in `docs/STATE.md` (the coverage-side library digest, an unresolved-store test case) are not this task's unless the worker touches those files anyway.

## Acceptance

| Criterion | Command or experiment | Expected result | Required artifact |
| --- | --- | --- | --- |
| DMA and block-move inventory | `access capture` on the race manifest with the DMA registers and `$00:0199` watched; `content provenance` | Every DMA transfer in frames 1200–1400 and during riding listed with channel, direction, B-bus target, A-bus source, size and initiating pc; every block move with source, destination and length; totals reconcile with the register-write counts in the access record | `docs/content/dragster-segment.md`, R-0008 |
| ROM provenance of the segment | Inspection of the inventory | The ROM file offsets and sizes supplying the visible track tiles, tile map, palette and MIKE's sprite tiles are named, each with the transfer or routine that moved it; a transformation is named as raw copy or as a decompressor with its entry point | Record |
| Decode is exact | `content decode` on the manifest; comparison with the work RAM staging buffer or the DMA source bytes | Byte-for-byte agreement with the bytes the original transferred (digest recorded in the content manifest); for a decompressor, the reimplementation reproduces the original's work RAM output exactly | Content manifest, report |
| Runtime rendering comparison | `content compare` against the frame image at two chosen frames | Pixel comparison over a stated rectangle with the mismatch count and the omitted PPU features declared; the tile-map region and rider tiles are recognisably the frame's content and any mismatch is explained | Comparison report, R-0008 |
| Collision or geometry use | From the access record's ROM reads during riding | Either one physics read is matched to a decoded table entry, or the candidate table is named with its reads and left undecoded, stated as such | R-0008 |
| ROM-free tests and CI | `test --suite synthetic` locally and on both CI platforms | Suite grows by the decoder, renderer and provenance tests on synthetic data; green on both platforms | Tests, CI run |
| Independent review of the exact candidate | Reviewer reproduces on the candidate commit from its own clone and decodes one item of its own choosing (another frame or another transfer) | Approve, or specific reproducible findings addressed | Record |

## Evidence and attempts

| Attempt | Hypothesis | Experiment | Observation | Next decision |
| --- | --- | --- | --- | --- |
| 3 | Watching the PPU address ports pairs every DMA with its destination and shows the load-phase transfers as raw copies | Windowed `access capture` 1200–1420 with `$2116`/`$2115`/`$2121`/`$2122`/`$2102`/`$2104` watched in banks $00/$04/$0F/$82, a work RAM series of `$0437`–`$047A` every frame (13 s, `access.json` `1508eafa9c8aed0e…`); whole-WRAM dumps after frames 1243, 1600 and 2400 with the worker's `--stop-after-frame`/`--wram-dump-out` | The load phase's port writes reconstruct to VRAM bytes `$0000`+2048 (BG3 font), `$0801`+3, `$2000`+992 (BG2 tiles) and `$E000`+8192 (BG2 64×64 tilemap) and CGRAM 256+96 bytes; the copier `$82:B296` reads ROM file ranges `0x058000`+992, `0x05BCC0`+10240, `0x07B540`+32, `0x07B760`+192, `0x07B9A0`+32, `0x020180`+32, `0x020380`+64 in that window (byte reader `LDA $0000,Y` with bank wrap; 5-byte directory entries bank/flag, address, length at `[$4F]`, default `$82:B332`; flag $80 selects the Huffman-LZ decompressor at `$81:B8E2` instead of the raw copy). Frame 1242: `$82:E12B` loads the track: asset `$77:074A`+$C2 decompressed to `$7F:0000`, then for each tile-set id in the track header 16×16 tiles are DMA'd raw in pairs (64-byte top rows to VMADD `$2000`+n·`$20`, bottom rows to +`$100`; 20 tiles from `$16:8080`, `$16:8100`, `$16:D900`–`$16:DAC0`, `$16:FF80`–`$17:83C0`, `$16:DD00`–`$16:E140`), the per-tile tables at `$17:A0A4`+ (copied to `$7E:A000`, 2 bytes per column) and `$17:C4E4`+ (to `$7E:C000`, 1 byte per tile). Riding: sprite tiles to `$6020`–`$6CD0` (rider), tilemap column from `$0459` to `$0CB5` (VMAIN `$81`) and row from `$0437` to `$0E65` at frame 1334 | Reimplement the decompressor and check it against the `$7F` dump; verify the raw copies against the ROM bytes; capture 1595–1605 and 2395–2405 with the ports watched for the render comparison |
| 2 | The DMA log and the block-move register log of a whole-run `access capture` locate how the race content reaches the PPU | `access capture` on the race manifest, whole run, `--watch-pc 0x000199`, frame images 1400/1600/2400 (77 s, `access.json` SHA-256 `4b2fa0f3e5502f2d…`; differs from the M1-02 whole-run digest only by the added watch) | 39,705 MDMAEN stores and 1,754 HDMAEN stores, unchanged from M1-02. The block-move routine at `$00:0199` runs 2,708 transfers (88,769 bytes) in frames 404–1054 only, none in the race load or the race. The race load (NMIs off, frames 1208–1328) writes VRAM by CPU stores, not DMA: `$82:D4B8` clears VRAM (32,768 words, frames 1208–1214), `$82:B1F8`/`$82:B1FF` write `$2118`/`$2119` byte-wise (54,800 each over the run; VMADD `$1000`, `$7000`, `$0000` set at `$82:B1E0` in frames 1214–1219) reading ROM through `$82:B296` (`absy`, banks $04–$0F), and `$82:B19D`/`$82:B1A3` write CGRAM (128 bytes at frames 1219–1220, DBR $0F). A second routine `$81:B974`/`$81:B97F` (`(dp),Y`) writes `$7F:0001`–`$7F:840D` in frames 1232–1242 reading ROM `$18:8011`+ through `[dp]`; the race code reads that buffer during riding (`$81:B3CF` `$7F:800F`–`$7F:83CD` feeds the tilemap staging buffers `$00:0437`/`$00:0459` via `$81:B3D3`; `$81:8AA1`–`$81:8AC0` read `$7F:6031`–`$7F:72C7`, `$81:AD95` `$7F:4031`–`$7F:7ACB`). Frame 1242: 40 DMAs of 64 bytes from banks $16/$17 to VRAM words `$2000`–`$23E0` (BG1 tiles; BG12NBA `$12`, BGMODE `$19`, BG1SC `$0C`, BG2SC `$73`, BG3SC `$18`, OBSEL `$83` at frame 1249). Riding (from 1334): per frame 26 DMAs of 32 bytes from ROM banks $27–$39 to VRAM `$6020`–`$6CD0` (the rider's tiles, unrolled at `$82:B907`–`$82:BDC0`), one DMA of 32 bytes from `$00:0459` with VMAIN `$81` (a tilemap column) and one of 34 bytes from `$00:0437` (a row), plus CGRAM port writes at `$82:D38E`–`$82:D48A` | Windowed capture 1200–1420 with the VRAM/CGRAM/OAM address ports watched to pair each DMA with its destination; whole-WRAM dumps after frames 1243, 1600 and 2400; disassemble the `$81:B9xx` decompressor, `$81:B3xx` column builder and `$82:E1xx` tile loader from the ROM (scratchpad only) |
| 1 | The baseline of the M1-02 handoff reproduces in this worktree | `doctor`, `bootstrap`, `build --preset lab-debug`, `test --suite synthetic`, `reference build` (reports under ignored `artifacts/m1-03/baseline/`) | All exit 0; suite 173 checks passed in 27.5 s; core build 9.8 s | Capture the whole race access record with the block-move site watched and frame images at 1600 and 2400 |

## Handoff

- Current base/head commit and uncommitted state:
- Verified findings:
- Current hypothesis and failed approaches:
- Commands executed, outcomes and report hashes:
- Unavailable/skipped checks:
- Exact next experiment/command:
- Remaining dependencies:
- Runtime needs (network, build time, fixtures, memory):
- Elapsed work and provider usage when known:

## Review and integration

- Reviewer and independent reproduction/withheld-case results:
- Required changes or acceptance rationale:
- Exact merge candidate and required-check results:
- Integrated commit and evidence location:
- Scope still unverified:
