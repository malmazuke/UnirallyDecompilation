# M0-06 — M0 acceptance report

## Assignment

- Status: ready (not claimed)
- Milestone: M0
- Coordinator: primary agent
- Worker/session/runtime/model: unassigned; a fresh session dispatched with the portable prompt in `docs/AGENT_WORKFLOW.md` and nothing else. When claimed, the coordinator writes the claim into this record and into the registry row of `tasks/README.md` in the same commit (M0-05 gap G1)
- Reviewer: fresh independent session, own clone
- Dependencies and evidence of acceptance: M0-00 through M0-05 accepted (see [the registry](README.md)); evidence records R-0001 to R-0004 and decision D-0001
- Base commit: `main` at dispatch time; record the hash here when claimed
- Branch and isolated worktree: `task/M0-06-acceptance-report`, own clone or worktree
- Owned paths and shared interfaces: `docs/research/R-0005-*` (the acceptance report), this record; `tests/tooling/test_replay.py` only for the one follow-up test named below; must not change any command, schema, manifest, the pinned core or `src/lab`
- Claim/lease/heartbeat/checkpoint location: this file and the registry row; checkpoint commits at least every 10 minutes
- Session time limit, concurrency allocation and actual spend authorization if relevant: bounded sessions of 45 minutes; one worker; no service purchases

## Outcome and boundaries

Assemble the M0 acceptance report: for every M0 gate in [the project plan](../docs/PROJECT_PLAN.md) and [build and validation](../docs/BUILD_AND_VALIDATION.md) (clean setup, repeated reference capture, deliberately changed input, comparator/report failure path, timeout and missing-prerequisite handling, restart after interruption, a failed check reported accurately, a task resumed from its persisted record) the evidence that meets it, with the machine/tool/ROM/core identities and the exact reproducible commands, taken from R-0001 to R-0004 and the task records; any gate without evidence is listed as unmet and blocks acceptance rather than being argued around. Re-run the recorded checks once on the report's own commit so the report describes a state that was actually observed. Update the next-stage estimates in the project plan from the observed effort of M0-01 to M0-05 (elapsed work is in each task's handoff). One bounded test follow-up from the M0-05 review: cover the `divergence_localized: failed` branch of `replay compare` (`consistent_with_first_runs` false) with a stubbed-worker case, so that forcing it true in `_localize` is caught.

Not in scope: new commands, gameplay work (M1), a scheduler, changes to any format.

## Inputs and prerequisites

- ROM as in M0-03 (`local/rom-location.txt`, manifest `tests/manifests/rom/unirally-pal.json`), built pinned core (`reference build`), replay manifests in `tests/manifests/replay/`.
- Runtime needs (M0-05 gap G5): network access for the pinned wheels and the core clone; about 15 s of core build; the ROM path arrangement above.
- Known baseline failures: none. Known limits: the state-origin manifest needs its local state regenerated (its `regeneration_command`); `av_identical` is informational after a restore; saving at boot-sequence points 29–96 perturbs the run (R-0002); CI is ROM-free (G7).

## Acceptance

| Criterion | Command or experiment | Expected result | Required artifact |
| --- | --- | --- | --- |
| Every M0 gate has evidence or is listed as unmet | Gate-by-gate table citing the record, commit, command and report digest | No gate is described as met without a cited observation; unmet gates block M0 acceptance | `docs/research/R-0005-*` |
| Identities and commands are reproducible | Re-run of the recorded checks on the report's commit | Same exit codes and digests as the cited records (`583d1ec544ec61a2…`, `f210eecacf63cbeb…`, frame 300, byte `0x00073`, 117+ synthetic checks) | Reports |
| Follow-up test | Stubbed-worker case for `divergence_localized: failed` | Suite grows by at least one check, green on both CI platforms; forcing `consistent_with_first_runs` true is caught | Test, CI run |
| Estimates updated from observation | Project plan next-stage estimates revised from the elapsed work in the M0 task handoffs | Estimates cite the observed figures; no calendar or cost promise beyond them | Plan diff |
| Independent review of the exact candidate | Reviewer reproduces on the candidate commit; CI on that commit | Approve, or specific reproducible findings addressed | Record |

## Evidence and attempts

| Attempt | Hypothesis | Experiment | Observation | Next decision |
| --- | --- | --- | --- | --- |

## Handoff

Not started. First command when claimed: `python3 tools/project.py doctor && python3 tools/project.py bootstrap && python3 tools/project.py build --preset lab-debug && python3 tools/project.py test --suite synthetic` (expect 117 checks), then with the ROM `python3 tools/project.py reference build` and `python3 tools/project.py replay compare --manifest tests/manifests/replay/boot-start-600.json` (expect exit 0, sample digest `583d1ec544ec61a2…`).

## Review and integration

Pending.
