# M4-00 — Original-game feature coverage inventory

## Assignment

- Status: review
- Milestone: M4 — Original game coverage
- Coordinator: OpenAI Codex Sol/medium (`/root`)
- Task provider: OpenAI
- Worker/session/runtime/model: coordinator acting as the single worker in this
  isolated worktree; Codex desktop, Sol/medium
- Actual model/reasoning effort and routing rationale: Sol/medium for coordination
  and durable edits; one bounded Astra/high consultation, explicitly requested by
  the user, audits the milestone-level classifications and next-task isolation
- Provider quota: weekly bucket sampled 13 September 2026 at 43% used / 57%
  remaining; preserve the final 20%, and stop discretionary implementation at
  63% used under the 20-point session allowance. Account-wide movement may
  include unrelated work. No reset redemption or spending is authorized.
- Reviewer: fresh sequential OpenAI reviewer required after the candidate
- Dependencies: accepted M3 at annotated tag `m3`, commit `09ce40e`
- Base commit: `09ce40e1ca785a7eaff29626835dfdc3140afb2c`
- Branch/worktree: `task/M4-00-feature-inventory` at
  `.worktrees/m4-00-feature-inventory`
- Owned paths: `README.md`, `tasks/M4-00.md`, `tasks/M4-01.md`, `tasks/M4-02.md`,
  `docs/research/R-0018-m4-feature-inventory.md`, coordinator registry/state and
  compact handoff updates; no gameplay, pack, presentation, reference-core or
  frozen-manifest changes
- Claim/lease/heartbeat: this record and the registry row are the claim; one
  worker only, no lease store or concurrent reference process
- Session limit: 45-minute reassessment with durable checkpoint before a costly
  experiment; no paid execution

## Outcome and boundaries

Produce an evidence-backed matrix of what the identified PAL original and the
native implementation cover across tracks, riders/opponents, modes, player
counts, menus/progression, persistence, audio, presentation and selected desktop
platforms. Keep four states distinct: accepted native coverage, reference-only
observation, names/options merely observed in a menu, and unobserved scope.

Compare the smallest accepted-slice closure with the first breadth experiment,
then write bounded work orders and choose their order while retaining the
accepted M3 identities and regressions. This is a
research/planning task: it does not expand the Classic pack, implement another
track, change frozen expectations, claim the full feature set, publish or deploy.

## Inputs and prerequisites

- Tracked M0–M3 task, decision and research records at `m3`.
- Tracked replay/content/native/presentation manifests and current source
  rejection boundaries.
- The supported PAL ROM is not required because this task inventories existing
  evidence; no new claims about ROM contents may be made without a new capture.
- Baseline failures: none recorded at `m3`.

## Acceptance

| Criterion | Command or experiment | Expected result | Required artifact |
| --- | --- | --- | --- |
| Repository baseline is applicable | `python3 tools/project.py doctor`, bootstrap/build, and `python3 tools/project.py test --suite synthetic --preset app-debug` | Required checks pass after clean-worktree prerequisites are installed; the initial missing-prerequisite result is recorded, not waived | ignored reports and hashes in handoff |
| Coverage matrix is evidence-bound | Cross-check every row against tracked manifests, M0–M3 evidence and current source boundaries | Each positive claim cites an exact record/path; menu labels are not promoted to implemented features; unknown population counts remain unknown | `docs/research/R-0018-m4-feature-inventory.md` |
| M3 boundary is unchanged | Inspect `git diff --name-only m3...HEAD` and `git diff --check` | Only owned documentation changes; no ROM, generated pack, capture, state, baseline or source change | task handoff |
| Next dependencies are bounded and ordered | Compare candidate dimensions by established evidence, variables changed and prerequisite depth | M4-01 closes the already-observed DRAGSTER loser-result presentation rejection; M4-02 is a reference-only second-track discovery with a gate for coupled opponent/event changes | `tasks/M4-01.md`, `tasks/M4-02.md` |
| Entry documentation is current | Compare `README.md` against accepted M3 state, implemented command inventory and M4 handoff | README names M3 as accepted, identifies the exact playable boundary, points to M4-00/M4-01, and does not advertise proposed M4 commands | `README.md` |
| Reviewability | A fresh reviewer follows citations, challenges one matrix classification and checks the proposed experiment's isolation | No unsupported claim or broadened acceptance boundary remains | review record (pending) |

## Evidence and attempts

| Attempt | Hypothesis | Experiment | Observation | Next decision |
| --- | --- | --- | --- | --- |
| 1 | Existing M3 records are sufficient to bound M4 work without running the ROM | Inventory tracked manifests, evidence claims and explicit source rejection strings | The matrix is supportable, but the first draft conflated loser gameplay/presentation and assumed the alternate track isolated one variable | Correct through the bounded audit; write separate M4-01 closure and M4-02 discovery orders |
| 2 | A milestone inventory benefits from frontier scrutiny before its sequence becomes durable | Bounded Astra/high audit of classifications, missing dimensions and the proposed one-variable experiment | The audit found that loser gameplay and loser-result presentation had been conflated, and that a second named track may couple opponent/event rules | Make loser-result presentation M4-01; retain track discovery as M4-02 with a coupling gate |
| 3 | The clean worktree can run the full ROM-free M3 suite without copied build state | Doctor, then an intentionally attempted pre-bootstrap suite, followed by tracked bootstrap/build and an exact rerun | The first suite exited 2 with missing toolchain/local prerequisites and one failed fixture-directory test; after bootstrap/build, all 314 required records passed | Record both outcomes; do not call the first run a pass |

## Handoff

- Current base/head and state: base `09ce40e`; claim commit `7e1ffed`;
  documentation candidate commit follows this handoff.
- Verified findings: R-0018 records the M3 feature matrix. The user-requested
  Astra/high audit caught the unaccepted loser-result rendering boundary and the
  alternate-track coupling risk; both changed the task order.
- Commands and reports: `doctor` passed required checks (report SHA-256
  `88a321a3...6db5`). The pre-bootstrap suite correctly exited 2 with missing
  prerequisites and one failed local-directory fixture setup
  (`52b5a14d...9d0a`). `bootstrap` passed (`685b5369...5c94`), `build --preset
  app-debug` passed (`080008bf...a6d`) and the rerun passed 314/314 required
  records (`ebc1b35a...d261`). `git diff --check` passes.
- Unavailable/skipped checks: independent review is pending; no ROM run was in
  scope. The first suite's five required skips are not acceptance evidence and
  were replaced only after the missing toolchain was bootstrapped.
- Exact next action: fresh reviewer challenges at least one R-0018
  classification, README status and M4-01/M4-02 sequencing on the immutable
  candidate, then coordinator integrates or returns findings.
- Remaining dependencies: fresh independent review before coordinator acceptance.
- Runtime needs: local toolchain for the ROM-free suite; no ROM, network or display.
- Aggregate time/usage: Sol coordinator plus one bounded Astra/high audit;
  weekly bucket moved from 43% to 44% used during this account-wide interval;
  unrelated work may contribute, so the one-point change is not attributed
  precisely to this task.

## Review and integration

- Reviewer results: pending.
- Exact candidate and required checks: pending.
- Integrated commit: pending.
- Scope still unverified: every feature outside the accepted M3 slice except the
  limited menu labels and reference behavior explicitly cited by R-0018.

Only the coordinator marks this task accepted after independent review.
