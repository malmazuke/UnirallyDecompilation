# M4-15 preparation documentation review

- Reviewer: fresh independent `gpt-5.6-sol`/medium session.
- Candidate: `b3f3b567764e6c7b71312ec75587628554bab8a2`.
- Base: `38c72e889d59b62270be6e26fca713b3fb3aa153`.
- Review branch/worktree: `codex/review-m415-prep`,
  `.worktrees/m4-15-preparation-review`.
- Scope: documentation/configuration preparation only; candidate files were
  treated as immutable. No gameplay, reference capture or native validation.

## Decision

**Approved. No material findings.**

The task, handoff, state, plan, registry and policy records consistently target
a complete native ZOOM ZOO race from the authentic end-1649 seed. They require
original reference exploration and authenticated escape/progress/finish evidence
before native tuning, preserve the accepted pre-seed history, and reject a later
seed, captured repair, CPU fallback or a longer run trapped in the same section.
The exact accepted domain remains bounded: completion from the existing seed,
not race-start initialization, frontend support or general track coverage.

The downstream fallback does not provide an easy-prefix loophole. It requires
evidence-led original exploration that fails to establish completion, recorded
strategy families and failure reasons, authenticated escape, multiple distinct
downstream markers, a substantial repeatable horizon frozen before native tuning,
and independent review of the rationale. Time pressure, native difficulty and
short prefixes are explicitly insufficient. Any accepted fallback must remain
labelled partial progression while the full-race objective stays outstanding.

Acceptance is robust against fixture-specific success. Both riders must match
every declared future-relevant byte through the frozen outcome with no later
captured dynamic inputs; restores span escape, new modes, lap/finish transitions
and late continuation. The fresh reviewer owns two material untuned variations,
each repeated twice in the original before native evaluation and each reaching
the same declared outcome. A variation used for tuning becomes a regression and
must be replaced by a fresh untuned case. Existing M4-12--14 and broad product
gates, sanitizer coverage, negative controls and exact merge/CI checks remain.

The audit preflight and validation ledger are narrow M4-15 requirements rather
than claims of implemented stable commands. The preflight checks the complete
expanded two-controller timeline before expensive capture and authenticates the
supplemental audit against whole-WRAM reference hashes afterward. The ignored
ledger records only evidence identities needed for safe result reuse, invalidates
affected entries after changes, and explicitly cannot replace fresh reference,
withheld, review, sanitizer, merge or CI evidence. The text rejects a cache
service or general orchestration framework, so it does not create an avoidable
infrastructure project.

D-0004, D-0006, AGENTS, workflow and task records consistently assign direct
Astra/medium ownership, one fresh Sol/medium reviewer in an isolated checkout,
and automatic correction/re-review/integration. The M4-15 exception waives the
20-point cap while preserving the final 20% weekly reserve and fresh quota
sampling. It authorizes no reset, purchase or provider switch. Historical
M4-14 stop language is superseded explicitly, and every current handoff boundary
stops before M4-16.

## Checks

- Inspected all 13 changed paths and the full diff against the accepted base.
- `git diff --check 38c72e8..b3f3b56` passed.
- All 124 local Markdown link targets and all eight fragment anchors in the
  changed documents resolve.
- `.codex/config.toml` parses; its candidate change is comment-only and leaves
  the actual model defaults unchanged.
- The changed range contains no source, test, fixture or frozen gameplay evidence
  paths.
- `origin/main` resolved to the accepted base, and GitHub Actions run
  `34761303975` was completed successfully at that exact SHA during review.
- Game/build suites were not run for this documentation-only review. Their
  historical results were not counted as candidate gameplay validation.

No candidate file was changed by this review.
