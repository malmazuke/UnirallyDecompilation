# M3-02A — Classic content pack and ROM extraction

## Assignment

- Status: planned
- Milestone: M3, prerequisite to native presentation/frontend integration
- Coordinator: assigned at claim
- Task provider (fixed for all children; record any user-initiated platform change): assigned at claim
- Worker/session/runtime/model: OpenAI Codex, Sol/medium unless D-0004 routing is revised at claim
- Actual model/reasoning effort, routing rationale and frontier escalation question (if any): pending
- Provider quota window/baseline timestamp, used/remaining or unknown, reserve and session allowance (D-0004): resample at claim; preserve the 20% reserve and record the checkpoint before work
- Reviewer: fresh independent session after a candidate exists
- Dependencies and evidence of acceptance: accepted M1 content decode and M2 content boundary; accepted [D-0005](../docs/decisions/D-0005-classic-content-distribution.md); integrate accepted M3-01 before changing shared native command/runtime paths
- Base commit: current `main` after M3-01 integration
- Branch and isolated worktree: `task/M3-02A-classic-content-pack`, to be created at claim
- Owned paths and shared interfaces: new content-pack schema/extractor/reader and authored fixtures; additive project CLI wiring; this task's records. Coordinate changes to shared native commands, `docs/STATE.md`, `docs/BUILD_AND_VALIDATION.md` and the task registry through the integrator.
- Claim/lease/heartbeat/checkpoint location: record in this file at claim
- Session time limit, concurrency allocation and actual spend authorization if relevant: 45-minute checkpoints; one worker by default; no spending, publication or deployment authorized

## Outcome and boundaries

Implement the D-0005 boundary for the accepted PAL ROM: one supported ROM is
verified and converted locally into a deterministic, versioned Classic content
pack whose entries are addressed by logical IDs. The native runtime can load
the validated pack without a ROM or loose extraction tree being present.

Keep the first schema limited to content required by the M3 Crawler/DRAGSTER
slice, while allowing M3-02 to add presentation entries without exposing ROM
offsets to the renderer. Preserve the existing directory-backed loader where it
is useful for research, but give it and the pack reader the same logical content
contract.

Not in scope: extracting every game asset, adding another ROM revision, public
release, legal clearance, replacement-art creation, a general mod marketplace,
SDL UI, renderer implementation, audio, or changing M3-01 gameplay behavior.

## Inputs and prerequisites

- PAL headerless 2 MiB ROM SHA-256
  `a1105819d48c04d680c8292bbfa9abbce05224f1bc231afd66af43b7e0a1fd4e`.
- Accepted content manifests and decoder from M1/M2; the thirteen-file M2
  inventory is the minimum gameplay set.
- Ignored `local/rom-location.txt`, `local/native/` and task-scoped ignored
  artifacts. No private input or generated pack may become tracked.
- A native playable-start state contract must replace the public dependency on
  the research-only WRAM/SRAM seed importer. The exact start boundary is chosen
  and evidenced within this task or split as its smallest explicit prerequisite.
- Known baseline failures: none recorded. The commands below are proposed and
  do not exist until this task implements them.

## Acceptance

| Criterion | Command or experiment | Expected result | Required artifact |
| --- | --- | --- | --- |
| Exact ROM gate | Proposed `python3 tools/project.py content pack --rom <PAL-ROM> --out <ignored-pack>` and the same command with a wrong/truncated/headered input | Supported original bytes pass; every other tested identity fails before output is committed | JSON reports with ROM identity and distinct invalid-input/missing-prerequisite outcomes |
| Deterministic atomic pack | Generate twice in separate empty ignored directories; interrupt one generation before commit | Successful packs are byte-identical with the same schema/entry manifest; interrupted output is absent or unmistakably invalid | Pack hashes, entry table and interruption report |
| Complete scoped inventory | Inspect the pack with proposed `content pack-inspect` and compare it with the M3 content manifest | Every required gameplay entry is present once with its logical ID, size and hash; presentation extension points are versioned | Tracked schema/manifest and private extraction report |
| ROM-independent runtime | Launch the native M3 runner using the generated pack, move the ROM away, then launch again | Both runs produce identical canonical states and projections; the second run performs no ROM access | Native compare/restore reports and input inventory |
| Public start state | Start the playable slice from a clean local installation with no WRAM/SRAM capture or emulator state | Native start state is reproducible, semantic, serializable and reference-checked; private captures are validation inputs only | Start-state contract, authored checks and private comparison report |
| Corruption/version handling | Mutate header, manifest, one entry and schema/rules/source identities | Each mutation is rejected with the responsible identity; no partial content is consumed | ROM-free mutation tests |
| ROM-free public checks | Run `python3 tools/project.py test --suite synthetic` with no `local/` directory | Pack parser/writer, logical lookup and failure paths pass using authored fixtures | Standard suite report on macOS and Linux |
| Repository hygiene | Inspect the staged tree and run an additive tracked-content audit | No ROM, normalized copy, generated Classic pack, loose original asset, save, capture or trace is tracked | Audit report and staged diff |

## Evidence and attempts

| Attempt | Hypothesis | Experiment | Observation | Next decision |
| --- | --- | --- | --- | --- |
| 0 | The existing named content spans can be assembled behind one logical pack without changing M3-01 simulation behavior | Pending after M3-01 integration | Not run | Reproduce M3-01 and inventory every runtime content read before freezing schema 1 |

## Handoff

- Current base/head commit and uncommitted state: not claimed
- Verified findings: D-0005 and accepted M1/M2 content boundaries only
- Current hypothesis and failed approaches: no implementation attempted
- Commands executed, outcomes and report hashes: none
- Unavailable/skipped checks: all acceptance checks pending
- Exact next experiment/command: after claim, reproduce the accepted native compare/restore cases and enumerate content reads from the runner against all existing content manifests
- Remaining dependencies: accepted M3-01 integration; no user access dependency beyond the already configured private ROM fixture
- Runtime needs (network, build time, fixtures, memory): existing toolchain/core and ignored PAL ROM; no new service
- Aggregate parent/child time, provider usage before/after (or unknown), other-account-work caveat: pending
- Accepted outcome, review/fix rounds and next routing decision: pending

## Review and integration

- Reviewer and independent reproduction/withheld-case results: pending
- Required changes or acceptance rationale: pending
- Exact merge candidate and required-check results: pending
- Integrated commit and evidence location: pending
- Scope still unverified: all M3-02A behavior

Only the coordinator marks accepted after integration and evidence checks.
