# M0-02 — Reproducible native/tooling bootstrap

- Status: review
- Coordinator: primary agent
- Base: `776824a` (M0-01 merge candidate; provides `tools/project.py`, the report schema and exit codes).
- Branch: `task/M0-02-laboratory`, primary checkout. Single sequential worker; the separate native worktree `.worktrees/native` on `task/M0-02-native` was never used and holds no commits beyond `7ce358e`. It can be removed by the user; the worker did not delete it.
- Session: checkpoint at least every 10 minutes; review at the end of this bounded implementation turn.
- Prerequisite: M0-00 baseline.
- Budget: current authorized interactive session; no additional service purchases.

## Work and ownership

Primary agent owns `tools/`, Python tooling tests, tool locks, schemas, CI and coordinator documentation. A native worker may own CMake files, `src/lab/`, and native tests in a separate worktree. Agree interfaces before dispatch. A reviewer inspects the exact integrated candidate and reproduces tests.

## Acceptance

1. `doctor`, `bootstrap`, `build`, `test --suite synthetic` and structured reports exist.
2. CMake/Ninja versions and download hashes are locked; bootstrap is isolated and idempotent.
3. A synthetic native runner builds and tests on this macOS host and Linux CI. It is explicitly not recovered game code.
4. Reports identify source/tool/input state and distinguish missing prerequisites, failures and timeouts from passes.
5. Clean-checkout bootstrap/build/test and a repeated bootstrap succeed; tooling error-path tests succeed.
6. Independent review finds no unresolved material defects; actual merge candidate is verified.

## Handoff

- Current base/head commit and uncommitted state: base `0d0a60a` (main after M0-01); head is the commit that adds this section on `task/M0-02-laboratory`, on top of `2d34fab` which holds the implementation. No uncommitted state after that commit. Branch pushed to origin.
- Verified findings (all on commit `2d34fab`):
  - Bootstrap fetches pinned CMake 3.31.10 and Ninja 1.13.2 wheel archives from PyPI, verifies SHA-256 against `tools/locks/toolchain.json`, extracts into ignored `local/toolchain/` and writes `local/toolchain/manifest.json`. First run on this host took 4.2 s; the repeat run reported `source=cache; extracted=False` in 0.3 s. Nothing is installed globally.
  - Synthetic probe hash after 1000 scripted steps is `0be347c529fadda9` on: this host (macOS 15.7.2 arm64, AppleClang 17.0.0.17000404 via the preset; Homebrew clang 19.1.7 at -O0/-O2/ASan+UBSan in a manual probe), GitHub `macos-15` (AppleClang 17.0.0.17000013), GitHub `ubuntu-24.04` x86_64 (GNU 13.3.0, debug and address/undefined sanitizers). Three fresh processes agree on every host.
  - The report pipeline distinguishes outcomes: the `lab-failure-probe` preset yields one `failed` and one `timeout` ctest check and exit code 4; a missing toolchain or build yields `missing` and exit 2; a digest mismatch yields `failed`, exit 1, and the bad download is discarded; an empty lock or unknown suite yields exit 3. A nested `test` invocation with Python tests enabled is refused with exit 3 (found the hard way: the first probe run recursed without bound and had to be killed).
  - Clean-checkout path: a fresh `git clone` of the branch into scratch (no `local/`, no cache) ran `doctor`, `bootstrap` (downloaded), `build --preset lab-debug`, `test --suite synthetic` with 35/35 passed; report `source.dirty` false at `2d34fab`.
- Current hypothesis and failed approaches: `urllib` cannot verify PyPI on this host's python.org build (no CA bundle), so the downloader prefers `curl` and records which was used. Ninja 1.12.1 has no PyPI wheel; 1.13.2 was pinned instead.
- Commands executed, outcomes and report hashes (local reports under ignored `artifacts/m0-02/`, clean-clone reports SHA-256: bootstrap `561e1217…61c2`, build `c88944fc…bbb`, doctor `fcfbcc1b…2f9`, test `4a6ce1a1…0824`):
  - `python3 tools/project.py doctor --report artifacts/m0-02/doctor-after.json`: passed; isolated tools detected.
  - `python3 tools/project.py bootstrap --report …` twice: passed; second run cache-only.
  - `python3 tools/project.py build --preset lab-debug --clean --report …`: passed (configure 0.6 s, build 0.5 s).
  - `python3 tools/project.py test --suite synthetic --preset lab-debug --artifacts artifacts/m0-02/test-debug --report artifacts/m0-02/test-debug.json`: passed, 35 checks (26 Python tooling checks including the native failure/timeout probe, 5 ctest checks, build availability, fresh-process repeatability), exit 0.
  - `build`/`test --preset lab-sanitize --no-python-tests`: passed natively on macOS.
  - CI run 34466886628 on `task/M0-02-laboratory`: both jobs green in 17 s (macOS) and 31 s (Linux); artifacts `reports-macos-15` and `reports-ubuntu-24.04` hold doctor, two bootstrap, build and test reports plus junit and build-info files. Both idempotency assertions passed in the workflow.
- Unavailable/skipped checks: Windows untested (later target). Linux aarch64 artifacts are locked but no aarch64 host has run them. Docker unused. Bit-identical build reproducibility is not claimed; the report records compiler identity instead.
- Exact next experiment/command: reviewer reproduces `python3 tools/project.py bootstrap && python3 tools/project.py build --preset lab-debug && python3 tools/project.py test --suite synthetic` on a clean clone, then tampers with one lock digest and one native test expectation to confirm the failure paths report honestly.
- Remaining dependencies: none for M0-02. M0-03 needs M0-01 (done) and this task.
- Elapsed work and provider usage when known: about 75 minutes of one interactive session including the recursion incident; usage not metered.

## Review and integration

- Reviewer and independent reproduction/withheld-case results: pending
- Required changes or acceptance rationale: pending
- Exact merge candidate and required-check results: pending
- Integrated commit and evidence location: pending
- Scope still unverified: Windows; Linux aarch64; any real emulator or game code (M0-03 onward)
