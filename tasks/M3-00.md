# M3-00 — Complete-race reference evidence and playable-slice inventory

## Assignment

- Status: claimed
- Milestone: M3 prerequisite for M3-01
- Coordinator: current Codex session, OpenAI Sol/medium
- Task provider (fixed for all children): OpenAI
- Worker/session/runtime/model: fresh Codex child, OpenAI `gpt-5.6-sol`, medium
  reasoning; exact agent identity is recorded in the first handoff checkpoint
- Actual model/reasoning effort and rationale: Sol/medium for bounded reference
  research and routine tooling under D-0004; no frontier escalation authorized
- Provider quota baseline: 12 September 2026 15:31 AEST, Codex seven-day
  window 40% used / 60% remaining, reset Unix time `1789780970`; retain the
  final 20%, checkpoint if this session reaches 50% used; account-wide change
  may include unrelated work
- Reviewer: fresh sequential OpenAI Sol/medium session after a candidate
- Dependencies: M2 accepted at `a369026` (tag `m2`); R-0006 through R-0011;
  accepted cold-start Crawler/DRAGSTER replay and pinned reference core
- Base commit: `a369026cd61e6250f941bc17c7a81266232ec017`
- Branch and isolated worktree: `task/M3-00-finish-evidence` in
  `.worktrees/m3-00-finish-evidence`
- Owned paths and shared interfaces: new M3 replay/reference/field manifests;
  new finish-analysis tooling and its tests under `tools/unirally_lab/` and
  `tests/tooling/`; new `docs/research/R-0012-*`, optional additive state/content
  records, and this task's handoff. Existing manifests, expected digests, core
  patch/lock, `src/core/`, `docs/STATE.md`, and `tasks/README.md` are frozen.
- Claim/lease/heartbeat/checkpoint: claimed 12 September 2026 15:34 AEST in
  this committed record; checkpoints in this file and ignored
  `artifacts/m3-00-*`; coordinator confirmed no live reference worker before
  assignment
- Session limit and authority: reassess each 45 minutes and checkpoint at least
  every 10 minutes; coordinator plus one worker only. Task-scoped local commits
  and private CI are authorized. No purchase, reset redemption, publication,
  deployment, provider switch, or tracked ROM/extracted-content artifacts.

## Outcome and boundaries

Extend the reviewed Crawler/DRAGSTER cold-start reference path through an actual
race finish. Establish the exact finish/race-state frame ranges and writer PCs,
the additional executed code and accessed content, and whether the accepted
decoded track buffer covers the whole race. Inventory evidence-backed native
dependencies for completion, controller handling, rider presentation, and a
minimal frontend, then split M3-01 along the observed interfaces.

This is a reference-research task, not native gameplay acceptance. It must not
add speculative frontend/game abstractions, claim full trick/render/audio
coverage, execute original CPU code behind a supposedly native result, alter
the 333-byte M2 state format without evidence, or implement newly observed
native behavior before its reference contract is frozen.

## Preregistered experiment

The baseline preserves the accepted menu path and riding inputs through frame
2999, then continues holding Right until a bounded maximum of 12,000 PAL frames.
The first exploratory run may stop earlier only after frame images and observed
state prove that the race has finished and a stable result/post-race state has
been reached. No finish address, frame, outcome, or required duration is assumed.

Before either replay is run, freeze a controller variation with Right released
for frames 3000–3299 and held again from frame 3300 to the same maximum. The
preregistered predictions are: both scenarios agree through frame 2999; the
first difference is the controller image at frame 3000; player speed/progress
subsequently differs; if both complete the race, the variation does not finish
earlier than the continuously accelerated baseline. A failure to finish within
the bound or an opponent-first result is an observation, not grounds to rewrite
these predictions after the run.

## Inputs and prerequisites

- PAL ROM manifest `tests/manifests/rom/unirally-pal.json`, SHA-256
  `a1105819d48c04d680c8292bbfa9abbce05224f1bc231afd66af43b7e0a1fd4e`;
  machine-local path from ignored `local/rom-location.txt`.
- Pinned bsnes core commit `7d5aa1e656b9171524d01b1b22917197d8121cb4`,
  patch SHA-256 `a719f5ffe2222dad4c1ab04336633319ad85004f74e32fc14893a058be333885`.
- Accepted baseline `tests/manifests/replay/race-crawler-dragster-3000-fields.json`,
  sample digest `72f618f2f7e3416eac64d38f882471b1a9ebee25018ed76143360a92cdd99b1f`.
- Track/content identity in `tests/manifests/content/dragster-segment.json` and
  R-0008. Large samples, traces, screenshots, decoded content, and states remain
  ignored and must be reproducible from the manifests and commands in R-0012.
- Baseline failures must be recorded before work; missing ROM/core/content is a
  prerequisite failure, never a skipped pass.

## Acceptance

| Criterion | Command or experiment | Expected result | Required artifact |
| --- | --- | --- | --- |
| Existing baseline | `doctor`, debug build, synthetic suite, reference build, then replay comparison on `race-crawler-dragster-3000-fields.json` | Required checks pass and accepted digest is unchanged | JSON reports with exact source/input identities |
| Complete-race baseline | Two fresh reference processes run the new continuous-Right manifest | Identical declared fields and A/V; an observed finish plus stable post-race interval occurs within the declared bound, or the bounded non-finish is reported as a failed hypothesis | Frozen replay manifest, report, samples hashes, selected frame images |
| Preregistered variation | Compare the delayed-acceleration manifest against the complete-race baseline | Agreement through 2999, first input/state divergence at 3000, measured progress/speed and finish timing reported without changing the predictions | Manifest and comparison/divergence reports |
| Finish contract | Trace/access capture around the first finish transition and at least 120 frames on each relevant side | Exact frame range, sampled values, writer PCs, update order, instances, and tested domain for finish/result/race state | R-0012 plus compact schema or state record |
| Coverage/content delta | Coverage map and access/content provenance relative to the 3000-frame scenario | New executed ranges/edges and content inputs are enumerated; whole-race sufficiency of the accepted decoded track buffer is answered with evidence | Machine-readable delta and R-0012 |
| Playable dependency inventory | Evidence review of controls, rider presentation, renderer and completion | Each dependency is observed, explicitly unknown, or deferred; M3-01 is split only at measured interfaces | R-0012 and proposed registry/task amendments |
| Regression and independent review | ROM-free suite plus exact-candidate reproduction in a separate checkout with one reviewer-owned boundary case | No required failure/skip; reviewer confirms claims and limitations | Reports and review section in this task |

## Evidence and attempts

| Attempt | Hypothesis | Experiment | Observation | Next decision |
| --- | --- | --- | --- | --- |
| 0 | Existing M2 reference inputs remain reproducible and a continuously accelerated rider can reach a finish within 12,000 frames | Preregister this task before creating/running extended manifests | Pending | Reproduce baseline, then freeze both new manifests before their first run |

## Handoff

- Current base/head and state: base `a369026`; administrative claim pending its
  own commit; no task implementation yet
- Verified findings: only the accepted M2/R-0011 domain; no complete-race claim
- Current hypothesis: continuous Right after frame 2999 reaches an observable
  finish by frame 11,999; delayed acceleration moves that finish no earlier
- Commands executed: repository/document inspection, quota sample, orphan
  process check; no new replay or gameplay experiment
- Unavailable/skipped checks: none assessed yet
- Exact next command: create the isolated worktree from the administrative
  claim, reproduce `doctor` and the accepted 3000-frame fields comparison, then
  add both frozen extended manifests before running either
- Remaining dependencies: none requiring user access; ROM pointer and core are
  present on this host
- Runtime needs: local ROM/core, bounded fresh processes, several GB of ignored
  artifacts possible; no network expected after bootstrap
- Aggregate time/usage: coordinator start only; baseline 40% used, 60% remaining
- Acceptance/review: not reviewed; only the coordinator may mark accepted

## Review and integration

- Reviewer and independent results: pending
- Required changes or rationale: pending
- Exact candidate and checks: pending
- Integrated commit/evidence: pending
- Scope still unverified: the entire outcome until the task passes review
