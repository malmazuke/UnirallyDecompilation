# M4-02 — Second-track reference discovery and coverage delta

## Assignment

- Status: accepted 13 September 2026 after one returned reproducibility finding,
  approved focused re-review and green macOS/Linux hosted validation
- Milestone: M4 — Original game coverage
- Coordinator: current OpenAI coordinator
- Planning consultation: bounded OpenAI Astra/high before claim because this
  task freezes a new replay and coverage evidence; order the navigation/coupling
  experiments and identify false-isolation risks under D-0004
- Task provider/model: OpenAI, Sol/medium coordinator/worker in an isolated
  worktree after the planning checkpoint; sample quota at claim under D-0004
  and do not spend or redeem reset credits
- Provider quota: sampled before planning on 13 September 2026 at 50% weekly
  used / 50% remaining; preserve the final 20% and stop discretionary work at
  70% used under the task's 20-point guardrail. Account-wide movement may
  include unrelated work. No spending or reset redemption is authorized.
- Reviewer: fresh sequential OpenAI Sol/medium reviewer with one withheld input
- Dependencies: accepted M4-00 and M4-01; accepted M3/tag `m3`; supported PAL
  ROM and pinned reference core locally available
- Base commit, branch/worktree and heartbeat: `2b40aea`,
  `task/M4-02-second-track`, `.worktrees/m4-02-second-track`; durable checkpoint
  after navigation/coupling, replay freeze, coverage/content inventory and at
  least every ten minutes
- Owned paths: new reference/replay manifests, derived coverage map/summary,
  research/task/review records; additive ROM-free tooling tests only if needed
- Frozen paths/interfaces: M3 manifests/expectations, ROM/core identities,
  Classic pack and native source

## Outcome and boundaries

Move one displayed CRAWLER track name from **label only** to a reproducible
reference observation. Prefer ZOOM ZOO as the next displayed selection, while
retaining PAL, `1P`, MIKE and CRAWLER. Before calling this a track-only
comparison, observe the selected opponent and event/rule screen. If either
necessarily changes, record the coupling and choose another same-event candidate
or create the smallest prerequisite; do not silently broaden the claim.

Freeze a cold-start replay through at least 30 seconds of riding, derive a
coverage delta against DRAGSTER and inventory the new load/content seam. Do not
implement native support, expand the pack, complete the race or claim other
tracks/modes/player counts.

## Acceptance

| Criterion | Experiment | Expected result |
| --- | --- | --- |
| Navigation and coupling gate | Add one bounded directional track-menu input to an ignored copy of the accepted schedule; capture selection and launch screens | Track, 1P, MIKE, CRAWLER, opponent and event/rules are explicitly observed before a manifest is frozen; unexpected coupling is recorded, not normalized away |
| Deterministic replay | Commit discovered inputs, then run two fresh reference processes | Exact declared-field and A/V identity for at least 3000 PAL frames; controller 2 remains silent; timing boundaries are observed rather than copied blindly from DRAGSTER |
| Predeclared perturbation | Change one acceleration/release boundary after the observed race start | Agreement before the input and localized divergence at/after it; field meanings are not assumed when writers/domain differ |
| Coverage delta | Two `coverage capture` runs and `coverage map --baseline` against the DRAGSTER map | Reproducible map with new/lost ranges and entry points; no semantic meaning inferred from coverage alone |
| Field/content inventory | Targeted access/provenance capture around load and riding | Existing fields classified by writer/cadence safety; track-specific sources/destinations and unresolved regions recorded; bytes remain ignored |
| Regression/review | ROM-free suite, selected M3 checks, hygiene, independent reproduction and reviewer perturbation | M3 identities unchanged; no private artifact tracked; review findings resolved |

## First experiment

Before claim, run the bounded Astra planning consultation above and record its
recommendations plus coordinator decisions. Then, at the observed CRAWLER
`PICK TRACK` screen, inject one short Down press with a
clear release before Start. Capture before/after selection, `NOW PLAYING`, load
boundary and first active frame. Change only direction/timing after a failed
hypothesis, preserving input and frame hashes. Freeze tracked inputs only after
the target and any coupled opponent/event changes are known.

Only the coordinator marks this task accepted after independent review.

## Astra planning checkpoint

Bounded OpenAI `gpt-6-astra`/high consultation completed read-only before claim
on 13 September 2026 against accepted M4-01 head `7bafe1e`. Coordinator
decisions:

- Begin every accepted attempt in a fresh reference process with a private,
  empty system/save directory. Record initial WRAM and cartridge-RAM identities;
  do not use a save-state origin or normalize an opponent in memory.
- Add only one short Down press at the confirmed `PICK TRACK` screen, with a
  clear release before Start. Capture the highlighted selection before/during/
  after the press and the complete `NOW PLAYING` screen. Preserve failed input
  and frame hashes, changing only direction or timing after a failed attempt.
- Gate the freeze on the observed track, `1P`, MIKE, CRAWLER, opponent and all
  displayed event/rule information. An unchanged name does not prove unchanged
  AI: inspect the established `$77:0825` initialization/read/write path and
  separate clean initial state from track-induced learning.
- Discover launch acceptance, loading, first visible race, first race update
  and first consumed movement input independently with sparse then adjacent
  frame captures. DRAGSTER frames 1329 and 1500 are hypotheses only. Require at
  least 3000 total PAL frames and 1500 observed active-riding frames, extending
  the run if needed without entering result/progression scope.
- Classify reused fields as supported, provisional or unsafe from writers,
  width, cadence, phase and range before freezing them. Keep whole-WRAM,
  registers and raw ranges diagnostic. Store A/V identity in evidence/reports,
  because replay schema 1 has no separate expected A/V keys.
- Predeclare one acceleration-release change comfortably inside confirmed
  riding. Require identical evidence before it, then input delivery plus a
  subsequent motion or relevant-writer response; reserve a distinct adjacent
  boundary for review.
- Run two complete coverage captures and compare with the accepted 3000-frame
  DRAGSTER map, explicitly treating menu, load-duration and riding-path changes
  as confounders. A matched DRAGSTER control is required only if the task needs
  a stronger track-causal claim.
- Center access/provenance capture on the newly observed load and one short
  riding window, including destination setup and ordered transfer records.
  Record source/destination ranges, sizes and hashes while leaving bytes,
  images and detailed traces ignored.

The strict boundary remains reference discovery for one observed second-track
path. It does not establish native support, a track-only cause when coupling is
unresolved, general AI/SRAM/event semantics, complete race/progression, audio,
other riders/modes/player counts or semantic meaning from coverage alone.

## Handoff

- Current base/head: task evidence base `2b40aea`; worktree began at claim
  commit `1c8657e`; first candidate `c1317eed9ffe4cd476935be627bfa0b4201c4419`;
  committed review record `258e5caa9d6ddcda1ed7411f8aa21bba018a6b83`.
  The corrected review candidate is the record-only submission commit containing
  this handoff; its exact hash is returned to the coordinator. Candidate changes
  are limited to two replay manifests, one equivalent reference script, one
  reference-only content manifest, a coverage map/summary, [R-0020](../docs/research/R-0020-zoom-zoo-reference.md)
  and this task record. No native source, Classic-pack manifest, accepted M3
  expectation, core/patch/lock or coordinator registry/state path changed.
- Verified result: the frozen 3,300-frame ZOOM ZOO path preserves the displayed
  `1P`, MIKE, CRAWLER, BRONSEN and three-lap Race coupling. Two fresh runs are
  exact on all declared fields, final state and A/V (`791a786370913136...`,
  `2a843314d19e60d1...`, A/V `dc7ca8ff6db1c2bb...`). Right is independently
  consumed at 1650; the timer begins at 1583, yielding 1,717 active frames.
  The predeclared 2500-2599 release first diverges at 2500 and localizes to 18
  WRAM bytes. Full observations and boundaries are in R-0020.
- Coverage/content result: two byte-identical coverage captures
  `476de96fa13b2cff...` produce byte-identical maps `b1e17356f656be5d...`.
  Against DRAGSTER: 4,014 bytes / 268 entry points only here, 1,185 bytes / 45
  entry points only in the baseline, and 33,715 shared bytes. The `0xC3` track
  object is 6,599 packed bytes (`3a8b470c673d0db3...`) and decodes to 50,665
  ignored bytes (`db6770152e399f9d...`). Load/riding provenance and the
  writer/cadence safety classification are recorded in R-0020.
- Commands/results: `replay validate` passes for both new manifests; `replay
  compare --runs 2` passes every identity check (report `28a9417d93835cb7...`);
  the predeclared `replay compare --against` exits 1 for the expected behavioral
  difference and writes divergence `877d113a46bfb5d...`; both `coverage capture`
  and `coverage map --baseline` pairs pass; targeted `access capture` passes
  with record `25c47f266e393ad7...`; load/riding `content provenance` pass with
  the two early unpaired OAM destinations explicitly reported; `content decode`
  of the tracked ZOOM ZOO inventory passes.
- Final regressions: `test --suite synthetic --preset lab-debug` passes 315/315
  (`2b7251624636be09...`); the existing DRAGSTER replay remains
  `72f618f2f7e3416e...` in two fresh processes (`d3b9cd3961ef2d5a...`); its ten
  decoded content entries remain exact (`5d6c44a75918ce15...`); the unchanged
  25-entry Classic pack passes inspection (`3e423fa8cc9ea285...`); pack-backed
  `native finish-check` with restores 1600/3213/3453/3678 passes all 18 checks
  (`d0f04de52a869125...`). CI is not a worker check and remains outstanding;
  independent review reproduced the evidence and returned only the bounded R1
  command-record finding, now corrected pending focused re-review.
- R1 correction: [R-0020](../docs/research/R-0020-zoom-zoo-reference.md) now
  tracks the exact canonical access regeneration command recovered from the
  ignored record, the implicit default ring of 262,144 entries, and both exact
  successful provenance commands including bounds, task and report paths. A
  fresh execution of that tracked access command regenerated SHA-256
  `25c47f266e393ad7071727e88475cf890974ce35b0750969c86ee07fcb51f85f`;
  rerunning the load and riding commands passed with provenance SHA-256 values
  `f968585d7736986bb53a8fbd22ce2e35e2da7be410f4053eec48c86aab928658`
  and `ad78116903821d9f2fcfb97b12e4f1642e0885365dd32ec7a987a42f22b8ce31`.
- Failures retained as evidence: the first baseline command used unsupported
  `--json-out` and exited 3 before action; discovery attempts with intentionally
  stale DRAGSTER expectations exited 1 while preserving samples; the first
  perturbation run likewise supplied its observed identities before the frozen
  compare; two native-regression invocations exited 3 (a pre-created artifact
  directory, then the wrong `native compare` case kind) before the correct
  `native finish-check` passed. No failed check is reported as a pass.
- Unresolved limits: coverage is scenario-level and confounded by duration,
  input and load timing; access is limited to frames 1150-1750; two early OAM
  destinations lack the `$80:2102` mirror watch; track-map gather, collision,
  camera, jump, finish and presentation meaning remain unknown; clean-SRAM
  BRONSEN behavior is not general AI evidence; local macOS reference execution
  and aggregate A/V identity do not reconstruct audio.
- Exact focused re-review start: fresh sequential OpenAI Sol/medium checkout of
  the corrected candidate. Inspect the tracked commands in R-0020, run the
  canonical access command in a fresh ignored output location or its tracked
  form and require SHA-256 `25c47f266e393ad7...`; run both tracked provenance
  commands and confirm their reported bounds and successful results. Check the
  focused diff and tracked-file hygiene. The first review already reproduced
  the behavioral, coverage, content and regression claims; do not broaden this
  correction review or reuse the worker's generated samples as fresh evidence.
- Model/usage: OpenAI `gpt-5.6-sol`, medium reasoning. Weekly usage sampled at
  50% used / 50% remaining at claim and 52% / 48% at the coverage/content
  checkpoint, then 53% / 47% for the R1 correction; account-wide delta from
  claim +3 points. The 70% discretionary stop and final 20% reserve were not
  reached. The available reset credit was not redeemed and no money was spent.
  Worker commands began at 07:33:20 AEST and the first candidate checks
  completed by 07:54 AEST (about 21 minutes wall clock).

## Review and integration

- [Independent review](M4-02-review.md) reproduced the replay, coverage,
  content, clean-SRAM coupling, DRAGSTER/native regressions and a reviewer-owned
  release-at-2501 boundary. It returned one record-only finding: the handoff
  claimed an exact access/provenance command that was not tracked.
- Correction `6f38439` added the complete access and provenance commands
  without changing evidence or implementation. [Focused re-review](M4-02-rereview.md)
  reproduced access SHA-256 `25c47f266e393ad7...` and provenance hashes
  `f968585d7736986b...` / `ad78116903821d9f...`, then approved it.
- Reviewed head `c95cc51` merged to `main` as
  `77885bee27e9429dbd165398da46ec6a2a21a6b5`. On that exact merge, the two-run
  replay, coverage/map, content decode and all 18 native finish/restore checks
  passed; app-debug passed 315/315. Report SHA-256 values are
  `d2671ac6698ef6e4...`, `8362636463d1c50d...`, `8ec26dd06bbcdcec...`,
  `73619e9fe8222402...`, `a56d8bcea02f9bc5...` and `ceab7a5f5366619b...`.
- Private `origin/main` was synchronized to `77885be`; hosted run
  `34722527701` passed macOS 15 and Ubuntu 24.04, including Linux SDL
  sanitizers. This acceptance update is pushed separately and verified.
- Accepted scope is deterministic reference evidence for the stated
  PAL/1P/MIKE/CRAWLER/ZOOM ZOO/BRONSEN/three-lap Race path. Native ZOOM ZOO,
  full collision/gather/finish/presentation semantics, generalized AI/SRAM,
  audio and other modes/tracks remain unverified.

## Worker evidence and attempts

- Navigation/coupling checkpoint (13 September 2026): a fresh ignored attempt
  added Down at frames 1000-1005 and deliberately delayed the two following
  Start windows by 50 frames. Its stale DRAGSTER expectations failed, as
  intended for discovery, while capture completed with sample digest
  `81cd821a0c65745e...`; frame images show the highlight move from DRAGSTER to
  ZOOM ZOO and `NOW PLAYING` explicitly shows `MIKE vs BRONSEN`, `OVER 3 LAPS
  ON ZOOM ZOO`, `RACE` and `EXIT`. Initial WRAM/cartridge-RAM digests are
  `fa43239bcee7...` / `7d2c7ac4888b...`, with no save files present.
- Timing checkpoint: a second fresh ignored run changed only the requested Down
  press in the accepted menu prefix, retained Start at 1050 and 1200, delayed
  acceleration until frame 1650 and extended the run to 3300 frames. It
  observes black presentation frames 1207-1377, the first established player-state
  writer at 1377, timer frame-counter advance at 1583, and same-frame Right
  delivery/throttle/speed response at 1650. Thus frames 1583-3299 provide 1717
  active timer frames. The accepted coverage later fixes the exact NMI-off
  boundary at 1208-1376; R-0020 records the content provenance.
- Predeclared perturbation, before either tracked-manifest perturbation run:
  release Right on frames 2500-2599 and resume at 2600. Expect exact agreement
  through 2499, a `joy1h_image` and throttle difference at 2500, then a speed or
  position response at/after the boundary. If the writers, widths or cadence
  differ from the targeted inventory, retain the divergence but do not reuse
  the DRAGSTER meaning. Reserve the adjacent release boundary for review.
