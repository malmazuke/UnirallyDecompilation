# M3-03 — Minimal frontend and controls

## Assignment

- Status: claimed on 12 September 2026
- Milestone: M3, prerequisite to playable-slice acceptance
- Coordinator: OpenAI Codex Sol/medium (`/root`)
- Worker: OpenAI Codex Sol/medium, one child in an isolated worktree
- Reviewer: fresh sequential OpenAI Codex Sol/medium session
- Dependencies: accepted M3-02 presentation, M3-02A Classic pack and M3-01 full-race simulation
- Starting commit: `759ed9e0b130819d09392b61946c27e433f44438`
- Branch/worktree: `task/M3-03-minimal-frontend` at `.worktrees/m3-03-minimal-frontend`
- Owned paths: `src/app/`, frontend CMake/dependency wiring, focused app tests,
  additive `tools/project.py` frontend/package commands, this task and its
  handoff/research/review records; accepted core/pack interfaces are frozen
- Model/usage: OpenAI Sol/medium default. Account-wide weekly bucket sampled at
  24% used on 12 September 2026. For this unattended run the user removed the
  project percentage stop/review reserve through M3, but explicitly excluded
  weekly reserve use: no reset-credit redemption or purchase. No publication or
  deployment is authorized. Retain 45-minute checkpoints and durable updates.
- Fixture access: worktree-local ignored links/copies to the existing pinned
  toolchain, supported ROM locator, reference tools, generated Classic pack and
  canonical states. Original/generated content remains ignored.

## Outcome and boundaries

Provide the smallest maintainable SDL3 desktop program that runs the accepted
CRAWLER/DRAGSTER native slice. It validates and consumes the Classic pack,
advances the native simulation at PAL 50 Hz, samples keyboard or one gamepad
into the existing two-controller input masks once per update, and displays the
accepted 256x224 presentation with deterministic integer scaling.

On first launch, absence of a valid pack leads to an explicit supported-ROM
selection/argument flow, exact ROM identity verification and atomic local pack
creation. Once created, the same application must relaunch from the pack with
the ROM removed. Cancel, missing ROM, wrong ROM, corrupt pack, renderer failure
and SDL failure must not become successful launches. Audio is intentionally
absent in M3 and must be declared in help/about/startup output.

Not in scope: menus beyond the minimal launch/error flow, audio implementation,
other tracks/riders/modes, interpolation that affects simulation, remapping UI,
configuration persistence beyond the pack path, packaging/release publishing,
online play or original CPU execution. M3-04 owns real sustained-play and
clean-checkout milestone acceptance.

## Required contract freeze

Before UI implementation, record and test:

1. The SDL3 version/source/checksum and local, non-global build arrangement on
   macOS and Linux; headless core/test builds must remain window-independent.
2. The exact 50 Hz monotonic-clock scheduler: maximum catch-up work, pause/
   resume behavior and how delayed display frames affect input/update/render.
3. Keyboard/gamepad button mapping to the accepted SNES masks, including
   simultaneous buttons, release, disconnect and focus loss.
4. The application pack path, explicit ROM input/selection precedence, atomic
   extraction handoff and all exit/error outcomes.
5. The display contract: 256x224 source, aspect-preserving integer scale,
   nearest-neighbor sampling and resize behavior. Presentation output must not
   alter canonical gameplay state.

## Acceptance

- A tracked command builds the desktop target reproducibly without global
  package installation; ROM-free debug/sanitizer CI and headless tests remain
  available when a display is absent.
- Authored tests exercise PAL scheduler boundaries with a fake clock, including
  no early update, exact deadline, bounded catch-up and long-stall recovery.
- Authored tests exercise keyboard/gamepad mask mapping, simultaneous and
  released inputs, disconnect/focus-loss clearing and two-port ownership where
  exposed. One input snapshot reaches exactly one simulation update.
- The app validates every Classic pack identity through the accepted native
  reader before gameplay. Missing-pack first launch exact-gates the supported
  ROM and creates the pack atomically; wrong/cancelled/missing input and corrupt
  pack fail clearly. A later launch passes with the ROM absent.
- The accepted 256x224 renderer is displayed with deterministic nearest-neighbor
  integer scaling; resize/display cadence changes do not alter the canonical
  state sequence for a frozen live-input script.
- Audio omission is explicit in tracked help/startup documentation and no audio
  success is claimed.
- Debug and sanitizer suites pass locally; macOS/Linux hosted CI is green;
  repository hygiene finds no ROM, extracted assets, generated packs, states or
  captures; a fresh independent reviewer approves the exact behavioral commit.

## First experiment

Inventory SDL3 availability on the development host and current CMake/bootstrap
mechanisms. Choose a pinned dependency approach that works in isolated build
directories without affecting headless CI. Before opening a real window, add a
pure scheduler/input adapter with fake-time tests and drive a short pack-backed
native run from a deterministic input sequence; prove its canonical states and
render digests are independent of display cadence.

Only the coordinator marks this task accepted after independent review and
integration evidence.

## Handoff

Claim commit, worker commit(s), exact commands/results, dependency identities,
known limitations and next experiment are appended here before every yield.

### Worker review-candidate handoff — 12 September 2026

- Worktree/branch: `.worktrees/m3-03-minimal-frontend` on
  `task/M3-03-minimal-frontend`, starting at claim commit `32b637d`. Worker was
  OpenAI Codex Sol/medium; no child or frontier consultation was used. The task
  remained within OpenAI and no reset credit, purchase, publication or deploy
  action occurred.
- [R-0016](../docs/research/R-0016-minimal-frontend.md) was frozen before SDL
  UI implementation. It separates source observations from the scheduler,
  input, first-launch, scaling, camera and omission decisions. SDL 3.4.10 is
  pinned to upstream source archive SHA-256
  `12b34280...f2785`; CMake reads the tracked lock, FetchContent verifies it,
  and builds it only in `app-debug`/`app-sanitize`. Headless presets leave SDL
  disabled and window-independent.
- `src/app/frontend.*` implements the pure 20,000,000 ns scheduler (four-update
  maximum and long-stall debt drop), complete 12-button keyboard/gamepad mask
  mappings for two ports, state clearing and integer viewport/camera helpers.
  The authored test covers early/exact/batched/stalled/pause/resume deadlines,
  simultaneous/released/disconnected/focus-cleared input, all mappings,
  controller ownership, 1x/3x viewports, one snapshot per update, identical
  canonical state sequences under two display cadences, 256x224 output and
  renderer state invariance.
- `src/app/sdl_main.cpp` validates the C++ pack before SDL/gameplay, initializes
  video and gamepad only (no audio), uses an RGB24 streaming texture with SDL
  nearest scaling, centers integer viewports after output resize, samples once
  per due PAL update and renders once per batch. Focus loss pauses and clears;
  gain resumes at a fresh deadline. SDL/window/renderer/texture errors fail the
  process. The first two connected standard gamepads own ports 0/1; the
  accepted slice consumes port 0 only.
- M3-02 recovered only five atlas pose pairs. Rather than changing that
  accepted core contract or inventing frames, the app holds the last supported
  render while other semantic poses advance and reports the omission once. Its
  first frame uses a presentation-only copy with the opening recovered pair;
  canonical gameplay is never changed. This is the main visible limitation for
  review and M3-04 real-play assessment.
- `frontend run` validates an existing pack without opening `--rom`; if the
  pack is absent it requires an explicit ROM argument, exact-gates it, uses the
  accepted atomic writer, revalidates, and launches. Existing corruption is
  never overwritten. Authored tooling tests cover first launch, ROM-absent
  relaunch, cancellation/empty input, missing/wrong ROM, corrupt pack and app
  failure. Help, README and startup output explicitly state that audio is not
  implemented.

Exact successful validation commands on the uncommitted candidate were:

```text
python3 tools/project.py build --preset app-debug --report artifacts/m3-03-final-app-debug-build.json --timeout 600 --task M3-03
python3 tools/project.py test --suite synthetic --preset app-debug --artifacts artifacts/m3-03-final-app-debug-synthetic-2 --report artifacts/m3-03-final-app-debug-synthetic-2/report.json --timeout 600 --test-timeout 120 --task M3-03
python3 tools/project.py build --preset app-sanitize --report artifacts/m3-03-final-app-sanitize-build.json --timeout 600 --task M3-03
python3 tools/project.py test --suite synthetic --preset app-sanitize --artifacts artifacts/m3-03-final-app-sanitize-synthetic --report artifacts/m3-03-final-app-sanitize-synthetic/report.json --timeout 600 --test-timeout 120 --task M3-03
SDL_VIDEODRIVER=dummy python3 tools/project.py frontend run --pack artifacts/m3-03-first-launch.pack --preset app-sanitize --hidden --updates 20 --timeout 30 --report artifacts/m3-03-app-sanitize-smoke.json --task M3-03
python3 tools/project.py native finish-check --manifest tests/manifests/native/full-race-continuous.case.json --content-pack artifacts/m3-03-first-launch.pack --save-frame 3213 --save-frame 3453 --preset lab-debug --artifacts artifacts/m3-03-finish-debug --report artifacts/m3-03-finish-debug/report.json --timeout 600 --task M3-03
python3 tools/project.py native presentation-check --manifest tests/manifests/presentation/classic-crawler-dragster-v1.json --fixtures artifacts/m3-03-private-fixtures --content-pack artifacts/m3-03-first-launch.pack --preset lab-debug --artifacts artifacts/m3-03-presentation-debug-2 --report artifacts/m3-03-presentation-debug-2/report.json --timeout 600 --task M3-03
git diff --check
```

Debug synthetic passed 281 Python tests, 20 CTests and three fresh-process lab
runs (304 passed report checks). Sanitizer synthetic's closing output passed
the same inventory; however its report retained two transient failures in
shared `lab-failure-probe` mutation tests, so it is not claimed as a clean
pass. Both tests passed immediately in focused reproduction. A clean exact-head
sanitizer rerun is required after commit. The private finish gate passed all 11
checks including restores at 3213/3453. The private presentation gate passed
the unchanged seven mismatch counts: 36, 697, 279, 445, 653, 962 and 961. The
first-launch pack has the supported ROM identity and 25 entries; both its
creation launch and a later ROM-unconsulted launch passed under SDL dummy.

Report SHA-256 values before the final exact-head rerun: debug build
`c7ec14d0...e76f`, debug suite `c9af7df8...27e7`, sanitizer build
`3c051796...9bfb`, sanitizer suite (retained failure)
`5f9fb693...5822`, first launch `07b38f1e...9816`, pack-only launch
`ecdcf930...7cf`, sanitizer SDL smoke `12037808...1c44`, finish
`966a3be3...f353`, presentation `c198053f...76a5`. An earlier presentation
invocation using a fixture path outside the worktree correctly failed its
authorization check; copying the same hashed fixtures below ignored
`artifacts/` produced the passing result above. An initial Python suite failed
only because the new test imported `tools.unirally_lab` instead of the runner's
`unirally_lab` path; the corrected full debug suite is the one reported.

Next: commit the coherent candidate, rerun sanitizer on that exact head until
the shared mutation tests are clean, record final hashes/commit here, inspect
the staged diff for private content, then submit for independent review. Do not
mark accepted; hosted macOS/Linux CI and real sustained play remain coordinator
/ M3-04 evidence.

### Final worker checkpoint

- Exact behavioral candidate: `6489c644a327e4eeb54b04822b2aa92e8869c2e0`
  (`Add minimal SDL3 frontend and launch flow`). This follow-up changes only the
  task handoff.
- Exact-candidate app-debug build and complete synthetic suite passed. Report
  SHA-256 values: build `1901807d...2aee`, suite
  `9815172d...a805` (281 Python tests, 20 CTests, three fresh processes; 304
  passed checks, no failure/skip).
- Exact-candidate app-sanitize build and complete synthetic suite passed on the
  clean rerun. Report SHA-256 values: build `9b32ea5d...3fa3`, suite
  `60eb2cdf...321` (the same 304 passed checks, no failure/skip). This supersedes
  only the earlier retained transient broad-run failure as candidate evidence;
  that failure remains documented above.
- The staged behavioral diff contained only the 17 listed source/test/doc/lock
  paths. `artifacts/`, `build/`, `local/`, packs, ROM locator, fixture states,
  images and Python caches remained ignored. `git diff --check` passed.
- Submit `6489c64` plus this handoff for fresh independent review. The reviewer
  should particularly challenge scheduler overflow/long-stall boundaries,
  focus/gamepad ownership, invalid existing-pack precedence, Linux SDL source
  configuration without a global SDL install, and whether the explicit held-
  pose presentation limitation is acceptable for M3-03 rather than M3-04.
