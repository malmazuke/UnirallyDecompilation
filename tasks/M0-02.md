# M0-02 — Reproducible native/tooling bootstrap

- Status: accepted
- Coordinator: primary agent
- Base: `776824a` (M0-01 merge candidate; provides `tools/project.py`, the report schema and exit codes).
- Branch: `task/M0-02-laboratory`, primary checkout. Single sequential worker; the separate native worktree `.worktrees/native` on `task/M0-02-native` was never used and holds no commits beyond `7ce358e`. It can be removed by the user; the worker did not delete it.
- Session: checkpoint at least every 10 minutes; review at the end of this bounded implementation turn.
- Prerequisite: M0-00 baseline.
- Budget: current authorized interactive session; no additional service purchases.

## Work and ownership

Primary agent owns `tools/`, Python tooling tests, tool locks, schemas, CI and coordinator documentation. A native worker may own CMake files, `src/lab/`, and native tests in a separate worktree. Agree interfaces before dispatch. A reviewer inspects the exact integrated candidate and reproduces tests.

## Acceptance

1. `doctor`, `bootstrap`, `build`, `test --suite synthetic` and structured reports exist.
2. CMake/Ninja versions and download hashes are locked; bootstrap is isolated and idempotent.
3. A synthetic native runner builds and tests on this macOS host and Linux CI. It is explicitly not recovered game code.
4. Reports identify source/tool/input state and distinguish missing prerequisites, failures and timeouts from passes.
5. Clean-checkout bootstrap/build/test and a repeated bootstrap succeed; tooling error-path tests succeed.
6. Independent review finds no unresolved material defects; actual merge candidate is verified.

## Handoff

- Current base/head commit and uncommitted state: base `0d0a60a` (main after M0-01). Implementation `2d34fab`; first review returned it; fixes `db51ad6` and `1c21dc9`; re-review approved `1c21dc9` and its recommended follow-ups landed in `6b48da5`, the merge candidate. No uncommitted state. Branch pushed to origin.
- Verified findings (final candidate `1c21dc9` unless stated):
  - Bootstrap fetches pinned CMake 3.31.10 and Ninja 1.13.2 wheel archives from PyPI, verifies SHA-256 against `tools/locks/toolchain.json`, extracts into ignored `local/toolchain/` and writes `local/toolchain/manifest.json`. A fresh clone bootstraps in about 2.5 s over curl; the repeat reports `source=cache; extracted=False` for both tools. Nothing is installed globally (first reviewer confirmed no writes outside the clone by timestamp search).
  - Synthetic probe hash after 1000 scripted steps is `0be347c529fadda9` on this host (macOS 15.7.2 arm64, AppleClang 17.0.0.17000404 through the preset; Homebrew clang 19.1.7 at -O0, -O2 and ASan+UBSan in a manual probe; the `lab-sanitize` preset natively), GitHub `macos-15` (AppleClang 17.0.0.17000013) and GitHub `ubuntu-24.04` x86_64 (GNU 13.3.0, `lab-debug` and `lab-sanitize`). Three fresh processes agree on every host.
  - `test` now brings the preset up to date with an incremental build before ctest and records the build outcome and source state (commit, dirty flag, diff digest including untracked files) in `build/<preset>/lab-build-info.json` and in the report. The first reviewer's failing scenario (change the expected hash, commit, test without building) now fails `ctest:lab_state_known_hash` with report and build both at the tampered commit; reproduced in a clean clone.
  - The report pipeline distinguishes outcomes: the `lab-failure-probe` preset yields one `failed` and one `timeout` ctest check and exit 4; a preset never built yields `missing` and exit 2; a build that fails to compile yields `native_build_available` `failed` and no ctest checks; a digest mismatch yields `failed`, exit 1 and the bad download is discarded; an unknown suite, unknown or hidden preset, non-positive timeout or empty lock yields exit 3; no downloader yields `missing` and exit 2. A nested `test` invocation with Python tests enabled is refused with exit 3 (the first probe run recursed without bound and had to be killed; hence the guard).
  - Timeouts kill the whole process group; a test proves a grandchild does not survive. Sub-second timeouts are rounded up, not truncated to zero.
  - Clean-checkout path at `6b48da5`: fresh `git clone` into scratch with no `local/` or cache; `doctor`, `bootstrap` (downloaded), `bootstrap` again (cache only), `build --preset lab-debug`, `test --suite synthetic` gave 44/44 passed with `source.dirty` false and no tree change during the run. Report SHA-256: doctor `1747669f…7c30`, bootstrap `652ccae8…7947`, bootstrap-2 `032a288d…92fe`, build `4d28cc9b…2012`, test `77a2e435…4435`. (The same sequence at `1c21dc9` gave 43/43.)
  - CI run 34469566942 on `6b48da5`: both jobs green; macOS and Linux `test-debug` and Linux `test-sanitize` reports each 44/44 passed, build outcome `passed`, probe hash `0be347c529fadda9`. (Run 34468338965 on `1c21dc9`: 43/43 on all three.) Earlier run 34468178281 on `db51ad6` failed on both platforms because a new test assumed a preset had already been built; fixed in `1c21dc9` (each test now builds what it needs).
- Current hypothesis and failed approaches: `urllib` cannot verify PyPI on this host's python.org build (no CA bundle), so the downloader prefers `curl` and records which was used. Ninja 1.12.1 has no PyPI wheel; 1.13.2 was pinned instead.
- Commands executed, outcomes and report hashes: see above; local reports under ignored `artifacts/m0-02/`, clean-clone reports under scratch, CI artifacts `reports-macos-15` and `reports-ubuntu-24.04` on run 34468338965.
- Unavailable/skipped checks: Windows untested (later target). Linux aarch64 artifacts are locked but no aarch64 host has run them. Docker unused. Bit-identical build reproducibility is not claimed; reports record compiler identity instead. `--no-python-tests` records a skipped optional check and must not be used for an acceptance run.
- Exact next experiment/command: re-reviewer reproduces the clean-clone sequence and the returned scenario at `1c21dc9`.
- Remaining dependencies: none for M0-02. M0-03 needs M0-01 (accepted) and this task.
- Elapsed work and provider usage when known: about two hours of one interactive session including two review rounds; usage not metered.

## Review and integration

- Reviewer and independent reproduction/withheld-case results: two independent reviewer sessions (same model, fresh contexts, own clones).
  - Review 1 of `6e96589`: reproduced the clean-clone sequence (35/35 then), verified isolation by timestamp search outside the clone, ran 12 withheld cases. Returned with one material defect: `test` could report a pass for a commit whose binaries were never rebuilt (stale-binary scenario). Minor: untracked files not counted as dirty; corrupt install stamp traceback; grandchildren surviving a timeout; sub-second timeouts truncated to zero; unvalidated test preset; missing downloader classed as failure; saturation not asserted directly.
  - Review 2 of `1c21dc9`: reproduced 43/43, reproduced the returned scenario and confirmed it now fails with report and build at the same commit, re-tested every minor item and approved. Recommended before merge: tests must not edit tracked files (a killed run could leave a wrong expectation in the tree) and `test` must reconfigure so a preset cache change is not silently ignored. Follow-ups noted: `--test-timeout` help text, interrupt handling, non-object JSON stamps.
- Required changes or acceptance rationale: all review-1 findings fixed in `db51ad6`/`1c21dc9`; all review-2 recommendations and follow-ups fixed in `6b48da5` (cache-variable test hooks `LAB_KNOWN_HASH_OVERRIDE` and `LAB_PROBE_COMPILE_ERROR` with `build --define`; reconfigure inside `test`; `source_at_finish`/`source_changed_during_run` in reports; JSON shape guards; process-group kill on interrupt; help text). The reviewer's exact stale-binary reproduction is now a test (`test_results_follow_the_current_source_not_stale_binaries`) and a clean-clone demonstration.
- Exact merge candidate and required-check results: `6b48da5`. Clean clone 44/44; CI run 34469566942 green on macOS and Linux including sanitizers; local `test --suite synthetic --preset lab-debug` 44/44.
- Integrated commit and evidence location: fast-forwarded into `main` (see `git log`); suite rerun on the integrated commit recorded in the coordinator's integration commit message. Evidence: this record, ignored `artifacts/m0-02/`, CI artifacts on the runs above.
- Scope still unverified: Windows; Linux aarch64 (locked, never run); any emulator, reference capture or game code (M0-03 onward). `src/lab` is infrastructure validation only.
